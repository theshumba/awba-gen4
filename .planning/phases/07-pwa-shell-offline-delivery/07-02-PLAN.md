---
phase: 07-pwa-shell-offline-delivery
plan: 02
type: execute
wave: 2
executor_hint: sonnet
depends_on: ["07-01"]
files_modified:
  - sw.js
  - learn.html
  - index.html
  - scripts/tests/pwa-audit.mjs   (extend to cover the SW + precache-list integrity)
autonomous: true
requirements: [PLT-02]
must_haves:
  truths:
    - "Per D-71: ONE hand-written root-scoped service worker `sw.js` at the repo root, < ~50 lines of logic, no dependency. install → precache the app shell into `awba-cache-vN` (a versioned constant); activate → `clients.claim()` + delete every cache key ≠ the current version (activate-time purge); fetch → same-origin GET only (cross-origin/opaque pass straight through untouched): NETWORK-FIRST for HTML navigations (`request.mode==='navigate'` or Accept: text/html) with the cached page (then a cached shell fallback) on failure, CACHE-FIRST for everything else (CSS/JS/fonts/img/icons) with a network fill-through on miss"
    - "Per D-71: the precache list = the 20 app pages (learn.html + 15 lessons + 4 reviews) + shared/awba-engine.css + shared/awba-engine.js + the 17 shared/fonts/*.woff2 + shared/img/grain.png + the 4 icons + manifest.webmanifest + index.html — ALL RELATIVE paths (no leading slash); preview.html is NOT precached (dev-only); the list is generated/verified against the real files on disk so it never drifts"
    - "Per D-71/CLAUDE.md: registration is added to learn.html + index.html <head>-script or a tiny classic inline script, guarded by BOTH `'serviceWorker' in navigator` AND `location.protocol !== 'file:'` (the SW must NEVER attempt registration over file:// — double-click review is unaffected); registration path is RELATIVE ('sw.js'); the registration touches no localStorage (learn.html localStorage grep stays 0 — use no storage, or AW.prefs if any flag is needed)"
    - "pwa-audit.mjs is extended: sw.js exists + parses + declares a versioned cache constant + an activate-purge + a navigate/network-first branch; every precache entry resolves to a real file on disk (no 404 would be cached); learn.html + index.html contain the guarded registration; still exit-code-first, exit 0 'PWA OK'"
    - "Per D-67: no build step (sw.js is a static hand-written file); render-smoke/validate-content/port-audit/contrast/rtl/glyph + the a11y suite exit 0; the node suite fail 0 / todo 0; @layer ×1; engine localStorage 13; learn localStorage 0; glyphCount 13; no gated literal in sw.js/learn.html/index.html; the SW registration is file://-guarded so render-smoke (file://) stays clean"
  artifacts:
    - path: "sw.js"
      provides: "the root-scoped offline service worker (precache + cache-first assets / network-first HTML + activate purge)"
      contains: "awba-cache"
    - path: "scripts/tests/pwa-audit.mjs"
      provides: "extended to gate the SW shape + precache-list file existence"
      contains: "sw.js"
---

## Phase Goal
**As a** learner on a patchy connection, **I want** Awba to open and let me keep walking the path after my first visit even with no signal, **so that** the un-loseable promise holds offline.
*(ROADMAP Phase-7 SC1 offline half + PLT-02. This wave adds the service worker; the install shell landed in 07-01.)*

<objective>
Write the one hand-written root-scoped `sw.js` (precache the 20-page app shell + engine + fonts + img + icons + manifest; cache-first static, network-first HTML navigations, activate-time purge of old versions, same-origin GET only); register it from `learn.html` + `index.html` guarded off `file://`; and extend `pwa-audit.mjs` to gate the SW shape + precache-list integrity. Zero dependency, < ~50 lines of logic, relative paths, no build step. The SW must not register over `file://` so double-click review + the render-smoke harness stay clean.

Executor routing: **sonnet** (a well-scoped hand-written SW — CLAUDE.md's "Minimal service worker (cache-first assets, network-first HTML, manual cache-busting)" is the exact pattern; keep it ≤ ~50 lines). Generate the precache array from the real on-disk file list (glob the 20 pages + fonts) so it can't drift; exclude preview.html.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-pwa-shell-offline-delivery/07-CONTEXT.md
@CLAUDE.md
<interfaces>
<!-- locate by content -->
- CLAUDE.md "Minimal service worker (cache-first assets, network-first HTML, manual cache-busting)" + "Service workers do NOT register over file:// — require http://localhost or HTTPS" — the pattern + the guard.
- `learn.html`/`index.html` `<head>` — add the guarded registration (a tiny classic inline `<script>`, or fold into the existing head script region; NEVER add localStorage).
- On-disk shell: `learn.html`, `lessons/*.html` (15), `reviews/*.html` (4), `shared/awba-engine.css`, `shared/awba-engine.js`, `shared/fonts/*.woff2` (17), `shared/img/grain.png`, `icons/*.png`, `manifest.webmanifest`, `index.html`. preview.html EXCLUDED.
- `scripts/tests/render-smoke.mjs` runs pages over file:// — the registration guard (`protocol!=='file:'`) keeps it a no-op there.
Machine notes: run node tests via glob; ugrep paren-wrap/`--`-separate; RELATIVE paths only; gated literals never in comments/strings; hand-edit STATE.md.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: sw.js — precache + cache-first assets / network-first HTML + activate purge</name>
  <files>sw.js</files>
  <action>Write `sw.js` at the repo root following the CLAUDE.md minimal-SW pattern: `const CACHE='awba-cache-v1';` + a `PRECACHE` array of the shell (the 20 pages + engine CSS/JS + the 17 fonts + grain.png + the 4 icons + manifest.webmanifest + index.html), all relative. `install`: `caches.open(CACHE).then(c=>c.addAll(PRECACHE))` + `skipWaiting()`. `activate`: `clients.claim()` + delete every cache key !== CACHE. `fetch`: return early (do nothing) for non-GET or cross-origin; for `e.request.mode==='navigate'` (or an Accept text/html) go network-first (fetch → cache put a clone → on reject serve the cached request, then a cached `learn.html`/`index.html` fallback); else cache-first (`caches.match` → else fetch → cache-fill). Keep the logic ≤ ~50 lines. Verify it parses (`node --check sw.js`) and every PRECACHE path resolves on disk. Commit `feat(07-02): root-scoped service worker — precache + cache-first/network-first + activate purge (D-71/PLT-02)`.</action>
  <verify>
    <automated>node --check sw.js && grep -qE "awba-cache-v[0-9]" sw.js && grep -q "skipWaiting\|clients.claim" sw.js && grep -qiE "navigate|text/html" sw.js && node -e "const fs=require('fs');const s=fs.readFileSync('sw.js','utf8');const m=s.match(/\[([\s\S]*?)\]/);if(!m)throw new Error('no precache array');const paths=(m[1].match(/'[^']+'|\"[^\"]+\"/g)||[]).map(x=>x.slice(1,-1)).filter(p=>!/^https?:/.test(p));let miss=paths.filter(p=>p&&p!=='./'&&!fs.existsSync(p));if(miss.length){console.log('MISSING PRECACHE:',miss.join(','));process.exit(1)}console.log('PRECACHE_OK '+paths.length+' entries')" && ! grep -qE "'/|\"/" sw.js</automated>
  </verify>
  <acceptance_criteria>
    - sw.js parses, declares a versioned cache, skipWaiting/clients.claim + activate purge, a navigate/network-first branch, cache-first otherwise; every precache path resolves on disk; no leading-slash (absolute) path.
  </acceptance_criteria>
  <done>The offline engine exists — the shell precaches and the fetch strategy is right.</done>
</task>

<task type="auto">
  <name>Task 2: file://-guarded registration on learn.html + index.html</name>
  <files>learn.html, index.html</files>
  <action>Add a tiny classic inline `<script>` to `learn.html` and `index.html` (head or end-of-body): `if ('serviceWorker' in navigator && location.protocol !== 'file:') { window.addEventListener('load', function(){ navigator.serviceWorker.register('sw.js'); }); }`. RELATIVE 'sw.js'. No localStorage. Do NOT let the word for browser-persistent storage enter learn.html (grep stays 0). Commit `feat(07-02): register the service worker, guarded off file:// (D-71)`.</action>
  <verify>
    <automated>grep -q "serviceWorker.register('sw.js')\|serviceWorker.register(\"sw.js\")" learn.html && grep -q "location.protocol !== 'file:'\|protocol!=='file:'" learn.html && grep -q "serviceWorker.register" index.html && test "$(grep -c 'localStorage' learn.html)" -eq 0 && SMOUT=$(node scripts/tests/render-smoke.mjs 2>&1); test $? -eq 0 && echo "$SMOUT" | grep -q 'SMOKE OK learn.html' && ! echo "$SMOUT" | grep -q 'SMOKE FAIL'</automated>
  </verify>
  <acceptance_criteria>
    - learn.html + index.html register sw.js guarded by `'serviceWorker' in navigator` AND `protocol !== 'file:'`; learn localStorage 0; render-smoke (file://) stays green (registration no-ops there).
  </acceptance_criteria>
  <done>The SW registers on a real server but never over file:// — review + the harness are unaffected.</done>
</task>

<task type="auto">
  <name>Task 3: extend pwa-audit.mjs to gate the SW + precache integrity; full-gate re-run</name>
  <files>scripts/tests/pwa-audit.mjs</files>
  <action>Extend `pwa-audit.mjs`: assert sw.js exists + `node --check`-parses, declares a versioned cache constant, has an activate-purge + a navigate branch; re-derive the precache paths and assert each resolves on disk; assert learn.html + index.html contain the guarded registration. Keep exit-code-first, "PWA OK" on success. Run the full standing gate set. Commit `feat(07-02): pwa-audit gates the SW shape + precache integrity (PLT-02)`.</action>
  <verify>
    <automated>POUT=$(node scripts/tests/pwa-audit.mjs 2>&1); PE=$?; SOUT=$(node --test scripts/tests/*.test.js 2>&1); SE=$?; SMOUT=$(node scripts/tests/render-smoke.mjs 2>&1); SME=$?; test $PE -eq 0 && echo "$POUT" | grep -q 'PWA OK' && test $SE -eq 0 && echo "$SOUT" | grep -qE 'fail 0' && test "$(echo "$SOUT" | grep -oE 'todo [0-9]+' | head -1 | grep -oE '[0-9]+')" -eq 0 && test $SME -eq 0 && echo "$SMOUT" | grep -q 'SMOKE OK' && ! echo "$SMOUT" | grep -q 'SMOKE FAIL' && test "$(grep -c 'localStorage' shared/awba-engine.js)" -eq 13 && test "$(grep -c 'localStorage' learn.html)" -eq 0 && test "$(grep -c '@layer tokens, base, components, screens, motion;' shared/awba-engine.css)" -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - pwa-audit.mjs exits 0 "PWA OK" now also gating the SW + precache; full suite fail 0 / todo 0; render-smoke green; engine localStorage 13; learn localStorage 0; @layer ×1.
  </acceptance_criteria>
  <done>The offline layer is proven by the permanent gate.</done>
</task>

</tasks>

<verification>
- sw.js (root-scoped, versioned cache, precache the 20-page shell + engine + fonts + img + icons + manifest, cache-first assets / network-first HTML, activate purge, same-origin GET only); guarded registration on learn.html + index.html (never over file://); pwa-audit gates it.
- Full suite fail 0 / todo 0; render-smoke + all standing gates green; learn localStorage 0; engine localStorage 13; @layer ×1; no gated literal; relative paths; no build step.
</verification>

<output>
Create `.planning/phases/07-pwa-shell-offline-delivery/07-02-SUMMARY.md` when done.
</output>
