---
phase: 07-pwa-shell-offline-delivery
plan: 01
type: execute
wave: 1
executor_hint: sonnet
depends_on: []
files_modified:
  - manifest.webmanifest
  - index.html
  - learn.html
  - icons/  (generated PNGs + the source SVG)
  - scripts/tests/pwa-audit.mjs  (new standing gate)
autonomous: true
requirements: [PLT-02]
must_haves:
  truths:
    - "Per D-69: the app icon is the surviving AW.KIT lantern (currentColor scene, NOT the retired lantern-gold variant or companion mascot) given an Athar home-screen treatment — gold ink on the Kiswah dark ground #131013 — generated as PNG at 192, 512, a 512 maskable (art ≤60% of the frame, ≥40% safe zone), and 180 apple-touch (no transparency); rasterised by rendering an Athar-treated square SVG through headless system-Chrome (the render-smoke Chrome, zero new dependency), 512 master then sips-resized for the smaller sizes; the source SVG is committed alongside the PNGs in icons/"
    - "Per D-70: index.html is a tiny redirect to learn.html (meta refresh + location.replace + a plain <a> fallback) carrying the manifest <link>, the dark theme-color #131013, and the apple-touch-icon; learn.html is NOT renamed; manifest start_url='learn.html', scope='./', display='standalone', background_color/theme per Athar (dark), name/short_name set, the icon family declared with purpose 'any' + 'maskable'; ALL icon/manifest paths are RELATIVE (no leading slash) so the app installs at a GitHub Pages subpath or a custom-domain root unchanged"
    - "Per D-70: learn.html gains the manifest <link rel=manifest>, the apple-touch-icon <link>, and (already present) the dark theme-color — added in <head> only (never inside the inline page <script>), so learn.html's localStorage grep stays 0 and no gated literal enters; index.html carries the same head trio"
    - "A new scripts/tests/pwa-audit.mjs standing gate (exit-code-first) parses manifest.webmanifest (valid JSON; required keys name/short_name/start_url/scope/display/icons; every icon src resolves to an existing file on disk; a maskable purpose present; start_url + scope relative), asserts index.html redirects to learn.html + links the manifest, and asserts learn.html links the manifest + apple-touch — exit 0 on success; it joins the full gate"
    - "Per D-67 discipline carried in: zero new hex in the type/colour SYSTEM (the icon PNG is a raster asset, its gold/#131013 are the shipped --gold/theme-color values, not new CSS tokens); no build step (the PNGs are committed static assets); render-smoke/validate-content/port-audit/contrast/rtl/glyph + the a11y suite all still exit 0; @layer order line ×1; engine localStorage 13; learn localStorage 0; glyphCount 13; no gated literal in learn.html/index.html/manifest"
  artifacts:
    - path: "manifest.webmanifest"
      provides: "the installable-PWA manifest (relative paths, lantern icon family, standalone, Athar dark)"
      contains: "maskable"
    - path: "index.html"
      provides: "the base-URL redirect to learn.html + install head (manifest/theme-color/apple-touch)"
      contains: "learn.html"
    - path: "icons/icon-512.png"
      provides: "the 512 lantern app icon (+ 192, maskable-512, apple-touch-180, + source SVG)"
      contains: ""
    - path: "scripts/tests/pwa-audit.mjs"
      provides: "the permanent PWA manifest/icon/redirect exit-code gate"
      contains: "manifest"
---

## Phase Goal
**As a** learner, **I want to** add Awba to my home screen and have it look and open like a real app, **so that** the course is a tap away and the un-loseable promise survives.
*(Derived from ROADMAP Phase-7 SC1 + PLT-02. This wave builds the installable shell — icons, manifest, the base redirect — leaving the offline service worker to 07-02.)*

<objective>
Build the installable PWA shell: generate the lantern app-icon family (D-69) by rasterising an Athar-treated SVG through headless Chrome; write `manifest.webmanifest` with relative paths and the icon family; create `index.html` as a redirect-to-`learn.html` that is itself installable; add the manifest/apple-touch head links to `learn.html` + `index.html`; and add `scripts/tests/pwa-audit.mjs` as a permanent gate. NO service worker yet (07-02). NO new CSS tokens/hex, no build step, relative paths only.

Executor routing: **sonnet** (mechanical asset-gen + static files). The icon rasterisation uses the SAME headless-Chrome binary render-smoke.mjs locates (read its Chrome-path logic and reuse it) — screenshot a 512×512 HTML/SVG page with a solid #131013 background, then `sips -z N N` to derive 192/180; render the maskable variant separately with ≥40% padding. If headless screenshot proves flaky, fall back to `rsvg-convert`/`qlmanage` if present; commit whatever PNGs are produced + the source SVG.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-pwa-shell-offline-delivery/07-CONTEXT.md
@CLAUDE.md
<interfaces>
<!-- locate by content — line numbers drift -->
- `AW.KIT.lantern` in `shared/awba-engine.js` (grep `lantern:`) — the currentColor lantern SVG (`viewBox 0 0 240 300`); extract its inner paths for the icon art (re-ink gold `#…` = the shipped `--gold` value; read `--gold` from `shared/awba-engine.css` grep `--gold:`).
- `scripts/tests/render-smoke.mjs` — its headless-Chrome discovery (grep `Chrome`/`chrome`/`executablePath`/the channel list); reuse that binary + the `--headless --screenshot --window-size=512,512 --default-background-color` invocation for a deterministic PNG.
- `learn.html` `<head>` (grep `theme-color`) — add the manifest + apple-touch links there; NEVER touch the inline page `<script>` (learn localStorage stays 0).
- Existing dark theme-color `#131013` (learn/reviews). The manifest `background_color`/`theme_color` = `#131013`.
Machine notes: run node tests via glob; ugrep paren-wrap/`--`-separate; RELATIVE paths only (no leading slash); gated literals never in comments/strings; hand-edit STATE.md.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: generate the lantern app-icon family (192 / 512 / maskable-512 / apple-touch-180) + source SVG</name>
  <files>icons/</files>
  <action>Read `AW.KIT.lantern` + the `--gold` and Kiswah `#131013` values. Author `icons/icon-src.svg` — a square (512×512) with a solid `#131013` background rect and the lantern art centred in gold (`--gold` value), art occupying ≤ ~62% for the plain icons; author `icons/icon-maskable-src.svg` with the art ≤ ~56% (≥40% total safe padding). Render each through the render-smoke headless-Chrome binary to `icons/icon-512.png` and `icons/icon-maskable-512.png` (`--headless --screenshot --window-size=512,512 --hide-scrollbars`, solid background so there's no transparency). Derive `icons/icon-192.png` (`sips -z 192 192 icon-512.png --out …`) and `icons/apple-touch-icon-180.png` (`sips -z 180 180 …`, no alpha). Verify each PNG exists and is non-empty (`sips -g pixelWidth`/`file`). Commit `feat(07-01): lantern app-icon family — gold-on-Kiswah, 192/512/maskable/apple (D-69/PLT-02)`.</action>
  <verify>
    <automated>for f in icons/icon-192.png icons/icon-512.png icons/icon-maskable-512.png icons/apple-touch-icon-180.png icons/icon-src.svg; do test -s "$f" || { echo "MISSING $f"; exit 1; }; done; test "$(sips -g pixelWidth icons/icon-512.png 2>/dev/null | grep -oE '[0-9]+$')" = "512" && test "$(sips -g pixelWidth icons/apple-touch-icon-180.png 2>/dev/null | grep -oE '[0-9]+$')" = "180" && echo ICONS_OK</automated>
  </verify>
  <acceptance_criteria>
    - Four PNGs (192/512/maskable-512/apple-touch-180) + the source SVG(s) exist, correct dimensions, non-empty, gold-on-#131013 (no transparency on apple-touch).
  </acceptance_criteria>
  <done>The lantern app icon exists at every size the manifest + iOS need.</done>
</task>

<task type="auto">
  <name>Task 2: manifest.webmanifest + index.html redirect + head links on learn.html/index.html</name>
  <files>manifest.webmanifest, index.html, learn.html</files>
  <action>Write `manifest.webmanifest` (valid JSON): name "Awba", short_name "Awba", description one line, start_url "learn.html", scope "./", display "standalone", orientation "portrait", background_color "#131013", theme_color "#131013", lang "en", dir "ltr", and an `icons` array with the 192 (any), 512 (any), 512 maskable (purpose "maskable"), each `src` RELATIVE (e.g. "icons/icon-192.png") + correct `sizes`/`type`. Write `index.html`: a minimal valid page with `<meta http-equiv="refresh" content="0; url=learn.html">`, `<link rel="manifest" href="manifest.webmanifest">`, `<meta name="theme-color" content="#131013">`, `<link rel="apple-touch-icon" href="icons/apple-touch-icon-180.png">`, a `<script>location.replace('learn.html')</script>`, and a plain `<a href="learn.html">Continue to Awba</a>` fallback in `<body>` (works with JS off / no meta support). Add to `learn.html` `<head>` (near the existing `theme-color`): `<link rel="manifest" href="manifest.webmanifest">` + `<link rel="apple-touch-icon" href="icons/apple-touch-icon-180.png">` — head only, never the inline script. Commit `feat(07-01): manifest + index redirect + install head links (D-70/PLT-02)`.</action>
  <verify>
    <automated>node -e "JSON.parse(require('fs').readFileSync('manifest.webmanifest','utf8'))" && node -e "const m=JSON.parse(require('fs').readFileSync('manifest.webmanifest','utf8'));const fs=require('fs');['name','short_name','start_url','scope','display','icons'].forEach(k=>{if(!(k in m))throw new Error('missing '+k)});if(m.start_url[0]==='/'||m.scope[0]==='/')throw new Error('absolute path');if(!m.icons.some(i=>/maskable/.test(i.purpose||'')))throw new Error('no maskable');m.icons.forEach(i=>{if(!fs.existsSync(i.src))throw new Error('missing icon '+i.src)});console.log('MANIFEST_OK')" && grep -q 'url=learn.html' index.html && grep -q 'rel="manifest"' index.html && grep -q 'rel="manifest"' learn.html && grep -q 'apple-touch-icon' learn.html && test "$(grep -c 'localStorage' learn.html)" -eq 0 && ! grep -qiE 'poppins|confetti|amber|lantern-gold|gold-bg|fonts\.googleapis' learn.html index.html manifest.webmanifest</automated>
  </verify>
  <acceptance_criteria>
    - Manifest is valid JSON with the required keys, relative start_url/scope, a maskable icon, and every icon src resolving on disk; index.html redirects to learn.html + is installable; learn.html + index.html link the manifest + apple-touch; learn localStorage 0; no gated literal.
  </acceptance_criteria>
  <done>Awba is installable — the base URL redirects into the app, and the manifest declares the lantern family.</done>
</task>

<task type="auto">
  <name>Task 3: scripts/tests/pwa-audit.mjs — the permanent PWA gate</name>
  <files>scripts/tests/pwa-audit.mjs</files>
  <action>Write `scripts/tests/pwa-audit.mjs` (Node, exit-code-first — exit 1 with a printed reason on any failure, print "PWA OK" + exit 0 on success): parse manifest.webmanifest (valid JSON; required keys; start_url/scope relative; a maskable purpose; each icon src exists on disk with the right pixel dimensions where cheaply checkable); assert index.html contains the learn.html redirect + the manifest link; assert learn.html links the manifest + apple-touch. Keep it dependency-free. Run it → PWA OK. Then run the full standing gate set to confirm no regression. Commit `feat(07-01): pwa-audit.mjs — permanent manifest/icon/redirect gate (PLT-02)`.</action>
  <verify>
    <automated>POUT=$(node scripts/tests/pwa-audit.mjs 2>&1); PE=$?; SOUT=$(node --test scripts/tests/*.test.js 2>&1); SE=$?; SMOUT=$(node scripts/tests/render-smoke.mjs 2>&1); SME=$?; test $PE -eq 0 && echo "$POUT" | grep -q 'PWA OK' && test $SE -eq 0 && echo "$SOUT" | grep -qE 'fail 0' && test "$(echo "$SOUT" | grep -oE 'todo [0-9]+' | head -1 | grep -oE '[0-9]+')" -eq 0 && test $SME -eq 0 && echo "$SMOUT" | grep -q 'SMOKE OK' && ! echo "$SMOUT" | grep -q 'SMOKE FAIL' && test "$(grep -c 'localStorage' shared/awba-engine.js)" -eq 13 && test "$(grep -c '@layer tokens, base, components, screens, motion;' shared/awba-engine.css)" -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - pwa-audit.mjs exits 0 with "PWA OK"; the full node suite is fail 0 / todo 0; render-smoke green; engine localStorage 13; @layer order line 1.
  </acceptance_criteria>
  <done>The installable shell is proven by a permanent gate that joins every future run.</done>
</task>

</tasks>

<verification>
- The lantern app-icon family (192/512/maskable/apple-touch) + source SVG generated gold-on-#131013; manifest.webmanifest valid, relative-pathed, maskable, icons-on-disk; index.html redirects + is installable; learn.html/index.html carry the manifest + apple-touch head links; pwa-audit.mjs is a permanent gate.
- Full suite fail 0 / todo 0; render-smoke + all standing gates green; learn localStorage 0; engine localStorage 13; @layer ×1; glyphCount 13; no gated literal; relative paths only; no build step.
</verification>

<output>
Create `.planning/phases/07-pwa-shell-offline-delivery/07-01-SUMMARY.md` when done.
</output>
