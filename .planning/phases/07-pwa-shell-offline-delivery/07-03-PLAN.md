---
phase: 07-pwa-shell-offline-delivery
plan: 03
type: execute
wave: 3
executor_hint: opus
depends_on: ["07-01", "07-02"]
files_modified:
  - learn.html
  - shared/awba-engine.js   (only if a generic AW.prefs key is needed for the nudge — NO new localStorage call, no schema bump)
  - README.md
  - .planning/phases/07-pwa-shell-offline-delivery/07-REGRESSION.md
autonomous: true
requirements: [PLT-03, PLT-04, PLT-05]
must_haves:
  truths:
    - "Per D-72: a mercy-toned, dismissible add-to-home nudge on learn.html — Android/Chromium captures `beforeinstallprompt` (preventDefault + stash), shows a gentle inline banner (shipped Athar tokens, no new hex; copy never says free/upgrade/unlock/locked and never nags), fires the stashed prompt on accept; iOS/iPadOS Safari (no beforeinstallprompt, detected without UA-sniffing brittleness where possible) shows a one-line Share → Add to Home Screen hint; the banner is a designed surface (@layer screens, register-correct on the Orbit ground, keyboard-operable native <button>s + the register focus ring, announced politely if surfaced), never a browser-chrome afterthought"
    - "Per D-72: dismissal (and 'already installed'/display-mode standalone) is REMEMBERED so it never re-nags — persisted THROUGH the engine (AW.prefs / AW.S), so learn.html's own localStorage grep stays 0 and shared/awba-engine.js's localStorage grep stays 13 (a new prefs KEY reuses the existing single storage call — NO new localStorage literal, NO schema version bump); shown at most once per uninstalled state, honoring dismissal"
    - "Per D-73 / PLT-04: a repo-root README.md covers the structure (one engine CSS + one engine JS + per-page data files; the shared/ layout), how Josh reviews (open any .html directly over file:// — no server, no build), how to run the content validator + the standing gates, how to test the PWA locally (npx serve / python3 -m http.server — the SW needs http://localhost, never file://), and how to deploy (GitHub Pages static, zero build, relative paths)"
    - "Per D-74 / PLT-05: a 07-REGRESSION.md ship-checklist maps each Gen-3 v1.1–v1.5 owner fix to where it is verified in the shipped code + tests (popup anchoring, footer/hero spacing, review timer teeth, accordion lenses, chest idempotency, TF selection visibility, back-button rules), each item marked verified-by-<test/audit> or carried to the human walk if purely visual; a new assertion is added ONLY where a real coverage gap is found"
    - "Per D-67: the nudge adds NO new hex/token (shipped Athar values only) and NO new glyph (glyphCount 13); no build step; the node suite fail 0 / todo 0; render-smoke/pwa-audit/validate-content/port-audit/contrast/rtl/glyph + the a11y suite exit 0; @layer ×1; engine localStorage 13; learn localStorage 0; no gated literal in learn.html/README/engine; the nudge banner has zero celebration primitive and never appears adjacent to scripture"
  artifacts:
    - path: "learn.html"
      provides: "the mercy-toned dismissible install nudge (beforeinstallprompt + iOS hint), dismissal via AW.prefs"
      contains: "beforeinstallprompt"
    - path: "README.md"
      provides: "structure + file:// review + validator + PWA-local-test + GitHub Pages deploy"
      contains: "file://"
    - path: ".planning/phases/07-pwa-shell-offline-delivery/07-REGRESSION.md"
      provides: "the PLT-05 Gen-3 v1.1–v1.5 regression ship-checklist mapped to coverage"
      contains: "chest idempotency"
---

## Phase Goal
**As a** learner, **I want** a gentle (never nagging) way to keep Awba on my home screen and the confidence the team shipped it right, **so that** the un-loseable promise survives iOS eviction and no old Gen-3 fix regressed.
*(ROADMAP Phase-7 SC2/SC3/SC4 + PLT-03/04/05.)*

<objective>
Ship the finishing layer: a mercy-toned, dismissible add-to-home nudge on learn.html (beforeinstallprompt on Chromium, a Share→Add hint on iOS, dismissal remembered through the engine so learn's localStorage stays 0); a repo-root README (structure / file:// review / validator / PWA-local-test / GitHub Pages deploy); and the PLT-05 Gen-3 v1.1–v1.5 regression ship-checklist mapped to its coverage. No new hex/glyph, no build step, all gates green.

Executor routing: **opus + read `~/.claude/skills/emil-design-eng/SKILL.md` directly** (no Skill tool) — the nudge is a NEW visible surface on the black Orbit ground; it must feel like Awba (calm, warm, gold/crimson tokens, the register focus ring, a real dismiss button), never a generic browser-install popup. The README + regression map are mechanical; opus does all. NEVER add a new localStorage call (route dismissal through AW.prefs), NEVER a celebration primitive on the banner, NEVER scripture-adjacent.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-pwa-shell-offline-delivery/07-CONTEXT.md
@CLAUDE.md
<interfaces>
<!-- locate by content -->
- `shared/awba-engine.js` `AW.prefs` (grep `AW.prefs` / `awba_prefs`) — the prefs wrapper (soundMuted/motion); reuse its single storage call for the nudge-dismissed flag (a new KEY, no new localStorage literal, no version bump). If a generic get/set isn't exposed, add a minimal key through the EXISTING storage call site.
- `learn.html` — the nudge banner mounts in the Orbit shell (`@layer screens` styling, shipped tokens); FrameCoach-style "free" language is banned (owner rule) — mercy-toned copy only. The register `:focus-visible` ring rides in from the engine.
- `matchMedia('(display-mode: standalone)')` + `navigator.standalone` (iOS) — detect already-installed to suppress the nudge; `beforeinstallprompt` for Chromium; iOS Safari lacks it → the Share hint.
- `~/.claude/skills/emil-design-eng/SKILL.md` — the banner's craft.
- Regression sources: `scripts/tests/*.test.js` (runner-review timer, components accordion, the +25-survives-Escape chest, runner-interaction back-button), `a11y-dialogs` (popup anchoring), `contrast-audit`/`a11y-keyboard` (TF selection cue), render-smoke (structure/spacing).
Machine notes: run node tests via glob; ugrep paren-wrap/`--`-separate; RELATIVE paths; gated literals never in comments/strings (incl. the view-transition property name, 'One religion, one thread' scoped checks); hand-edit STATE.md.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: the mercy-toned dismissible install nudge (learn.html + AW.prefs dismissal)</name>
  <files>learn.html, shared/awba-engine.js</files>
  <action>Read the emil SKILL.md. Add to learn.html a designed `@layer screens` nudge banner + a small classic-script controller: suppress entirely when `matchMedia('(display-mode: standalone)').matches || navigator.standalone` (already installed) OR the AW.prefs dismissed flag is set; on Chromium capture `beforeinstallprompt` (preventDefault + stash `e`), reveal the banner, and on the "Add to home" native `<button>` call the stashed `prompt()`; on iOS Safari (no beforeinstallprompt within a short window AND an iOS-ish standalone-capable Safari) show the Share → Add to Home Screen hint variant; a native dismiss `<button>` sets the AW.prefs dismissed flag (through the engine's existing storage call — NO new localStorage literal in learn.html or a new call in the engine's storage site; NO schema bump) and hides the banner. Copy mercy-toned, never free/upgrade/unlock/locked; no celebration primitive; register-correct on Orbit; keyboard-operable + the register focus ring; optionally `AW.announce` on reveal. Commit `feat(07-03): mercy-toned dismissible add-to-home nudge (D-72/PLT-03)`.</action>
  <verify>
    <automated>grep -q 'beforeinstallprompt' learn.html && grep -qiE 'display-mode: standalone|navigator.standalone' learn.html && grep -q 'AW.prefs' learn.html && test "$(grep -c 'localStorage' learn.html)" -eq 0 && test "$(grep -c 'localStorage' shared/awba-engine.js)" -eq 13 && test "$(grep -c '@layer tokens, base, components, screens, motion;' shared/awba-engine.css)" -eq 1 && ! grep -qiE '\bfree\b|upgrade|unlock' learn.html && SMOUT=$(node scripts/tests/render-smoke.mjs 2>&1); test $? -eq 0 && echo "$SMOUT" | grep -q 'SMOKE OK learn.html' && ! echo "$SMOUT" | grep -q 'SMOKE FAIL' && SOUT=$(node --test scripts/tests/*.test.js 2>&1); test $? -eq 0 && echo "$SOUT" | grep -qE 'fail 0' && test "$(echo "$SOUT" | grep -oE 'todo [0-9]+' | head -1 | grep -oE '[0-9]+')" -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - The nudge is designed, mercy-toned, keyboard-operable, suppressed when installed/dismissed, dismissal remembered through AW.prefs; learn localStorage 0; engine localStorage 13; @layer ×1; no free/upgrade language; render-smoke + suite green.
  </acceptance_criteria>
  <done>A gentle, one-time invitation to keep Awba close — never a nag, never louder than the path.</done>
</task>

<task type="auto">
  <name>Task 2: README.md (PLT-04)</name>
  <files>README.md</files>
  <action>Write a repo-root README.md: what Awba Gen-4 is (one line), the structure (index.html redirect → learn.html front door; per-page lesson/review data files calling AwbaLesson/AwbaReview; the single shared/awba-engine.css + shared/awba-engine.js; shared/fonts + shared/img + icons; the manifest + sw.js), how Josh reviews (open any .html directly over file:// — no server, no build, no npm install), how to run the content validator (`node scripts/validate-content.js lessons/*.html reviews/*.html` + the standing gates: render-smoke/port-audit/contrast-audit/rtl-audit/glyph gate/pwa-audit + `node --test scripts/tests/*.test.js`), how to test the PWA locally (`npx serve` or `python3 -m http.server` then open http://localhost — the SW never registers over file://), and how to deploy (GitHub Pages: push, Pages serves the static files, zero build; relative paths so it works at a project subpath or a custom domain). No gated literal. Commit `docs(07-03): README — structure, file:// review, validator, PWA-local-test, deploy (PLT-04)`.</action>
  <verify>
    <automated>test -s README.md && grep -q 'file://' README.md && grep -q 'validate-content' README.md && grep -qiE 'npx serve|http.server|localhost' README.md && grep -qiE 'github pages|deploy' README.md && ! grep -qiE 'poppins|confetti|lantern-gold|fonts\.googleapis' README.md</automated>
  </verify>
  <acceptance_criteria>
    - README covers structure, file:// review, the validator + gates, local PWA testing, and GitHub Pages deploy; no gated literal.
  </acceptance_criteria>
  <done>Anyone can open, review, validate, test, and ship Awba from the README alone.</done>
</task>

<task type="auto">
  <name>Task 3: 07-REGRESSION.md — the PLT-05 Gen-3 v1.1–v1.5 ship-checklist</name>
  <files>.planning/phases/07-pwa-shell-offline-delivery/07-REGRESSION.md</files>
  <action>Write 07-REGRESSION.md mapping each Gen-3 owner fix to its live coverage, running the referenced check to confirm: popup anchoring (a11y-dialogs + learn render — the .npop placePop edge-clamp), footer/hero spacing (render-smoke structure + the @layer screens .foot/.hero — pure pixel spacing is a human-walk item, note it), review timer teeth (runner-review tests: 14s + .low + timeout), accordion lenses (components tests: .lens.open > .lb), chest idempotency (the +25-survives-Escape / __awbaClaimChest write-once test), TF selection visibility (a11y-keyboard aria-pressed + contrast-audit 3px border), back-button rules (runner-interaction: no back in reviews, lesson back advances). For each: state PASS (verified-by-X, with the command run) or GAP → add a minimal pinning assertion to the relevant test file and re-run. Then run the FULL gate set. Commit `docs(07-03): PLT-05 Gen-3 regression ship-checklist mapped + verified`.</action>
  <verify>
    <automated>test -s .planning/phases/07-pwa-shell-offline-delivery/07-REGRESSION.md && grep -qi 'chest idempotency' .planning/phases/07-pwa-shell-offline-delivery/07-REGRESSION.md && grep -qi 'back' .planning/phases/07-pwa-shell-offline-delivery/07-REGRESSION.md && POUT=$(node scripts/tests/pwa-audit.mjs 2>&1); PE=$?; SOUT=$(node --test scripts/tests/*.test.js 2>&1); SE=$?; SMOUT=$(node scripts/tests/render-smoke.mjs 2>&1); SME=$?; test $PE -eq 0 && echo "$POUT" | grep -q 'PWA OK' && test $SE -eq 0 && echo "$SOUT" | grep -qE 'fail 0' && test "$(echo "$SOUT" | grep -oE 'todo [0-9]+' | head -1 | grep -oE '[0-9]+')" -eq 0 && test $SME -eq 0 && echo "$SMOUT" | grep -q 'SMOKE OK' && ! echo "$SMOUT" | grep -q 'SMOKE FAIL'</automated>
  </verify>
  <acceptance_criteria>
    - Every Gen-3 v1.1–v1.5 regression item is mapped to live coverage (PASS verified-by-X or a newly-added pinning assertion); full suite fail 0 / todo 0; pwa-audit + render-smoke green.
  </acceptance_criteria>
  <done>The Gen-3 owner fixes are proven intact before ship — nothing regressed.</done>
</task>

</tasks>

<verification>
- The mercy-toned dismissible install nudge (beforeinstallprompt + iOS hint, dismissal via AW.prefs, register-correct, keyboard-operable); README (structure/file:// review/validator/PWA-local-test/deploy); 07-REGRESSION.md mapping every Gen-3 fix to coverage.
- Full suite fail 0 / todo 0; pwa-audit + render-smoke + all standing gates green; learn localStorage 0; engine localStorage 13; @layer ×1; glyphCount 13; no new hex/glyph; no gated literal; no free/upgrade language; no build step.
</verification>

<output>
Create `.planning/phases/07-pwa-shell-offline-delivery/07-03-SUMMARY.md` when done.
</output>
