---
phase: 05-learn-page-cross-page-view-transitions
plan: 02
type: execute
wave: 2
executor_hint: opus
depends_on: ["05-01"]
files_modified:
  - learn.html
  - shared/awba-engine.css
  - scripts/port-audit.mjs
autonomous: true
requirements: [LRN-01, LRN-05]
must_haves:
  truths:
    - "Per D-53: learn.html exists at repo ROOT (asset paths shared/awba-engine.css and shared/awba-engine.js with NO ../ prefix — it mirrors preview.html's head, never a lessons/ head) and renders ONE .reg-orbit screen composed top→bottom: HUD marginalia → Ring hero → streak strip + constellation → continue card → daily ayah; render-smoke loads it with zero console errors and a reg-orbit root"
    - "Per D-53: the Ring hero calls AW.ringSVG({ atomsDone: AW.atomsDone(AW.state()), size: 300 }) with NO animateFrom so the established ring renders fully STATIC on load (law 9 — the draw belongs to lesson-complete only, never a home-visit replay); it is the only macro map, the composition carries no second ring or big progress bar"
    - "Per D-60: the HUD is transparent .ls-hud marginalia — a course chip (mini cream Farag square + active-course scene icon), a returns .hstat (flame glyph + count), a noor .hstat (spark glyph + count), and the shared 44px mute toggle via AW.muteBtnHtml()/AW.bindMuteBtn(); no pill fills, and the streak band is re-voiced to a quiet Courier line 'N returns · your streak never breaks' + an AW.weekCal() constellation (lit gold on-days, never a gap/red/miss)"
    - "Per D-59: the 7-verse DAILY pool is spliced BYTE-VERBATIM from Gen-3 learn.html (˹ ˺ brackets intact, never retyped) and a port-audit DAILY SHA gate proves byte-identity; rotation uses AW.dailyIndex(new Date(), DAILY.length) (day-of-year), the ayah renders under scripture law (Amiri Quran, --go:0, strongest ink + the one permitted glow, translation + source line + pending pill), NOTHING celebratory adjacent, and it never carries a view-transition-name"
    - "The UNITS node data ports verbatim from Gen-3 (id/href/label/review/chest) with the retired color field DROPPED; the continue card points at the first 'active' node from AW.deriveNodeState(flat, AW.state()) with the English unit title as the chapter-term (R-6 — Arabic terms are owner-sourced, never invented); learn.html contains no raw localStorage and no gated literal; the suite stays green (fail 0, tests >= 98)"
  artifacts:
    - path: "learn.html"
      provides: "the Orbit front-door page — head, UNITS/DAILY data, HUD, Ring hero, streak/constellation, continue card, daily ayah"
      contains: "reg-orbit"
      min_lines: 120
    - path: "shared/awba-engine.css"
      provides: "new @layer screens content for the continue card, constellation, course chip and daily-ayah card (order line untouched)"
    - path: "scripts/port-audit.mjs"
      provides: "a DAILY-pool byte-fidelity SHA gate vs the Gen-3 source"
      contains: "DAILY"
  key_links:
    - from: "learn.html Ring hero + --dawn"
      to: "AW.atomsDone(AW.state()) / AW.ringSVG / AW.skyDawn"
      via: "the static ring reads the 61-atom map (05-01); no animateFrom"
      pattern: "AW\\.ringSVG\\("
    - from: "learn.html daily ayah"
      to: "the spliced DAILY pool + AW.dailyIndex"
      via: "day-of-year rotation over a byte-verbatim pool, SHA-gated"
      pattern: "AW\\.dailyIndex"
---

## Phase Goal

**As a** learner, **I want to** open Awba's front door and walk a beautiful winding path through the whole Aqeedah course — seeing where I am, what is next, and a daily verse — with calm page-to-page transitions, **so that** returning to learn feels like coming home to a living manuscript, not opening a game.

*(Vertical slice 1 of the walkable page: after this plan a learner double-clicks `learn.html` and the Orbit home renders — their Ring, their streak, where to continue, and today's verse — even before the path and sheets exist.)*

<objective>
Create `learn.html` at repo root and compose the top of the Orbit home per the ATHAR "day in Awba" moment 1: HUD marginalia → the static Ring hero → the re-voiced streak strip + constellation → the navy continue card → the daily ayah under scripture law. Port the UNITS node data (verbatim, color dropped) and the DAILY verse pool (byte-verbatim, SHA-gated) so the continue card and ayah are truthful. Every surface consumes shipped primitives; the only new CSS is `@layer screens` composition.

Purpose: deliver LRN-01 (HUD, streak, daily-ayah card) + LRN-05 (day-of-year ayah) as a real, smoke-passing front door — the spine the path (05-03), popup/nav (05-04) and sheets/chest (05-05) hang from.
Output: `learn.html`, new `@layer screens` content, the port-audit DAILY gate.

Executor routing: **opus** (flagship visual composition + CSS). Before starting, READ `~/.claude/skills/emil-design-eng/SKILL.md` directly (you have no Skill tool). This must read as the SAME product as `preview.html`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-learn-page-cross-page-view-transitions/05-UI-SPEC.md
@.planning/phases/05-learn-page-cross-page-view-transitions/05-PATTERNS.md
@.planning/phases/05-learn-page-cross-page-view-transitions/05-CONTEXT.md

<interfaces>
<!-- Locate by content; line numbers drift. -->
Root-page head analog: `preview.html` head (root-relative `shared/awba-engine.css` + `shared/awba-engine.js`, preload readex-pro-400 + amiri-quran-400, theme-color #131013 for the Orbit ground). NOT the `../`-prefixed lessons/ head.
Body shape: `<main class="reg-orbit" id="app">…</main>` + ONE inline classic `<script>` holding UNITS, DAILY, render() and wiring (it is a page, not an AwbaLesson cfg).
Shipped seams: `AW.state()`, `AW.deriveNodeState(flat, progress)`, `AW.ringSVG({atomsDone,size})` (omit animateFrom ⇒ static, law 9), `AW.skyDawn` (boot stamps --dawn already — the page needs only the .reg-orbit root), `AW.weekCal()` (DOM-free `[{label,on}]`), `AW.icon(name,{size,label})`, `AW.UNIT_ICON` `{u1:compass,u2:lanterns,u3:kaaba,u4:mosque}`, `AW.muteBtnHtml()`/`AW.bindMuteBtn(refresh)` (exposed in 05-01), `AW.dailyIndex(date,poolLen)` (05-01), `AW.atomsDone(progress)` (05-01), `AW.sheet(html)` (singleton, append-to-body) for the ayah cite tap.
Shipped CSS: `.reg-orbit` (Kiswah ground + grain), `.ls-hud`/`.hstat` (marginalia; `.reg-orbit .hstat{color:var(--paper-62)}` already handles the dark ground), `.scard`(--go:0)/`.ayah` (Amiri Quran strongest ink + `text-shadow:0 0 24px rgba(244,240,247,.30)` glow on dark), `.weekcal .day`/`.day.here` (gold on-day), `--navy`/`--gold`/`--ember`/`--powder`/`--paper-62/45`/`--cream`/`--kiswah`/`--madder`/`--crimson`/`--r-square`/`--r-3`/`--edge-top`/`--edge-line`/`--dur-settle`/`--ease`. The `@layer tokens, base, components, screens, motion;` order line is IMMUTABLE — add content to `@layer screens` only.
Gen-3 source (splice/verbatim target): `/Users/theshumba/Downloads/AWBA APP/_MVP-BUILD/learn.html` — UNITS block (id/href/label/review/chest + the retired `color` field to DROP), DAILY block (7 verses, ˹ ˺ intact). hrefs are root-relative (`lessons/u1-m1.html`, `reviews/u1-review.html`) exactly as Gen-3 wrote them.
Copy (verbatim, UI-SPEC §Copywriting): node labels, unit titles/descs, streak strip "{N} returns · your streak never breaks", daily-ayah kicker "A little light for today · {ref}", source line "The Clear Quran · Dr. Mustafa Khattab · pending review", pill "unverified · pending review", continue card kicker "Continue" / "Circuit {n} · {chapter-term}" / button "Continue the path".
port-audit: `SOURCE_ROOT='/Users/theshumba/Downloads/AWBA APP/_MVP-BUILD'`, `sha256()`, `CFG_RE`/`listPages` (grep those) — add a SEPARATE root-file DAILY compare (not via listPages).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: learn.html scaffold + UNITS data port + HUD marginalia</name>
  <files>learn.html, shared/awba-engine.css</files>
  <read_first>
    - `~/.claude/skills/emil-design-eng/SKILL.md` (design-engineering craft — read the file directly).
    - 05-UI-SPEC.md §S1 (HUD marginalia contract) + §0 (Engine Seams) + §Copywriting (node labels, unit titles).
    - 05-PATTERNS.md "page shell + head" (the preview.html root-relative head analog + the anti-pattern list) and "HUD marginalia + mute toggle" (the setHUD .ls-hud/.hstat idiom).
    - `preview.html` head (copy the root-relative structure) and Gen-3 `_MVP-BUILD/learn.html` UNITS block (drop the color field on port).
  </read_first>
  <action>Create `learn.html` at repo root: a `preview.html`-shaped head (root-relative `shared/awba-engine.css` + `shared/awba-engine.js` — NO `../`; preload `shared/fonts/readex-pro-400.woff2` + `shared/fonts/amiri-quran-400.woff2` crossorigin; `theme-color #131013`; viewport-fit=cover), `<body>` with `<main class="reg-orbit" id="app"></main>` and ONE inline classic `<script>`. In the script: port the `UNITS` array verbatim from Gen-3 (each node's id/href/label + review:true/chest:true) and DROP the retired `color` field; build `flat` by flattening UNITS nodes. Render the HUD as a transparent `.ls-hud` marginalia row (reuse — do NOT re-author): left = a course chip (a mini cream Farag square, `--r-square` + `--sp-2` `--rule` frame, holding the active-course scene icon via `AW.icon`); right cluster = a returns `.hstat` (`AW.icon('flame')` + `AW.state().returns`), a noor `.hstat` (`AW.icon('spark')` + `AW.state().noor`), and the shared mute toggle `AW.muteBtnHtml()` bound with `AW.bindMuteBtn(refresh)`. Add ONLY the small new course-chip class to `@layer screens` (never re-declare the order line). The HUD taps (returns/noor/chip → sheets) are wired in 05-05 — render them now, do not fake a sheet. Emit NO gated literal (poppins/confetti/amber/lantern-gold/PERFECT/class="combo"/gold-bg/fonts.googleapis/rgba(37,54,/--accent) and NO raw localStorage (all state via AW.state/AW.S — D-24). Confirm render-smoke picks up learn.html and it loads clean.</action>
  <verify>
    <automated>test -f learn.html && grep -qE 'class="[^"]*reg-orbit' learn.html && grep -q 'shared/awba-engine.js' learn.html && ! grep -q '\.\./shared/awba-engine' learn.html && ! grep -q 'localStorage' learn.html && ! grep -qiE 'confetti|class="perfect"|class="combo"|poppins|fonts\.googleapis|lantern-gold|gold-bg|amber' learn.html && ! grep -qE 'rgba\(37,54,' learn.html && ! grep -q -- '--accent' learn.html && test "$(grep -c '@layer tokens, base, components, screens, motion;' shared/awba-engine.css)" -eq 1 && node scripts/tests/render-smoke.mjs 2>&1 | grep -q 'SMOKE OK learn.html\|learn.html' && ! node scripts/tests/render-smoke.mjs 2>&1 | grep -q 'SMOKE FAIL'</automated>
  </verify>
  <acceptance_criteria>
    - `learn.html` exists at root with a root-relative head (no `../`) and a `<main class="reg-orbit">` root; render-smoke loads it with zero console errors.
    - UNITS ported verbatim with `color` dropped; the HUD is `.ls-hud` marginalia with the shared mute toggle; no pill fills.
    - No gated literal, no raw `localStorage` in learn.html; the `@layer` order line count stays 1.
  </acceptance_criteria>
  <done>The Orbit page exists and boots clean with its data and marginalia HUD.</done>
</task>

<task type="auto">
  <name>Task 2: Ring hero (static) + streak strip + constellation + continue card</name>
  <files>learn.html, shared/awba-engine.css</files>
  <read_first>
    - `~/.claude/skills/emil-design-eng/SKILL.md`.
    - 05-UI-SPEC.md §S1 (Ring hero STATIC, streak/constellation, continue-card navy panel + copy) + §Color (Orbit reserved-for list: gold/ember/powder/navy only; crimson banned on Orbit).
    - 05-PATTERNS.md "Ring hero, STATIC on load" + "streak constellation from AW.weekCal()".
    - `shared/awba-engine.css` `.ring`/`.rw-ring .ring` (ring frame sizing) + `.weekcal .day`/`.day.here` (constellation dot precedent).
  </read_first>
  <action>Below the HUD render, in composition order: (1) the Ring hero — `document.getElementById('ringHost').innerHTML = AW.ringSVG({ atomsDone: AW.atomsDone(AW.state()), size: 300 })` with NO `animateFrom` (fully static, law 9); centre it with `--sp-8` breathing in a new `@layer screens` hero frame mirroring `.rw-ring .ring`'s width cap. (2) the streak strip — a quiet Courier line `"{returns} returns · your streak never breaks"` (`--paper-62`, `--fs-marg`) directly under the Ring + a constellation row built from `AW.weekCal()`: each `{label,on}` → a small round dot, `on` = lit `--gold`, off = un-lit `--powder`/`--paper-45` (NEVER a gap/red/miss). (3) the continue card — a `--navy` panel (cream text, `--r-3`, `--edge-top`, never a drop shadow on dark): find the first `active` node via `AW.deriveNodeState(flat, AW.state())`; render Courier kicker "Continue", `"Circuit {n} · {chapter-term}"` where `{n}` is that node's unit number and `{chapter-term}` = the English unit title in a cream Farag square (R-6 — no Arabic term is supplied, never invent one), the next node's label (`--fs-ui`), and a gold-ghost `.btn` "Continue the path" whose href is the active node's href. When no lesson is active (course complete), point the card at the final review or a quiet "You've walked the whole path" (Courier) — no celebration primitive here. Reserve ALL new classes to `@layer screens`. NO celebration primitive (`dab`/`thread`/`plate`/`rosette`) anywhere in the HUD/Ring/streak/continue region.</action>
  <verify>
    <automated>grep -q 'AW.ringSVG(' learn.html && ! grep -qE 'AW\.ringSVG\([^)]*animateFrom' learn.html && grep -q 'AW.weekCal' learn.html && grep -q 'AW.deriveNodeState' learn.html && test "$(grep -c '@layer tokens, base, components, screens, motion;' shared/awba-engine.css)" -eq 1 && node scripts/tests/render-smoke.mjs 2>&1 | grep -q 'learn.html' && ! node scripts/tests/render-smoke.mjs 2>&1 | grep -q 'SMOKE FAIL' && node --test scripts/tests/*.test.js 2>&1 | grep -qE 'fail 0' && test "$(node --test scripts/tests/*.test.js 2>&1 | grep -oE 'tests [0-9]+' | head -1 | grep -oE '[0-9]+')" -ge 98</automated>
  </verify>
  <acceptance_criteria>
    - The Ring hero renders via `AW.ringSVG` with NO `animateFrom` (static); it is the only ring on the page.
    - The streak strip + `AW.weekCal()` constellation render (lit gold on-days, no miss state); the continue card is a navy panel pointing at the live `active` node with the English-title chapter-term.
    - No celebration primitive in S1; `@layer` order line count 1; render-smoke clean; suite `fail 0`.
  </acceptance_criteria>
  <done>The learner sees their traced Ring, their streak, and exactly where to continue — all static and truthful.</done>
</task>

<task type="auto">
  <name>Task 3: daily ayah card (scripture law) + DAILY splice + port-audit DAILY SHA gate</name>
  <files>learn.html, shared/awba-engine.css, scripts/port-audit.mjs</files>
  <read_first>
    - `~/.claude/skills/emil-design-eng/SKILL.md`.
    - 05-UI-SPEC.md §S2 (daily-ayah scripture-law contract) + §Typography "Scripture law" + §Copywriting (kicker/source line/pill).
    - 05-PATTERNS.md "daily ayah under scripture law" (the .scard/--go:0/.ayah glow + AW.sheetRef cite tap) + "DAILY-pool byte-fidelity" (the CFG_RE/sha256 idiom to reuse).
    - Gen-3 `_MVP-BUILD/learn.html` DAILY block (the 7 verses to splice byte-for-byte) and its `DAILY[getDate()%…]` line (the bug being retired).
  </read_first>
  <action>Splice the 7-verse `DAILY` pool byte-verbatim from the Gen-3 source into learn.html's inline script (copy the bytes — the ˹ ˺ brackets intact; NEVER retype scripture). Render the day's verse via `const ayah = DAILY[AW.dailyIndex(new Date(), DAILY.length)]` in a new `@layer screens` ayah card on the Orbit ground: `--go:0` (no grain over scripture, no `.grain` pseudo), a Courier kicker "A little light for today · {ref}" (spark glyph), the ayah in `.ayah` Amiri Quran ≥24px lh 1.9 `lang="ar" dir="rtl"` `unicode-bidi:isolate` with strongest ink (cream/moonmilk on Kiswah) + the one permitted glow, the translation beneath in `--fs-body --paper-85` (carries ˹ ˺), the source line + the lowercase "unverified · pending review" pill in Courier `--paper-62`. Reveal = a quiet opacity fade (≤300ms, one pass, `--ease`) — reverent, not celebratory; NOTHING celebratory in or beside the card (grep-gated); NEVER a `view-transition-name` on the ayah. Tap → `AW.sheet` rendering the cite face-split (`.r-src`/`.r-ar.ayah`/`.r-mean`/`.r-ref`/`.r-pill`) for the day's verse — Qur'an, so pending pill only, no grade pill. THEN add a `checkDailyFidelity()` function to `scripts/port-audit.mjs`: define a `DAILY_RE` that captures the `const DAILY=[ … ];` region in both learn.html and the Gen-3 source, SHA-256 each, and print `DAILY BYTES OK` / `DAILY BYTES DRIFT` (exit non-zero on drift) — a separate root-file compare, not via `listPages`.</action>
  <verify>
    <automated>grep -q 'AW.dailyIndex(new Date()' learn.html && grep -qE 'class="[^"]*ayah' learn.html && grep -q 'unverified · pending review' learn.html && ! grep -qE '(dab|thread|plate|rosette|view-transition-name)' learn.html && grep -q 'DAILY' scripts/port-audit.mjs && node scripts/port-audit.mjs 2>&1 | grep -q 'DAILY BYTES OK' && ! node scripts/port-audit.mjs 2>&1 | grep -qiE 'DAILY BYTES DRIFT' && node scripts/tests/render-smoke.mjs 2>&1 | grep -q 'learn.html' && ! node scripts/tests/render-smoke.mjs 2>&1 | grep -q 'SMOKE FAIL'</automated>
  </verify>
  <acceptance_criteria>
    - The DAILY pool is byte-verbatim (port-audit `DAILY BYTES OK`); rotation is `AW.dailyIndex` (day-of-year).
    - The ayah renders under scripture law (Amiri Quran, --go:0, glow, translation + source + pending pill); tap opens the cite sheet; nothing celebratory adjacent; no `view-transition-name` on scripture.
    - render-smoke clean over learn.html; the DAILY gate exits non-zero on any drift.
  </acceptance_criteria>
  <done>The daily verse rotates correctly, reverently, and provably verbatim — the day's little light.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author content → innerHTML | node labels + spliced verbatim scripture are trusted authored strings; no user/URL input is interpolated on this page |
| progress storage → render | reads via `AW.state()`/`AW.S` only (D-24); no direct `awba_*` key access, no writes this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02a | Tampering | scripture byte-fidelity | mitigate | the port-audit DAILY SHA gate fails on any byte-drift of the spliced pool (incl. the ˹ ˺ brackets); scripture is spliced, never retyped |
| T-05-02b | Information disclosure | CDN/font leakage | mitigate | the head is root-relative self-hosted; the gated-literal grep (`fonts.googleapis`) proves no CDN request survives |
| T-05-02c | Tampering | DOM XSS via innerHTML | mitigate | only author-controlled strings (labels, verbatim scripture) are injected; no user input reaches any sink on this page |
| T-05-SC | Tampering | npm installs | accept | zero packages; no `[ASSUMED]`/`[SUS]` package to gate |
</threat_model>

<verification>
- render-smoke loads root `learn.html` with zero console errors + a `reg-orbit` root; the Ring is static (no `animateFrom`).
- port-audit `DAILY BYTES OK`; `AW.dailyIndex` drives rotation; no celebration primitive / no `view-transition-name` on the ayah.
- No gated literal, no raw `localStorage` in learn.html; `@layer` order line count 1; suite `fail 0` (tests >= 98).
</verification>

<success_criteria>
- LRN-01 (HUD, streak band, daily-ayah card) + LRN-05 (day-of-year ayah) render as a real, smoke-passing Orbit front door.
- The page reads as the same calm product as `preview.html`; the Ring is THE map and nothing competes with it.
</success_criteria>

<output>
Create `.planning/phases/05-learn-page-cross-page-view-transitions/05-02-SUMMARY.md` when done.
</output>
