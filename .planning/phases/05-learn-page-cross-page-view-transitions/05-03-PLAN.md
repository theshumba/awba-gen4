---
phase: 05-learn-page-cross-page-view-transitions
plan: 03
type: execute
wave: 3
executor_hint: opus
depends_on: ["05-02"]
files_modified:
  - learn.html
  - shared/awba-engine.css
autonomous: true
requirements: [LRN-02, LRN-04, CNT-03]
must_haves:
  truths:
    - "Per D-53: four unit-header cards render identity via chapter-term + AW.UNIT_ICON scene icon (u1 compass / u2 lanterns / u3 kaaba / u4 mosque) with NO unit colour-coding — a cream Farag-square-fronted card carrying the English unit title + desc verbatim; the Gen-3 u.color gradient and the 'SECTION 1 · UNIT n' eyebrow stay retired"
    - "Per D-54: node states derive live from AW.deriveNodeState(flat, AW.state()) mapped to the shipped thermal data-state grammar — locked→not-yet (hollow powder ring + lock glyph, gentle, no breathe), active/available→progress (ember half-dab + the ONLY path ambient, a quiet breathe reduced-motion stills), done lesson→mastered (filled gold + keyline + check + Courier star count + a single sprout stamp per R-3), done review→gold rosette seal; review nodes are gold rosette-framed (never crimson — banned on Orbit), chest nodes are the plate-framed gift node (no IC_CHEST art)"
    - "Per D-54/LRN-04: the winding path is a hand-inked journey — real DOM <button> nodes laid with alternating offsets (NOT Duolingo circles) connected by one continuous SVG thread <path> between node centres (recomputed after layout on load + resize/ResizeObserver); the earned portion re-inks gold via the shipped .thread + ink-draw over stroke-dashoffset and is STATIC on learn-page load (law 9 — no replay; the draw fires only on a genuine unit-complete on the reward page)"
    - "Per CNT-03: walking the storage-derived states over the ported UNITS reproduces the Gen-3 unlock order exactly (strictly sequential incl. m2/m3b/m2b splits; chest opens only when its unit review has stars) — the seam is the SHIPPED AW.deriveNodeState (extended by the state→data-state mapping, never forked); node labels port verbatim"
    - "Per D-61: on load learn.html scrollIntoView the next available node, and honours a #<node-id> hash ONLY after matching it against the known UNITS id set (never interpolating the raw hash into markup); the Ibrahim 14:24 line is a Courier English epigraph spliced from a verified source (R-7), not a second Amiri panel, with nothing celebratory adjacent and no view-transition-name; render-smoke stays clean and the suite stays green (fail 0, tests >= 98)"
  artifacts:
    - path: "learn.html"
      provides: "unit-header cards + the winding node path + node grammar + SVG thread + Ibrahim epigraph + hash auto-scroll"
      contains: "ResizeObserver"
    - path: "shared/awba-engine.css"
      provides: "@layer screens: .path container, winding node/noderow offsets, unit-header card, plate-framed gift node, sprout stamp (order line untouched)"
  key_links:
    - from: "each node button data-state"
      to: "AW.deriveNodeState(flat, AW.state()) state → {mastered|progress|not-yet}"
      via: "the shipped thermal shape grammar consumed, never restyled"
      pattern: "data-state"
    - from: "the earned SVG thread sub-path"
      to: "the shipped .thread class + ink-draw keyframe"
      via: "stroke-dasharray/--len set to earnedLen/totalLen, static on load"
      pattern: "thread"
---

## Phase Goal

**As a** learner, **I want to** open Awba's front door and walk a beautiful winding path through the whole Aqeedah course — seeing where I am, what is next, and a daily verse — with calm page-to-page transitions, **so that** returning to learn feels like coming home to a living manuscript, not opening a game.

*(Vertical slice 2: after this plan the whole journey renders — four unit sections, a winding hand-inked path, and every node showing its true storage-derived state and star count.)*

<objective>
Render the heart of the page: the four unit-header cards (identity via chapter-term + scene icon, no colour-coding), the winding node path drawn as a hand-inked journey (real button nodes + one continuous SVG thread with the earned portion re-inked gold, static on load), the thermal node grammar mapped live from `AW.deriveNodeState` (locked/active/done/review-rosette/chest-plate), the Ibrahim 14:24 Courier epigraph, and the D-61 auto-scroll (next node + validated hash).

Purpose: LRN-02 (live node states), LRN-04 (the path as a real visual journey), CNT-03 (unlock order verified by walking storage-driven states) — the navigable index below the Ring.
Output: the path + node grammar + unit headers + Ibrahim line in `learn.html`, plus their `@layer screens` composition CSS.

Executor routing: **opus** (the flagship winding-path geometry is the one discretionary design surface). Before starting, READ `~/.claude/skills/emil-design-eng/SKILL.md` directly. The path must read as a hand-inked journey on black, NOT alternating circles re-skinned.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-learn-page-cross-page-view-transitions/05-UI-SPEC.md
@.planning/phases/05-learn-page-cross-page-view-transitions/05-PATTERNS.md
@.planning/phases/05-learn-page-cross-page-view-transitions/05-CONTEXT.md

<interfaces>
<!-- Locate by content; line numbers drift. -->
Node-state seam: `AW.deriveNodeState(flat, AW.state())` → `[{id,state}]`, state ∈ locked|available|active|done. Map: done→`mastered`, active/available→`progress`, locked→`not-yet`.
Shipped thermal grammar (consume, do NOT restyle) — `shared/awba-engine.css`: `[data-state="not-yet"]` (hollow, border `--powder` on Orbit), `[data-state="progress"]` (ember half-dab `linear-gradient(90deg,var(--st) 50%,transparent)`), `[data-state="mastered"]` (filled `--gold`, dark check). `.ls-dab` sizing shell + the runner `paintProg()` idiom (grep `paintProg`) show the "state → data-state span + check glyph" pattern.
Celebration primitives (shipped): `.rosette` (review seal / review-node frame), `.plate` (folk keyline for the chest gift node), `.thread` (`stroke:var(--gold)` + `animation:ink-draw` over `stroke-dashoffset`), `@keyframes ink-draw` (grep `ink-draw`). Glyphs via `AW.icon`: `lock`, `star`, `check`, `trophy`, `chest`, `spark`. Unit scene icons via `AW.UNIT_ICON` through `AW.icon`.
Motion tokens: `--dur-drift` (node entrance), `--dur-amb` (the single active-node breathe), `--dur-draw`, `--ease`. `AW.reducedMotion()` self-guards; the shipped dual-trigger reduced-motion block already stills ambients.
Layout APIs (universal on target): `getBoundingClientRect()`, `ResizeObserver`, `Element.scrollIntoView({block:'center'})`.
Copy (verbatim, UI-SPEC §Copywriting): unit titles/descs (U1 "The Foundation" / "What belief is, and how to hold it", U2 "The Drift" / …, U3 "The Heart of It: Tawhid" / …, U4 "The Pillars" / …), node labels (already in the ported UNITS from 05-02); Ibrahim 14:24 = a Courier English translation-of-meaning fragment (the "good word / good tree" line) spliced verbatim from a verified Clear Quran source (R-7 — never generated).
Security (V5): the `#<node-id>` hash is used ONLY to match a known UNITS id and as a `scrollIntoView` target — never interpolated into markup.
The `@layer` order line is IMMUTABLE — add to `@layer screens` only. learn.html carries no raw localStorage / no gated literal.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: four unit-header cards (chapter-term + scene icon, no colour-coding)</name>
  <files>learn.html, shared/awba-engine.css</files>
  <read_first>
    - `~/.claude/skills/emil-design-eng/SKILL.md`.
    - 05-UI-SPEC.md §S3 "Unit headers" + Ruling R-6 (Aref-Ruqaa term is owner-sourced → English-title Farag-square fallback) + §Color (Orbit palette; crimson only inside a cream Page object).
    - 05-PATTERNS.md "node grammar" (glyph/AW.UNIT_ICON accessors) + the anti-pattern list (no u.color, no SECTION eyebrow).
  </read_first>
  <action>Render one header per unit as a cream Farag-square-fronted card in `@layer screens` (the card `--r-3` + `--edge-top`; the front square `--r-square`): the unit scene icon via `AW.UNIT_ICON` (`--icon-accent` gold on the dark card); the chapter-term in the cream Farag square = the English unit title (Aref Ruqaa mechanism specified, but NO Arabic term is supplied — R-6, never invent one, use the English title in the square); the English title (`--fs-h2`) + desc (`--fs-body --paper-62`), all verbatim from the copy table. Do NOT reintroduce the Gen-3 `u.color` gradient or the "SECTION 1 · UNIT n" eyebrow. Each header precedes its unit's node section (built in Task 2). No celebration primitive in the header.</action>
  <verify>
    <automated>grep -q 'AW.UNIT_ICON' learn.html && ! grep -qE 'SECTION 1|data-unit|u\.color' learn.html && ! grep -q -- '--accent' learn.html && test "$(grep -c '@layer tokens, base, components, screens, motion;' shared/awba-engine.css)" -eq 1 && node scripts/tests/render-smoke.mjs 2>&1 | grep -q 'learn.html' && ! node scripts/tests/render-smoke.mjs 2>&1 | grep -q 'SMOKE FAIL'</automated>
  </verify>
  <acceptance_criteria>
    - Four unit-header cards render identity via `AW.UNIT_ICON` scene icon + English-title chapter-term in a cream Farag square; no unit colour-coding, no SECTION eyebrow.
    - `@layer` order line count 1; render-smoke clean.
  </acceptance_criteria>
  <done>Each unit announces itself by chapter-term + icon, in the calm Athar voice.</done>
</task>

<task type="auto">
  <name>Task 2: the winding node path + thermal node grammar (live states)</name>
  <files>learn.html, shared/awba-engine.css</files>
  <read_first>
    - `~/.claude/skills/emil-design-eng/SKILL.md`.
    - 05-UI-SPEC.md §S3 "The winding path" + the node-grammar state table + "Node entrance motion".
    - 05-PATTERNS.md "node grammar: thermal data-state shapes" (the shipped [data-state] rules + paintProg idiom) + "node-state derivation — CONSUME, do not fork".
    - `shared/awba-engine.css` `[data-state]` rules + `.rosette` + `.plate` + `.ls-dab`; `shared/awba-engine.js` grep `paintProg`.
  </read_first>
  <action>For each unit section, render a relative `.path` container with the unit's nodes as real DOM `<button class="node" data-state="…">` laid out with alternating horizontal offsets that read as a continuous winding journey (Claude's discretion on the exact geometry — MUST NOT read as Duolingo alternating circles; 44px minimum each). Derive each node's `data-state` from `AW.deriveNodeState(flat, AW.state())` (done→`mastered`, active/available→`progress`, locked→`not-yet`) — consume the shipped thermal shapes, do NOT restyle them. Per state: locked → hollow powder ring + `AW.icon('lock')` at `--paper-45`, NO breathe; active/available → ember half-dab + a quiet breathe (`--dur-amb`, the ONLY path ambient, `AW.reducedMotion()`-stilled); done lesson → filled gold + a keyline + `AW.icon('check')` + the Courier star count from `AW.state().stars[id]` (1–3) + a single sprout stamp (R-3 — one sprout glyph, NOT the full doodle pool; the pool is logged as the D-55 fast-follow); done review → a gold `.rosette` seal. Review nodes (any state) carry a gold `.rosette` frame (`trophy` glyph when not-yet) — never crimson. Chest nodes render the plate-framed gift node: a folk `.plate` keyline frame + `AW.icon('chest')` + the label ("A gift"/"The course gift") — the Gen-3 white/amber IC_CHEST treasure-box art stays retired. Node entrance = a staggered settle/drift (`--dur-drift`, `animation-delay` ≤300ms total per section, one pass then still); reduced motion → nodes appear in place. Add the `.path`/`.node`/`.noderow`/gift-node/sprout classes to `@layer screens` only. Tap handling (popup) is 05-04 — render the states now.</action>
  <verify>
    <automated>grep -qE 'data-state' learn.html && grep -q 'AW.deriveNodeState' learn.html && grep -q 'rosette' learn.html && grep -q 'plate' learn.html && ! grep -qiE 'IC_CHEST|lantern-gold|amber|mascot' learn.html && test "$(grep -c '@layer tokens, base, components, screens, motion;' shared/awba-engine.css)" -eq 1 && node scripts/tests/render-smoke.mjs 2>&1 | grep -q 'learn.html' && ! node scripts/tests/render-smoke.mjs 2>&1 | grep -q 'SMOKE FAIL' && node --test scripts/tests/*.test.js 2>&1 | grep -qE 'fail 0' && test "$(node --test scripts/tests/*.test.js 2>&1 | grep -oE 'tests [0-9]+' | head -1 | grep -oE '[0-9]+')" -ge 98</automated>
  </verify>
  <acceptance_criteria>
    - Nodes are real `<button>`s laid in a winding journey (not alternating circles); each carries the correct `data-state` from `AW.deriveNodeState`.
    - Locked = hollow powder + lock (no breathe); active = ember half-dab + the single breathe (reduced-motion stilled); done lesson = gold + check + Courier stars + one sprout; done review = gold rosette; chest = plate-framed gift node (no IC_CHEST art); review nodes rosette-framed (no crimson).
    - No retired literal; `@layer` order line count 1; render-smoke clean; suite `fail 0`.
  </acceptance_criteria>
  <done>Every node shows its true storage-derived state — the whole journey is legible at a glance.</done>
</task>

<task type="auto">
  <name>Task 3: the SVG thread + earned gold re-ink + Ibrahim epigraph + hash auto-scroll</name>
  <files>learn.html, shared/awba-engine.css</files>
  <read_first>
    - `~/.claude/skills/emil-design-eng/SKILL.md`.
    - 05-UI-SPEC.md §S3 "The winding path" (SVG thread + earned re-ink STATIC on load) + "Ibrahim 14:24 line" + Ruling R-7 + §S6 reduced-motion inventory (earned path static on load).
    - 05-PATTERNS.md "the winding path: SVG thread + earned gold re-ink" (the .thread-on-a-sub-path precedent + recompute-after-layout note) + Gen-3 `_MVP-BUILD/learn.html` (the source for the verbatim Ibrahim fragment / a verified Clear Quran Ibrahim 14:24 source).
  </read_first>
  <action>Add an absolutely-positioned, `pointer-events:none` inline `<svg>` overlay per path section (or one spanning the path) drawing ONE continuous thread `<path>` connecting node centres. Compute the `d` from node `getBoundingClientRect` centres AFTER layout, and recompute on `load` + a `resize`/`ResizeObserver` handler so the winding line follows the nodes 320→desktop. Draw the earned portion as a second sub-path (or the same path clipped) carrying the shipped `.thread` class (`stroke:var(--gold)` + `ink-draw`) with `stroke-dasharray`/`--len` set to the earned fraction `earnedLen/totalLen` (earned = through the last `done`/`mastered` node). On learn-page load the earned gold is STATIC (law 9 — set the dashoffset to its final value without replaying `ink-draw`; the animated draw fires only on a genuine unit-complete on the reward page, not here). Add the Ibrahim 14:24 epigraph beneath the path, above where the tab bar will sit: a Courier English translation-of-meaning fragment (the "good word / good tree" line) in `--fs-marg --paper-62`, centred, with a small ref — spliced verbatim from a verified source (R-7 — NOT a second Amiri panel, never generated), nothing celebratory adjacent, never a `view-transition-name`. Finally wire D-61 auto-scroll: on load, `scrollIntoView({block:'center'})` the next available node; if `location.hash` is present, match `#<id>` against the known UNITS id set and scroll to that node instead — validate against the id set, NEVER interpolate the raw hash into any markup. Reserve new classes to `@layer screens`.</action>
  <verify>
    <automated>grep -q 'ResizeObserver' learn.html && grep -qE 'thread' learn.html && grep -qE 'scrollIntoView' learn.html && grep -qiE 'ibrahim|14:24|14-24' learn.html && ! grep -qE 'view-transition-name' learn.html && test "$(grep -c '@layer tokens, base, components, screens, motion;' shared/awba-engine.css)" -eq 1 && ! grep -q 'localStorage' learn.html && node scripts/tests/render-smoke.mjs 2>&1 | grep -q 'learn.html' && ! node scripts/tests/render-smoke.mjs 2>&1 | grep -q 'SMOKE FAIL' && node --test scripts/tests/*.test.js 2>&1 | grep -qE 'fail 0'</automated>
  </verify>
  <acceptance_criteria>
    - One continuous SVG thread connects node centres, recomputed on load + resize; the earned portion re-inks gold via the shipped `.thread` and is STATIC on load (no replay).
    - The Ibrahim 14:24 Courier epigraph renders (verbatim, sourced, not a second Amiri panel); no `view-transition-name` on it.
    - On load the next available node (or a validated `#<node-id>`) is scrolled into view; no raw hash interpolation; render-smoke clean; suite `fail 0`.
  </acceptance_criteria>
  <done>The path reads as a hand-inked journey with the earned road glowing gold, and returning to the page lands you at the right node.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL hash → scroll target | `#<node-id>` is matched against the known UNITS id set and used only as a `scrollIntoView` target; never interpolated into markup (V5) |
| author content → innerHTML | node labels + the verbatim Ibrahim fragment are trusted authored strings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03a | Tampering | URL-hash DOM injection | mitigate | the `#<node-id>` hash is validated against the UNITS id set and used only as a scroll target; the raw hash never reaches a markup sink |
| T-05-03b | Tampering | node-state derivation drift | mitigate | states come from the SHIPPED `AW.deriveNodeState` (CNT-03-pinned in 05-01); the page only maps state→data-state, never forks the rule |
| T-05-03c | Tampering | scripture generation | mitigate | the Ibrahim epigraph is spliced verbatim from a verified source (R-7); no religious content is generated |
| T-05-SC | Tampering | npm installs | accept | zero packages; no `[ASSUMED]`/`[SUS]` package to gate |
</threat_model>

<verification>
- Node states map from `AW.deriveNodeState`; the winding path + one SVG thread render; the earned portion re-inks gold statically (no replay).
- Unit headers carry no colour-coding; the Ibrahim line is a verbatim Courier epigraph; auto-scroll honours a validated hash.
- No gated literal / no raw `localStorage` in learn.html; `@layer` order line count 1; render-smoke clean; suite `fail 0` (tests >= 98).
</verification>

<success_criteria>
- LRN-02 (live node states) + LRN-04 (the path as a real visual journey) + CNT-03 (unlock order via storage-driven states) render as the navigable index below the Ring.
</success_criteria>

<output>
Create `.planning/phases/05-learn-page-cross-page-view-transitions/05-03-SUMMARY.md` when done.
</output>
