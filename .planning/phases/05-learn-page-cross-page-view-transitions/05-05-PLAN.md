---
phase: 05-learn-page-cross-page-view-transitions
plan: 05
type: execute
wave: 5
executor_hint: opus
depends_on: ["05-04"]
files_modified:
  - learn.html
  - shared/awba-engine.css
autonomous: true
requirements: [LRN-06, LRN-07, RWD-04]
must_haves:
  truths:
    - "Per D-60: streak, noor and course-switcher bottom sheets are ONE shared implementation on the shipped AW.sheet singleton (no duplicated HTML) — the streak sheet (Marcellus returns count + day/days copy + AW.weekCal() week row + the verbatim never-breaks note) opens from the HUD returns stat AND the streak strip AND the Returns tab; the noor sheet opens from the HUD noor stat; the course switcher (Aqeedah ACTIVE + Fiqh/Seerah/Qur'an as polished COMING SOON rows, never disabled-dead) opens from the HUD course chip"
    - "Per D-60/LRN-07: the tab bar has Learn active and Practice/Returns/Profile/More each open a designed coming-soon AW.sheet (scene icon + one calm line) — never a dead tap; Returns → the streak sheet (one implementation); the active-tab cue on Orbit is GOLD (a .reg-orbit .tab.active gold override + 2px gold top-rule in @layer screens, mirroring the shipped .rv-shell gold precedent) because crimson is 2.65:1 on Kiswah (banned) — the shipped crimson .tab.active rule is OVERRIDDEN, never edited"
    - "Per D-56/RWD-04: the chest claim grants +25 noor EXACTLY ONCE via a write-once guard on the chests slot in AW.S (var chests=AW.state().chests; if(!chests[id]){chests[id]=true; AW.S.set('chests',chests); AW.S.set('noor',AW.S.get('noor',0)+25);}) — deterministic, NEVER randomized, idempotent (Gen-3 chest_* keys already migrate); contents are implied before tap (a sure gift, no gambling)"
    - "Per D-56: claiming opens a Festival interstitial OVERLAY (a separate threshold ground, .reg-festival — the learn screen stays Orbit, law 1): the dated folk circuit plate stamps in (.plate, the stamp verb, ~150ms; Rakkas label valid only inside this threshold), CIRCLE crowd-arrival .dab drift-settle around the Ring, that circuit's gold thread arc closes; the plate is private/maker's-marked (date + ring seed, Courier); reduced motion renders plate/dabs/arc final-static and the +25 noor is granted regardless (the mechanic never depends on motion)"
    - "learn.html reads/writes storage ONLY via AW.state()/AW.S (no raw localStorage), carries no gated literal; the @layer order line stays declared exactly once and localStorage grep-count in shared/awba-engine.js stays EXACTLY 13; the suite stays green (fail 0, tests >= 98)"
  artifacts:
    - path: "learn.html"
      provides: "the shared sheet family (streak/noor/switcher) + tab bar wiring + chest claim + Festival interstitial"
      contains: "AW.sheet"
    - path: "shared/awba-engine.css"
      provides: "@layer screens: the .reg-orbit .tab.active gold override, coming-soon row styling, the Festival interstitial composition (order line untouched)"
      contains: "tab.active"
  key_links:
    - from: "HUD returns/noor/chip taps + streak strip + Returns tab"
      to: "the ONE AW.sheet streak/noor/switcher implementation"
      via: "one function per sheet, opened from every surface (no duplicated HTML)"
      pattern: "AW\\.sheet\\("
    - from: "the chest claim (+25 once)"
      to: "AW.S chests slot + noor + the Festival interstitial"
      via: "write-once guard then the threshold overlay; learn screen stays Orbit"
      pattern: "chests"
---

## Phase Goal

**As a** learner, **I want to** open Awba's front door and walk a beautiful winding path through the whole Aqeedah course — seeing where I am, what is next, and a daily verse — with calm page-to-page transitions, **so that** returning to learn feels like coming home to a living manuscript, not opening a game.

*(Vertical slice 4: after this plan every tap is alive — the HUD stats, streak strip, course chip and tabs open their sheets, and claiming a gift grants +25 noor with the Festival circuit-plate threshold.)*

<objective>
Bring the remaining taps alive with ONE shared sheet family and complete the reward mechanic: the streak / noor / course-switcher sheets (all on the shipped `AW.sheet` singleton, opened from every surface), the tab bar (Learn active, gold override on Orbit, every other tab a designed coming-soon, Returns → the streak sheet), and the chest claim (+25 noor exactly once, idempotent) with its Festival circuit-plate interstitial threshold.

Purpose: LRN-06 (one shared sheet implementation + coming-soon switcher) + LRN-07 (no dead taps) + RWD-04 (deterministic idempotent gift).
Output: the sheet family + tab wiring + chest claim + Festival interstitial in `learn.html`, plus the tab gold override + coming-soon + Festival CSS in `@layer screens`.

Executor routing: **opus** (the Festival circuit-plate threshold is a flagship celebratory moment). Before starting, READ `~/.claude/skills/emil-design-eng/SKILL.md` directly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-learn-page-cross-page-view-transitions/05-UI-SPEC.md
@.planning/phases/05-learn-page-cross-page-view-transitions/05-PATTERNS.md
@.planning/phases/05-learn-page-cross-page-view-transitions/05-CONTEXT.md

<interfaces>
<!-- Locate by content; line numbers drift. -->
Sheet singleton: `AW.sheet(html)` / `AW.sheetClose()` (grep `AW.sheet =`) — append-to-body, outside-tap + Esc close, scroll-lock. Shipped `.grip` / `.sheet-row` (press) classes. `AW.weekCal()` → `[{label,on}]` for the streak week row.
Tab bar: shipped `.tab` / `.tab.active` (grep `.tab.active` in CSS) — inks crimson (2.65:1 on Kiswah, BANNED). The gold-override precedent is `.rv-shell` re-inking the Orbit press to gold (grep `.rv-shell` in CSS). Add `.reg-orbit .tab.active { color: var(--gold); }` + a 2px gold top-rule to `@layer screens` — OVERRIDE, never edit the shipped crimson rule.
Chest state: `AW.state().chests` (defensive copy) + `AW.S.get/set` — the write-once guard. Gen-3 `awba_chest_*` already migrates into `chests` (verified in state-storage.test.js). NEVER randomize; +25 exactly once.
Festival primitives (shipped): `.reg-festival` ground (grep `.reg-festival`), `.plate` + `@keyframes stamp` (`--dur-stamp` ~150ms), `.rosette`, the crowd-arrival `.dab` drift precedent `.rv-ringdabs` (grep `.rv-ringdabs`), `AW.ringSeed()` (maker's mark). Rakkas (`--fs-festival`) is valid ONLY inside this Festival threshold, never on the Orbit learn screen. `AW.reducedMotion()` self-guard; `AW.animate` collapses to ~1ms under reduce.
Copy (verbatim, UI-SPEC §Copywriting): streak sheet note "This number can never break and never reset. Every return adds to it, however long the gap. That is the point of this place."; noor sheet note "Light you collect as you learn. It is never spent against you, never dangled, and it never runs out."; switcher "Aqeedah · Level 1" ACTIVE + "Fiqh · Level 1" / "Seerah" / "Qur'an" COMING SOON; coming-soon tab body e.g. "Practice is on its way. For now, keep walking the path." (warm, never "locked"/"upgrade"/"unavailable").
The chest claim hook the popup calls: `window.__awbaClaimChest(nodeId)` (stubbed in 05-04) — implement it here. On success re-render the path so the chest node reads `done`. The `@layer` order line is IMMUTABLE; learn.html uses AW.state/AW.S only (no raw localStorage); `localStorage` count in shared/awba-engine.js stays 13 (no engine JS edit this plan).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: the ONE shared sheet family (streak / noor / course switcher) + HUD taps</name>
  <files>learn.html, shared/awba-engine.css</files>
  <read_first>
    - `~/.claude/skills/emil-design-eng/SKILL.md`.
    - 05-UI-SPEC.md §S4 "The shared sheet family" + §Copywriting (streak/noor/switcher copy verbatim) + §Typography (Marcellus display numerals ≥28px).
    - 05-PATTERNS.md "HUD marginalia + … sheets" (the AW.sheet single-call idiom + `.sheet-row`/`.grip`).
    - `shared/awba-engine.js` grep `AW.sheet =` (the singleton) + `AW.weekCal` (the week row source).
  </read_first>
  <action>Build three sheet-builder functions in learn.html, each a single `AW.sheet(html)` call (no duplicated HTML): (1) the streak sheet — a `.grip`, a Marcellus returns count + "day you came back"/"days you came back" (N===1 vs not), an `AW.weekCal()` week row rendered as `.weekcal .day`/`.day.here` presence dots (never a gap/miss), and the verbatim never-breaks note; (2) the noor sheet — a `.grip`, a Marcellus gold-filled noor count + "noor gathered", and the verbatim noor note; (3) the course switcher — a `.grip`, an "Aqeedah · Level 1" row with an ACTIVE pill (`--olive` ink), then three `.sheet-row.off` COMING SOON rows (Fiqh/Seerah/Qur'an) each with a scene icon + a calm neutral COMING SOON pill (`--ink-62` + `--rule` hairline — never disabled-dead grey). Wire the openers (now made live): the HUD returns `.hstat` AND the streak strip → the streak sheet; the HUD noor `.hstat` → the noor sheet; the HUD course chip → the switcher. Add only the coming-soon-row styling to `@layer screens`. Reads via `AW.state()` only.</action>
  <verify>
    <automated>grep -q 'AW.sheet(' learn.html && grep -q 'AW.weekCal' learn.html && grep -q 'noor gathered' learn.html && grep -qE 'COMING SOON' learn.html && ! grep -q 'localStorage' learn.html && test "$(grep -c '@layer tokens, base, components, screens, motion;' shared/awba-engine.css)" -eq 1 && node scripts/tests/render-smoke.mjs 2>&1 | grep -q 'learn.html' && ! node scripts/tests/render-smoke.mjs 2>&1 | grep -q 'SMOKE FAIL'</automated>
  </verify>
  <acceptance_criteria>
    - Three sheets, each a single `AW.sheet` call (no duplicated HTML); streak opens from HUD returns + streak strip; noor from HUD noor; switcher from the course chip.
    - Switcher shows Aqeedah ACTIVE + three calm COMING SOON rows (not disabled-dead); copy verbatim; `@layer` order line count 1; render-smoke clean.
  </acceptance_criteria>
  <done>The HUD and streak strip are fully tappable through one shared sheet implementation.</done>
</task>

<task type="auto">
  <name>Task 2: the tab bar — Learn active (gold override) + coming-soon sheets, no dead taps</name>
  <files>learn.html, shared/awba-engine.css</files>
  <read_first>
    - `~/.claude/skills/emil-design-eng/SKILL.md`.
    - 05-UI-SPEC.md §S4 "Tab bar" + Ruling R-5 (gold override, not crimson) + §Copywriting (tab labels + coming-soon body).
    - 05-PATTERNS.md "tab bar + the crimson-on-Orbit fix" (the `.rv-shell` gold-override precedent) + 05-RESEARCH.md §Pitfall 4.
    - `shared/awba-engine.css` grep `.tab.active` (the shipped crimson rule to OVERRIDE) + `.rv-shell` (the gold-override precedent).
  </read_first>
  <action>Render a bottom `.tab` row with Learn active (44px min per tab, safe-area inset bottom). Add to `@layer screens` a `.reg-orbit .tab.active { color: var(--gold); }` plus a 2px gold top-rule for the active tab (mirroring the shipped `.rv-shell` gold-override precedent) — because crimson is 2.65:1 on Kiswah (banned). OVERRIDE the shipped crimson `.tab.active`, never edit it (it survives for cream pages). Inactive icons `--paper-62`, `--icon-accent` gold on Orbit. Wire each inactive tab to open a designed coming-soon `AW.sheet` (Practice/Profile/More: a scene icon + one calm verbatim line — warm, never "locked"/"upgrade"); the Returns tab opens the streak sheet (the SAME implementation from Task 1 — one function). Never a dead tap.</action>
  <verify>
    <automated>grep -qE 'reg-orbit \.tab\.active|reg-orbit\.tab\.active' shared/awba-engine.css && grep -qiE 'Practice|Profile|More' learn.html && grep -q 'AW.sheet(' learn.html && test "$(grep -c '@layer tokens, base, components, screens, motion;' shared/awba-engine.css)" -eq 1 && node scripts/tests/render-smoke.mjs 2>&1 | grep -q 'learn.html' && ! node scripts/tests/render-smoke.mjs 2>&1 | grep -q 'SMOKE FAIL' && node --test scripts/tests/*.test.js 2>&1 | grep -qE 'fail 0' && test "$(node --test scripts/tests/*.test.js 2>&1 | grep -oE 'tests [0-9]+' | head -1 | grep -oE '[0-9]+')" -ge 98</automated>
  </verify>
  <acceptance_criteria>
    - The tab bar has Learn active with a GOLD cue on Orbit (override in `@layer screens`, shipped crimson rule untouched); no dead taps.
    - Every inactive tab opens a designed coming-soon `AW.sheet`; Returns → the streak sheet (one implementation); `@layer` order line count 1; suite `fail 0`.
  </acceptance_criteria>
  <done>Every tab gives designed feedback; the active cue is gold, never invisible crimson.</done>
</task>

<task type="auto">
  <name>Task 3: chest claim (+25 once, idempotent) + the Festival circuit-plate interstitial</name>
  <files>learn.html, shared/awba-engine.css</files>
  <read_first>
    - `~/.claude/skills/emil-design-eng/SKILL.md`.
    - 05-UI-SPEC.md §S5 (the chest→circuit-plate claim moment: the write-once guard + the interstitial contract + the reduced-motion behaviour) + §Motion (Festival = stamp; law 1 — the learn screen stays Orbit).
    - 05-PATTERNS.md "chest claim: idempotent +25 noor" (the write-once guard + the Festival primitives) + 05-RESEARCH.md §Pattern 3.
    - `shared/awba-engine.css` grep `.reg-festival`, `.plate`, `@keyframes stamp`, `.rv-ringdabs`; `shared/awba-engine.js` grep `AW.ringSeed` + `AW.reducedMotion` + `AW.animate`.
  </read_first>
  <action>Implement `window.__awbaClaimChest(nodeId)` (the hook the 05-04 popup calls): `var chests = AW.state().chests; if (chests[nodeId]) { return; /* already opened */ } chests[nodeId] = true; AW.S.set('chests', chests); AW.S.set('noor', AW.S.get('noor',0) + 25);` — deterministic, exactly once, NEVER randomized. Then open a Festival interstitial OVERLAY appended to `document.body` on the `.reg-festival` ground (a separate threshold — the learn screen stays `.reg-orbit`, law 1): the dated folk circuit `.plate` stamps in via the `stamp` verb (`AW.animate(..., '--dur-stamp')`; the plate label may use Rakkas `--fs-festival`, valid ONLY inside this threshold); a CIRCLE crowd-arrival of ink `.dab`s drift-settle around a small Ring (mirror the `.rv-ringdabs` drift precedent, staggered `animation-delay`); that circuit's gold thread arc closes (the `draw` verb / `.thread`). Maker's-mark the plate privately with the date + `AW.ringSeed()` in Courier. Guard all motion with `AW.reducedMotion()` — under reduce the plate/dabs/arc render final-static and the +25 noor is granted regardless (the mechanic never depends on motion). Dismiss the overlay → re-render the path so the chest node now reads `done`. Add the interstitial composition CSS to `@layer screens` only. NEVER randomize the reward; the contents are implied before tap (the popup already said "+25 noor").</action>
  <verify>
    <automated>grep -q '__awbaClaimChest' learn.html && grep -qE "AW.S.set\('chests'" learn.html && grep -qE "AW.S.get\('noor'[^)]*\) *\+ *25|noor'[^)]*\)\+25" learn.html && grep -q 'reg-festival' learn.html && grep -q 'AW.ringSeed' learn.html && ! grep -q 'localStorage' learn.html && ! grep -qiE 'Math.random|randomInt' learn.html && test "$(grep -c '@layer tokens, base, components, screens, motion;' shared/awba-engine.css)" -eq 1 && test "$(grep -c 'localStorage' shared/awba-engine.js)" -eq 13 && node scripts/tests/render-smoke.mjs 2>&1 | grep -q 'learn.html' && ! node scripts/tests/render-smoke.mjs 2>&1 | grep -q 'SMOKE FAIL' && node --test scripts/tests/*.test.js 2>&1 | grep -qE 'fail 0'</automated>
  </verify>
  <acceptance_criteria>
    - `__awbaClaimChest` grants +25 noor exactly once via the write-once `chests` guard; a second claim is a no-op; never randomized (no Math.random on the reward path).
    - Claiming opens the `.reg-festival` interstitial overlay (plate stamp + crowd dabs + closing thread arc, maker's-marked); the learn screen stays Orbit; reduced motion renders it final-static with the +25 still granted.
    - `@layer` order line count 1; `localStorage` count in the engine still 13; learn.html has no raw `localStorage`; suite `fail 0`.
  </acceptance_criteria>
  <done>The gift is a sure, once-only +25 with the Festival circuit-plate threshold — a celebration that never touches the Orbit screen or scripture.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chest claim → storage | idempotency lives in the write-once guard against the `chests` slot in `AW.S`; `nodeId` comes from the ported UNITS, never user input |
| author content → innerHTML | sheet + interstitial copy are trusted authored strings; no user input reaches a sink |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05a | Tampering | double-claim / reward inflation | mitigate | the write-once `chests` guard makes the +25 exactly-once and idempotent; a second claim is a no-op (Gen-3 v1.1 fix preserved) |
| T-05-05b | Tampering | randomized reward | mitigate | the reward is a fixed +25; no `Math.random` on the claim path (grep-gated) — a sure gift, no gambling |
| T-05-05c | Tampering | localStorage tampering | accept | device-local cosmetic progress; `AW.S` tolerates corrupt/foreign blobs non-destructively (un-loseable-by-mercy premise) |
| T-05-SC | Tampering | npm installs | accept | zero packages; no `[ASSUMED]`/`[SUS]` package to gate |
</threat_model>

<verification>
- Three sheets on the ONE `AW.sheet` singleton (no duplicated HTML), opened from every surface; the switcher shows calm COMING SOON rows.
- The tab bar has no dead taps and a GOLD active cue on Orbit (override, shipped crimson rule untouched); Returns → the streak sheet.
- The chest grants +25 exactly once (idempotent, never randomized) with the `.reg-festival` interstitial threshold; the learn screen stays Orbit; reduced motion final-static, +25 still granted.
- `@layer` order line count 1; `localStorage` count in the engine 13; no raw `localStorage` / no gated literal in learn.html; suite `fail 0` (tests >= 98).
</verification>

<success_criteria>
- LRN-06 (one shared sheet implementation + coming-soon switcher) + LRN-07 (no dead taps) + RWD-04 (deterministic idempotent gift) render and every tap on the learn page is alive.
</success_criteria>

<output>
Create `.planning/phases/05-learn-page-cross-page-view-transitions/05-05-SUMMARY.md` when done.
</output>
