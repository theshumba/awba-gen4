---
phase: 05-learn-page-cross-page-view-transitions
plan: 06
type: execute
wave: 6
executor_hint: sonnet
depends_on: ["05-05"]
files_modified: []
autonomous: false
requirements: [LRN-01, LRN-02, LRN-03, LRN-04, LRN-05, LRN-06, LRN-07, CNT-03, RWD-04, MOT-02]
must_haves:
  truths:
    - "The full automated gate passes as PRECHECKS before the human is asked: node --test scripts/tests/*.test.js (fail 0, tests >= 98 baseline never shrunk); node scripts/validate-content.js lessons/*.html reviews/*.html → exit 0 with exactly the 3 expected accepted note warnings; node scripts/validate-content.js --self-test → OK"
    - "The port + smoke harnesses pass: node scripts/port-audit.mjs → BYTES OK for all 19 + DAILY BYTES OK (the daily pool byte-verbatim) + holds intact; node scripts/tests/render-smoke.mjs → every page SMOKE OK incl. root learn.html (zero console errors) AND the file:// learn→lessons/u1-m1.html navigation is error-free (MOT-02 guard proven)"
    - "Every grep gate passes: no gated literal in learn.html or shared/ (no fonts.googleapis/confetti/class=\"combo\"/poppins/lantern-gold/gold-bg/amber/rgba(37,54,/--accent); no raw localStorage in learn.html; grep -c localStorage shared/awba-engine.js == 13; the @layer order line appears exactly once; @view-transition is present; the reduced-motion VT kill block is present; no view-transition-name adjacent to a scripture (.ayah/.scard) block and no celebration primitive (dab|thread|plate|rosette) adjacent to the daily ayah)"
    - "The human gate is a plain-language, ~10-item checklist Melusi walks against learn.html over file:// (open the front door, walk the path, tap nodes/HUD/tabs, claim a gift, toggle reduced motion) plus a one-time npx serve pass to see the cross-document morph; a NO on ANY item is a blocker that routes back to the responsible plan — this checkpoint is BLOCKING and NEVER auto-approved by an executor (workflow.auto_advance is ignored here)"
  artifacts: []
  key_links:
    - from: "the automated prechecks"
      to: "the human plain-language checklist"
      via: "prechecks must all pass before the human is asked to walk the learn page"
      pattern: "fail 0"
---

## Phase Goal

**As a** learner, **I want to** open Awba's front door and walk a beautiful winding path through the whole Aqeedah course — seeing where I am, what is next, and a daily verse — with calm page-to-page transitions, **so that** returning to learn feels like coming home to a living manuscript, not opening a game.

*(This is the phase-closing gate: every automated check green as a precondition, then Melusi walks the real learn page and confirms it feels world-class. Verification-only — it modifies no source.)*

<objective>
Close Phase 5 with a two-stage gate that mirrors 04-07: run the full automated precheck set (test suite + validator over all 19 + self-test + port-audit byte-fidelity/holds + DAILY fidelity + whole-corpus render-smoke incl. root learn.html + the file:// VT-guard nav check + every grep gate) as a hard precondition, then STOP and present Melusi a ~10-item plain-language visual checklist to walk against `learn.html` over `file://` (plus one `npx serve` pass to see the cross-document morph). A failing precheck routes back to the owning plan (never patched at the gate); the human checkpoint is BLOCKING and is never auto-approved.

Purpose: the phase-level proof that the learn page renders the full journey with live storage-derived states, the daily verse rotates verbatim, popups/sheets/tabs/chest are alive, celebration never touches scripture, transitions morph gracefully, and the whole thing reads as the same calm manuscript as the preview (LRN-01..07, CNT-03, RWD-04, MOT-02).
Output: a green automated-precheck report, then a blocking human checkpoint.

Executor routing: **sonnet** (mechanical / verification). Run the prechecks exactly; do NOT edit source to make a gate pass — a failing gate routes back to its owning plan (01 seams/tests/atom-rewire, 02 shell/HUD/Ring/ayah, 03 path/nodes/thread, 04 popup/nav/VT, 05 sheets/tabs/chest). The human checkpoint is BLOCKING and must NEVER be auto-approved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-learn-page-cross-page-view-transitions/05-UI-SPEC.md
@.planning/phases/05-learn-page-cross-page-view-transitions/05-VALIDATION.md

<interfaces>
<!-- The gate reads only; it changes nothing. -->
Full suite: `node --test scripts/tests/*.test.js` (glob only; fail 0, tests >= 98).
Content gate: `node scripts/validate-content.js lessons/*.html reviews/*.html` (exit 0, the 3 accepted `note:` warnings — u3-m1 unused ref baqarah-2-163, u3-m3 unused ref imran-3-19, u4-m2 unused term rububiyah — none to be "fixed") + `--self-test` (OK).
Port harnesses: `scripts/port-audit.mjs` (BYTES OK all 19 + DAILY BYTES OK + holds), `scripts/tests/render-smoke.mjs` (all 20 incl. root learn.html SMOKE OK + the file:// learn→u1-m1 nav check).
Grep gates: no `fonts.googleapis|confetti|class="perfect"|class="combo"|poppins|lantern-gold|gold-bg` in learn.html/shared; no `amber` / `rgba(37,54,` / `--accent` in learn.html + shared/awba-engine.css; no raw `localStorage` in learn.html; `grep -c localStorage shared/awba-engine.js` == 13; `@layer tokens, base, components, screens, motion;` count == 1; `@view-transition` present; `view-transition-group` (the reduced-motion kill block) present; no `view-transition-name` on `.ayah`/`.scard`; no `(dab|thread|plate|rosette)` adjacent to the daily-ayah `.ayah`/`.scard` block.
Manual-only checks (05-VALIDATION "Manual-Only Verifications"): the path reads as a hand-inked journey; popup anchoring/clamping feel at screen edges; the cross-doc morph (needs an http origin — `npx serve`); the Festival circuit-plate claim moment; the daily-ayah reverence on the dark ground.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: run the full automated precheck set (suite + validator + port-audit + DAILY + render-smoke + grep gates)</name>
  <files></files>
  <read_first>
    - 05-VALIDATION.md (full-suite command, sampling, manual-only list) + 05-RESEARCH.md §Validation Architecture (the requirement→test map) + §Common Pitfalls (the file:// VT no-op check).
    - 05-UI-SPEC.md §S6 (the VT gates) — confirm the placement/kill-block gates before asserting them.
  </read_first>
  <action>Run and record pass/fail for the full precheck set as automated preconditions (use `! grep -q` for every absence gate; paren-wrap or `--`-separate any `--`-leading pattern for ugrep). Suite: `node --test scripts/tests/*.test.js` → `fail 0`, `tests >= 98`. Content: `node scripts/validate-content.js lessons/*.html reviews/*.html` → exit 0 with exactly the 3 expected `note:` warnings (no ERROR/FAIL) and `node scripts/validate-content.js --self-test` → OK. Port harnesses: `node scripts/port-audit.mjs` → `BYTES OK` for all 19 + `DAILY BYTES OK` + holds recorded (no `BYTES DRIFT`); `node scripts/tests/render-smoke.mjs` → every page `SMOKE OK` incl. root `learn.html`, no `SMOKE FAIL`, and the `file://` learn→u1-m1 nav check error-free. Grep gates: no gated literal in `learn.html`/`shared/`; no raw `localStorage` in `learn.html`; `grep -c localStorage shared/awba-engine.js` == 13; `@layer` order line count == 1; `@view-transition` present; the reduced-motion VT kill block present; no `view-transition-name` on a `.ayah`/`.scard` block; no `(dab|thread|plate|rosette)` adjacent to the daily-ayah scripture block. If ANY precheck fails, STOP and report which gate failed and which plan owns it — do NOT edit source to force a pass.</action>
  <verify>
    <automated>node --test scripts/tests/*.test.js 2>&1 | grep -qE 'fail 0' && test "$(node --test scripts/tests/*.test.js 2>&1 | grep -oE 'tests [0-9]+' | head -1 | grep -oE '[0-9]+')" -ge 98 && VOUT=$(node scripts/validate-content.js lessons/*.html reviews/*.html 2>&1) && test "$(echo "$VOUT" | grep -c 'note:')" -eq 3 && ! echo "$VOUT" | grep -q 'amber:' && SOUT=$(node scripts/validate-content.js --self-test 2>&1) && ! echo "$SOUT" | grep -qi 'FAIL' && node scripts/port-audit.mjs 2>&1 | grep -q 'BYTES OK' && node scripts/port-audit.mjs 2>&1 | grep -q 'DAILY BYTES OK' && ! node scripts/port-audit.mjs 2>&1 | grep -qiE 'BYTES DRIFT' && node scripts/tests/render-smoke.mjs 2>&1 | grep -q 'learn.html' && ! node scripts/tests/render-smoke.mjs 2>&1 | grep -q 'SMOKE FAIL' && ! grep -rqiE 'fonts\.googleapis|confetti|class="perfect"|class="combo"|poppins|lantern-gold|gold-bg' learn.html shared/awba-engine.js shared/awba-engine.css && ! grep -qi 'amber' learn.html && ! grep -q 'localStorage' learn.html && test "$(grep -c 'localStorage' shared/awba-engine.js)" -eq 13 && test "$(grep -c '@layer tokens, base, components, screens, motion;' shared/awba-engine.css)" -eq 1 && grep -q '@view-transition' shared/awba-engine.css && grep -qE 'view-transition-group\(\*\)' shared/awba-engine.css && ! grep -q -- '--accent' learn.html && ! grep -q -- '--accent' shared/awba-engine.css && ! grep -q 'view-transition-name' learn.html && ! grep -qE '(ayah|scard).{0,60}viewTransitionName|viewTransitionName.{0,60}(ayah|scard)' shared/awba-engine.js</automated>
  </verify>
  <acceptance_criteria>
    - Suite `fail 0` (tests >= 98); validator over all 19 exit 0 with exactly the 3 accepted warnings; self-test OK.
    - port-audit `BYTES OK` all 19 + `DAILY BYTES OK` + holds; render-smoke every page `SMOKE OK` incl. learn.html; the file:// nav check is error-free.
    - Every grep gate passes (no gated literal / no raw localStorage in learn.html; engine localStorage==13; order line==1; @view-transition + reduced-motion kill present; no view-transition-name/celebration adjacent to scripture).
    - Any failure is reported with its owning plan, not patched here.
  </acceptance_criteria>
  <done>Every automated precondition of the phase is green (or precisely routed back).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Melusi walks the ~10-item plain-language visual gate for learn.html (file:// + one npx serve pass)</name>
  <files></files>
  <action>Present the ~10-item plain-language checklist below to Melusi, then STOP and wait. This is a BLOCKING human checkpoint — do NOT auto-approve it (workflow.auto_advance is ignored here) and do NOT edit source at the gate. Resume only on the owner's explicit "approved" (all YES) or a list of NO items; a NO routes back to the owning plan named beside that item.</action>
  <what-built>
    The learn page — Awba's front door — now renders the full journey and every automated precheck (Task 1) is green: it is one calm dark Orbit screen with your Ring at the top (traced in ink, static — never re-drawing on a home visit), a quiet streak line and a little constellation of your return days, a navy "continue" card pointing at your next lesson, today's verse under the reverence it deserves, four unit sections, and a winding hand-inked path where every node shows its true state (locked, next, done with stars). Tapping a node opens a cream slip-of-paper with START/REVIEW/LEGENDARY; the HUD numbers, streak line, course chip and every tab open a calm sheet (Fiqh/Seerah/Qur'an as polished "coming soon"); a gift grants +25 noor exactly once with a Festival circuit-plate moment; and page-to-page navigation morphs gently on a real web server (over file:// it just navigates normally, by design).

    NOTE — items on the owner ledger (unchanged, not gate blockers): the 15 verified Arabic chapter-terms (R-6, English titles shown until sourced), the verbatim Ibrahim 14:24 English source (R-7), sound-cue assets (D-52, silent), scholar gate, and the full ~20-doodle plant pool (R-3 fast-follow — a single sprout ships now).
  </what-built>
  <how-to-verify>
    Double-click `learn.html` (file:// — zero CDN, no server) and answer YES/NO to each. For item 10 only, run `npx serve` once and open the served learn page to watch the page-to-page morph.
    1. Does the front door feel like the same calm, world-class book as the preview — a dark page warmed at the edges, crisp margins, nothing shouting?
    2. Is your Ring the clear hero at the top, sitting still (not re-animating every time you open the page), with the path below reading as a companion index rather than a second competing map?
    3. Does the winding path read as a real hand-inked journey — a flowing line with warm dabs — and NOT like re-skinned Duolingo circles?
    4. Does every node clearly show where you are — locked nodes quiet (a gentle "not yet", never a buzzer or shake), your next lesson glowing warm, finished nodes gold with their stars?
    5. Tap a node: does the little slip-of-paper popup appear anchored to that node with a visible START/REVIEW button, and does tapping outside or a far-edge node behave gracefully (no clipping, no invisible buttons)?
    6. Is today's verse the most beautiful, most important thing in its card — quiet, reverent, on a clean ground, with nothing celebratory next to it — and does it change day to day (not the same verse all month)?
    7. Do the HUD numbers, the streak line, the course chip and the tabs ALL open a calm sheet (never a dead tap), with Fiqh/Seerah/Qur'an shown as polished "coming soon", and does the active tab read gold (not an invisible dark-red)?
    8. Claim a gift on a finished unit: does it feel like a sure +25 (never a gamble), play the Festival circuit-plate moment once, and NOT re-grant if you claim again?
    9. Turn on your OS "reduce motion": does everything go still and calm — the Ring and path static, no breathing, the gift moment shown finished — while the page still reads perfectly?
    10. (npx serve) Walking path → lesson → back: does the navigation cross-fade/morph gently rather than hard-cutting, and never morph the Arabic verse?
    A NO on any item is a blocker; note which item and it routes back to the responsible plan (item 1/2 Ring+shell → 05-02; item 3/4 path+nodes → 05-03; item 5 popup → 05-04; item 6 daily ayah → 05-02 + AW.dailyIndex 05-01; item 7 sheets/tabs → 05-05; item 8 chest/Festival → 05-05; item 9 reduced-motion → the owning surface; item 10 View Transitions → 05-04).
  </how-to-verify>
  <resume-signal>Type "approved" if all ~10 are YES, or list the item numbers that are NO with a one-line note each.</resume-signal>
  <done>Melusi has walked the ~10 items and either approved the phase or returned specific NO items routed to their owning plans; the checkpoint was never auto-approved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verification-only | this plan reads and asserts; it modifies no source. No new trust surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06 | Repudiation / process | the gate itself | mitigate | the human checkpoint is blocking and never auto-approved (workflow.auto_advance ignored); a failing precheck routes to the owning plan rather than being patched at the gate |
| T-05-SC | Tampering | npm installs | accept | no package installs; verification-only. No `[ASSUMED]`/`[SUS]` package to gate |
</threat_model>

<verification>
- Every automated precheck (suite fail 0 tests>=98 · validator 19 exit 0 + self-test · port-audit BYTES OK + DAILY BYTES OK + holds · render-smoke all 20 incl. learn.html SMOKE OK + file:// nav clean · every grep gate) passes before the human is asked.
- The ~10-item plain-language checklist is presented; the checkpoint blocks and is never auto-approved; a NO routes to the owning plan.
</verification>

<success_criteria>
- LRN-01..07, CNT-03, RWD-04, MOT-02 are verified automatically and then confirmed by the owner's plain-language walk of the real learn page.
- Phase 5 is gate-passed (or precisely routed back): the learn front door renders the full journey, world-class and Athar-coherent, with graceful cross-page transitions.
</success_criteria>

<output>
Create `.planning/phases/05-learn-page-cross-page-view-transitions/05-06-SUMMARY.md` when done.
</output>
