---
phase: 05-learn-page-cross-page-view-transitions
plan: 01
type: execute
wave: 1
executor_hint: sonnet
depends_on: []
files_modified:
  - shared/awba-engine.js
  - scripts/tests/learn-state.test.js
  - scripts/tests/ring.test.js
  - scripts/tests/sky.test.js
  - scripts/tests/render-smoke.mjs
  - preview.html
autonomous: true
requirements: [CNT-03, LRN-05]
must_haves:
  truths:
    - "Per D-57: a single NODE_ATOMS map (u1m1:3, u1m2:4, u1m3:5, u1m4:5, u2m1:5, u2m2:4, u2m3:3, u2m3b:3, u3m1:4, u3m2:5, u3m3:5, u4m1:3, u4m2:4, u4m2b:4, u4m3:4) sums to EXACTLY 61, and AW.atomsDone(progress) returns Σ (NODE_ATOMS[id]||0) over ids present in progress.stars — review/chest nodes contribute 0; per-lesson atom counts are NEVER invented (they are the verified RESEARCH §Atom Map)"
    - "Per D-57: the 65→61 denominator sweep lands as ONE atomic commit — DEF_STRUCT.atoms, SKY_ATOMS, both reward-runner ATOMS_PER_NODE proxy callers and the boot --dawn proxy all read the real map/61, and every paired assertion in ring.test.js, sky.test.js and preview.html moves to 61 in the SAME commit; the suite is green at that commit and every ported lesson still renders"
    - "Per D-59: AW.dailyIndex(date, poolLen) is pure and computes a day-of-YEAR index from LOCAL date parts (never toISOString, never new Date(ymdString)); AW.dailyIndex(new Date(y,0,8),7) !== AW.dailyIndex(new Date(y,1,8),7) so the Gen-3 getDate()%7 monthly-repeat bug is fixed"
    - "Per D-54: CNT-03 is proven by walking the SHIPPED AW.deriveNodeState over the canonical Gen-3 flat order with staged stars/chests fixtures — the exact locked→active→done + chest-after-review transitions hold; the deriving seam is EXTENDED, never forked"
    - "AW.muteBtnHtml and AW.bindMuteBtn are exported on AW (the module-private helpers exposed, no 14th glyph added — glyphCount stays 13); the full node:test suite stays green (fail 0, tests >= 98 baseline never shrunk) and localStorage grep-count in shared/awba-engine.js stays EXACTLY 13"
  artifacts:
    - path: "scripts/tests/learn-state.test.js"
      provides: "headless proof of CNT-03 unlock walk + NODE_ATOMS sum + AW.atomsDone + AW.dailyIndex + chest idempotency"
      contains: "AW.atomsDone"
    - path: "shared/awba-engine.js"
      provides: "NODE_ATOMS map, AW.atomsDone, AW.dailyIndex, AW.muteBtnHtml/AW.bindMuteBtn exports, the 61 denominator"
      contains: "NODE_ATOMS"
    - path: "scripts/tests/render-smoke.mjs"
      provides: "findPages() extended to include root learn.html (register-root regex already matches reg-orbit)"
      contains: "learn.html"
  key_links:
    - from: "AW.atomsDone(progress)"
      to: "AW.ringSVG callers (learn hero 05-02 + reward runner) and AW.skyDawn + the boot --dawn stamp"
      via: "one constant, one place — every atom count reads NODE_ATOMS, never a *3 proxy or an invented number"
      pattern: "AW\\.atomsDone"
---

## Phase Goal

**As a** learner, **I want to** open Awba's front door and walk a beautiful winding path through the whole Aqeedah course — seeing where I am, what is next, and a daily verse — with calm page-to-page transitions, **so that** returning to learn feels like coming home to a living manuscript, not opening a game.

*(Derived from ROADMAP Phase-5 `**Goal:**` + PROJECT.md's established learner role, per the 04-01 precedent. This plan is the Wave-0 test foundation + engine seams the vertical slices build on — it renders nothing yet.)*

<objective>
Lay the testable ground before `learn.html` exists: add the four pure/export engine seams the learn page consumes (`AW.dailyIndex`, the `NODE_ATOMS` map + `AW.atomsDone`, and the two mute exports), pin every one of them plus the CNT-03 unlock contract in a new headless test (RED → GREEN), land the 65→61 atom-denominator sweep as ONE atomic commit (engine touch-points + every paired test/preview assertion), and extend `render-smoke.mjs` to discover the root `learn.html` the later waves create.

Purpose: close the 05-VALIDATION Wave-0 gaps so CNT-03/LRN-05/D-57 are provable without a DOM, and resolve D-57 (A1) to the locked denominator 61 so a completed course fully inks the Ring.
Output: the engine seams + the 61 denominator in `shared/awba-engine.js`, `scripts/tests/learn-state.test.js`, updated `ring.test.js`/`sky.test.js`/`preview.html`, extended `render-smoke.mjs`.

Executor routing: **sonnet** (mechanical / test authoring — no flagship visual work). Copy the atom map + math byte-faithfully from RESEARCH §Atom Map; do NOT invent any per-lesson count.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-learn-page-cross-page-view-transitions/05-RESEARCH.md
@.planning/phases/05-learn-page-cross-page-view-transitions/05-PATTERNS.md
@.planning/phases/05-learn-page-cross-page-view-transitions/05-VALIDATION.md

<interfaces>
<!-- Extracted from the codebase this session; the executor uses these directly. Locate by content — line numbers drift. -->

Engine seams already shipped (`shared/awba-engine.js`):
- `AW.deriveNodeState(nodesFlat, progress)` — PURE, DOM-free (grep `AW.deriveNodeState`). Returns `[{id,state}]` with state ∈ locked|available|active|done; already encodes the Gen-3 unlock + chest rules (chest `available` iff the immediately-preceding node has stars AND unopened, `done` if opened; lesson/review `done` iff starred, else `active` iff every prior non-chest node has stars, else `locked`).
- `AW.state()` — snapshot `{noor, returns, stars, days, lastDay, chests}` (defensive copy). `AW.S.get/set` — the only localStorage surface (D-24).
- `AW.skyDawn` (grep `AW.skyDawn = skyDawn`) — pure `min(SKY_DAWN_CAP, atomsDone/SKY_ATOMS)`; `SKY_DAWN_CAP=0.6`, currently `SKY_ATOMS=65`.
- `AW.ringSVG(cfg)` `DEF_STRUCT = { circuits:4, lessons:15, atoms:65 }` (grep `DEF_STRUCT`); the reward runner's `ATOMS_PER_NODE = 3` proxy (grep `ATOMS_PER_NODE`, TWO callers: `preLessonAtoms` at init, `postAtoms` after done()); the boot `--dawn` proxy (grep `TOTAL_NODES`, `var ATOMS = 65, TOTAL_NODES = 19`).
- `function muteBtnHtml()` / `function bindMuteBtn(refresh)` — module-private (grep `function muteBtnHtml`); they close over `SPEAKER_ON/OFF` + `AW.prefs`. No `AW.muteBtnHtml` exists yet.

Test harness (`scripts/tests/ls-stub.js`): `makeLS(seed)` (Map-backed localStorage), `loadEngine(ls, probeSrc)` (engine + probe in ONE runInContext; `const AW` is lexical), `readOut(sandbox)` (JSON round-trip for object/array returns). Precedent test files to copy the header/idiom from: `scripts/tests/sky.test.js`, `scripts/tests/state-storage.test.js`.

Canonical Gen-3 flat unlock order (CNT-03 contract, from `/Users/theshumba/Downloads/AWBA APP/_MVP-BUILD/learn.html` UNITS): u1m1→u1m2→u1m3→u1m4→u1r→u1c → u2m1→u2m2→u2m3→u2m3b→u2r→u2c → u3m1→u3m2→u3m3→u3r→u3c → u4m1→u4m2→u4m2b→u4m3→u4r→u4c (reviews carry review:true, chests carry chest:true).

Machine notes: run tests via glob ONLY — `node --test scripts/tests/*.test.js` (directory form breaks Node v24.13.0). ugrep is the grep — paren-wrap or `--`-separate any `--`-leading pattern. Hand-edit STATE.md (gsd-sdk state handlers mis-parse it).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: learn-state.test.js (RED) → AW.dailyIndex + NODE_ATOMS + AW.atomsDone + mute exports (GREEN)</name>
  <files>shared/awba-engine.js, scripts/tests/learn-state.test.js</files>
  <read_first>
    - 05-RESEARCH.md §Atom Map (the verbatim per-node counts summing to 61) + §Pattern 2 (the AW.dailyIndex body) + §Pitfall 6 (the two mute exports).
    - 05-PATTERNS.md "the four engine seams" + "learn-state.test.js" (the loadEngine/makeLS probe idiom + the CNT-03 walk / chest-idempotency fixtures).
    - `scripts/tests/sky.test.js` (header + per-probe idiom to copy verbatim) and `scripts/tests/state-storage.test.js` (the `awba_chest_*` → chests seed idiom).
    - `shared/awba-engine.js`: grep `AW.deriveNodeState` (place NODE_ATOMS + AW.atomsDone immediately AFTER it — BEFORE the boot block so the boot --dawn can call it), grep `AW.skyDawn = skyDawn` (place AW.dailyIndex nearby), grep `function bindMuteBtn` (add the two exports right after it).
  </read_first>
  <behavior>
    - Σ over NODE_ATOMS values === 61; NODE_ATOMS has exactly the 15 lesson ids (no review/chest ids).
    - AW.atomsDone({stars:{}}) === 0; AW.atomsDone({stars:{u1m1:3}}) === 3; AW.atomsDone({stars:{u1m1:3,u1m3:2}}) === 8; AW.atomsDone({stars:{u1m1:3,u1r:3,u1c:1}}) === 3 (review + chest add 0).
    - AW.dailyIndex(new Date(2026,0,8),7) !== AW.dailyIndex(new Date(2026,1,8),7) (Jan-8 vs Feb-8 differ — the exact Gen-3 monthly-repeat bug); the result is always an integer in [0,6]; leap-safe (a Dec-31 fixture returns a valid index).
    - typeof AW.muteBtnHtml === 'function' && typeof AW.bindMuteBtn === 'function'.
    - CNT-03 walk over the canonical flat order: empty stars → u1m1 'active', every later lesson/review 'locked', u1c 'locked'; stars={u1m1..u1m4,u1r all set} → u1c 'available' (chest after review) and u2m1 'active'; add chests={u1c:true} → u1c 'done'.
    - Chest idempotency (drive AW.S via makeLS): claiming u1c once → noor increases by 25 and chests.u1c===true; a second claim → no further change.
  </behavior>
  <action>Write `scripts/tests/learn-state.test.js` FIRST (it MUST fail — the new seams do not exist), asserting every line of the behavior block via `loadEngine(makeLS(seed), 'globalThis.__out = …;')` probes (primitives read off `sandbox.__out`; objects/arrays via `readOut`). The CNT-03 walk builds the flat array inline from the canonical order in the interfaces block (id + review/chest flags) and drives the SHIPPED `AW.deriveNodeState` — do NOT reimplement node-state. THEN add to `shared/awba-engine.js`: (a) a `NODE_ATOMS` object with the 15 verbatim counts from RESEARCH §Atom Map + `AW.atomsDone = function(progress){ var s=(progress&&progress.stars)||{}; var t=0; for (var id in s){ t += (NODE_ATOMS[id]||0); } return t; };`, placed immediately after `AW.deriveNodeState` so the parse-time boot block can call it; (b) `AW.dailyIndex = function(date, poolLen){ … }` per RESEARCH §Pattern 2 — day-of-year from LOCAL `getFullYear/getMonth/getDate` only, safe modulo `((doy%poolLen)+poolLen)%poolLen`, NEVER toISOString; (c) `AW.muteBtnHtml = muteBtnHtml;` and `AW.bindMuteBtn = bindMuteBtn;` right after `function bindMuteBtn`. Do NOT add a speaker glyph to AW.GLYPHS (glyphCount is frozen at 13). Run the suite: RED → GREEN. This task does NOT touch the 65 denominator (that is Task 2) so ring.test.js/sky.test.js stay green here.</action>
  <verify>
    <automated>node --test scripts/tests/learn-state.test.js 2>&1 | grep -qE 'fail 0' && node --test scripts/tests/*.test.js 2>&1 | grep -qE 'fail 0' && test "$(node --test scripts/tests/*.test.js 2>&1 | grep -oE 'tests [0-9]+' | head -1 | grep -oE '[0-9]+')" -ge 98 && grep -q 'AW.atomsDone' shared/awba-engine.js && grep -q 'AW.dailyIndex' shared/awba-engine.js && grep -q 'AW.muteBtnHtml' shared/awba-engine.js && test "$(grep -c 'localStorage' shared/awba-engine.js)" -eq 13</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/tests/learn-state.test.js` exists and every behavior-block assertion passes.
    - `NODE_ATOMS`, `AW.atomsDone`, `AW.dailyIndex`, `AW.muteBtnHtml`, `AW.bindMuteBtn` exist on the engine; NODE_ATOMS + atomsDone sit before the boot block.
    - Suite `fail 0`, `tests >= 98`; `grep -c localStorage shared/awba-engine.js` === 13; glyphCount test (components.test.js) still green.
  </acceptance_criteria>
  <done>The learn page's four consumed seams exist and are proven headlessly; the CNT-03 contract is pinned.</done>
</task>

<task type="auto">
  <name>Task 2: the atomic 65→61 atom-denominator sweep (engine + every paired assertion, ONE commit)</name>
  <files>shared/awba-engine.js, scripts/tests/ring.test.js, scripts/tests/sky.test.js, preview.html</files>
  <read_first>
    - 05-UI-SPEC.md Ruling R-1 (the four touch-points + the paired assertions) and 05-RESEARCH.md §Atom Map "the 61-vs-65 reconciliation" (61 = corpus-minus-holds; NEVER invent counts).
    - `shared/awba-engine.js`: grep `DEF_STRUCT` (atoms:65), grep `SKY_ATOMS` (=65), grep `ATOMS_PER_NODE` (=3, both `preLessonAtoms` + `postAtoms` callers), grep `TOTAL_NODES` (boot `var ATOMS = 65`).
    - `scripts/tests/ring.test.js` (every "65"/"of 65 inked"/`data-atoms="65"`/`4/15/65` assertion), `scripts/tests/sky.test.js` (every `/65` scaling + the `skyDawnAt(65)===0.6` cap-boundary), `preview.html` (grep `65` — the `atomsDone`/caption strings "X of 65", "0/65", "28/65", "65/65").
  </read_first>
  <action>In ONE atomic change (staged so the commit is self-consistent): set `DEF_STRUCT.atoms` 65→61 and `SKY_ATOMS` 65→61; replace BOTH reward-runner proxy computations `Object.keys(stars).length * ATOMS_PER_NODE` with `AW.atomsDone(AW.state())` (preLessonAtoms at init, postAtoms after done()) so lesson-complete inks the true frontier — retire the now-unused `ATOMS_PER_NODE = 3` constant; re-wire the boot `--dawn` proxy (the `var ATOMS = 65, TOTAL_NODES = 19` block) to stamp `--dawn` from `AW.skyDawn(AW.atomsDone(AW.state()))` (drop the coarse node-count proxy). THEN move every paired assertion in the SAME commit: in `ring.test.js` change `atomsDone: 65`/`of 65 inked`/`data-atoms="65"`/the `4/15/65` fallback to 61 (a full ring is now atomsDone 61, label "61 of 61 inked", `data-atoms="61"`, clamp ceiling 61); in `sky.test.js` change every `/65` to `/61` and the cap-boundary input from 65 to 61 (61/61 = 1.0 → capped 0.6; keep one exact below-cap linear assertion, e.g. 26/61); in `preview.html` change the caption/atomsDone strings so no "65" atom denominator survives (0/61, a mid value < 61, 61/61). Run the full suite AND render-smoke over the 19 shipped lessons/reviews to prove no lesson-complete regression. Commit message: `feat(05-01): wire NODE_ATOMS map, atom denominator 65→61 (D-57 R-1)`.</action>
  <verify>
    <automated>node --test scripts/tests/*.test.js 2>&1 | grep -qE 'fail 0' && test "$(node --test scripts/tests/*.test.js 2>&1 | grep -oE 'tests [0-9]+' | head -1 | grep -oE '[0-9]+')" -ge 98 && ! grep -qE 'atoms: *65|of 65 inked|data-atoms="65"|SKY_ATOMS *= *65|ATOMS *= *65' shared/awba-engine.js && test "$(grep -c 'localStorage' shared/awba-engine.js)" -eq 13 && ! grep -qE '/ *65|of 65|65 */ *65' preview.html && node scripts/tests/render-smoke.mjs 2>&1 | grep -q 'SMOKE OK' && ! node scripts/tests/render-smoke.mjs 2>&1 | grep -q 'SMOKE FAIL'</automated>
  </verify>
  <acceptance_criteria>
    - No 65-atom denominator survives in `shared/awba-engine.js`, `ring.test.js`, `sky.test.js`, or `preview.html`; every ring/sky assertion is on /61.
    - Both reward-runner ring callers + the boot --dawn read `AW.atomsDone`; the `ATOMS_PER_NODE = 3` proxy is gone.
    - Suite `fail 0` (tests >= 98); render-smoke over all 19 shipped pages `SMOKE OK` (no lesson-complete regression); `localStorage` count still 13.
  </acceptance_criteria>
  <done>The Ring, Sky and reward frontier all read the verified 61-atom map from one place; a completed course fully inks.</done>
</task>

<task type="auto">
  <name>Task 3: extend render-smoke.mjs findPages() to discover root learn.html</name>
  <files>scripts/tests/render-smoke.mjs</files>
  <read_first>
    - 05-RESEARCH.md §Pitfall 7 + 05-PATTERNS.md "render-smoke.mjs (MOD)".
    - `scripts/tests/render-smoke.mjs`: grep `findPages` (the `['lessons','reviews']` dir loop), grep `ROOT` (`path.join(__dirname,'..','..')`), grep `REGISTER_ROOT_RE` (already matches `reg-(page|orbit)` — no regex change needed).
  </read_first>
  <action>In `findPages()`, before the `['lessons','reviews']` loop, push `path.join(ROOT, 'learn.html')` when it exists (`existsSync`) — so the root front-door page joins the smoke set the moment 05-02 creates it, and is simply skipped until then. Change ONLY the file-discovery list; the register-root regex already matches `reg-orbit`. Do NOT change how pages are loaded/asserted. learn.html does not exist yet, so render-smoke must still exit 0 over the current 19 pages.</action>
  <verify>
    <automated>grep -q 'learn.html' scripts/tests/render-smoke.mjs && node scripts/tests/render-smoke.mjs 2>&1 | grep -q 'SMOKE OK' && ! node scripts/tests/render-smoke.mjs 2>&1 | grep -q 'SMOKE FAIL'</automated>
  </verify>
  <acceptance_criteria>
    - `findPages()` includes root `learn.html` behind an `existsSync` guard; regex unchanged.
    - render-smoke exits clean over the current 19 pages (root learn.html absent → skipped).
  </acceptance_criteria>
  <done>The smoke harness is ready to cover learn.html automatically from 05-02 onward.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| progress storage → engine pure helpers | `AW.atomsDone`/`AW.dailyIndex` read the defensively-copied `AW.state()` snapshot; no user/URL input reaches them |
| dev tooling → filesystem | render-smoke reads the repo + spawns headless Chrome over `file://`; no writes, no network |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01a | Tampering | atom-count integrity | mitigate | NODE_ATOMS is the verified RESEARCH §Atom Map (61 taught); the ΣNODE_ATOMS===61 assertion + the "no 65 survives" grep gate prevent silent invention of counts |
| T-05-01b | Tampering | localStorage read surface | accept | device-local cosmetic progress; the pure helpers only read via `AW.state()`, never write; `localStorage`-count gate holds at 13 |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | zero packages installed (zero-dep static app); no `[ASSUMED]`/`[SUS]` package to gate |
</threat_model>

<verification>
- `node --test scripts/tests/*.test.js` → `fail 0`, `tests >= 98`; `learn-state.test.js` pins CNT-03/LRN-05(dailyIndex)/D-57(atomsDone,NODE_ATOMS sum)/chest idempotency.
- No 65-atom denominator survives anywhere; both ring callers + boot --dawn read `AW.atomsDone`; render-smoke over all 19 shipped pages `SMOKE OK`.
- `grep -c localStorage shared/awba-engine.js` === 13; `@layer` order line untouched (no CSS this plan); glyphCount === 13 (no mute glyph added).
</verification>

<success_criteria>
- CNT-03 / LRN-05 / D-57 are pure, tested `AW.*` contracts the later waves consume (interface-first).
- The 61 denominator (Ruling R-1) is live in one place; the learn page can be built against a proven foundation.
</success_criteria>

<output>
Create `.planning/phases/05-learn-page-cross-page-view-transitions/05-01-SUMMARY.md` when done.
</output>
