# Phase 5 Discussion Log — auto mode

**Date:** 2026-07-13 · **Mode:** `--auto` (owner directive: fully autonomous chain — "finish executing everything until the entire app is finished") · **Single pass, cap honoured**

All gray areas auto-selected; for each question the recommended option was chosen and logged below. Scout evidence: Gen-3 learn.html read (UNITS :120-150, chest rule :181-185, DAILY pool :153-160 = exactly 7 verses, rotation uses `getDate()` day-of-month — the LRN-05 bug confirmed live).

## Areas + auto-selected decisions

1. **Home register & page architecture** — Q: one Orbit screen per ATHAR moment-1, or a hybrid Orbit-hero + cream path section? → Selected: **one Orbit screen** (law 1; hybrid = second ground = constitution violation). → D-53.
2. **Path & node grammar** — Q: re-skin Gen-3's circle nodes or re-voice to thermal ink grammar? → Selected: **thermal shape-first dabs on an ink thread, earned portion gold, draw verb** (recommended; Duolingo-circles re-skin fails LRN-04's "real journey" bar). → D-54.
3. **Seed-rows / plant stamps placement** (Phase-4 deferred item) — Q: on-node, in-popup, or defer again? → Selected: **seed-row in the popup slip + plant stamp on done nodes, MVP fallback = sprout glyph** (bounded by ATHAR asset kit; never a 65-plant map). → D-55.
4. **Chest expression** — Q: keep a gift-box node, or bind the chest claim to the Festival circuit-plate threshold? → Selected: **chest claim = the circuit-plate moment** (mechanic byte-preserved +25/idempotent; Festival's sanctioned threshold; retires Gen-3 treasure-box art; also lands the Phase-4-deferred crowd-arrival wiring). → D-56.
5. **Ring atom wiring** — Q: wire exact atoms or keep the proxy? → Selected: **researcher locates Josh's authoritative per-lesson atom map; exact if verifiable, centralized proxy if not; never invent counts**. → D-57.
6. **View Transitions scope** — Q: bare cross-fade everywhere vs + one shared-element morph? → Selected: **cross-fade baseline + ONE node-square→opener-square morph; scripture never named; Firefox = plain nav** (researched pattern, progressive enhancement). → D-58.
7. **Daily ayah** — Q: keep Gen-3 rotation or fix to day-of-year? → Selected: **fix to local day-of-year % 7 (the requirement mandates it); pool spliced byte-verbatim**. → D-59.
8. **HUD/streak band/tabs/sheets** — Q: bespoke sheets per surface vs the one singleton? → Selected: **one AW.sheet implementation for streak/noor/switcher/coming-soons; marginalia HUD; constellation strip as Sky tokens**. → D-60.
9. **Navigation wiring** — Q: anchor-scroll behaviour on return-to-path? → Selected: **auto-scroll to next available node, honour #node-id hash; zero cfg edits**. → D-61.

## Deferred (captured, not acted on)
Eid dressing · completion poster/share cards · full doodle pool (if split) · Nightfall auto-trigger (D-48) · real Practice/Profile surfaces (V2-07) · returns heatmap (V2-03).

## Notes
- Scope creep: none encountered; owner's in-chat scope question (onboarding/profile/settings) was answered earlier today — confirmed out of scope for v1 (PROJECT.md Out of Scope; coming-soon stubs per LRN-07).
- Canonical refs accumulated into 05-CONTEXT.md `<canonical_refs>`.
