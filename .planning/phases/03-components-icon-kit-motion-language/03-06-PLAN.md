---
phase: 03-components-icon-kit-motion-language
plan: 06
type: execute
wave: 1
executor_hint: opus-visual
depends_on: []
files_modified: [shared/awba-engine.css]
autonomous: true
requirements: [MOT-01, MOT-04]
must_haves:
  truths:
    - "Per §2/§3 the cascade `@layer tokens, base, components, screens, motion;` line is NEVER re-declared — this plan rewrites only the CONTENT of `@layer tokens { … }` and `@layer base { … }`; the layer-order statement appears exactly once in the file"
    - "Per the cut ledger §10 + §3.5 the four `[data-unit=\"u1..u4\"]` colour blocks and every `--accent*` custom-property DEFINITION are deleted; `AW.UNIT_ICON` (pure JS) is untouched so `data-unit` survives as the unit→icon key only, never a colour scale"
    - "Per §2.2 Poppins is fully retired from the stylesheet (no `poppins` `@font-face`, no `poppins` in any stack, none in comments); `'Inter'` appears exactly twice — one `@font-face` family + one slot in `--font-work` (the silent U+02F9/U+02FA fallback), nowhere else"
    - "Per §2.1 the 17 Athar colour tokens land verbatim-hex with the two ink/paper opacity ramps; per D-A1 the Phase-1 4px `--sp-*` scale is retained verbatim; per D-A2 the shallow print radii replace the gummy-bubble scale; per D-A3 depth is warm-ink shadow + keyline on cream and light-edge on dark (no `rgba(37,54,` authored in tokens); per D-A11 Ember is dark-ground text / cream shape-only and Gold/Powder never lone-indicate on cream"
    - "Per §2.6 one grain tile is referenced CSS-relative as `url('img/grain.png')` (never a leading slash) with per-ground `--go`; per D-A7 the four register grounds `.reg-orbit/.reg-page/.reg-sky-night/.reg-festival` are scoped classes, home Sky is a `data-sky` TINT over the Kiswah Orbit ground (law 1), and `.reg-festival` gets its checker trim"
    - "Per D-A8 thermal `[data-state=\"not-yet|progress|mastered\"]` maps `--st` (powder/ember/gold) as the token layer of a shape-first ramp; per D-A10 focus rings recolour Crimson on cream grounds and Gold on dark grounds (both AA ≥3:1 non-text)"
    - "Per §2.7/D-A4 the one easing family `cubic-bezier(0.23,1,0.32,1)` is the sole `--ease`, with UI `--dur-*` ≤300ms and ambient `--dur-amb*` 4–6s; the gummy `--press-rest/--press-active`, `--scrim` navy, and `--overlay-hero` indigo tokens are removed from `:root`"
  artifacts:
    - path: "shared/awba-engine.css"
      provides: "@layer tokens rewritten to the Athar sheet (fonts, 17 colours + ink ramps, print radii, ink/edge depth, grain var, one-easing motion tokens) with data-unit/accent dismantled; @layer base re-grounded with the four register scopes, data-sky tints, data-state token map, ground-scoped focus, and scripture law"
      contains: "--kiswah"
  key_links:
    - from: ".reg-* ground scopes"
      to: "--icon-accent / --go / :focus-visible recolour consumed by icons (plan 08) + components (plan 09)"
      via: "register-scoped custom properties set on each ground class"
      pattern: "--icon-accent"
    - from: ":root --grain"
      to: "shared/img/grain.png committed tile"
      via: "CSS-relative url from shared/awba-engine.css"
      pattern: "url\\('img/grain.png'\\)"
---

<objective>
Re-ground the whole visual system: rewrite the `@layer tokens` values and refill `@layer base` in `shared/awba-engine.css` to the Athar sheet (§2 token sheet + §3 register architecture). This is the foundation every later re-cut plan builds on and the enabler that lets the Ring SPOF (plan 07) render on real colour tokens. Nothing outside `@layer tokens` and `@layer base` is touched — `@layer components` / `@layer motion` keep their Gen-3 content (dangling `var(--accent…)` references between this plan and plan 09 are an ACCEPTED transient; the old preview may look broken and must NOT be gated on).

Purpose: MOT-01 (the one easing family + duration tokens) and MOT-04 (the reduced-motion duration tokens) re-expressed under Athar, plus the colour/type/radii/grain/register foundation for FND-04/ENG-06 re-skins downstream.
Output: `@layer tokens` = the Athar token sheet with `data-unit`/`--accent` dismantled and Poppins retired; `@layer base` = the four register grounds, `data-sky` tints, `data-state` token map, ground-scoped focus, grain, and scripture law.

Executor routing: opus + load the `frontend-design` skill (flagship visual plan). The 17-token palette, the ink/edge depth model, and the register grounds are load-bearing craft — transplant the spec's ready-made CSS faithfully, and let the AA rulings in §2.1 govern every text/ground pairing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-components-icon-kit-motion-language/03-UI-SPEC-ATHAR.md
@docs/superpowers/specs/2026-07-12-athar-adoption-design.md

<interfaces>
<!-- Exact edit surface in the current file (read once, edit in place — do NOT rewrite whole layers wholesale). -->
shared/awba-engine.css :16  the immutable `@layer tokens, base, components, screens, motion;` order line — NEVER re-write it.
shared/awba-engine.css :18-198  `@layer tokens { … }` — the block whose CONTENT this plan replaces: the Poppins/Inter `@font-face` set (:30-37), the `:root` body (:42-172: font roles, --sp-*, --fs-*, colours, radii, indigo --sh-*, motion tokens, --press-rest/active/--scrim/--overlay-hero, the default --accent* set), and the four `[data-unit="u1..u4"]` blocks (:182-197).
shared/awba-engine.css :200-312  `@layer base { … }` — body/reset (:206-216), the .app-shell responsive shell (:223-272, KEEP — PLT-01 structural, only re-token its colours), :focus-visible (:277-287, recolour per ground), Arabic/RTL + .ayah (:289-311, refine to scripture law).
shared/awba-engine.css :314-810  `@layer components` / `@layer screens` / `@layer motion` — DO NOT TOUCH in this plan (plan 09 owns them). Their `var(--accent…)`/`var(--scrim)`/`var(--sh-*)` references dangle until plan 09; that is the accepted transient.
Committed asset (already on disk): shared/img/grain.png (128×128 tileable PNG-8); shared/fonts/{readex-pro-300..700, amiri-400/700, amiri-quran-400, marcellus-400, aref-ruqaa-400/700, rakkas-400, courier-prime-400, inter-400}.woff2.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite @layer tokens to the Athar sheet + dismantle data-unit/accent</name>
  <files>shared/awba-engine.css</files>
  <read_first>
    - shared/awba-engine.css :18-198 (the entire `@layer tokens` block being replaced) + :16 (the layer-order line to preserve untouched).
    - 03-UI-SPEC-ATHAR.md §2.1 (the 17 colours verbatim-hex + the ink/paper opacity ramps + the load-bearing contrast rulings), §2.2 (the `@font-face` block transplant + the FINAL `--font-*` role stacks + the type-role table), §2.3 (D-A1 retain `--sp-*`), §2.4 (D-A2 print radii), §2.5 (D-A3 ink/edge depth), §2.6 (grain var + `--go`), §2.7 (D-A4 one-easing motion tokens), §3.5 (D-A dismantle `data-unit`), §10 cut ledger, §11 D-A1..A4/A11.
  </read_first>
  <action>Inside `@layer tokens { … }`, replace the block content (do NOT rewrite line 16's layer-order statement). (1) Replace the Poppins/Inter `@font-face` set with the §2.2 `@font-face` block verbatim — the 5 Readex Pro weights, Amiri 400/700, Amiri Quran 400, Marcellus 400, Aref Ruqaa 400/700, Rakkas 400, Courier Prime 400, and the single Inter 400 (marked the silent U+02F9/U+02FA fallback); every `src` is CSS-relative `url('fonts/…')`, never a leading slash. (2) In `:root`, set the `--font-*` role stacks EXACTLY per §2.2 (`--font-work` with `'Inter'` in the second slot, `--font-quran`, `--font-scrip`, `--font-display`, `--font-term`, `--font-festival`, `--font-marg`) — no Poppins anywhere, and the ONLY two `'Inter'` occurrences in the whole file are the `@font-face` family and the `--font-work` slot. (3) Keep the `--sp-*` scale verbatim (D-A1). (4) Author the §2.2 type-role sizes (`--fs-body/--fs-ar-ui/--fs-ui/--fs-h2/--fs-h1/--fs-display/--fs-term/--fs-festival/--fs-marg/--fs-ayah/--fs-scrip`). (5) Author the §2.1 17 colour tokens verbatim-hex (`--kiswah --cream --crimson --ember --gold --navy --madder --nightfall --lastthird --moonmilk --apricot --powder --rose --harissa --olive --mustard --pink`) + the two ink ramps (`--ink/--ink-85/--ink-62/--ink-40/--rule` and `--paper-85/--paper-62/--paper-45/--edge`). (6) Replace the radii with the §2.4 print scale (`--r-square 0`, `--r-1 3px`, `--r-2 6px`, `--r-3 10px`, `--r-4 14px`, `--r-round 50%`, `--r-pill 999px`). (7) Replace the shadows with the §2.5 ink/edge set (`--sh-1/-2/-3`, `--keyline`, `--edge-top`, `--edge-line`) — NO `rgba(37,54,` authored anywhere. (8) Add `--grain: url('img/grain.png')` (§2.6). (9) Author the §2.7 motion tokens: one `--ease: cubic-bezier(0.23,1,0.32,1)`, `--dur-press/--dur-fade/--dur-stamp/--dur-draw/--dur-settle/--dur-sheet/--dur-drift` (all ≤300ms), `--dur-amb`/`--dur-amb-drift`. (10) DELETE the four `[data-unit="u1..u4"]` blocks and the default `--accent*` set entirely, and REMOVE the retired `--press-rest`, `--press-active`, `--scrim`, `--overlay-hero`, and every legacy semantic token no longer named by §2 (`--amber*`, `--green*` except where §2 reassigns — Athar uses `--olive` for the grade signal, so drop `--green*`; keep `--danger*` only if §2 references it — it does not, so drop it). Do NOT mention any retired name (`poppins`, `--accent`, `rgba(37,54`, `gummy`) in a comment — gated literals must not live in prose. Leave `@layer base` and everything below for the other tasks/plans.</action>
  <verify>
    <automated>test "$(grep -c '@layer tokens, base, components, screens, motion;' shared/awba-engine.css)" -eq 1 && ! sed -n '/^@layer tokens {/,/^@layer base {/p' shared/awba-engine.css | grep -qiE 'poppins' && test "$(grep -c "'Inter'" shared/awba-engine.css)" -eq 2 && ! grep -qE '\[data-unit="u[1-4]"\]' shared/awba-engine.css && ! sed -n '/^@layer tokens {/,/^@layer base {/p' shared/awba-engine.css | grep -qE 'rgba\(37,54,' && ! grep -qE "url\(['\"]?/" shared/awba-engine.css && grep -q -- '--kiswah:' shared/awba-engine.css && grep -q -- '--ease: cubic-bezier(0.23,1,0.32,1)' shared/awba-engine.css && grep -q "url('img/grain.png')" shared/awba-engine.css && node --test scripts/tests/*.test.js 2>&1 | grep -qE 'pass 37'</automated>
  </verify>
  <acceptance_criteria>
    - Layer-order line appears exactly once; no `poppins` within `@layer tokens` (residue in `@layer components` prose dies in plan 09; whole-file closure gated at 03-12); `'Inter'` appears exactly twice file-wide (font-face + --font-work).
    - No `[data-unit="u1..u4"]` block and no `--accent` DEFINITION remain in `@layer tokens`; `rgba(37,54,` is authored nowhere within `@layer tokens` (the `@layer motion` glow literal dies in plan 09); no leading-slash `url(/…)`.
    - The 17 colour tokens (`--kiswah` … `--pink`), the one `--ease`, and `url('img/grain.png')` are present.
    - `node --test scripts/tests/*.test.js` → `pass 37 / fail 0` (CSS-only edit; JS suite untouched).
  </acceptance_criteria>
  <done>@layer tokens is the Athar sheet: Athar fonts (Poppins retired, Inter twice), 17 colours + ink ramps, print radii, ink/edge depth, grain var, one-easing motion tokens; data-unit/accent + gummy/scrim/overlay tokens dismantled; 37 tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Re-ground @layer base — register grounds, data-sky, data-state, focus, scripture law</name>
  <files>shared/awba-engine.css</files>
  <read_first>
    - shared/awba-engine.css :200-312 (the `@layer base` block: reset/body, .app-shell shell KEEP-and-retoken, :focus-visible, Arabic/RTL + .ayah).
    - 03-UI-SPEC-ATHAR.md §2.5 usage rule, §2.6 grain application (.grain + .grain::after + per-ground --go + the scripture exclusion), §3.1 (the four scoped grounds verbatim CSS), §3.2 (D-A7 the `data-sky` tint over Orbit + the five `--sky-tint` values), §3.3 (D-A8 the `[data-state]` `--st` map + the ramp bar; shape rules are plan 09), §3.4 (Nightfall `.nightfall`), §2.2 scripture-law binding block + type table, §2.1 focus-ring rulings (D-A10).
  </read_first>
  <action>Refill `@layer base` (below the reset). (1) Re-token `body` to the Athar ground: `font-family: var(--font-work); color: var(--ink); background: var(--cream)` and keep the existing `.app-shell` responsive-shell STRUCTURE (grid rows, safe-area insets, the ≥600px column) — only swap its colour/radius/shadow values to Athar tokens (`--cream`, `--r-4`, `--sh-3`/`--keyline`). (2) Add the `.grain` / `.grain::after` utility per §2.6 (the tiled `var(--grain)`, `--go` opacity knob, `z-index:0`, `border-radius:inherit`) and note in code that scripture wrappers set `--go:0`. (3) Add the four register grounds verbatim per §3.1 — `.reg-orbit` (kiswah, `--go:.07`, `--icon-accent:var(--gold)`), `.reg-page` (cream, `--go:.028`, `--rule`, `--icon-accent:var(--crimson)`), `.reg-sky-night` (the last-third gradient + the `::before` apricot horizon glow, `--icon-accent:var(--gold)`), `.reg-festival` (cream + the `::before` conic checker trim + `padding-top:44px`, `--icon-accent:var(--harissa)`). (4) Add the `data-sky` tint layer per §3.2 — `.reg-orbit[data-sky]::after` reading `--sky-tint`, and the five `[data-sky="dawn|day|dusk|night|lastthird"]` gradients (law 1: the ground stays Kiswah). (5) Add the `[data-state="not-yet|progress|mastered"]` `--st` map + the ramp-bar gradient per §3.3 (the token layer only — the hollow/half/filled SHAPE rules belong to plan 09). (6) Recolour `:focus-visible` per D-A10: keep a default ring, then scope `.reg-page :focus-visible` / `.reg-festival :focus-visible` → Crimson ring, and `.reg-orbit :focus-visible` / `.reg-sky-night :focus-visible` / `.nightfall :focus-visible` → Gold ring (replace the old `[data-register="night"]` gold hook). (7) Refine the Arabic/RTL block to scripture law per §2.2: `.ayah` = Amiri Quran, `--fs-ayah`, lh 1.9, ls 0, `dir=rtl`, `unicode-bidi:isolate`, strongest ink, and the ONLY-glow rule permitted on dark grounds (`text-shadow: 0 0 24px rgba(244,240,247,.30)` scoped to scripture on `.reg-sky-night`/`.nightfall`); add `.scripture` (general Amiri, `--fs-scrip`, lh 1.85) for hadith/du'a; keep general Arabic UI on `--font-work` (`[lang="ar"]:not(.ayah):not(.scripture)`) per law 4. (8) Add the `.nightfall` interstitial per §3.4 (ground `--nightfall`, text `--moonmilk`, one ayah + the Courier "Sit with this" line, no grain, no celebration). Do NOT touch `@layer components`/`@layer motion`.</action>
  <verify>
    <automated>grep -q '\.reg-orbit' shared/awba-engine.css && grep -q '\.reg-page' shared/awba-engine.css && grep -q '\.reg-sky-night' shared/awba-engine.css && grep -q '\.reg-festival' shared/awba-engine.css && grep -q '\[data-sky="lastthird"\]' shared/awba-engine.css && grep -q '\[data-state="mastered"\]' shared/awba-engine.css && grep -q '\.reg-page :focus-visible' shared/awba-engine.css && grep -q '\.reg-orbit :focus-visible' shared/awba-engine.css && grep -q '\.nightfall' shared/awba-engine.css && grep -q '\.grain::after' shared/awba-engine.css && test "$(grep -c '@layer tokens, base, components, screens, motion;' shared/awba-engine.css)" -eq 1 && node --test scripts/tests/*.test.js 2>&1 | grep -qE 'pass 37'</automated>
  </verify>
  <acceptance_criteria>
    - All four `.reg-*` grounds, the five `data-sky` tints, the three `data-state` `--st` mappings, the `.grain::after` utility, the `.nightfall` interstitial, and per-ground `:focus-visible` recolours (crimson on cream, gold on dark) are present.
    - The layer-order line still appears exactly once (base refill did not re-declare it).
    - `node --test scripts/tests/*.test.js` → `pass 37 / fail 0`.
  </acceptance_criteria>
  <done>@layer base is re-grounded: four register scopes, home Sky-as-tint (law 1), the thermal token map, ground-scoped focus rings, grain utility, scripture law (.ayah/.scripture + the sole permitted glow), and Nightfall — 37 tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored CSS → rendered document | Pure static stylesheet content; no dynamic data, no user input, no network. The grain/font `url()`s resolve same-origin, CSS-relative. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | Tampering (asset path) | grain/font `url()` references | mitigate | All `url()` are CSS-relative (`fonts/…`, `img/grain.png`) — never a leading slash — so `file://` double-click and same-origin serving both resolve; the no-leading-slash grep gate enforces it. |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs, zero CDN, zero build — only committed static `.woff2`/PNG assets referenced. No `[ASSUMED]`/`[SUS]` package to gate. |
</threat_model>

<verification>
- Layer-order line exactly once; Poppins gone from `@layer tokens`; `'Inter'` exactly twice; no `[data-unit]` colour blocks; no `rgba(37,54,` in `@layer tokens`; no leading-slash `url()`.
- 17 colours, one `--ease`, grain var, four register grounds, five `data-sky` tints, thermal `--st` map, ground-scoped focus, scripture law, Nightfall all present.
- `node --test scripts/tests/*.test.js` → `pass 37 / fail 0` (CSS-only; JS untouched).
- NOTE for the gate (plan 12): `! grep -q -- '--accent'` and `! grep -qE 'rgba\(37,54,'` are NOT fully closed here — `@layer components`/`@layer motion` still reference the retired tokens; plan 09 removes the last usages.
</verification>

<success_criteria>
- MOT-01: the one easing family + duration tokens re-expressed under Athar in `@layer tokens`.
- MOT-04: the reduced-motion-ready `--dur-*` set (UI ≤300ms; ambient separated) in place; scripture-only glow rule established.
- Foundation ready: 17 colours, print radii, ink/edge depth, grain, four register grounds, data-sky/data-state, ground focus — the Ring (07), icons (08), components (09), sky (10), and preview (11) all build on these tokens.
</success_criteria>

<output>
Create `.planning/phases/03-components-icon-kit-motion-language/03-06-SUMMARY.md` when done.
</output>
