---
phase: 03-components-icon-kit-motion-language
plan: 11
type: execute
wave: 6
executor_hint: opus-visual
depends_on: ["03-10"]
files_modified: [preview.html, shared/fonts/poppins-500.woff2, shared/fonts/poppins-600.woff2, shared/fonts/poppins-700.woff2, shared/fonts/poppins-800.woff2]
autonomous: true
requirements: [FND-04, ENG-06, MOT-01, MOT-03, MOT-04]
must_haves:
  truths:
    - "Per §8 preview.html is rebuilt as the Athar living reference showing REAL AW.* output (not a spec dump): the eight sections are register worlds, type specimens, thermal ramp, component inventory, scripture demo, Ring demo, Sky demo, reduced-motion proof; the old indigo/gummy/confetti §1-12 are REMOVED, not retained"
    - "Per §8/CLAUDE.md the page is zero-CDN and file:// double-clickable: `! grep -qE 'https?://(fonts.googleapis|cdn)' preview.html` passes; the engine is a page-relative classic `<script src=\"shared/awba-engine.js\">` (no defer/async/module); font preloads are page-relative `shared/fonts/...` (never a leading slash)"
    - "Per §8.5 + scripture law the scripture demo opens the real citation sheet for hujurat-49-15 (Qur'an → .ayah / Amiri Quran, no grade) AND muslim-8 (hadith → general Amiri + olive grade pill) plus a .term gloss, on clean cream (no grain), strongest ink, with the always-on unverified · pending review pill and NOTHING celebratory adjacent — the demoCfg refs/terms are BYTE-VERBATIM from the existing preview (originally from _MVP-BUILD), never retyped or regenerated"
    - "Per §8.6 the Ring demo renders AW.ringSVG at three states (0 atoms, mid, 65 + closed thread) on the Orbit ground with a deterministic-per-seed note and a reduced-motion static render beside the animated one; per §8.7 the Sky demo shows the five data-sky temperatures as home cards plus the --dawn degree shown separately; per §8.8 a data-motion=reduce toggle proves the breathe stops, dabs settle in place, and the Ring renders static"
    - "Per §2.2/§8.2 the type specimens show the six faces at their rationed roles including REAL vocalised Arabic under scripture law (Amiri Quran / Amiri only for heavy tashkeel), with a note that Rakkas and Aref Ruqaa never share a screen; and Poppins is fully retired — the four unreferenced shared/fonts/poppins-*.woff2 files are deleted now that nothing (CSS or preview) references them"
  artifacts:
    - path: "preview.html"
      provides: "the rebuilt Athar living reference: 8 sections of real AW.* output (register worlds, type specimens, thermal ramp, component inventory, scripture demo, Ring demo, Sky demo, reduced-motion proof), zero-CDN, file://-openable"
      contains: "reg-orbit"
  key_links:
    - from: "preview scripture demo"
      to: "AW.cite/AW.wire → AW.sheetRef byte-verbatim scripture"
      via: "the demoCfg object literal copied byte-identical from the prior preview"
      pattern: "hujurat-49-15"
    - from: "preview Ring demo"
      to: "AW.ringSVG deterministic output"
      via: "three progress states + a reduced-motion static render"
      pattern: "ringSVG"
---

<objective>
Rebuild preview.html as the Athar living reference — the flagship visual plan and the vehicle the human gate (§9, plan 12) walks. Every section renders REAL AW.* output on the real register grounds so the page FEELS like Athar, not a spec dump. The old indigo/gummy/confetti sections are removed wholesale. This plan also removes the last Poppins reference (the preview's font preload) and deletes the now-unreferenced Poppins font files.

Purpose: FND-04 (the icon registry proven on the register grounds), ENG-06 (the citation/gloss sheet under scripture law), MOT-01/MOT-03/MOT-04 (the motion verbs, the one press, and both reduced-motion paths proven live).
Output: a rebuilt preview.html with §8's eight Athar sections, zero-CDN, file://-openable; the four shared/fonts/poppins-*.woff2 files deleted.

Executor routing: opus + load the frontend-design skill (flagship visual plan). This is the reference the owner judges — it must be beautiful and truthful. Show real AW.icon/AW.ringSVG/AW.cite/AW.sheetRef output, real register grounds, the real thermal shapes on both cream and dark, and the real reduced-motion behaviour. Scripture is byte-verbatim; never retype or regenerate it, and never place anything celebratory in or beside the scripture block (§9 gate item 4).

CONTRACT NOTE (Poppins deletion timing): the CSS stopped referencing Poppins in plan 06, but the OLD preview.html still preloaded poppins-700 until now, and the font files stayed on disk — so there was never a dangling 404. This plan removes the preview's Poppins preload AND deletes the four poppins-*.woff2 files in the SAME step, so zero references exist at deletion. (Amiri/Amiri Quran/Inter woff2 stay; Inter remains the silent ˹ ˺ fallback only.)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-components-icon-kit-motion-language/03-UI-SPEC-ATHAR.md

<interfaces>
<!-- Source of the byte-verbatim scripture + the engine surface the preview consumes. -->
preview.html (current) :2 (html data-unit — replace with register scopes), :10-16 (head: the poppins-700 + inter-400 preloads to replace + the classic engine include to KEEP), :1157-1163 (the demoCfg object literal: refs hujurat-49-15 / muslim-8, terms aqeedah — COPY THIS BLOCK BYTE-FOR-BYTE into the new file; it is verbatim scripture, never retype).
shared/awba-engine.js AW.icon(name,{size?,label?}) · AW.ringSVG(cfg) (plan 07) · AW.cite/AW.wire/AW.sheetRef (Qur'an .ayah / hadith olive grade pill + always-on pending pill) · AW.reducedMotion · AW.skyTemp (plan 10). AW.KIT is now 20 scenes (lantern-gold removed) — the registry-loop grid must not reference lantern-gold.
shared/awba-engine.css the register grounds (.reg-orbit/.reg-page/.reg-sky-night/.reg-festival), [data-sky] tints, [data-state] shapes, the paper-press inventory, the sheet, .ring, --dawn/.sky-breathe — all real classes the preview renders against.
Committed font files: shared/fonts/{readex-pro-400, amiri-quran-400, amiri-400}.woff2 (preload candidates); shared/fonts/poppins-{500,600,700,800}.woff2 (DELETE — unreferenced after this plan).
03-UI-SPEC-ATHAR.md §8 (the eight sections, :866-890), §2.2 (type roles + scripture law for §8.2), §3.3 (thermal shapes for §8.3), §4 (component inventory + law-8 wrong-answer for §8.4), §6 (Ring states for §8.6), §7 (five temperatures + --dawn for §8.7), §9 (what the gate will look for).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild preview head + §1 register worlds + §2 type specimens + §3 thermal ramp</name>
  <files>preview.html</files>
  <read_first>
    - preview.html :1-53 (the current head + preloads + engine include + the opening showcase style) and the overall section skeleton (:392-747) being replaced.
    - 03-UI-SPEC-ATHAR.md §8 sections 1-3, §2.2 (the six faces + roles + scripture-law block + the "Rakkas and Aref never share a screen" rationing note), §3.1 (the four grounds), §3.3 (thermal ramp + the three data-state shapes), §2.1 (the AA rulings so specimens sit on legible grounds).
  </read_first>
  <action>Rewrite preview.html from the top. In `<head>`: keep the single `<link rel="stylesheet" href="shared/awba-engine.css">` and the page-relative classic `<script src="shared/awba-engine.js"></script>` (no defer/async/module); REPLACE the font preloads with page-relative Athar preloads (`shared/fonts/readex-pro-400.woff2` + `shared/fonts/amiri-quran-400.woff2`, `crossorigin`) — remove BOTH poppins-700 and inter-400 preloads; zero CDN links. Set the document to a neutral shell (no `data-unit`). Author §1 REGISTER WORLDS — the four grounds side by side, each a real `.reg-orbit`/`.reg-page`/`.reg-sky-night`/`.reg-festival` panel with its grain `--go`, its `--icon-accent`, a real `AW.icon(...)` scene icon, and its motion verb named (draw/settle/breathe/drift/stamp) — proving law 1 (one ground each) at a glance. Author §2 TYPE SPECIMENS — the six faces at their rationed roles: Readex Pro (both scripts), Amiri + Amiri Quran under scripture law with REAL vocalised Arabic (heavy tashkeel only in the Amiri faces — never Readex/Aref/Rakkas), Marcellus display ≥28px, Aref Ruqaa term ≥40px in a Farag square, Rakkas festival ≥28px, Courier marginalia; include the note that Rakkas and Aref Ruqaa never co-appear. Author §3 THERMAL RAMP — the ramp bar + the three `[data-state]` dabs (not-yet hollow ring / progress half-dab / mastered filled+check) shown on BOTH cream and dark, proving the shape-cue holds where colour contrast is weak. Delete all old indigo/gummy/confetti/per-unit sections and the `pv-*` unit-switch script. Keep the closing `<script>` scaffold minimal for now (sections 4-8 land in Task 2).</action>
  <verify>
    <automated>! grep -qE 'https?://(fonts\.googleapis|cdn)' preview.html && ! grep -qiE 'poppins' preview.html && ! grep -qE '<script[^>]*type="module"' preview.html && grep -q 'src="shared/awba-engine.js"' preview.html && ! grep -qE 'href="/' preview.html && grep -q 'reg-orbit' preview.html && grep -q 'reg-page' preview.html && grep -q 'reg-sky-night' preview.html && grep -q 'reg-festival' preview.html && grep -q 'data-state="mastered"' preview.html && node --test scripts/tests/*.test.js 2>&1 | grep -qE 'fail 0'</automated>
  </verify>
  <acceptance_criteria>
    - Zero CDN links; no `poppins`; no `type="module"`; the classic engine include and no leading-slash `href="/"` remain; the four register grounds + the thermal `data-state` shapes are present.
    - The old per-unit/indigo/gummy/confetti sections are gone.
    - node --test scripts/tests/*.test.js → fail 0 (no engine files touched).
  </acceptance_criteria>
  <done>preview.html §1-3 render the four register worlds, the six type specimens under scripture law, and the thermal ramp on both grounds; head is zero-CDN, classic-script, page-relative, Poppins-free.</done>
</task>

<task type="auto">
  <name>Task 2: §4 inventory + §5 scripture demo (verbatim) + §6 Ring demo + §7 Sky demo + §8 reduced-motion proof + delete Poppins files</name>
  <files>preview.html, shared/fonts/poppins-500.woff2, shared/fonts/poppins-600.woff2, shared/fonts/poppins-700.woff2, shared/fonts/poppins-800.woff2</files>
  <read_first>
    - preview.html :1157-1163 (the demoCfg refs/terms object literal — copy byte-for-byte) and the closing IIFE pattern.
    - 03-UI-SPEC-ATHAR.md §8 sections 4-8, §4 (the component inventory + the law-8 wrong-answer: grey blot + one-line explanation + rose retry frame), §4.3 (the sheet face-split + olive grade pill + always-on pending pill), §6.6 (the Ring states), §7 (the five temperatures + --dawn), §9 (the ten gate items this will be walked against).
  </read_first>
  <action>Add the remaining sections. §4 COMPONENT INVENTORY — real AW.* on the Page ground: `.btn` (Page crimson) + an Orbit ghost-gold `.btn` on a dark panel, `.opt`/`.tf`/`.tile`/`.tab`/`.hstat`, `.cite`/`.term`, all sharing the one paper-press; plus a wrong-answer demo (grey ink-blot + a one-line explanation + the rose retry frame, law 8) placed FAR from any scripture. §5 SCRIPTURE DEMO (under scripture law) — a real `.cite` (via `AW.cite` + `AW.wire`) opening the citation sheet for `hujurat-49-15` (Qur'an → `.ayah`/Amiri Quran, no grade) AND `muslim-8` (hadith → general Amiri + the olive grade pill), plus a `.term` gloss for `aqeedah`; render on clean cream (no grain), strongest ink, with the always-on `unverified · pending review` pill and NOTHING celebratory in or beside the block. COPY the `demoCfg` refs/terms object literal byte-for-byte from the prior preview (:1157-1163) — it is verbatim scripture; never retype or regenerate it. §6 RING DEMO — `AW.ringSVG` at three states (0 atoms, mid, 65 + closed thread) on the `.reg-orbit` ground, a note that it is deterministic per seed, and a reduced-motion static render beside the animated one. §7 SKY DEMO — the five temperatures as a row of home cards (`data-sky` dawn/day/dusk/night/lastthird) plus the `--dawn` warmth degree shown separately (label it ambient, not the metric). §8 REDUCED-MOTION PROOF — a `data-motion="reduce"` toggle + a note about the OS setting; the reviewer sees the Sky `.sky-breathe` stop, the dabs render settled-in-place, and the Ring render static. Then DELETE the four unreferenced font files `shared/fonts/poppins-500.woff2`, `poppins-600.woff2`, `poppins-700.woff2`, `poppins-800.woff2` (nothing references them after Task 1). Keep the closing `<script>` real: registry-loop the icon grid from `Object.keys(AW.KIT)` (20 scenes, no lantern-gold), wire `AW.cite`/`AW.wire` with the verbatim demoCfg, render the three Ring states, and the sky/reduced-motion toggles.</action>
  <verify>
    <automated>grep -q 'hujurat-49-15' preview.html && grep -q 'muslim-8' preview.html && grep -q 'aqeedah' preview.html && grep -q 'ringSVG' preview.html && grep -q 'data-sky="lastthird"' preview.html && grep -q 'data-motion="reduce"' preview.html && ! grep -qiE 'poppins' preview.html && ! ls shared/fonts/poppins-700.woff2 2>/dev/null && ! ls shared/fonts/poppins-500.woff2 2>/dev/null && ls shared/fonts/readex-pro-400.woff2 >/dev/null && ls shared/fonts/amiri-quran-400.woff2 >/dev/null && ! grep -qE 'https?://(fonts\.googleapis|cdn)' preview.html && node --test scripts/tests/*.test.js 2>&1 | grep -qE 'fail 0'</automated>
  </verify>
  <acceptance_criteria>
    - §4-8 present: the component inventory + law-8 wrong-answer, the verbatim scripture demo (`hujurat-49-15` + `muslim-8` + `aqeedah`, no grain, pending pill, nothing celebratory adjacent), the three-state Ring demo, the five-temperature Sky demo, and the reduced-motion toggle.
    - The four `shared/fonts/poppins-*.woff2` files are deleted; `readex-pro-400.woff2` + `amiri-quran-400.woff2` remain; no `poppins` string anywhere in preview.html; zero CDN.
    - node --test scripts/tests/*.test.js → fail 0.
  </acceptance_criteria>
  <done>preview.html is the full Athar living reference (8 sections of real AW.* output, scripture verbatim under law, Ring + Sky + reduced-motion proven); the unreferenced Poppins font files are deleted; zero CDN, file://-openable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| demoCfg scripture → sheet innerHTML | Verbatim author scripture (byte-copied from _MVP-BUILD), never user input — same trust model as plan 03-03/03-04. |
| preview page → assets | All font/CSS/JS references are same-origin, page-relative; zero CDN, so file:// and served both resolve. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-11 | Tampering (content integrity) | demoCfg scripture | mitigate | Scripture is copied byte-for-byte from the prior preview (originally _MVP-BUILD), never retyped or regenerated; the `˹ ˺` brackets + full tashkeel survive; a grep gate confirms the ref IDs are present. |
| T-03-SC | Tampering | npm/CDN | accept | Zero CDN, zero package installs; only committed same-origin assets referenced (grep gate on CDN). No [ASSUMED]/[SUS] package to gate. |
</threat_model>

<verification>
- Zero CDN; no `poppins`; classic engine include; page-relative assets; the four register grounds + thermal shapes present.
- Scripture demo has `hujurat-49-15` + `muslim-8` + `aqeedah`, on clean cream, pending pill, nothing celebratory adjacent; demoCfg byte-verbatim.
- Ring demo (`ringSVG`, three states + reduced-motion static); Sky demo (five `data-sky` temperatures + --dawn); `data-motion="reduce"` toggle.
- The four poppins-*.woff2 files deleted; readex/amiri-quran remain.
- node --test scripts/tests/*.test.js → fail 0.
</verification>

<success_criteria>
- FND-04: the icon registry proven on the four register grounds (20 scenes, register-inked).
- ENG-06: the citation/gloss sheet demoed under scripture law (Qur'an .ayah / hadith olive grade pill / always-on pending pill), nothing celebratory adjacent.
- MOT-01/03/04: the register verbs, the one paper-press, and both reduced-motion paths proven live; the Ring + Sky flagships shown; Poppins fully retired from the repo.
</success_criteria>

<output>
Create `.planning/phases/03-components-icon-kit-motion-language/03-11-SUMMARY.md` when done.
</output>
