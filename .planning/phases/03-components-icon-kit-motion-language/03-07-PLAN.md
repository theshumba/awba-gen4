---
phase: 03-components-icon-kit-motion-language
plan: 07
type: execute
wave: 2
executor_hint: opus-visual
depends_on: ["03-06"]
files_modified: [shared/awba-engine.js, shared/awba-engine.css, scripts/tests/ring.test.js]
autonomous: true
requirements: [MOT-01]
must_haves:
  truths:
    - "Per §6.2 + D-A13 the Ring generator is fully deterministic: a seeded mulberry32 PRNG (~6 lines, inline) draws every jitter in a fixed order, so the SAME `seed` + same `atomsDone`/`circuitsDone` yields BYTE-IDENTICAL SVG markup across reloads and machines; there is NO `Date` and NO `Math.random` inside the generator"
    - "Per §6.1 + D-A13 the learner seed is stored ONCE in `awba_state.ringSeed` via an `AW.ringSeed()` accessor (read `awba_state.ringSeed`; if absent, generate a 32-bit int and `AW.S.set('ringSeed', …)` once, then it is stable) — the accessor path is used deliberately INSTEAD of an `awba_state` schema bump so the CR-01/WR-01 \"blob survives untouched on disk\" state tests stay green; the seed is the maker's mark (law 10)"
    - "Per §6.3 the SVG is concentric jittered pilgrim-rows (15 lesson rows banded into 4 circuits) of short 2–4-point dab `<path>`s with per-dab stroke-width in [1.8,4.1] and opacity in [0.45,0.95] and round linecaps (ink-bleed by variance, NEVER a `<filter>` blur), plus one outer `--gold` thread drawn as 4 arcs (one per completed circuit) and a single static `--gold` head-dot at the inking frontier — the centre never animates (law 9)"
    - "Per §6.4 progressive inking maps `atomsDone` (0..65) to which dabs are inked in course order (circuit→lesson→atom): faint un-inked dabs (`--navy`-ish) warm toward `--ember` in-progress, brighten to `--cream` when complete, and carry `--gold` when sealed; the `draw` (Orbit verb) `ink-draw` keyframe plays only on newly-inked dabs and the existing ring never re-draws"
    - "Per §6.5 reduced motion returns the EXACT final inked state statically (no `stroke-dash` draw, no head pulse) via an `AW.reducedMotion()` gate at call time; per §6.6 the output is ≤600 path nodes, carries no `<filter>`, and two different seeds produce visibly different fingerprints"
  artifacts:
    - path: "shared/awba-engine.js"
      provides: "AW.ringSeed() lazy accessor + mulberry32 PRNG + AW.ringSVG(cfg) deterministic generator"
      contains: "AW.ringSVG"
    - path: "scripts/tests/ring.test.js"
      provides: "node:test determinism + progression + reduced-motion + node-budget coverage for AW.ringSVG"
      contains: "ringSVG"
    - path: "shared/awba-engine.css"
      provides: "the ink-draw (Orbit draw verb) keyframe + .ring wrapper, reduced-motion-gated"
      contains: "ink-draw"
  key_links:
    - from: "AW.ringSVG determinism"
      to: "scripts/tests/ring.test.js byte-equality assertion"
      via: "same seed+progress → identical string"
      pattern: "assert.equal"
    - from: "AW.ringSVG reduced-motion branch"
      to: "AW.reducedMotion() call-time guard (plan 03-03)"
      via: "omit animation nodes when reduced motion is on"
      pattern: "reducedMotion"
---

<objective>
Build the Ring generator — the tawaf fingerprint, the named single point of failure — and judge it on visual output before anything depends on it. It is scheduled here, immediately after the token foundation (03-06), because that is the earliest its colour-token dependencies allow. The risky core is the DETERMINISM and the hand-inked read, both delivered and tested in this plan; the visual skin is minimal CSS layered on the token grounds. The Ring is proven visually in the rebuilt preview (§8, plan 11) and at the human gate (§9, plan 12).

Purpose: MOT-01 — the Orbit `draw` verb realised as the Ring's progressive inking; the macro-progress artefact (law 10 maker's mark) that Phase 4 lessons ink into.
Output: `AW.ringSeed()` + a seeded `AW.ringSVG(cfg)` generator in `shared/awba-engine.js`; the `ink-draw` keyframe + `.ring` wrapper in `shared/awba-engine.css`; `scripts/tests/ring.test.js` proving determinism, progression, the reduced-motion static render, and the ≤600-node budget.

Executor routing: opus + load the `frontend-design` skill (flagship visual plan / SPOF). The hand-inked tawaf read — jittered rows, variable stroke, ink-bleed by variance not blur — is the deliverable; mechanical determinism is what makes it trustworthy. Study §6.3's harvested ranges before authoring the construction loop.

CONTRACT NOTE (deliberate deviation from §6.1's parenthetical): §6.1 sketches "add to defaultState; migrate: absent → int". A live-file audit shows bumping the `awba_state` schema to v2 rewrites legacy v1 blobs on load, which breaks the CR-01 "from-the-future blob survives untouched" and WR-01 "seeded blob survives untouched on disk" tests. The lazy `AW.ringSeed()` accessor achieves the IDENTICAL observable behaviour D-A13 requires (seed created once, persisted once to `awba_state.ringSeed`, stable thereafter) with ZERO churn to the 37-test baseline. Do NOT bump `CURRENT` or add a migration entry.

KNOWN LIMITATION (plan-checker W1): `AW.S.set()` persists unconditionally — including over an unrecognized-future-schema blob that `load()` deliberately declined to persist (the CR-01 protection). `AW.ringSeed()` introduces a frequent new `AW.S.set` call site, so its lazy first write could clobber such a blob. IF a small guard (≤10 lines inside the STATE seam: an internal in-memory-fallback flag set by the unknown-schema branch, consulted by `set()` to skip `persist`) closes this cleanly, implement it WITH one new test proving a future-schema blob survives a `ringSeed()` call — the existing 37 stay untouched and green. If it needs anything more invasive, do NOT touch the state layer; record the limitation in the SUMMARY as a known pre-existing gap instead.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03-components-icon-kit-motion-language/03-UI-SPEC-ATHAR.md

<interfaces>
<!-- Where to write + the survivors this generator uses. -->
shared/awba-engine.js STATE IIFE (:27-253): AW.S.get(k,d)/AW.S.set(k,v) is the ONLY persistence seam — AW.ringSeed() reads/writes `ringSeed` through AW.S, never localStorage directly (D-24). `defaultState()` (:59-60) stays byte-untouched (adding a random field there would defeat the CR-01/WR-01 blob-equality tests).
shared/awba-engine.js RUNNERS banner (:1088-1090) — author a new `RING` block ABOVE it (after COMPONENTS, before RUNNERS) so AW.ringSeed/AW.ringSVG sit with the other AW.* builders; `globalThis.AW = AW` (:444) auto-exports them.
shared/awba-engine.js AW.reducedMotion (:920-934) — the call-time gate the reduced-motion branch consults (matchMedia OR documentElement[data-motion]='reduce'), guarded for headless.
shared/awba-engine.css @layer motion (:742-810) — add the `ink-draw` keyframe as an ADDITIVE block; do NOT rewrite the layer or delete the Gen-3 keyframes (plan 09 retires those). Colour tokens (--gold/--ember/--cream/--navy) come from 03-06's @layer tokens.
scripts/tests/ls-stub.js — makeLS + loadEngine(ls, probeSrc); scripts/tests/state-helpers.test.js is the node:test file shape to mirror.
03-UI-SPEC-ATHAR.md §6 (all of it: inputs :731-742, determinism :746-751, construction :753-772, progressive inking :774-780, reduced-motion+perf :782-788, acceptance :790-797) + §2.7 the `draw` verb recipe + the `--ease`/`--dur-draw` tokens.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AW.ringSeed() lazy accessor (stored once in awba_state.ringSeed)</name>
  <files>shared/awba-engine.js</files>
  <behavior>
    - AW.ringSeed() with no stored seed → returns a 32-bit unsigned integer AND persists it once via AW.S.set('ringSeed', seed).
    - AW.ringSeed() called again → returns the SAME value (read back from awba_state.ringSeed; no re-generation, no re-persist beyond the first).
    - A pre-seeded awba_state.ringSeed is returned verbatim, never overwritten.
    - `defaultState()` output is byte-unchanged; no `CURRENT` bump; no migration entry added — the CR-01/WR-01 "blob survives untouched" tests stay green.
  </behavior>
  <read_first>
    - shared/awba-engine.js :27-253 (STATE IIFE: defaultState, load, AW.S.get/set, the migration seam) — confirm AW.S.set persists and AW.S.get returns a defensive copy.
    - scripts/tests/state-storage.test.js :145-167 (the CR-01 future-blob + WR "survive untouched" assertions this plan must NOT break) and 03-UI-SPEC-ATHAR.md §6.1 + D-A13 + §11 (the maker's-mark rationale).
  </read_first>
  <action>In a new `RING` section (above the RUNNERS banner), add `AW.ringSeed = function(){ var s = AW.S.get('ringSeed', null); if (s === null || typeof s !== 'number') { s = (Math.random() * 0x100000000) >>> 0; AW.S.set('ringSeed', s); } return s; }`. The seed is a 32-bit unsigned int; `AW.S.set` persists it once through the existing state seam (D-24 — never touch localStorage directly here). Do NOT modify `defaultState()`, do NOT bump `CURRENT`, do NOT add a `migrations[]` entry (the accessor is the "absent → generate + persist once" path, deviating deliberately from §6.1's parenthetical to keep the blob-equality tests green). Keep the definition parse-time-safe (no DOM access).</action>
  <verify>
    <automated>node -e "const {makeLS,loadEngine}=require('./scripts/tests/ls-stub');const ls=makeLS({});const s=loadEngine(ls,'globalThis.__a=AW.ringSeed();globalThis.__b=AW.ringSeed();');const okType=(typeof s.__a==='number')&&(s.__a>>>0)===s.__a;const okStable=s.__a===s.__b;if(!(okType&&okStable)){console.error({a:s.__a,b:s.__b});process.exit(1);}console.log('RINGSEED_OK',s.__a);" && node --test scripts/tests/*.test.js 2>&1 | grep -qE 'pass 37'</automated>
  </verify>
  <acceptance_criteria>
    - `AW.ringSeed()` returns a stable 32-bit unsigned integer across repeated calls in one session; persisted through AW.S.
    - `defaultState()` is byte-unchanged; no schema bump; the full 37-test baseline stays green (`pass 37 / fail 0`).
  </acceptance_criteria>
  <done>AW.ringSeed() materialises the learner seed once into awba_state.ringSeed and is stable thereafter, with no schema bump and no baseline test churn; 37 tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AW.ringSVG(cfg) deterministic generator + ink-draw keyframe</name>
  <files>shared/awba-engine.js, shared/awba-engine.css</files>
  <behavior>
    - AW.ringSVG({seed:1, atomsDone:0}) === AW.ringSVG({seed:1, atomsDone:0}) (byte-identical; determinism).
    - AW.ringSVG({seed:1, atomsDone:0}) !== AW.ringSVG({seed:2, atomsDone:0}) (different seed → different fingerprint).
    - atomsDone:0 → all dabs faint; atomsDone:65 + circuitsDone:4 → all dabs inked + the gold thread closes (4 arcs) + a head-dot.
    - The returned string starts with `<svg`, contains no `<filter`, and has ≤600 `<path` (+ shape) nodes.
    - Under reduced motion (stubbed) the returned SVG contains no `stroke-dasharray`/`ink-draw` animation nodes — the final state, static.
  </behavior>
  <read_first>
    - 03-UI-SPEC-ATHAR.md §6.2 (mulberry32, fixed draw order, no Date/Math.random inside), §6.3 (rows=15 lessons banded into 4 circuits; short dab-strokes ~14–22/row; stroke-width [1.8,4.1]; opacity [0.45,0.95]; round caps; colour-by-state; the 4-arc gold thread; the static head-dot), §6.4 (atomsDone→inked-dab mapping in circuit→lesson→atom order; draw only on newly-inked), §6.5 (reduced-motion static render + ≤600 nodes + no filter), §2.7 the `draw` verb recipe (`ink-draw` keyframe on `stroke-dashoffset`, `--dur-draw var(--ease)`).
    - shared/awba-engine.js the AW.ringSeed accessor from Task 1 + AW.reducedMotion (:920-934); shared/awba-engine.css @layer motion (:742-810, additive insertion point).
  </read_first>
  <action>In the `RING` section add a ~6-line inline `mulberry32(seed)` PRNG returning a `function(){…}` in `[0,1)`, then `AW.ringSVG = function(cfg){…}`. Inputs per §6.1 (`seed`, `atomsDone` 0..65, `circuitsDone` 0..4, `structure={circuits:4,lessons:15,atoms:65}`, `size=300`, optional `inked`). Initialise the PRNG from `seed` and draw EVERY jitter (row radius, dab angle, positional wobble, per-dab stroke-width in [1.8,4.1], per-dab opacity in [0.45,0.95]) from it in a FIXED order — no `Date`, no `Math.random` anywhere in the generator. Build 15 rows banded into 4 circuits as broken arcs of ~14–22 short 2–4-point `<path>` dab-strokes with `stroke-linecap="round"`, colour-by-state per §6.3/§6.4 (faint un-inked `--navy`-ish `#4A5C82`@~.5 → in-progress warm `--ember` → complete bright `--cream` → sealed `--gold`), mapping `atomsDone` to inked dabs in circuit→lesson→atom order. Add one outer `--gold` thread as up to 4 arcs (one per `circuitsDone`; at 4 the ring closes) and a single static `--gold` head-dot at the inking frontier (no pulse — the centre never animates). NO `<filter>`; ink-bleed is the stroke/opacity variance + round caps only. Keep total nodes ≤600 (15×~22 + thread + head ≈ 340). Gate the animation: `if (AW.reducedMotion()) { …emit the final state with no stroke-dasharray/ink-draw… } else { …emit newly-inked dabs with the ink-draw draw animation… }`. Then, in `shared/awba-engine.css` `@layer motion`, ADD the `@keyframes ink-draw` (per §2.7: `stroke-dashoffset` from `var(--len)` to 0, an 8% opacity kick) and a `.ring` wrapper (positioning; content `z-index:1`) — additive only; do NOT delete or rewrite the existing Gen-3 keyframes (plan 09 retires those). Do not mention retired token names in comments.</action>
  <verify>
    <automated>node -e "const {makeLS,loadEngine}=require('./scripts/tests/ls-stub');const s=loadEngine(makeLS({}),'globalThis.__a=AW.ringSVG({seed:1,atomsDone:0});globalThis.__a2=AW.ringSVG({seed:1,atomsDone:0});globalThis.__b=AW.ringSVG({seed:2,atomsDone:0});globalThis.__full=AW.ringSVG({seed:1,atomsDone:65,circuitsDone:4});');const det=s.__a===s.__a2;const diff=s.__a!==s.__b;const svg=s.__a.indexOf(\"<svg\")===0;const nofilter=s.__full.indexOf(\"<filter\")===-1;const nodes=(s.__full.match(/<path/g)||[]).length;const budget=nodes<=600;if(!(det&&diff&&svg&&nofilter&&budget)){console.error({det:det,diff:diff,svg:svg,nofilter:nofilter,nodes:nodes});process.exit(1);}console.log('RINGSVG_OK nodes',nodes);" && grep -q 'ink-draw' shared/awba-engine.css && ! grep -qE 'rgba\(37,54,' /dev/null; node --test scripts/tests/*.test.js 2>&1 | grep -qE 'pass 37'</automated>
  </verify>
  <acceptance_criteria>
    - Determinism: identical seed+progress → byte-identical string; different seed → different string.
    - `atomsDone:65 + circuitsDone:4` fully inks + closes the gold thread; output starts `<svg`, has no `<filter`, ≤600 `<path` nodes.
    - `ink-draw` keyframe present in `@layer motion`; the Gen-3 keyframes are untouched (retired later by plan 09).
    - `node --test scripts/tests/*.test.js` → `pass 37 / fail 0` (ring.test.js is added in Task 3).
  </acceptance_criteria>
  <done>AW.ringSVG is a deterministic seeded generator emitting a hand-inked tawaf fingerprint (jittered rows, variable stroke, ink-bleed by variance, 4-arc gold thread, static head-dot), reduced-motion-static, ≤600 nodes, no filter; the ink-draw keyframe is added additively; 37 tests green.</done>
</task>

<task type="auto">
  <name>Task 3: ring.test.js — determinism + progression + reduced-motion + node-budget</name>
  <files>scripts/tests/ring.test.js</files>
  <read_first>
    - scripts/tests/state-helpers.test.js (full — the require(node:test)+node:assert/strict + loadEngine + `globalThis.__out` probe idiom to copy) and scripts/tests/ls-stub.js :45-52 (loadEngine).
    - 03-UI-SPEC-ATHAR.md §6.6 acceptance criteria 1–4/6 (determinism, seed-difference, 0→all-faint / 65+4→inked+closed, reduced-motion exact-final-static, ≤600 nodes / no filter) — criterion 5 (reads as hand-inked) is the human §9 gate, not a unit test.
  </read_first>
  <action>Create `scripts/tests/ring.test.js` requiring `node:test` + `node:assert/strict` + `{ makeLS, loadEngine }` from `./ls-stub`. Add tests, all via the loadEngine(ls, probeSrc) + `globalThis.__out` trick: (1) DETERMINISM — `AW.ringSVG({seed:7,atomsDone:20,circuitsDone:1})` equals a second identical call (byte-for-byte). (2) SEED DIFFERENCE — seed 7 vs seed 8 (same progress) differ. (3) PROGRESSION — `atomsDone:0` output differs from `atomsDone:65,circuitsDone:4` output, and the full-progress output contains the gold thread/head markers your generator emits. (4) REDUCED MOTION — with a stubbed `matchMedia(){return {matches:true}}` (or `document.documentElement[data-motion]='reduce'`), the returned SVG contains no `stroke-dasharray`/`ink-draw` animation node. (5) NODE BUDGET / NO FILTER — full-progress output has ≤600 `<path` occurrences and zero `<filter`. Keep every assertion a pure string/value check (visual read is the human gate). The suite is run ONLY via `node --test scripts/tests/*.test.js` (glob; the directory form breaks on this Node).</action>
  <verify>
    <automated>OUT=$(node --test scripts/tests/*.test.js 2>&1); echo "$OUT" | grep -qE 'fail 0' && echo "$OUT" | grep -qE 'pass (4[2-9]|[5-9][0-9]|[0-9]{3,})' && node --test scripts/tests/ring.test.js 2>&1 | grep -qE 'fail 0'</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/tests/ring.test.js` exists under `scripts/tests/` and is picked up by the glob.
    - Tests cover determinism, seed-difference, 0-vs-full progression, reduced-motion static render, and the ≤600-node / no-filter budget (≥5 new tests).
    - `node --test scripts/tests/*.test.js` → total rises to `37 + ring tests`, `fail 0`.
  </acceptance_criteria>
  <done>ring.test.js proves the SPOF's determinism, seed-uniqueness, progressive inking, reduced-motion static render, and node budget; the full glob suite passes with fail 0 and a raised total.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AW.ringSVG output → innerHTML | The generated SVG string is later injected via innerHTML (home/preview). It is built entirely from a seeded PRNG + fixed numeric structure — no user input, no author free-text enters the markup. |
| awba_state.ringSeed → persistence | The seed is a locally-generated integer stored through AW.S; device-local, no network. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Tampering (DOM) | AW.ringSVG innerHTML | mitigate | The SVG is composed only from numbers (seeded jitter, coordinates) and fixed token names — no dynamic string field is interpolated; determinism tests pin the exact output shape. |
| T-03-SC | Tampering | npm installs | accept | No package installs; mulberry32 is ~6 hand-authored lines; zero runtime deps, nothing fetched. No `[ASSUMED]`/`[SUS]` package to gate. |
</threat_model>

<verification>
- Determinism (same seed+progress → identical string), seed-difference, 0→faint / 65+4→inked+closed, reduced-motion static, ≤600 nodes / no `<filter>` — all proven by the node one-liners + ring.test.js.
- AW.ringSeed() stable + persisted once; defaultState unchanged; no schema bump.
- ink-draw keyframe present; Gen-3 keyframes untouched.
- `node --test scripts/tests/*.test.js` → 37 baseline + ring tests, `fail 0`.
</verification>

<success_criteria>
- MOT-01: the Orbit `draw` verb realised as the Ring's progressive inking, on the one easing family.
- The SPOF is de-risked early and testably: deterministic per seed, hand-inked read, reduced-motion static, within the perf budget — ready for the preview demo (11) and the human visual gate (12).
</success_criteria>

<output>
Create `.planning/phases/03-components-icon-kit-motion-language/03-07-SUMMARY.md` when done.
</output>
