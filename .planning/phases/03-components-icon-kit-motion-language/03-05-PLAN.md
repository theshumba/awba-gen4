---
phase: 03-components-icon-kit-motion-language
plan: 05
type: execute
wave: 4
executor_hint: sonnet-mechanical
depends_on: ["03-04"]
files_modified: []
autonomous: false
requirements: [FND-04, ENG-06, MOT-01, MOT-03, MOT-04]
must_haves:
  truths:
    - "Per D-44, the phase gate is a human visual verify (like Phase 1's D-12): Melusi opens preview.html over file:// and walks the checklist — icons crisp at UI sizes, gold lantern intentional on night, sheets premium, press gummy-not-mushy, reduced-motion collapses everything via BOTH trigger paths"
    - "Per D-44, automated prechecks run FIRST and must be green before the human sign-off: KIT count (21) + GLYPHS (13), zero CDN in preview, both reduced-motion triggers in the CSS, no runtime recolor, no per-page icon constants, and the full test suite"
    - "The checklist includes the u4-context combo-chip check (the gold combo still reads as an event on gold-unit pages — UI-SPEC Color addendum) and BOTH reduced-motion trigger paths (OS 'Reduce Motion' setting AND the in-app data-motion toggle)"
  artifacts: []
  key_links:
    - from: "reviewer (Melusi)"
      to: "preview.html + automated prechecks"
      via: "the D-44 checklist walked after the objective gates pass"
      pattern: "preview.html"
---

<objective>
Run the phase's designed verification vehicle: the reviewer opens `preview.html` over `file://` and walks the D-44 checklist to confirm the Phase-3 success criteria that are inherently visual (icon crispness, gold-lantern intentionality, sheet premium-ness, press feel, reduced-motion quieting) — the things grep and node:test cannot prove. The automated prechecks run FIRST as the objective gate; the human walk is LAST. This mirrors Phase 1's D-12 gate and Josh's real workflow (open the file directly).

Purpose: D-44 — the human visual gate before `/gsd:verify-work`.
Output: reviewer approval, or a specific punch list of gaps to route to `/gsd:plan-phase --gaps`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/03-components-icon-kit-motion-language/03-UI-SPEC.md
@.planning/phases/03-components-icon-kit-motion-language/03-VALIDATION.md
@preview.html
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Reviewer walkthrough of preview.html against the D-44 checklist</name>
  <files>(none — verification only)</files>
  <read_first>
    - 03-UI-SPEC.md "preview.html Contract" (§9-12), "Color › u4 overlap addendum" (the u4-context combo check), "Reduced Motion" (both trigger paths), "Gold Lantern Variant" (night-register intentionality), "Component Anatomy" (sheet premium, press feel).
    - 03-RESEARCH.md "Human-visual (D-44) — the irreducibly non-automatable gate" (the exact visual criteria) + "Phase Requirements → Test/Check Map" (the automated precheck commands).
    - 03-VALIDATION.md "Manual-Only Verifications" (the D-44 instruction: OS Reduce Motion toggle + AW.prefs.set('motion','reduce') both demonstrated).
  </read_first>
  <action>Run the automated prechecks below FIRST (they gate the human sign-off — the visual approval cannot pass while any objective gate is red). Then present preview.html to the reviewer and walk the D-44 checklist, collecting a verdict. Do not mark the phase complete until every checklist item passes or specific gaps are recorded for `/gsd:plan-phase --gaps`.</action>
  <what-built>
    The complete shared component layer rendered in preview.html §9-12: the single 20-icon registry (+ 13 glyphs + authored gold lantern), the citation/gloss bottom-sheet system with the Quran/hadith face-split and always-on pending pill, one gummy press across the full tappable inventory, one `linear()`-token motion vocabulary, and global dual-trigger reduced-motion — all real `AW.*` engine output, zero CDN, with the 26+ test suite green.
  </what-built>
  <how-to-verify>
    Open `preview.html` directly (double-click / `file://`) and confirm each D-44 item:
    1. §9 icon grid: all 20 scene icons render crisply at UI sizes and the grid reads as a premium BRAND MOMENT (consistent tiles, cream field, soft radii, indigo shadows) — not an asset dump; the 13 glyphs render as real glyphs (no tofu).
    2. §9 gold lantern: `lantern-gold` on its dark night-register swatch reads INTENTIONAL and holds contrast (bright structural highlights visible on the dark field) — clearly an authored variant, not a flat tint.
    3. §9 unit switch: toggling u1→u4 recolours the accent-tinted icons; on u4 (the gold unit) the set still reads coherently.
    4. §10 Quran citation (al-Ḥujurāt 49:15): tapping the chip opens a sheet whose Arabic is the Amiri Quran (`.ayah`) face, with NO grade pill, the calm lowercase `unverified · pending review` pill present, and the ˹ ˺ brackets rendering in the translation. Nothing celebratory anywhere near this block.
    5. §10 hadith citation (Ṣaḥīḥ Muslim 8): tapping opens a sheet in general Amiri WITH a green `Sahih` grade pill AND the pending pill; the term chip (aqeedah) opens the gloss sheet (Arabic / transliteration / word / definition / context). Sheets feel premium — gentle slide-in, soft top corners, comfortable spacing.
    6. §11 press inventory: press each of `.btn / .opt / .tf / .tile / .cite / .tab / .hstat / .sheet-row` (the full MOT-03 inventory — sheet rows also pressable inside the open §10 sheets) — the press is one coherent GUMMY feel (shadow collapses + surface drops), gummy-not-mushy, identical timing across the inventory.
    7. §11/§12 u4-context combo: confirm the combo chip (gold register) still reads as a celebratory EVENT — distinguished by pill shape + spark glyph + pop-in — even against a gold-unit context (it is not conflated with the amber miss colour).
    8. §12 reduced-motion via OS setting: enable the OS "Reduce Motion" setting, reload — the confetti button no-ops (nothing spawns), the companion bob/glow loop STOPS (static), and transitions/press collapse to near-instant.
    9. §12 reduced-motion via in-app toggle: with OS reduce-motion OFF, click the `data-motion="reduce"` toggle (or run `AW.prefs.set('motion','reduce')` then reload) — the SAME quieting happens (confetti no-op, companion loop stopped). Both trigger paths proven.
    10. Overall: the new stretch reads as a premium living style reference; nothing celebratory ever sits over or adjacent to the scripture demo (§10).
  </how-to-verify>
  <verify>
    <automated>node --test scripts/tests/*.test.js 2>&1 | grep -qE 'fail 0' && node -e "const {makeLS,loadEngine}=require('./scripts/tests/ls-stub');const s=loadEngine(makeLS({}),'globalThis.__k=Object.keys(AW.KIT).length;globalThis.__g=Object.keys(AW.GLYPHS).length;');if(s.__k!==21||s.__g!==13)process.exit(1);" && ! grep -qE 'https?://(fonts\.googleapis|gstatic|cdn)' preview.html && grep -q 'prefers-reduced-motion: reduce' shared/awba-engine.css && grep -q '\[data-motion="reduce"\]' shared/awba-engine.css && ! grep -q '\.replace(/#' shared/awba-engine.js && ! grep -rqE 'UIC|IC_STAR|IC_CHEST|IC_TROPHY|IC_LOCK|LANTERNG' shared/ && echo "AUTOMATED PRECHECKS PASS"</automated>
    <human-check>Reviewer confirms all 10 D-44 checklist items above on a real browser, including the u4-context combo item and BOTH reduced-motion trigger paths.</human-check>
  </verify>
  <acceptance_criteria>
    - Automated prechecks pass: full suite `fail 0`; `AW.KIT`=21 and `AW.GLYPHS`=13; zero CDN in preview; both reduced-motion triggers in the CSS; no `.replace(/#` runtime recolor; no per-page `UIC`/`IC_*`/`LANTERNG` constants in shared/.
    - Reviewer confirms items 1-10; any failure is captured as a specific gap (which item, what was wrong) for `/gsd:plan-phase --gaps`.
  </acceptance_criteria>
  <done>Reviewer has confirmed all 10 D-44 checklist items (or a specific gap list is recorded); automated prechecks are green.</done>
  <resume-signal>Type "approved" to close Phase 3, or list the failing checklist item(s) and what was wrong.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none | Verification-only plan — no code changes, no new runtime surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-VER | Verification integrity | manual visual sign-off | accept | The objective gates (test suite, KIT/GLYPHS counts, zero-CDN, both reduced-motion triggers, no-recolor, no per-page constants) run automatically as prechecks BEFORE the human sign-off, so the visual approval cannot pass while any objective gate is red. No runtime security surface exists in a verification-only plan. |
| T-03-SC | Tampering | npm installs | accept | No installs; nothing fetched; the gate only reads existing committed assets. |
</threat_model>

<verification>
- `node --test scripts/tests/*.test.js` → `fail 0`.
- `AW.KIT`=21, `AW.GLYPHS`=13 (node one-liner).
- `! grep -qE 'https?://(fonts\.googleapis|gstatic|cdn)' preview.html`.
- Both reduced-motion triggers present in `shared/awba-engine.css`.
- `! grep -q '\.replace(/#' shared/awba-engine.js` and `! grep -rqE 'UIC|IC_STAR|IC_CHEST|IC_TROPHY|IC_LOCK|LANTERNG' shared/`.
- Reviewer approves the D-44 10-item checklist.
</verification>

<success_criteria>
- All four Phase-3 success criteria are confirmed: one icon registry (crisp, single-source, authored gold lantern); citation/gloss sheets (both faces, grade + pending pills); one shared press + motion vocabulary; global reduced-motion quieting via both trigger paths.
</success_criteria>

<output>
Create `.planning/phases/03-components-icon-kit-motion-language/03-05-SUMMARY.md` recording the reviewer verdict (approved / gap list) when done.
</output>
