---
phase: 03-components-icon-kit-motion-language
plan: 04
type: execute
wave: 3
executor_hint: opus-visual
depends_on: ["03-01", "03-02", "03-03"]
files_modified: [preview.html]
autonomous: true
requirements: [FND-04, ENG-06, MOT-01, MOT-03, MOT-04]
must_haves:
  truths:
    - "Per D-43, preview.html loads the real engine via <script src=\"shared/awba-engine.js\"></script> (page-relative, classic, no defer/async/type=module) and adds §9-12 demoing REAL AW.* output (not pv- mocks); §5's mocks stay as the Phase-1 token reference, superseded not deleted"
    - "Per D-43, §9 renders all 20 scenes via AW.icon(name) at scene-tile size + the 13 GLYPHS row + lantern-gold on a night-register (dark gradient) swatch; the grid reads as a brand moment (premium), and the live unit-switch recolours accent-tinted icons"
    - "Per D-38, §10 opens the citation sheet for hujurat-49-15 (Quran → .ayah/Amiri Quran, no grade pill) AND muslim-8 (hadith → general Amiri + grade pill 'Sahih'), plus the aqeedah gloss — all content ported VERBATIM from Josh's u1-m1.html; NOTHING celebratory renders in or adjacent to this scripture block"
    - "Per D-40/D-42, §11 shows .btn/.opt/.tf/.tile/.cite/.tab all sharing the gummy :active, and §12 labels each motion signature with its token, demos the confetti + PERFECT WAAPI exemplar SPATIALLY SEPARATED from §10, and exposes BOTH reduced-motion trigger paths (OS setting note + a data-motion=\"reduce\" toggle) so confetti no-ops and the companion loop stops"
  artifacts:
    - path: "preview.html"
      provides: "§9 icon grid, §10 sheet demos (verbatim scripture), §11 press inventory, §12 motion + reduced-motion; real engine script include"
      contains: "shared/awba-engine.js"
  key_links:
    - from: "§10 citation chips"
      to: "AW.cite + AW.wire + AW.sheetRef"
      via: "real engine output, verbatim demoCfg refs/terms"
      pattern: "hujurat-49-15"
    - from: "§12 reduced-motion toggle"
      to: "documentElement[data-motion]"
      via: "aria-pressed toggle → both trigger paths demonstrated"
      pattern: "data-motion"
---

<objective>
Extend `preview.html` — the one living style reference — with four new sections that demo the REAL Phase-3 engine output (not preview mocks), proving every Phase-3 deliverable in a browser opened over `file://`. Add the engine `<script>` include, then §9 (the 20-icon brand-moment grid + glyphs + gold lantern on night), §10 (the citation + gloss sheets with byte-verbatim scripture), §11 (the gummy press inventory), and §12 (the motion vocabulary + confetti/PERFECT exemplar + the dual reduced-motion toggle). This is the FLAGSHIP visual plan and the vehicle the D-44 human gate (plan 05) walks.

Purpose: FND-04/ENG-06/MOT-01/MOT-03/MOT-04 all become visually confirmable; D-43's "living reference of the real thing."
Output: `preview.html` grows from §1-8 to §1-12 with a real engine include; §5's mocks stay as the Phase-1 token reference (superseded, not deleted).

Load the `frontend-design` skill during execution. §9 is the phase's headline — the branded 20-set must read as a brand moment, not an asset dump: consistent tile sizing, cream field, soft radii, indigo shadows, the gold lantern on its dark home. Mercy law binds the layout: §10's scripture block has ZERO confetti/PERFECT/combo adjacency; those live only in §12, spatially separated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-components-icon-kit-motion-language/03-UI-SPEC.md
@.planning/phases/03-components-icon-kit-motion-language/03-RESEARCH.md
@.planning/phases/03-components-icon-kit-motion-language/03-PATTERNS.md

<interfaces>
<!-- Insertion points + real engine API now available (plans 02-03). -->
preview.html :14-15 stylesheet link (add the engine <script> right after) · :783 §8 opens · :798 </main> (new §9-12 go before this) · :800-852 the closing classic-script IIFE (extend for the icon-grid loop + sheet wiring + motion toggle) · existing pv- section idioms: §7 glyph grid :669-778, §4 tiles :547-579, §5 motion mocks + replay :584-624/:831-840, §2 unit-switch aria-pressed toggle :458-461/:816-828.
Engine API to call: AW.icon(name,{size?,label?}) · AW.cite(id,label) · AW.wire(root,cfg) · AW.sheet(html) · AW.sheetRef(refs,id) · AW.sheetTerm(terms,id) · AW.confetti(n) · AW.animate(el,keyframes,durToken,easeToken) · AW.KIT / AW.GLYPHS keys · AW.UNIT_ICON.
Icon sizing (UI-SPEC): scene-tile 56px width; glyph-ui 18px; scene-hero 128px (lantern-gold on night).
</interfaces>

<verbatim_demo_content>
<!-- D-38: copy the refs/terms objects BYTE-IDENTICAL from RESEARCH "Demo content to port VERBATIM" (which recorded them from _MVP-BUILD/lessons/u1-m1.html). NEVER retype/paraphrase the Arabic or translation. Source of truth: 03-RESEARCH.md "Code Examples › Demo content to port VERBATIM (D-38)". Build demoCfg = { refs: { 'hujurat-49-15': {…}, 'muslim-8': {…} }, terms: { aqeedah: {…} } }. -->
- hujurat-49-15 : Qur'an, NO grade → .ayah / Amiri Quran face; its `mean` carries the ˹true˺ brackets (Poppins→Inter fallback).
- muslim-8 : hadith, kind:'The hadith', grade:'Sahih' → general Amiri + grade pill.
- aqeedah : term gloss (ar/tl/word/def/ctx).
</verbatim_demo_content>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Engine include + §9 icon grid + §10 sheet demos (verbatim scripture)</name>
  <files>preview.html</files>
  <read_first>
    - preview.html :14-15 (stylesheet link — add the engine script after), :669-778 (§7 glyph grid — the section→.pv-wrap→.pv-head→grid-of-cells skeleton to copy), :604-618 + :842-850 (§5 sheet mock markup + getElementById/addEventListener wiring idiom to SUPERSEDE with real AW.*), :800-852 (closing IIFE), :783/:798 (§8 close / </main>).
    - 03-UI-SPEC.md "preview.html Contract" (§9 + §10 specs, the focal-point rule, mercy binding) + "Icon Sizing Scale" + "Copywriting Contract › Verbatim demo content".
    - 03-RESEARCH.md "Code Examples › Demo content to port VERBATIM (D-38)" (the exact byte-identical refs/terms objects) + "preview.html extension (D-43)" (§9/§10 + must add the engine <script>).
    - 03-PATTERNS.md "preview.html — §9 Icon registry grid" (grid CSS + real AW.icon() wiring) and "§10 Sheet demos" (real AW.cite/AW.wire example + the zero-celebration mercy binding).
  </read_first>
  <action>In `<head>` immediately after the stylesheet link (line 15), add `<script src="shared/awba-engine.js"></script>` — page-relative (no leading slash), classic (no `defer`/`async`/`type="module"`), loading BEFORE the closing inline `<script>`. Before `</main>` (line 798), add `<section class="pv-section" id="s9">` copying the §7 skeleton (`.pv-wrap` → `.pv-head` with `<span class="pv-num">9</span>Icon registry` + a one-line intro `<p>`): a grid (reuse `.pv-glyph`/`.pv-gcell` or a new `.pv-icell` pair) of all 20 `AW.KIT` scenes rendered via `AW.icon(name)` at scene-tile 56px in `--paper`/`--r-md`/`--sh-1` tiles with `--fs-kicker` name labels, a GLYPHS row (the 13 glyphs at glyph-ui ~18px), and ONE wide dark-gradient (night-register) swatch cell rendering `lantern-gold` at scene-hero ~128px so it reads intentional and holds contrast. Wire the grid in the closing IIFE by looping over `Object.keys(AW.KIT)`/`Object.keys(AW.GLYPHS)` and setting each tile's art via `AW.icon(...)` (classic DOM, no document.write). Then add `<section ... id="s10">` for the sheet demos: build a prose block containing `AW.cite('hujurat-49-15','al-Ḥujurāt 49:15')` + `AW.cite('muslim-8','Ṣaḥīḥ Muslim 8')` + a `<span class="term" data-term="aqeedah">aqeedah</span>`, inject it, then `AW.wire(thatRoot, demoCfg)` where `demoCfg` carries the three VERBATIM entries (copied byte-identical from RESEARCH). Tapping the Quran chip must open the `.ayah`/Amiri-Quran sheet (no grade pill); the hadith chip the general-Amiri sheet WITH the `Sahih` grade pill; both with the always-on pending pill; the term chip the gloss sheet. This section must contain NO confetti/PERFECT/combo element or `AW.confetti(` call anywhere (D-38 mercy law). Do not alter §1-8 markup.</action>
  <verify>
    <automated>grep -q '<script src="shared/awba-engine.js"></script>' preview.html && ! grep -qE 'awba-engine\.js"[^>]*(defer|async|type="module")' preview.html && for s in s9 s10; do grep -q "id=\"$s\"" preview.html || exit 1; done && grep -q 'hujurat-49-15' preview.html && grep -q 'muslim-8' preview.html && grep -q 'aqeedah' preview.html && grep -q 'Sahih' preview.html && grep -q '˹true˺' preview.html && ! sed -n '/id="s10"/,/id="s11"/p' preview.html | grep -qiE 'confetti|class="perfect"|class="combo"|AW\.confetti\(' && ! grep -qE 'https?://(fonts\.googleapis|gstatic|cdn)' preview.html && echo PREVIEW_A_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '<script src="shared/awba-engine.js"></script>' preview.html` passes; the include carries NO `defer`/`async`/`type="module"`.
    - `id="s9"` and `id="s10"` sections exist.
    - Verbatim markers present: `hujurat-49-15`, `muslim-8`, `aqeedah`, `Sahih`, and the bracket string `˹true˺` (proves the Qur'an `mean` was copied byte-identical, not retyped).
    - Mercy isolation: `! sed -n '/id="s10"/,/id="s11"/p' preview.html | grep -qiE 'confetti|class="perfect"|class="combo"|AW\.confetti\('` passes (no celebration in/adjacent to the scripture block).
    - Zero CDN: `! grep -qE 'https?://(fonts\.googleapis|gstatic|cdn)' preview.html` passes.
  </acceptance_criteria>
  <done>preview.html loads the real engine; §9 renders the 20-icon brand-moment grid + glyphs + lantern-gold on night; §10 opens real citation (Quran + hadith) and gloss sheets from verbatim content with zero celebration adjacency; zero CDN.</done>
</task>

<task type="auto">
  <name>Task 2: §11 press inventory + §12 motion vocabulary + dual reduced-motion toggle</name>
  <files>preview.html</files>
  <read_first>
    - preview.html :547-579 (§4 labelled-tile-row idiom for §11), :592-618 + :831-840 (§5 motion cell + replay-button idiom for §12), :458-461 + :816-828 (§2 aria-pressed toggle convention for the data-motion toggle), :800-852 (closing IIFE to extend).
    - 03-UI-SPEC.md "preview.html Contract" (§11 + §12 specs), "Press Physics" (the inventory), "Motion Vocabulary" (signature→token labels + WAAPI exemplar target = PERFECT scale-pop), "Reduced Motion" (both trigger paths; confetti no-op; loop stop).
    - 03-PATTERNS.md "§11 Press-physics inventory" (real .btn/.opt/.tf/.tile/.cite/.tab in tiles) and "§12 Motion vocabulary + reduced-motion" (replay idiom + the data-motion toggle script).
  </read_first>
  <action>After §10, add `<section ... id="s11">` (§11 press inventory): a `.pv-card` holding a labelled row where each tile contains a REAL engine class instance — `<button class="btn">Begin, gently</button>`, `<button class="opt">Option</button>`, `<button class="tf">True</button>`, `<div class="tile">tile</div>`, a `<span class="cite">al-Ḥujurāt 49:15</span>`, a `<button class="tab">Tab</button>`, a `<button class="hstat">🔥 3</button>`-style HUD stat chip, and a `<button class="sheet-row">Sheet row</button>` — the FULL MOT-03 inventory (all 8 surface classes) sharing the gummy/light `:active` from @layer components (no `pv-gummy` mock), so the reviewer confirms one coherent press. Then add `<section ... id="s12">` (§12 motion + reduced-motion): one `.pv-mcell`-style cell per vocabulary signature (press / hover / pop-in / sheet-in / fade / ambient), each labelled with its token pair (e.g. `--dur-5 · --ease-spring`) and a replay button using the force-reflow idiom; a confetti trigger button calling `AW.confetti(24)` and a PERFECT-overlay demo driven by the `AW.animate(el, keyframes, '--dur-5','--ease-spring')` WAAPI exemplar (scale-pop) — these celebration demos live HERE, spatially separated from §10; and a reduced-motion `<button id="pv-motion-toggle" aria-pressed="false">` that toggles `document.documentElement.setAttribute('data-motion', reduced?'system':'reduce')` (mirroring the §2 aria-pressed convention) plus a short note that the OS "Reduce Motion" setting is the second trigger — so a reviewer proves BOTH paths (confetti no-ops, companion bob/glow stops) without editing prefs. Wire all buttons in the closing IIFE (classic addEventListener). Do not alter §1-10.</action>
  <verify>
    <automated>for s in s11 s12; do grep -q "id=\"$s\"" preview.html || exit 1; done && grep -q 'id="pv-motion-toggle"' preview.html && grep -q 'data-motion' preview.html && grep -q 'aria-pressed' preview.html && grep -q 'AW.confetti(' preview.html && grep -q 'AW.animate(' preview.html && for c in 'class="btn"' 'class="opt"' 'class="tf"' 'class="tile"' 'class="tab"' 'class="hstat"' 'class="sheet-row"'; do grep -q "$c" preview.html || exit 1; done && ! grep -qE 'https?://(fonts\.googleapis|gstatic|cdn)' preview.html && node --test scripts/tests/*.test.js 2>&1 | grep -qE 'pass 2[6-9]|pass [3-9][0-9]' && echo PREVIEW_B_OK</automated>
  </verify>
  <acceptance_criteria>
    - `id="s11"` and `id="s12"` sections exist.
    - §11 contains real engine-class instances for the FULL inventory: `class="btn"`, `class="opt"`, `class="tf"`, `class="tile"`, `class="tab"`, `class="hstat"`, `class="sheet-row"` (and a `.cite`) sharing the gummy/light press.
    - §12 has the `pv-motion-toggle` button with `aria-pressed`, drives `document.documentElement` `data-motion`, and calls both `AW.confetti(` and `AW.animate(` (confetti + PERFECT exemplar) — located in §12, not §10.
    - Zero CDN still holds; `node --test scripts/tests/*.test.js` still green (no regression from preview edits).
  </acceptance_criteria>
  <done>§11 proves one gummy press across the real inventory; §12 labels every motion signature with its token, demos confetti + the PERFECT WAAPI exemplar spatially separate from §10, and exposes both reduced-motion trigger paths via the data-motion toggle + OS note.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| demo cfg (author content) → innerHTML | The §10 demoCfg refs/terms are copied verbatim from Josh's data file, injected via AW.cite/AW.wire/AW.sheetRef — author-controlled, not user input. |
| preview page → shared/awba-engine.js | preview loads the engine as a page-relative, same-origin classic script; no third-party origin. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Tampering (DOM) | §10 demo innerHTML | accept | Demo content is author-controlled verbatim scripture copied from RESEARCH; no user input reaches it. Same posture as T-03-03. |
| T-03-06 | Tampering / integrity (supply chain) | external resource loading | mitigate | Zero-CDN grep gate (`! grep -qE 'https?://(fonts\.googleapis|gstatic|cdn)'`) enforced in both tasks; the engine is loaded page-relative same-origin, classic (no defer/async/module) — no third-party fetch, offline/file:// safe. |
| T-03-SC | Tampering | npm installs | accept | No installs; preview references only same-origin vendored assets. |
</threat_model>

<verification>
- Engine `<script>` present, page-relative, classic; §9-12 exist.
- Verbatim markers (hujurat-49-15, muslim-8, aqeedah, Sahih, ˹true˺) present; §10 has zero celebration adjacency.
- §11 real inventory shares gummy press; §12 has the data-motion toggle + confetti + PERFECT exemplar, separate from §10.
- Zero CDN; 26+ tests still green.
</verification>

<success_criteria>
- FND-04: §9 proves the single 20-icon registry + glyphs + authored gold lantern render inline at UI sizes.
- ENG-06: §10 proves both citation faces (Quran/hadith) + grade pill + always-on pending pill + gloss sheet, from verbatim content, no celebration near scripture.
- MOT-01/03: §11 proves one gummy press across the inventory; §12 labels the one motion vocabulary.
- MOT-04: §12 proves both reduced-motion trigger paths (OS + data-motion) quieten confetti and the companion loop.
</success_criteria>

<output>
Create `.planning/phases/03-components-icon-kit-motion-language/03-04-SUMMARY.md` when done.
</output>
