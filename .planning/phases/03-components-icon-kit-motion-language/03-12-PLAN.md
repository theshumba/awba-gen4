---
phase: 03-components-icon-kit-motion-language
plan: 12
type: execute
wave: 7
executor_hint: sonnet
depends_on: ["03-11"]
files_modified: []
autonomous: false
requirements: [FND-04, ENG-06, MOT-01, MOT-03, MOT-04]
must_haves:
  truths:
    - "Per §12 all re-cut grep gates pass as automated prechecks BEFORE the human is asked: no `rgba(37,54,`; no `[data-unit=\"u[1-4]\"]`; no `--accent`; no `poppins`; `'Inter'` appears exactly twice; no `AW.confetti`; no `KIT['lantern-gold']`; no `.replace(/#`; no leading-slash `url()`; no CDN in preview.html; the AW.cite span byte-shape intact; the `@layer tokens, base, components, screens, motion;` line appears exactly once"
    - "Per §6.6/§7.5 the machine-checkable Ring + Sky acceptance criteria pass as prechecks: AW.ringSVG is deterministic per seed, differs by seed, inks 0→faint / 65+4→closed, is reduced-motion-static, and is ≤600 nodes with no `<filter>`; the sky temperature function maps the five fixed-now fixtures correctly, honours skyMode:off, and fires no geolocation"
    - "Per §9 the human gate is a ten-item, zero-jargon plain-language checklist Melusi walks against the rebuilt preview.html; a NO on ANY item is a blocker that routes back to the responsible plan — this checkpoint is BLOCKING and is NEVER auto-approved by an executor (workflow.auto_advance is ignored here)"
    - "Per the adoption + §9 the D-44 checklist is superseded and never walked; the full node test suite (37 baseline + ring + sky + component-count adjustments) is green (fail 0) as the final automated precondition of the gate"
  artifacts: []
  key_links:
    - from: "the §12 automated prechecks"
      to: "the §9 human checklist"
      via: "prechecks must all pass before the human is asked to walk the preview"
      pattern: "fail 0"
---

<objective>
Close the re-cut with the new human visual gate: run every §12 grep gate + the full test suite + the machine-checkable §6/§7 Ring and Sky acceptance criteria as automated prechecks, then STOP and present Melusi the §9 ten-item plain-language checklist to walk against the rebuilt preview.html. This is verification-only — it modifies no source files. The old D-44 checklist is superseded and must never be walked.

Purpose: the phase-level proof that the Athar re-skin is coherent, gentle, shape-cued, deterministic-per-learner, time-truthful, and calm under reduced motion (FND-04/ENG-06/MOT-01/MOT-03/MOT-04 re-expressed under Athar).
Output: a green automated-precheck report, then a blocking human checkpoint carrying the §9 checklist.

Executor routing: sonnet (mechanical / verification). Run the prechecks exactly; do NOT edit source to make a gate pass — a failing gate routes back to the responsible plan (06 tokens/base, 07 Ring, 08 icons, 09 components/motion, 10 sky, 11 preview). The human checkpoint is BLOCKING and must NEVER be auto-approved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-components-icon-kit-motion-language/03-UI-SPEC-ATHAR.md

<interfaces>
<!-- The gate reads only; it changes nothing. -->
03-UI-SPEC-ATHAR.md §12 (the grep gates), §6.6 (Ring acceptance), §7.5 (Sky acceptance), §9 (the ten-item human checklist verbatim).
shared/awba-engine.css / shared/awba-engine.js / preview.html — the surfaces the gates inspect; scripts/tests/*.test.js — the suite (37 baseline + ring + sky).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run all §12 grep gates + the full suite + the §6/§7 machine-checkable acceptance</name>
  <files></files>
  <read_first>
    - 03-UI-SPEC-ATHAR.md §12 (every grep gate), §6.6 (Ring acceptance 1-4/6), §7.5 (Sky acceptance 1-4/6).
  </read_first>
  <action>Run, and record pass/fail for, the full §12 gate set as automated prechecks (using `! grep -q` for every absence gate — never a bare `grep -c` in an && chain): no `rgba(37,54,` in the CSS; no `[data-unit="u[1-4]"]` in the CSS; no `--accent` in the CSS; no `poppins` in the CSS or preview; `'Inter'` appears exactly twice in the CSS; the `@layer tokens, base, components, screens, motion;` line appears exactly once; no leading-slash `url(/…)` in the CSS; no `AW.confetti` and no `KIT['lantern-gold']` and no `.replace(/#` in the JS; no CDN in preview.html; and `AW.cite('x','y')` still returns the `<span class="cite" data-ref="x">…` byte-shape (validator self-test). Then run the machine-checkable Ring (§6.6: determinism, seed-difference, 0→faint/65+4→closed, reduced-motion static, ≤600 nodes / no filter) and Sky (§7.5: five fixed-now fixtures, skyMode:off⇒day, boundary shift, no geolocation) acceptance via the ring/sky tests. Finally run the full suite `node --test scripts/tests/*.test.js` and confirm `fail 0`. If ANY precheck fails, STOP and report which gate failed and which plan owns it — do NOT edit source to force a pass.</action>
  <verify>
    <automated>! grep -qE 'rgba\(37,54,' shared/awba-engine.css && ! grep -qE '\[data-unit="u[1-4]"\]' shared/awba-engine.css && ! grep -q -- '--accent' shared/awba-engine.css && ! grep -qiE 'poppins' shared/awba-engine.css && ! grep -qiE 'poppins' preview.html && test "$(grep -c "'Inter'" shared/awba-engine.css)" -eq 2 && test "$(grep -c '@layer tokens, base, components, screens, motion;' shared/awba-engine.css)" -eq 1 && ! grep -qE "url\(['\"]?/" shared/awba-engine.css && ! grep -q 'AW.confetti' shared/awba-engine.js && ! grep -q \"KIT\\['lantern-gold'\\]\" shared/awba-engine.js && ! grep -q '\.replace(/#' shared/awba-engine.js && ! grep -qE 'https?://(fonts\.googleapis|cdn)' preview.html && node scripts/validate-content.js --self-test && node --test scripts/tests/*.test.js 2>&1 | grep -qE 'fail 0'</automated>
  </verify>
  <acceptance_criteria>
    - Every §12 grep gate passes; the validator self-test passes (AW.cite byte-shape intact).
    - The Ring + Sky machine-checkable acceptance (via ring.test.js + sky.test.js) is green.
    - node --test scripts/tests/*.test.js → fail 0 (37 baseline + ring + sky, component-count adjustments accounted).
  </acceptance_criteria>
  <done>All §12 gates + the full suite + the §6/§7 machine acceptance pass; any failure is reported with its owning plan rather than patched here.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Melusi walks the §9 ten-item plain-language visual gate</name>
  <what-built>
    The full Athar re-skin is complete and every automated precheck (Task 1) is green: the token/register foundation, the Ring generator (deterministic per learner), the re-inked icons, the re-skinned components + motion vocabulary, the prayer-clock sky, and the rebuilt preview.html living reference. Nothing celebratory sits near scripture; wrongness is a gentle grey mark, not red; reduced motion stills everything.
  </what-built>
  <how-to-verify>
    Open preview.html by double-clicking it (file:// — zero CDN, no server needed). Walk the eight sections and answer YES/NO to each of the ten §9 questions:
    1. Does every screen feel like it belongs to one calm app — five different moods, but clearly the same product?
    2. Does the black home screen feel like the sacred centre — quiet, a little sky-warmed, with your ring at the heart?
    3. Does the cream lesson page feel like a real page of a book — warm paper, crisp margins, easy to read?
    4. Is the Arabic Qur'an the most important, most beautiful thing on any page it appears on — and is it never sitting on a busy background, never next to anything celebratory?
    5. When you get an answer wrong, does it feel gentle — a soft grey mark and a one-line explanation, nothing red, nothing shaking, nothing flashing?
    6. Do the "not yet / in progress / done" marks make sense even without colour — can you tell them apart by their shape (empty circle / half-filled / filled with a tick)?
    7. Does the ring look hand-drawn and personal — like your own path traced in ink, not a plain loading bar? And would two people's rings clearly look different?
    8. Does the sky match the time of day — brighter after dawn, violet late at night — without it feeling like a fake reward?
    9. Is the celebration rare and tasteful — a gold thread, a few ink dabs, a festival plate only at big milestones — never confetti, never a mascot, never a party mid-quiz?
    10. When you turn on "reduce motion," does everything go still and calm — the sky stops breathing, the ring shows finished, nothing keeps moving?
    A NO on any item is a blocker; note which item and it routes back to the responsible plan (item 2/7 → Ring plan 07; item 8/10-sky → sky plan 10; item 1/3/6 → tokens/base plan 06 + components plan 09; item 4/5/9 → components plan 09; item 5 wrong-answer → components plan 09; icon/register feel → icons plan 08 + preview plan 11).
  </how-to-verify>
  <resume-signal>Type "approved" if all ten are YES, or list the item numbers that are NO with a one-line note each.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verification-only | This plan reads and asserts; it modifies no source. No new trust surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-12 | Repudiation / process | the gate itself | mitigate | The human checkpoint is blocking and never auto-approved (workflow.auto_advance ignored); a failing precheck routes to the owning plan rather than being patched at the gate. |
| T-03-SC | Tampering | npm installs | accept | No package installs; verification-only. No [ASSUMED]/[SUS] package to gate. |
</threat_model>

<verification>
- Every §12 grep gate + the validator self-test + the Ring/Sky machine acceptance + the full suite (fail 0) pass as prechecks.
- The §9 ten-item checklist is presented; the checkpoint blocks and is never auto-approved; a NO routes to the owning plan.
</verification>

<success_criteria>
- FND-04/ENG-06/MOT-01/MOT-03/MOT-04 re-expressed under Athar are verified automatically and then confirmed by the owner's plain-language visual walk.
- The re-cut Phase 3 is gate-passed (or precisely routed back), with the D-44 checklist superseded and the Athar system standing before the Phase-4 lesson port.
</success_criteria>

<output>
Create `.planning/phases/03-components-icon-kit-motion-language/03-12-SUMMARY.md` when done.
</output>
