---
phase: 03-components-icon-kit-motion-language
plan: 03
type: execute
wave: 2
executor_hint: opus-visual
depends_on: ["03-02"]
files_modified: [shared/awba-engine.js, scripts/tests/components.test.js]
autonomous: true
requirements: [FND-04, ENG-06, MOT-04]
must_haves:
  truths:
    - "Per D-32, AW.icon(name,{size?,label?}) returns the inline SVG string with aria-hidden=\"true\" focusable=\"false\" by default, OR role=\"img\"+escaped aria-label when a label is passed; a missing/malformed name returns '' (never throws); raw AW.KIT[name] stays available"
    - "Per D-37, AW.cite(id,label) is synchronous/DOM-free/parse-time and returns exactly <span class=\"cite\" data-ref=\"ID\">{cite glyph}{escaped label}</span> — byte-compatible with the Phase-2 validator stub so the validator's data-ref regex keeps matching; AW.wire(root,cfg) binds .cite[data-ref]→sheetRef and .term[data-term]→sheetTerm"
    - "Per D-35/D-36, AW.sheet(html) is ONE lazily-created singleton (scrim wraps role=dialog/aria-modal sheet on body; outside-tap/Escape/close-button close; opening replaces content); AW.sheetRef applies the Quran→.ayah / hadith→general-Amiri face-split, shows the grade pill iff grade, and appends the 'unverified · pending review' pill on EVERY citation"
    - "Per D-41/D-42, AW.confetti(n) and AW.animate(...) self-guard at call time via AW.reducedMotion() (matchMedia('(prefers-reduced-motion: reduce)') OR documentElement[data-motion]='reduce') — confetti no-ops and the WAAPI exemplar sets dur=1 under reduced motion; components never touch localStorage (prefs via AW.prefs, D-24)"
  artifacts:
    - path: "shared/awba-engine.js"
      provides: "COMPONENTS: escapeHtml/escapeAttr, AW.icon, AW.cite, AW.wire, AW.sheet(+sheetRef+sheetTerm), AW.confetti, AW.reducedMotion, AW.animate"
      contains: "unverified · pending review"
    - path: "scripts/tests/components.test.js"
      provides: "node:test unit coverage for AW.icon, AW.cite, escapeHtml, AW.reducedMotion, KIT/GLYPHS counts"
      contains: "AW.icon"
  key_links:
    - from: "AW.cite output span shape"
      to: "scripts/validate-content.js data-ref extractor"
      via: "byte-compatible <span class=\"cite\" data-ref=\"ID\"> markup"
      pattern: "data-ref="
    - from: "AW.confetti / AW.animate"
      to: "AW.reducedMotion() call-time guard"
      via: "matchMedia OR [data-motion=reduce], never the CSS token-collapse"
      pattern: "matchMedia"
---

<objective>
Fill the COMPONENTS banner section of `shared/awba-engine.js` with the shared UI builders the Phase-4/5 runners consume: the a11y icon accessor (`AW.icon`), the citation/term primitives (`AW.cite`, `AW.wire`), the singleton bottom-sheet (`AW.sheet` + `AW.sheetRef` with the Quran/hadith face-split + `AW.sheetTerm`), the reward primitive (`AW.confetti`) with its day-one reduced-motion guard, and the WAAPI orchestration exemplar (`AW.reducedMotion` + `AW.animate`) that Phase 4 copies rather than invents. Ship the new `scripts/tests/components.test.js` unit file alongside.

Purpose: FND-04 (the `AW.icon` accessor + a11y), ENG-06 (citation/gloss sheets), the JS half of MOT-04 (call-time self-guards). These re-point Gen-3's proven shapes at Gen-4 tokens and add the two genuine elevations Gen-3 lacked (icon a11y + the face-split).
Output: COMPONENTS section filled (banner at lines 450-452); `scripts/tests/components.test.js` created; the validator and all prior tests stay green.

Load the `frontend-design` skill during execution. The sheet must feel premium; the citation face-split (Quran → Amiri Quran `.ayah`; hadith → general Amiri + grade pill) is the load-bearing elevation over Gen-3. Nothing celebratory ever renders in or around a citation sheet.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-components-icon-kit-motion-language/03-UI-SPEC.md
@.planning/phases/03-components-icon-kit-motion-language/03-RESEARCH.md
@.planning/phases/03-components-icon-kit-motion-language/03-PATTERNS.md

<interfaces>
<!-- Insertion point + contracts the executor must honor. -->
shared/awba-engine.js :450-452 COMPONENTS banner (write directly after) · :429-438 the `typeof document !== 'undefined'` boot-stamp (the safe documentElement.getAttribute('data-motion') read pattern to mirror in AW.reducedMotion; it stamps ONLY on the in-app override, never the OS setting) · :444 globalThis.AW export (auto-covers new AW.*).
AW.KIT / AW.GLYPHS / AW.UNIT_ICON  — authored in plan 03-02 (AW.icon reads from these; missing name → '').
scripts/validate-content.js :47-53 the cite stub `'<span class="cite" data-ref="'+id+'">'+(label||'')+'</span>'` and :301 the extractor `/data-ref=["']([^"']+)["']/g` — AW.cite MUST preserve this outer span shape.
Test harness: scripts/tests/ls-stub.js loadEngine(ls, probeSrc) + makeLS; scripts/tests/state-helpers.test.js is the file shape to copy (require node:test + node:assert/strict; globalThis.__out probe; plain string returns need no JSON round-trip).
Gen-3 behaviour ground truth (shapes to elevate + re-token, do NOT copy escaping): _MVP-BUILD/shared/awba-engine.js AW.cite(:71) AW.wire(:103-106) AW.sheetRef(:88-96) AW.sheetTerm(:97-102) AW.confetti(:82-87).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: escapeHtml/escapeAttr + AW.icon + AW.cite + AW.wire</name>
  <files>shared/awba-engine.js</files>
  <behavior>
    - AW.icon('mosque') → string containing `aria-hidden="true"` and `focusable="false"`, injected right after the first `<svg`.
    - AW.icon('mosque',{label:'Unit 1'}) → contains `role="img"` and `aria-label="Unit 1"` (label HTML-escaped), NOT aria-hidden.
    - AW.icon('does-not-exist') → '' (empty string, never undefined, never throws).
    - AW.icon reads GLYPHS as a fallback source when the name is not in KIT.
    - AW.cite('hujurat-49-15','al-Ḥujurāt 49:15') → matches /^<span class="cite" data-ref="hujurat-49-15">/ and ends with </span>; label is escaped.
    - escapeHtml('<b>&"') → '&lt;b&gt;&amp;&quot;' (or equivalent entity-escaping).
  </behavior>
  <read_first>
    - shared/awba-engine.js :450-452 (insertion point) + :257-303 (AW.prefs IIFE — accessor idiom) .
    - 03-RESEARCH.md Pattern 2 (AW.icon safe first-`<svg`-insertion + escapeAttr; String.replace(str,…) hits only the first match; edge cases) and Pattern 5 (AW.cite/AW.wire exact shapes + the validator-compatibility requirement).
    - 03-UI-SPEC.md "Icon Registry (FND-04)" accessor rules (D-32: default aria-hidden, label→role=img, missing→'', size→width+height:auto) and "AW.cite / AW.wire" contract.
    - 03-PATTERNS.md "COMPONENTS section" (validate-content.js:47-53 stub + :301 extractor — load-bearing shape) and the "escapeAttr/escapeHtml before innerHTML" shared pattern.
  </read_first>
  <action>After the COMPONENTS banner (line 452), add `escapeHtml`/`escapeAttr` helpers (entity-escape `& < > "` — closes the ENGINE-CONTRACT §6 no-escaping gap for the params this phase controls). Add `AW.icon = function(name, opts){…}`: resolve `raw` from `AW.KIT[name] || AW.GLYPHS[name] || ''`; if `raw` does not start with `<svg`, return `''`; build the attribute string — default `aria-hidden="true" focusable="false"`, or (when `opts.label`) `role="img" aria-label="{escapeAttr(label)}"`; if `opts.size`, add `width` + `height:auto` (viewBox preserves aspect); return `raw.replace('<svg', '<svg ' + attrs)` (string first-arg = first match only). Add `AW.cite = function(id,label){ return '<span class="cite" data-ref="' + id + '">' + AW.icon('cite') + escapeHtml(label==null?'':label) + '</span>'; }` — synchronous, DOM-free, the outer `<span class="cite" data-ref="…">…</span>` shape BYTE-preserved so the validator regex keeps matching. Add `AW.wire = function(root,cfg){…}` binding `root.querySelectorAll('.cite[data-ref]')` clicks → `AW.sheetRef(cfg.refs||{}, el.dataset.ref)` and `.term[data-term]` clicks → `AW.sheetTerm(cfg.terms||{}, el.dataset.term)` (sheetRef/sheetTerm land in Task 2 — wiring references them by name). Keep everything parse-time-safe (no DOM access at definition time). Do not touch STATE/KIT.</action>
  <verify>
    <automated>node -e "const {makeLS,loadEngine}=require('./scripts/tests/ls-stub');const s=loadEngine(makeLS({}),'globalThis.__d=AW.icon(\"mosque\");globalThis.__l=AW.icon(\"mosque\",{label:\"Unit 1\"});globalThis.__m=AW.icon(\"nope\");globalThis.__c=AW.cite(\"hujurat-49-15\",\"al-Ḥujurāt 49:15\");');const ok=/aria-hidden=\"true\"/.test(s.__d)&&/focusable=\"false\"/.test(s.__d)&&/role=\"img\"/.test(s.__l)&&/aria-label=\"Unit 1\"/.test(s.__l)&&s.__m===''&&/^<span class=\"cite\" data-ref=\"hujurat-49-15\">/.test(s.__c);if(!ok){console.error({d:s.__d.slice(0,60),l:s.__l.slice(0,60),m:s.__m,c:s.__c.slice(0,60)});process.exit(1);}console.log('ICON_CITE_OK');" && node scripts/validate-content.js --self-test && node --test scripts/tests/*.test.js 2>&1 | grep -qE 'pass 26'</automated>
  </verify>
  <acceptance_criteria>
    - `AW.icon('mosque')` contains `aria-hidden="true"` and `focusable="false"`; `AW.icon('mosque',{label:'Unit 1'})` contains `role="img"` and `aria-label="Unit 1"`; `AW.icon('nope')` === `''`.
    - `AW.cite('hujurat-49-15','al-Ḥujurāt 49:15')` matches `/^<span class="cite" data-ref="hujurat-49-15">/`.
    - `node scripts/validate-content.js --self-test` passes (the cite shape is still validator-compatible).
    - `node --test scripts/tests/*.test.js` → `pass 26 / fail 0`.
  </acceptance_criteria>
  <done>escapeHtml/escapeAttr, AW.icon (a11y injection + missing→''), AW.cite (validator-compatible escaped span), and AW.wire exist; the validator self-test and 26 tests stay green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AW.sheet singleton + sheetRef (face-split) + sheetTerm + AW.confetti + AW.reducedMotion + AW.animate</name>
  <files>shared/awba-engine.js</files>
  <behavior>
    - AW.reducedMotion() → true when matchMedia('(prefers-reduced-motion: reduce)').matches is true; true when documentElement.getAttribute('data-motion')==='reduce'; false when neither (works with stubbed window/document; guarded so bare window/document access does not throw headless).
    - AW.sheetRef source contains the literal 'unverified · pending review' appended unconditionally; the grade pill is emitted only when the ref has a `grade`; a Quran ref (no grade) tags its Arabic block with class `ayah`, a hadith ref does not.
    - AW.confetti's first executable line is a reducedMotion guard that returns before spawning any div.
  </behavior>
  <read_first>
    - shared/awba-engine.js :429-438 (boot-stamp — the safe documentElement.getAttribute pattern) + the AW.cite/AW.wire from Task 1.
    - 03-RESEARCH.md Pattern 4 (AW.sheet singleton IIFE — lazy scrim+sheet on body, outside-tap/Escape/close-button, invoker capture for Phase-6 focus-restore, .sheet-lock; what to DEFER to Phase 6), Pattern 6 (the citation face-split rule + Gen-3→Gen-4 token translation), Pattern 9 (confetti decoupled + call-time guard + brand palette via getComputedStyle), Pattern 10 (AW.reducedMotion + AW.animate WAAPI exemplar, ms tokens, linear() passthrough).
    - 03-UI-SPEC.md "Bottom-sheet primitive", "Citation sheet content (AW.sheetRef)" (7-item top→bottom + pending pill ALWAYS + grade pill iff grade), "Term gloss sheet", "Reward Primitives" (confetti bounds + day-one guard + mercy), "Motion Vocabulary" WAAPI exemplar, "Reduced Motion" (JS self-guard contract).
    - 03-PATTERNS.md "COMPONENTS section" Gen-3 AW.sheetRef/sheetTerm/confetti shapes (:82-102) to elevate + re-token, and the AW.reducedMotion synthesis note.
  </read_first>
  <action>Build `AW.sheet` as a singleton IIFE (mirror the AW.prefs closure idiom): private `scrim/sheet/invoker`; an `ensure()` that lazily creates `<div class="scrim"><div class="sheet" role="dialog" aria-modal="true">` on `document.body`, wires scrim/outside-tap close and a document-level Escape handler; a returned `api` with `open(html)` (capture `invoker=document.activeElement`; set `sheet.innerHTML = '<button class="sheet-x" aria-label="Close">×</button>' + html`; wire the close button; add `.open` to scrim; add `.sheet-lock` to `<html>`) and an idempotent `close()` (remove `.open`/`.sheet-lock`, restore `invoker` focus). Export `AW.sheet(html)` (calls open, returns api) and `AW.sheetClose`. DO NOT build the focus-trap / inert / iOS position-fixed lock (Phase 6). Build `AW.sheetRef(refs,id)`: early-return no-op on unknown id; compose the sheet HTML top→bottom per UI-SPEC — `.r-src` (`{kind||'The verse'} · {ref}` + grade pill iff `r.grade`), `.r-ar` with the FACE-SPLIT (add `ayah` class when `grade` is absent → renders Amiri Quran; hadith → plain `.r-ar` general Amiri), `.r-mean`, `.r-ref` (`r.src`), then the pill row containing the ALWAYS-present `<span class="r-pill">unverified · pending review</span>` and, iff hadith, the `.r-pill.grade` — then `AW.sheet(html)`. Build `AW.sheetTerm(terms,id)`: early-return no-op; compose `.g-ar/.g-tl/.g-wd/.g-df/.g-cx` → `AW.sheet(html)`. Build `AW.reducedMotion = function(){ return (typeof window!=='undefined' && window.matchMedia && matchMedia('(prefers-reduced-motion: reduce)').matches) || (typeof document!=='undefined' && document.documentElement.getAttribute('data-motion')==='reduce'); }`. Build `AW.confetti = function(n){ if(AW.reducedMotion()) return; … }` — lazily create/find a `.confetti` overlay on body (decoupled from any skeleton), spawn capped `n` (~16-30) `.cf` divs with random left, colour drawn from the brand tokens `--gold2/--flame2/--accent-bright/--green` via `getComputedStyle(document.documentElement)`, animation-duration 1.6-2.7s, each self-removing on end. Build `AW.animate = function(el,keyframes,durToken,easeToken){ … }` reading dur (parseFloat of the ms token) + ease (the linear() string) via `getComputedStyle(:root)`, self-guarding `if(AW.reducedMotion()) dur=1`, returning `el.animate(keyframes,{duration:dur,easing:ease,fill:'both'})` (`.finished` awaitable). Components read prefs only via AW.prefs / attributes — NEVER localStorage (D-24). Keep every definition parse-time-safe.</action>
  <verify>
    <automated>node -e "const {makeLS,loadEngine}=require('./scripts/tests/ls-stub');const s=loadEngine(makeLS({}),'globalThis.window={matchMedia:function(){return{matches:true}}};globalThis.__r1=AW.reducedMotion();globalThis.window={matchMedia:function(){return{matches:false}}};globalThis.document={documentElement:{getAttribute:function(){return \"reduce\"}}};globalThis.__r2=AW.reducedMotion();globalThis.document={documentElement:{getAttribute:function(){return null}}};globalThis.__r3=AW.reducedMotion();');if(!(s.__r1===true&&s.__r2===true&&s.__r3===false)){console.error({r1:s.__r1,r2:s.__r2,r3:s.__r3});process.exit(1);}console.log('REDUCEDMOTION_OK');" && grep -q 'unverified · pending review' shared/awba-engine.js && grep -q 'ayah' shared/awba-engine.js && grep -q 'matchMedia' shared/awba-engine.js && ! sed -n '/^   KIT  ·/,$p' shared/awba-engine.js | grep -q localStorage && node --test scripts/tests/*.test.js 2>&1 | grep -qE 'pass 26'</automated>
  </verify>
  <acceptance_criteria>
    - `AW.reducedMotion()` returns true via matchMedia stub, true via `[data-motion=reduce]` stub, false when neither — and the engine still loads headless (guarded window/document access, no parse-time throw).
    - `grep -q 'unverified · pending review' shared/awba-engine.js` passes (pending pill present in COMPONENTS).
    - `grep -q 'ayah' shared/awba-engine.js` passes (the Quran face-split branch tags `.r-ar` with `ayah`).
    - `grep -q 'matchMedia' shared/awba-engine.js` passes (JS self-guard present for confetti/animate/reducedMotion).
    - No `localStorage` reference appears anywhere from the KIT banner onward (`! sed -n '/^   KIT  ·/,$p' shared/awba-engine.js | grep -q localStorage` — anchored to the section banner, not a line number) — components read prefs via AW.prefs only (D-24).
    - `node --test scripts/tests/*.test.js` → `pass 26 / fail 0`.
  </acceptance_criteria>
  <done>AW.sheet singleton + sheetRef (face-split + always-on pending pill + conditional grade pill) + sheetTerm + AW.confetti (guarded) + AW.reducedMotion (both triggers) + AW.animate exist; engine loads headless; components never touch localStorage; 26 tests green.</done>
</task>

<task type="auto">
  <name>Task 3: components.test.js — node:test unit coverage</name>
  <files>scripts/tests/components.test.js</files>
  <read_first>
    - scripts/tests/state-helpers.test.js (full file — the require/test/assert shape + loadEngine + globalThis.__out probe idiom) and scripts/tests/ls-stub.js :45-52 (loadEngine) + the note that plain string returns need no JSON round-trip.
    - 03-PATTERNS.md "scripts/tests/components.test.js (new)" (the exact analog + example test bodies) and 03-RESEARCH.md "Wave 0 Gaps" (the unit-test list) + "Phase Requirements → Test/Check Map".
  </read_first>
  <action>Create `scripts/tests/components.test.js` requiring `node:test` + `node:assert/strict` + `{ makeLS, loadEngine }` from `./ls-stub`. Add tests: (1) AW.icon default injects `aria-hidden="true"` + `focusable="false"`; (2) AW.icon with `{label}` injects `role="img"` + escaped `aria-label`, and NOT aria-hidden; (3) AW.icon missing name → `''`; (4) AW.cite preserves the `<span class="cite" data-ref="…">` validator-compatible shape and escapes the label; (5) escapeHtml entity-escapes `& < > "`; (6) AW.reducedMotion returns true via a stubbed matchMedia, true via a stubbed `data-motion="reduce"`, false when neither; (7) KIT/GLYPHS integrity: `Object.keys(AW.KIT).length===21`, `Object.keys(AW.GLYPHS).length===13`, `'lantern-gold' in AW.KIT`, every value starts with `<svg`. Use the loadEngine(ls, probeSrc) + `globalThis.__out` concatenation trick (set window/document stubs inside the probe for the reducedMotion cases). These are pure-string / value assertions — no DOM/sheet/motion behaviour is unit-tested here (those go to grep gates + the D-44 human gate).</action>
  <verify>
    <automated>OUT=$(node --test scripts/tests/*.test.js 2>&1); echo "$OUT" | tail -4; echo "$OUT" | grep -q 'fail 0' && echo "$OUT" | grep -qE '^ℹ pass (3[3-9]|[4-9][0-9]|[0-9]{3,})'</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/tests/components.test.js` exists under `scripts/tests/` and is picked up by the glob.
    - `node --test scripts/tests/*.test.js` reports the total pass count rising to `26 + (new component tests)` with `fail 0` (baseline 26 remain green; at least 7 new tests added).
    - The new file contains tests named for AW.icon, AW.cite, escapeHtml, AW.reducedMotion, and KIT/GLYPHS counts.
  </acceptance_criteria>
  <done>components.test.js adds ≥7 node:test unit tests for the pure string helpers + registry counts; the full glob suite passes with fail 0 and a raised total.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cfg.refs / cfg.terms (author content) → innerHTML | AW.sheetRef/sheetTerm inject verbatim ref/term fields (Arabic, translation, source) into the sheet via innerHTML. Content is Josh's author-time data files, not user input — same trust model as Gen-3. |
| AW.icon `label` / AW.cite `label` params → attribute/markup | The only dynamic string params this phase controls; could in principle carry markup. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Tampering / Information disclosure (DOM XSS via innerHTML) | AW.sheetRef / AW.sheetTerm | accept | Content is author-controlled verbatim scripture/gloss (Josh's data files), never user input — v1 is device-local with no live input surface reaching these. Full systematic escaping of runner HTML is Phase-4/6 scope (ENGINE-CONTRACT §6 documents the Gen-3 zero-escaping posture being closed incrementally); accepting here matches that posture. |
| T-03-04 | Tampering (reflected param) | AW.icon `label`, AW.cite `label` | mitigate | `escapeAttr(label)` on the `aria-label` path and `escapeHtml(label)` on the cite label before concatenation — the one applicable ASVS V5 output-encoding control this phase touches (RESEARCH Security Domain). Unit-tested in Task 3. |
| T-03-SC | Tampering | npm installs | accept | No package installs; zero runtime deps; nothing fetched (D-34). No `[ASSUMED]`/`[SUS]` package to gate. |
</threat_model>

<verification>
- AW.icon (aria-hidden default / role=img label / missing→'') , AW.cite (validator-compatible escaped span) proven via node one-liner + components.test.js.
- `node scripts/validate-content.js --self-test` passes (cite shape compatible).
- AW.reducedMotion true/true/false across both stubbed triggers; engine loads headless.
- `grep -q 'unverified · pending review'`, `grep -q 'ayah'`, `grep -q 'matchMedia'` pass; no localStorage in COMPONENTS.
- `node --test scripts/tests/*.test.js` → baseline 26 + ≥7 new, `fail 0`.
</verification>

<success_criteria>
- FND-04: AW.icon accessor injects a11y (aria-hidden default, role=img+name opt-in), missing→''.
- ENG-06: citation chip → sheet with RTL Arabic (Quran face vs hadith face), verbatim translation, source line, grade pill for hadith, always-on pending pill; term chip → gloss sheet (Arabic/tl/word/def/context); AW.cite/AW.wire preserve Gen-3 shapes and validator compatibility.
- MOT-04 (JS half): AW.confetti/AW.animate self-guard at call time via AW.reducedMotion(); components never touch localStorage.
</success_criteria>

<output>
Create `.planning/phases/03-components-icon-kit-motion-language/03-03-SUMMARY.md` when done.
</output>
