---
phase: 03-components-icon-kit-motion-language
plan: 02
type: execute
wave: 1
executor_hint: opus-visual
depends_on: []
files_modified: [shared/awba-engine.js]
autonomous: true
requirements: [FND-04]
must_haves:
  truths:
    - "Per D-31, AW.KIT holds the canonical 20 scene icons hand-authored from the 20-SVG folder as readable backtick template literals (one per icon, each preceded by a naming comment), with xmlns/width/height stripped and viewBox=\"0 0 240 300\" kept — svgo is NOT run (D-31 amendment: the id-collision audit found zero ids/defs/gradients)"
    - "Per D-33, the gold lantern is an AUTHORED committed entry AW.KIT['lantern-gold'] (hand-recoloured from 03-lantern.svg: #2E6BF5→--gold value, #2536CE→--gold-deep value, night-register-safe brightest highlights at --gold2) — NOT a runtime regex recolor; AW.recolor/LANTERNG are not ported and never appear"
    - "Per D-33, the small standalone glyphs Gen-3 used (flame, spark, check, star, cite, lamp, lock, chest, trophy, fact, remember, fard, angle) join AW.GLYPHS as ONE 13-key sub-map — no per-page UIC/IC_* constants survive anywhere in shared/"
    - "Per D-34, all icon art ships at author-time inside the engine JS (zero runtime fetch, no external sprite); AW.UNIT_ICON preserves Gen-3 semantics (u1→compass, u2→lanterns, u3→kaaba, u4→mosque)"
  artifacts:
    - path: "shared/awba-engine.js"
      provides: "AW.KIT (21 keyed entries: 20 scenes + lantern-gold), AW.GLYPHS (13 glyphs), AW.UNIT_ICON (4)"
      contains: "lantern-gold"
  key_links:
    - from: "AW.KIT / AW.GLYPHS entries"
      to: "consumers (AW.icon in plan 03, preview §9 in plan 04)"
      via: "single author-time registry, values start with <svg, viewBox preserved"
      pattern: "viewBox=\"0 0 240 300\""
---

<objective>
Author the ONE icon registry — the phase's headline deliverable and a brand moment. Fill the KIT banner section of `shared/awba-engine.js` with `AW.KIT` (the canonical 20 scene icons hand-transformed from the branded SVG folder + the authored `lantern-gold` variant = 21 keyed entries), `AW.GLYPHS` (the 13 small UI glyphs Gen-3 used, re-homed into one sub-map), and `AW.UNIT_ICON` (the u1-u4 mapping). No accessor logic here — that is COMPONENTS (plan 03). This is pure author-time static data.

Purpose: FND-04 — one inline, single-source, no-per-page-constants icon registry; the gold lantern as a properly authored variant, not a regex recolor.
Output: `shared/awba-engine.js` KIT section filled (banner at lines 446-448); the file grows by ~300 lines but stays ONE file (D-22).

Load the `frontend-design` skill during execution. The gold lantern is CRAFT: it renders primarily on the `.gold-bg` night gradient (`#241A05→#3A2A08`), so author it against that dark register and use the brightest structural highlights at `--gold2` (#FFD34D) so it holds contrast — mirroring the Phase-1 night focus-ring decision. It must read intentional, not a tinted afterthought.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-components-icon-kit-motion-language/03-UI-SPEC.md
@.planning/phases/03-components-icon-kit-motion-language/03-RESEARCH.md
@.planning/phases/03-components-icon-kit-motion-language/03-PATTERNS.md

<interfaces>
<!-- Insertion point (already in file) — write between these two banners, do not add a new section. -->
shared/awba-engine.js :446-448  KIT banner (write content directly after) · :450-452 COMPONENTS banner (do NOT touch — plan 03) · :444 `globalThis.AW = AW` (already exports every new AW.* automatically).
Source SVGs (canonical 20, 240×300, blob-bg + brand-blue + sparkle): /Users/theshumba/Downloads/AWBA APP/_ORGANIZED/03_Branding/icon files/01-mosque.svg … 20-ramadan-calendar.svg
Gold recolor source: 03-lantern.svg  (measured hex roles in UI-SPEC "Gold Lantern Variant" table)
Gen-3 glyph ground truth (path data to elevate): /Users/theshumba/Downloads/AWBA APP/_MVP-BUILD/shared/awba-engine.js :11-27 (AW.FLAME/SPARK/CHECK/CITE/LP/STARG/STARE + AW.GLY fact/remember/fard/angle) and _MVP-BUILD/learn.html IC_* constants (lock/chest/trophy if not in engine).
Gold token VALUES to bake into the lantern-gold art (read from shared/awba-engine.css :18-163): --gold #E8A400 · --gold-deep #B47F00 · --gold2 #FFD34D.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author AW.KIT (20 scene icons, hand-transformed) + AW.UNIT_ICON</name>
  <files>shared/awba-engine.js</files>
  <read_first>
    - shared/awba-engine.js :446-448 (KIT banner insertion point) + :27-62 (AW.S IIFE — the module-scoped-object-literal-as-registry + banner-then-flush-left-declaration discipline to mirror; KIT needs NO IIFE, it is pure static data).
    - All 20 source SVGs in the icon folder (read each once, extract the `<svg …>…</svg>` markup).
    - 03-UI-SPEC.md "Icon Registry (FND-04)" — the Authoring transform (strip xmlns/width/height, KEEP viewBox), the 20 registry keys + their source-file mapping, AW.UNIT_ICON mapping, the Icon Sizing Scale.
    - 03-RESEARCH.md Pattern 1 (per-file deterministic transform + naming, e.g. 01-mosque→mosque, 16-qibla-compass→compass; preserve UNIT_ICON semantics) and the id-collision audit finding (zero ids/defs/gradients → svgo unnecessary).
    - 03-PATTERNS.md "KIT section" (structural analog + the Gen-3 double-quoted/escaped shape that D-31 SUPERSEDES — author backtick literals, no \" escaping).
  </read_first>
  <action>Directly after the KIT banner (line 448), declare `AW.KIT = { … }` as a readable multi-line object literal. For each of the 20 source SVGs, apply the deterministic transform: strip the root `xmlns="http://www.w3.org/2000/svg"`, `width="240"`, `height="300"`; KEEP `viewBox="0 0 240 300"` (so CSS sizes without distorting the 0.8 portrait aspect); store the resulting `<svg …>…</svg>` as a single backtick template literal (no `\"` escaping needed); precede each entry with a one-line naming comment (e.g. `// 05-crescent-star`). Use the 20 keys exactly per the UI-SPEC registry list, mapping source→key: mosque, carpet(02-prayer-carpet), lantern(03), lanterns(04), crescent(05-crescent-star), hijab(06-woman-hijab), man(07), family(08), prostration(09), standing(10-standing-prayer), quran-stand(11), beads(12-prayer-beads), kaaba(13), dua(14-hands-dua), dates(15-iftar-dates), compass(16-qibla-compass), ewer(17-water-ewer), night(18-ramadan-night), pattern(19-star-pattern), calendar(20-ramadan-calendar). Do NOT run svgo, do NOT invent path data — copy the source SVG geometry byte-for-byte (only the three attrs are stripped). Then declare `AW.UNIT_ICON = { u1:'compass', u2:'lanterns', u3:'kaaba', u4:'mosque' }` (Gen-3 semantics preserved so runner defaults resolve). Leave `lantern-gold` and `AW.GLYPHS` for Task 2. Do not touch STATE (above) or COMPONENTS (below).</action>
  <verify>
    <automated>node -e "const {makeLS,loadEngine}=require('./scripts/tests/ls-stub');const s=loadEngine(makeLS({}),'globalThis.__k=Object.keys(AW.KIT).length;globalThis.__u=Object.keys(AW.UNIT_ICON).length;globalThis.__svg=Object.values(AW.KIT).every(function(v){return typeof v===\"string\"&&v.indexOf(\"<svg\")===0});');if(s.__k<20||s.__u!==4||!s.__svg){console.error('KIT',s.__k,'UNIT',s.__u,'allsvg',s.__svg);process.exit(1);}console.log('KIT',s.__k,'UNIT',s.__u);" && grep -q 'viewBox="0 0 240 300"' shared/awba-engine.js && ! grep -q 'xmlns' shared/awba-engine.js && node --test scripts/tests/*.test.js 2>&1 | grep -qE 'pass 26'</automated>
  </verify>
  <acceptance_criteria>
    - The engine loads headless (no parse-time DOM access) and `Object.keys(AW.KIT).length >= 20`, `Object.keys(AW.UNIT_ICON).length === 4`, every `AW.KIT` value is a string starting with `<svg`.
    - `grep -q 'viewBox="0 0 240 300"' shared/awba-engine.js` passes (portrait viewBox preserved on scenes).
    - `! grep -q 'xmlns' shared/awba-engine.js` passes (xmlns stripped from all authored SVG strings).
    - `AW.UNIT_ICON` maps u1→compass, u2→lanterns, u3→kaaba, u4→mosque.
    - `node --test scripts/tests/*.test.js` → `pass 26 / fail 0`.
  </acceptance_criteria>
  <done>AW.KIT holds the 20 scene icons as named backtick literals with viewBox kept and xmlns/width/height stripped; AW.UNIT_ICON preserves Gen-3 semantics; engine loads headless; 26 tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Author the lantern-gold variant + AW.GLYPHS (13 glyphs)</name>
  <files>shared/awba-engine.js</files>
  <read_first>
    - 03-lantern.svg (the recolor source) + the AW.KIT['lantern'] entry authored in Task 1.
    - 03-UI-SPEC.md "Gold Lantern Variant — usage rules" (the hex→target recolor table: #2E6BF5→--gold #E8A400 firm map, #2536CE→--gold-deep #B47F00 firm map, #EAF0FE→#FFF3CC authored art literal, #C9D7F5→dark-warm authored art literal; night-register safety → brightest structural highlights at --gold2 #FFD34D) and "Glyph keys (13)".
    - 03-RESEARCH.md Pattern 3 (gold-lantern hex→role mapping + night-register-contrast rationale) and D-33 in user_constraints.
    - 03-PATTERNS.md "KIT section" — the Gen-3 GLY sub-map shape (_MVP-BUILD/awba-engine.js:22-27 fact/remember/fard/angle) and the standalone constants (:11-21 FLAME/SPARK/CHECK/CITE/LP/STARG) that become AW.GLYPHS; and the LANTERNG anti-pattern (:29,34) to NOT port.
    - Gen-3 _MVP-BUILD/shared/awba-engine.js :11-27 and _MVP-BUILD/learn.html (grep for IC_LOCK/IC_CHEST/IC_TROPHY glyph strings if lock/chest/trophy are not in the engine file).
  </read_first>
  <action>Add `'lantern-gold': \`<svg viewBox="0 0 240 300" …>\`` as the 21st keyed entry of `AW.KIT` — hand-recoloured from the `lantern` entry: replace the 3 `#2E6BF5` occurrences with the `--gold` value `#E8A400`, the 7 `#2536CE` with the `--gold-deep` value `#B47F00`, the inner-light `#EAF0FE` with `#FFF3CC`, and the blob halo `#C9D7F5` with a dark warm tone; then, because its home is the dark `.gold-bg` night gradient, lift the brightest STRUCTURAL highlight strokes to `--gold2` `#FFD34D` so the lantern holds contrast on the dark field (art literals are permitted here — these are SVG art internals, not @layer components rules). This is an authored, committed variant — do NOT introduce any `.replace(/#…/g, …)` runtime recolor and do NOT port AW.recolor / AW.LANTERNG. Then declare `AW.GLYPHS = { … }` as ONE 13-key sub-map (≈24×24 square glyphs, single- or backtick-quoted): `flame, spark, check, star, cite, lamp, lock, chest, trophy, fact, remember, fard, angle` — porting the path data from Gen-3's standalone glyph constants and AW.GLY marker glyphs (source them from _MVP-BUILD; for any glyph not found in Gen-3 source, author a simple brand-consistent 24×24 glyph). These replace all per-page UIC/IC_* constants (single source, no per-page constants).</action>
  <verify>
    <automated>node -e "const {makeLS,loadEngine}=require('./scripts/tests/ls-stub');const s=loadEngine(makeLS({}),'globalThis.__k=Object.keys(AW.KIT).length;globalThis.__g=Object.keys(AW.GLYPHS).length;globalThis.__lg=(\"lantern-gold\" in AW.KIT);globalThis.__gsvg=Object.values(AW.GLYPHS).every(function(v){return typeof v===\"string\"&&v.indexOf(\"<svg\")===0});');if(s.__k!==21||s.__g!==13||!s.__lg||!s.__gsvg){console.error('KIT',s.__k,'GLYPHS',s.__g,'lantern-gold',s.__lg,'gsvg',s.__gsvg);process.exit(1);}console.log('KIT',s.__k,'GLYPHS',s.__g);" && ! grep -q '\.replace(/#' shared/awba-engine.js && ! grep -rqE 'UIC|IC_STAR|IC_CHEST|IC_TROPHY|IC_LOCK|LANTERNG' shared/ && node --test scripts/tests/*.test.js 2>&1 | grep -qE 'pass 26'</automated>
  </verify>
  <acceptance_criteria>
    - `Object.keys(AW.KIT).length === 21` (20 scenes + `lantern-gold`), `'lantern-gold' in AW.KIT` is true.
    - `Object.keys(AW.GLYPHS).length === 13` and every glyph value is a string starting with `<svg`.
    - NO runtime recolor: `! grep -q '\.replace(/#' shared/awba-engine.js` passes.
    - NO per-page constants anywhere in shared/: `! grep -rqE 'UIC|IC_STAR|IC_CHEST|IC_TROPHY|IC_LOCK|LANTERNG' shared/` passes.
    - `node --test scripts/tests/*.test.js` → `pass 26 / fail 0`.
  </acceptance_criteria>
  <done>AW.KIT has 21 entries including the authored night-safe lantern-gold; AW.GLYPHS is one 13-key sub-map; zero runtime recolor and zero per-page icon constants remain; 26 tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author-time SVG art → HTML | Icon strings are later injected via innerHTML (by AW.icon, plan 03). The strings originate from author-controlled static art files, never user input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02 | Tampering (DOM) | inline SVG strings in AW.KIT / AW.GLYPHS | mitigate | Strings are author-controlled static art; the RESEARCH id-collision audit confirmed all 20 files are flat path/shape drawings — zero `<script>`/`<foreignObject>`/`<defs>`/gradients/ids. No dynamic data enters the registry; copy geometry byte-for-byte (only xmlns/width/height stripped). |
| T-03-SC | Tampering | svgo / npm | accept | `svgo` skipped per the D-31 amendment — hand-authored, no `npx`, nothing fetched at runtime (D-34, CLAUDE.md zero-CDN). RESEARCH Package Legitimacy Audit lists `svgo` as optional-and-dropped; no `[ASSUMED]`/`[SUS]` install to gate. |
</threat_model>

<verification>
- Engine loads headless; `AW.KIT`=21, `AW.GLYPHS`=13, `AW.UNIT_ICON`=4; `lantern-gold` present; all values start with `<svg`.
- `grep -q 'viewBox="0 0 240 300"'` passes; `! grep -q 'xmlns'` passes.
- `! grep -q '\.replace(/#' shared/awba-engine.js` and `! grep -rqE 'UIC|IC_STAR|IC_CHEST|IC_TROPHY|IC_LOCK|LANTERNG' shared/` pass.
- `node --test scripts/tests/*.test.js` → `pass 26 / fail 0`.
</verification>

<success_criteria>
- FND-04: one icon registry from the canonical 20-SVG source (inline, single source), the gold lantern an authored variant not a regex recolor, no duplicate per-page icon constants remain, all art author-time (zero runtime fetch).
</success_criteria>

<output>
Create `.planning/phases/03-components-icon-kit-motion-language/03-02-SUMMARY.md` when done.
</output>
