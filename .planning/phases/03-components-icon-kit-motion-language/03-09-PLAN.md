---
phase: 03-components-icon-kit-motion-language
plan: 09
type: execute
wave: 4
executor_hint: opus-visual
depends_on: ["03-08"]
files_modified: [shared/awba-engine.css, shared/awba-engine.js, scripts/tests/components.test.js]
autonomous: true
requirements: [ENG-06, MOT-01, MOT-03, MOT-04]
must_haves:
  truths:
    - "Per §4.1 + D-A4 the gummy press is retired for the ONE Athar paper-press across the full tappable inventory (.btn/.opt/.tf/.tile/.tab/.hstat/.cite/.term): `translateY(1px)` + a one-step ink-deepen over `--dur-press var(--ease)`, no drop shadow, no bounce — the retired `--press-rest`/`--press-active`/`--scrim`/`--overlay-hero` usages are all gone and `! grep -q -- '--accent'` and `! grep -qE 'rgba(37,54,'` both pass on the stylesheet"
    - "Per §4.1/§4.2 every component is re-skinned to register tokens: `.btn` = crimson ink block on Page / cream-key or gold-ghost on dark; `.opt` selected-wrong follows law 8 (grey ink-blot + one-line explanation in `--ink-85` + a `--rose` 2px retry frame — never red, no shake, no flash); `.cite` = crimson rubricated reference mark (border-bottom, byte-shape of the AW.cite span PRESERVED); `.tab` active = crimson top-rule not a filled pill; `.hstat` reads as Courier/Marcellus marginalia with no pill fill"
    - "Per §4.3 the singleton sheet is re-skinned (scrim `rgba(19,16,19,.52)`; `.sheet` cream ground, `--r-4` top corners, `--sh-3`, settle motion; `.r-ar.ayah` Amiri Quran on clean cream / `.r-ar` hadith general Amiri; pending `.r-pill` calm-neutral `unverified · pending review`; grade `.r-pill.grade` in `--olive` per D-A9) — the sheet stays a Page object so scripture renders under rubrication"
    - "Per §4.4 the retired classes AND their keyframes are deleted: `.combo`, `.perfect` (+ `--overlay-hero` usage), `.confetti`/`.cf`, `.companion` (+ `@keyframes bob`/`glow`), `.breathing-ring` (+ its pulse `@keyframes breathe`); `AW.confetti` is removed from the engine JS (D-A14); components.test.js is updated CONSCIOUSLY (count adjusted, never silently shrunk)"
    - "Per §2.7/§4.4 the new motion vocabulary + celebration classes land in `@layer motion`/`@layer components`: keyframes `settle`/`breathe`(Sky ambient)/`drift`/`stamp` on the one `--ease`, and `.dab`(drift) `.thread`(Orbit gold-thread draw) `.plate`(Festival stamp) `.rosette`(gold review seal) — the plan 07 `ink-draw` keyframe and `.ring` wrapper are PRESERVED, not clobbered"
    - "Per §3.3 + D-A8 thermal `data-state` styling is shape-first: not-yet = a hollow ring (border 2px; `--ink-40` on cream / `--powder` on dark), progress = a half-inked dab (`linear-gradient(90deg,var(--st) 50%,transparent 50%)`), mastered = a filled dab + check glyph (`--gold` fill + a `--rule` keyline on cream) — shape carries meaning on every ground; per MOT-04 both reduced-motion triggers still quiet ambient loops"
  artifacts:
    - path: "shared/awba-engine.css"
      provides: "@layer components re-skinned to Athar (paper-press inventory, register-scoped chrome, sheet, r-*/g-*, data-state shapes, .dab/.thread/.plate/.rosette) + @layer motion rebuilt (settle/breathe/drift/stamp; Gen-3 keyframes retired; ink-draw preserved)"
      contains: "@keyframes settle"
    - path: "shared/awba-engine.js"
      provides: "AW.confetti removed; AW.icon/cite/wire/sheet/sheetRef/sheetTerm/reducedMotion/animate retained (D-A14)"
      contains: "AW.sheetRef"
    - path: "scripts/tests/components.test.js"
      provides: "updated suite with any confetti-specific case removed consciously; sheet/cite/icon coverage retained"
      contains: "AW.cite"
  key_links:
    - from: ".opt selected-wrong (law 8)"
      to: "grey ink-blot + --ink-85 explanation + --rose retry frame"
      via: "shape+text carry the signal; rose only frames"
      pattern: "--rose"
    - from: "the one paper-press"
      to: ".btn/.opt/.tf/.tile/.tab/.hstat/.cite/.term"
      via: "shared translateY(1px)+ink-deepen over --dur-press var(--ease)"
      pattern: "--dur-press"
---

<objective>
Re-skin every component to the Athar registers and rewrite the motion vocabulary: retire the gummy press, indigo shadows, and the whole celebration menagerie (`.combo`/`.perfect`/`.confetti`/`.companion`/`.breathing-ring` + `AW.confetti`), and replace them with the one paper-press, the register-scoped chrome, the law-8 gentle wrong-answer, the re-skinned singleton sheet, the shape-first thermal states, and the four new celebration primitives (`.dab`/`.thread`/`.plate`/`.rosette`) driven by the `settle`/`breathe`/`drift`/`stamp` keyframes on the single easing family. This plan closes the CSS §12 gates that plan 06 could not (`--accent`, `rgba(37,54,`) and the JS confetti gate.

Purpose: ENG-06 (the citation/gloss sheet re-skinned with the Quran/hadith face-split under rubrication), MOT-01 (the per-register motion verbs), MOT-03 (the one press across the full inventory), MOT-04 (reduced-motion supremacy preserved; `AW.confetti` removed rather than guarded-for-nothing).
Output: `@layer components` + `@layer motion` re-skinned in `shared/awba-engine.css`; `AW.confetti` removed from `shared/awba-engine.js`; `scripts/tests/components.test.js` updated consciously.

Executor routing: opus + load the `frontend-design` skill (flagship visual plan). The law-8 wrong-answer, the sheet face-split under rubrication, and the paper-press-over-gummy are the elevations — coherence over per-element flourish. Preserve the plan-07 `ink-draw` keyframe and `.ring` wrapper when you retire the Gen-3 keyframes (surgical edits, never a wholesale layer rewrite).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-components-icon-kit-motion-language/03-UI-SPEC-ATHAR.md

<interfaces>
<!-- Surgical edit surface — retire/re-skin specific selectors, do NOT rewrite whole layers. -->
shared/awba-engine.css @layer components (:314-740): the chips (.cite :329-361, .term), the sheet (.scrim/.sheet/.grip/.sheet-x :367-425), the citation-sheet rows (.r-* :427-486) + term gloss (.g-* :488-524), the tappable inventory (.btn/.opt/.tf/.tile/.tab/.hstat :526-616), the gummy press rule (:619-632, RETIRE), hovers (:644-652), and the RETIRE targets .combo (:657-678) / .perfect (:683-718) / .confetti+.cf (:723-739).
shared/awba-engine.css @layer motion (:742-810): RETIRE @keyframes fall/bob/breathe/glow/popIn + .companion/.companion svg/.breathing-ring loops (:757-791); KEEP the dual reduced-motion block (:793-809, re-point the animation:none list to the new ambient classes) + the plan-07 `ink-draw` keyframe and `.ring` wrapper (PRESERVE).
shared/awba-engine.js AW.confetti (:1033-1073) — REMOVE the whole primitive (D-A14); KEEP AW.icon/cite/wire/sheet/sheetRef/sheetTerm/reducedMotion/animate. AW.cite (:891-899) markup byte-shape stays (validator).
scripts/tests/components.test.js — remove any AW.confetti-specific case; keep AW.icon/AW.cite/escapeHtml/AW.reducedMotion/registry coverage.
03-UI-SPEC-ATHAR.md §4 (all: buttons/tappables :564-616, chips :606-616-ish, sheet :618-641, retired→replacement table :643-654 + the new celebration classes to author :653-655), §3.3 thermal shape rules, §2.5 depth usage rule, §2.7 verbs, §11 D-A3/A4/A8/A9/A12/A14.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-skin @layer components — paper-press inventory, chips, sheet, r-*/g-*, thermal shapes, new celebration classes</name>
  <files>shared/awba-engine.css</files>
  <read_first>
    - shared/awba-engine.css :314-740 (the whole `@layer components` block being re-skinned/retired).
    - 03-UI-SPEC-ATHAR.md §4.1 (per-element rest/hover/press/focus/disabled in Athar tokens + the one paper-press D-A4), §4.2 (.cite rubrication + byte-shape note, .term dotted underline), §4.3 (.scrim/.sheet/.grip/.sheet-x + .r-* face-split + pending pill ALWAYS + grade pill `--olive` D-A9 + .g-*), §4.4 (retired classes + the new .dab/.thread/.plate/.rosette to author), §3.3 (thermal hollow/half/filled+check shapes).
  </read_first>
  <action>Re-skin `@layer components` to register tokens (surgical, selector-by-selector). (1) The tappable inventory — re-skin `.btn` (Page: `background:var(--crimson);color:var(--cream)`; dark: cream-key OR gold-ghost `box-shadow:var(--edge-line)`, `--r-3`, Readex 600), `.opt` (`--cream`/`--ink`/`2px var(--rule)`, `--r-2`, settle on mount; selected-correct = a drawn gold dot + `--gold` border + `rgba(217,164,65,.10)` wash; selected-wrong per law 8 = grey ink-blot dab + one-line `--ink-85` explanation + a 2px `--rose` retry frame, never red/shake/flash), `.tf`/`.tile` (Page rest chrome `--r-2`), `.tab` (active = `color:var(--crimson)` + a 2px `--crimson` top-rule, icon via `--icon-accent`, keep 44px target), `.hstat` (marginalia: `background:transparent;border:none;color:var(--paper-62)`/`--ink-62`, numeral in `--font-marg`/Marcellus, leading glyph via `--icon-accent`, no pill). (2) REPLACE the gummy press rule with the ONE Athar paper-press applied to `.btn,.opt,.tf,.tile,.tab,.hstat,.cite,.term`: `:active{ transform:translateY(1px) }` + a one-step ink-deepen of border/fill, `transition` on `transform`/border/background over `--dur-press var(--ease)` — no `box-shadow` drop, no `--press-rest`/`--press-active`/`--accent-deep`. (3) Re-skin the chips: `.cite` = crimson rubricated mark (`color:var(--crimson);background:transparent;border-bottom:1.5px solid var(--crimson);border-radius:0`, leading cite glyph `--icon-accent`; hover `rgba(163,44,33,.08)`) — the `<span class="cite" data-ref="…">` byte-shape is untouched (only CSS changes); `.term` = `underline dotted var(--crimson)`, hover solid. (4) Re-skin the sheet: `.scrim{background:rgba(19,16,19,.52)}`, `.sheet{background:var(--cream);color:var(--ink);border-radius:var(--r-4) var(--r-4) 0 0;box-shadow:var(--sh-3)}` with settle IN/OUT on `--dur-sheet var(--ease)`, `.grip{background:var(--rule)}`, `.sheet-x{color:var(--ink-40)}` hover `--ink`. (5) Re-skin the rows: `.r-src` Courier kicker `--madder`; `.r-ar.ayah` Amiri Quran `--fs-ayah` lh1.9 `--kiswah` on clean cream (no grain); `.r-ar` hadith Amiri `--fs-scrip`; `.r-mean` `--fs-body var(--ink-85)`; `.r-ref` Courier `--fs-marg italic var(--madder)`; `.r-pill` calm-neutral `unverified · pending review` (`--ink-62`/`--rule`, `--r-pill`, transparent); `.r-pill.grade` `--olive` ink + `--olive` hairline (D-A9). Re-skin `.g-*` (Aref Ruqaa in a Farag square for the 15 chapter terms, else Amiri; `.g-tl` `--ink-62`; `.g-wd` Readex 600; `.g-df` body; `.g-cx` `--fs-ui var(--ink-62)`). (6) Add the thermal SHAPE rules (§3.3) consuming `--st`: not-yet hollow ring, progress half-inked dab, mastered filled dab + check + `--rule` keyline on cream. (7) Author the new celebration classes `.dab` (drift), `.thread` (Orbit gold-thread draw), `.plate` (Festival stamp), `.rosette` (gold seal + inked rim + check, stamp). (8) DELETE `.combo`, `.perfect` (+ its `--overlay-hero` use), `.confetti`, `.cf` entirely, and gate the hovers to `(hover:hover) and (pointer:fine)`. Ensure NO `var(--accent…)`, `--press-rest`, `--press-active`, `--scrim`, `--overlay-hero`, or `rgba(37,54,` remains in `@layer components`; do not name any retired token in a comment.</action>
  <verify>
    <automated>! grep -q -- '--accent' shared/awba-engine.css && ! grep -qE '\.combo|\.perfect|\.confetti|\.cf ' shared/awba-engine.css && ! grep -qE '--press-rest|--press-active|--overlay-hero' shared/awba-engine.css && grep -q '\.dab' shared/awba-engine.css && grep -q '\.thread' shared/awba-engine.css && grep -q '\.plate' shared/awba-engine.css && grep -q '\.rosette' shared/awba-engine.css && grep -q -- '--dur-press' shared/awba-engine.css && grep -q 'unverified · pending review' shared/awba-engine.css && grep -q -- '--olive' shared/awba-engine.css && test "$(grep -c '@layer tokens, base, components, screens, motion;' shared/awba-engine.css)" -eq 1 && node --test scripts/tests/*.test.js 2>&1 | grep -qE 'fail 0'</automated>
  </verify>
  <acceptance_criteria>
    - No `--accent`, `--press-rest`, `--press-active`, `--overlay-hero`, `.combo`, `.perfect`, `.confetti`, `.cf` remain in the stylesheet; the paper-press (`--dur-press` + `translateY(1px)`) and the new `.dab`/`.thread`/`.plate`/`.rosette` are present.
    - The sheet keeps `unverified · pending review`; the grade pill uses `--olive`; layer-order line still exactly once.
    - `node --test scripts/tests/*.test.js` → `fail 0`.
  </acceptance_criteria>
  <done>@layer components is re-skinned to Athar: one paper-press across the inventory, register-scoped chrome, law-8 gentle wrong-answer, the re-skinned sheet with the face-split + olive grade pill, shape-first thermal states, and the four new celebration primitives; the celebration menagerie + gummy/indigo/accent tokens are gone.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild @layer motion — settle/breathe/drift/stamp; retire Gen-3 keyframes; preserve ink-draw</name>
  <files>shared/awba-engine.css</files>
  <read_first>
    - shared/awba-engine.css :742-810 (the `@layer motion` block: Gen-3 keyframes + ambient loops + the dual reduced-motion block) and the plan-07 additions in this layer (`@keyframes ink-draw` + `.ring`) to PRESERVE.
    - 03-UI-SPEC-ATHAR.md §2.7 (the verb→recipe table: draw/settle/breathe/drift/stamp keyframes + the press feedback recipe), §4.4 (retired ambient → replacement), §11 D-A3/A4.
  </read_first>
  <action>In `@layer motion`, DELETE the Gen-3 vocabulary — `@keyframes fall`, `@keyframes bob`, `@keyframes glow`, `@keyframes popIn`, the node-pulse `@keyframes breathe`, and the `.companion`/`.companion svg`/`.breathing-ring` ambient-loop rules (these carried the retired `rgba(37,54,` glow). PRESERVE the plan-07 `@keyframes ink-draw` and `.ring` wrapper (do not touch them). ADD the Athar keyframes per §2.7: `@keyframes settle` (opacity 0→1 + translateY 8px→0 over `--dur-settle`), `@keyframes breathe` (the SKY ambient opacity .72→1 on `--dur-amb var(--ease) infinite alternate`; the halo variant adds scale .97→1), `@keyframes drift` (the Circle dab arrival translate+rotate+fade), `@keyframes stamp` (the Festival wax-seal scale 1.03→1 over `--dur-stamp`). Wire the ambient loops to the new celebration/register classes (e.g. the Sky `breathe` on a `.sky-breathe`/Sky halo hook; `drift` on `.dab`; `stamp` on `.plate`/`.rosette`; `settle` on `.opt`/`.sheet` reveals). Update the DUAL reduced-motion block (`@media (prefers-reduced-motion: reduce)` AND `[data-motion="reduce"]`) so its `animation:none !important` list names the NEW ambient classes (Sky breathe / drift / any `[data-ambient]`) instead of the deleted `.companion`/`.breathing-ring`; keep the finite-duration collapse behaviour. Ensure `! grep -qE 'rgba(37,54,'` now passes on the whole stylesheet. Do not name retired tokens/classes in comments.</action>
  <verify>
    <automated>! grep -qE 'rgba\(37,54,' shared/awba-engine.css && ! grep -qE '@keyframes (fall|bob|glow|popIn)\b' shared/awba-engine.css && ! grep -q '\.companion' shared/awba-engine.css && ! grep -q '\.breathing-ring' shared/awba-engine.css && grep -q '@keyframes settle' shared/awba-engine.css && grep -q '@keyframes drift' shared/awba-engine.css && grep -q '@keyframes stamp' shared/awba-engine.css && grep -q '@keyframes ink-draw' shared/awba-engine.css && grep -qE '@media \(prefers-reduced-motion: reduce\)' shared/awba-engine.css && grep -q '\[data-motion="reduce"\]' shared/awba-engine.css && node --test scripts/tests/*.test.js 2>&1 | grep -qE 'fail 0'</automated>
  </verify>
  <acceptance_criteria>
    - Gen-3 keyframes (`fall`/`bob`/`glow`/`popIn`/node-pulse `breathe`) and `.companion`/`.breathing-ring` are gone; `rgba(37,54,` no longer appears anywhere in the stylesheet.
    - The Athar `settle`/`breathe`(Sky)/`drift`/`stamp` keyframes exist; the plan-07 `ink-draw` keyframe is preserved; both reduced-motion triggers remain and target the new ambient classes.
    - `node --test scripts/tests/*.test.js` → `fail 0`.
  </acceptance_criteria>
  <done>@layer motion is the Athar vocabulary (settle/breathe/drift/stamp + the preserved draw/ink-draw) on the one easing family; the Gen-3 keyframes + ambient mascot loops are retired; indigo shadow is gone stylesheet-wide; both reduced-motion triggers quiet the new ambient loops.</done>
</task>

<task type="auto">
  <name>Task 3: Remove AW.confetti from the engine + update components.test.js consciously</name>
  <files>shared/awba-engine.js, scripts/tests/components.test.js</files>
  <read_first>
    - shared/awba-engine.js :1033-1073 (AW.confetti) + :1074-1086 (AW.animate — KEEP) + :920-934 (AW.reducedMotion — KEEP).
    - scripts/tests/components.test.js (scan for any AW.confetti reference or count that changes when it is removed).
  </read_first>
  <action>Delete the entire `AW.confetti = function(n){…}` primitive and its banner comment from `shared/awba-engine.js` (D-A14). Do NOT touch `AW.icon`/`AW.cite`/`AW.wire`/`AW.sheet`/`AW.sheetRef`/`AW.sheetTerm`/`AW.reducedMotion`/`AW.animate` — they are skin-agnostic and drive the re-skinned components. Then update `scripts/tests/components.test.js` CONSCIOUSLY: if any test references `AW.confetti`, remove exactly that case (celebration is now CSS-driven `.dab`/`.thread`/`.plate`/`.rosette`, validated by the grep gates + the human §9 gate, not a unit test) — never silently shrink unrelated coverage; keep the AW.icon/AW.cite/escapeHtml/AW.reducedMotion/registry tests intact. Confirm `! grep -q 'AW.confetti' shared/awba-engine.js` passes.</action>
  <verify>
    <automated>! grep -q 'AW.confetti' shared/awba-engine.js && grep -q 'AW.animate' shared/awba-engine.js && grep -q 'AW.sheetRef' shared/awba-engine.js && node scripts/validate-content.js --self-test && OUT=$(node --test scripts/tests/*.test.js 2>&1); echo "$OUT" | tail -4; echo "$OUT" | grep -qE 'fail 0'</automated>
  </verify>
  <acceptance_criteria>
    - `! grep -q 'AW.confetti' shared/awba-engine.js` passes; `AW.animate`/`AW.reducedMotion`/`AW.sheetRef` and the rest of the retained builders remain.
    - `node scripts/validate-content.js --self-test` passes (AW.cite byte-shape intact).
    - `node --test scripts/tests/*.test.js` → `fail 0` (any confetti case removed consciously, other coverage intact).
  </acceptance_criteria>
  <done>AW.confetti is removed (D-A14) with the rest of the AW.* builders retained; components.test.js is updated consciously; the validator self-test and the full suite are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cfg.refs/cfg.terms (author content) → sheet innerHTML | The sheet re-skin does not change AW.sheetRef/sheetTerm's data handling — same author-controlled verbatim scripture/gloss trust model as plan 03-03. |
| CSS re-skin → rendered document | Pure static stylesheet content; no dynamic data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-09 | Tampering (DOM) | AW.sheetRef/sheetTerm innerHTML | accept | Content is author-controlled verbatim scripture/gloss (Josh's data files), never user input; systematic runner-HTML escaping is Phase-4/6 scope. Unchanged from the plan 03-03 posture. |
| T-03-SC | Tampering | npm installs | accept | No package installs; CSS/JS hand-edited; nothing fetched. No `[ASSUMED]`/`[SUS]` package to gate. |
</threat_model>

<verification>
- `! grep -q -- '--accent'`, `! grep -qE 'rgba(37,54,'`, `! grep -q 'AW.confetti'` all pass; `.combo`/`.perfect`/`.confetti`/`.companion`/`.breathing-ring` gone.
- Paper-press (`--dur-press` + `translateY(1px)`) across the inventory; `.dab`/`.thread`/`.plate`/`.rosette` + `settle`/`breathe`/`drift`/`stamp` present; `ink-draw` preserved; both reduced-motion triggers retained.
- Sheet keeps `unverified · pending review` + `--olive` grade pill; validator self-test green.
- `node --test scripts/tests/*.test.js` → `fail 0`.
</verification>

<success_criteria>
- ENG-06: the citation/gloss sheet re-skinned with the Quran/hadith face-split under Page rubrication, always-on pending pill, olive grade pill.
- MOT-01: the per-register verbs (settle/breathe/drift/stamp + draw) on the one easing family.
- MOT-03: the one paper-press across the full tappable inventory (gummy retired).
- MOT-04: reduced-motion supremacy preserved (both triggers); AW.confetti removed rather than guarded.
</success_criteria>

<output>
Create `.planning/phases/03-components-icon-kit-motion-language/03-09-SUMMARY.md` when done.
</output>
