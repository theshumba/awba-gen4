---
phase: 03-components-icon-kit-motion-language
plan: 08
type: execute
wave: 3
executor_hint: opus
depends_on: ["03-07"]
files_modified: [shared/awba-engine.js, scripts/tests/components.test.js]
autonomous: true
requirements: [FND-04]
must_haves:
  truths:
    - "Per §5.1 + D-A5 every AW.KIT / AW.GLYPHS SVG is re-inked to the one currentColor + `--icon-accent` model by an AUTHORED, committed pass (no runtime `.replace()` recolour): structural ink (`#2E6BF5`/`#2536CE`) → `currentColor`; halos/panels (`#C9D7F5`/`#EAF0FE`/`#F4F7FE`/`#DCE6FB`) → `currentColor` at authored low opacity (.12 halo / .06 panel) or `fill=\"none\"`; accent detail (sparkles/flame/key marks) → `var(--icon-accent)`; kaaba greys flatten to `currentColor` opacity steps"
    - "Per §5.2 icons inherit `color` from the host register so one asset re-inks by ground (Orbit cream, Page kiswah, Sky moonmilk) and `--icon-accent` supplies the single expressive spot per icon (Page crimson / Orbit·Sky gold / Festival harissa) — no crimson/gold hex is hardcoded in any icon's markup"
    - "Per §5.3 + D-A6 `AW.KIT['lantern-gold']` is deleted (its entry removed) — the currentColor model renders the one lantern gold automatically on dark grounds, so the bespoke second asset is redundant; `AW.UNIT_ICON` never referenced it, so nothing breaks and KIT returns to 20 scenes"
    - "Per §5 the glyph re-ink map is applied: flame → `var(--ember)`, spark → `var(--gold)`, star → `var(--gold)` + `--icon-accent` stroke, cite → `currentColor`, lamp → `var(--gold)`, check → `currentColor`, lock → `currentColor`@.45, chest → `currentColor` + `var(--gold)` latch, trophy → `var(--gold)`, and the 4 lesson markers (fact/remember/fard/angle) → `var(--icon-accent)`"
    - "Per D-A5/D-A6 the retired blue source hexes (`#2E6BF5`/`#2536CE`/`#C9D7F5`/`#EAF0FE`) no longer appear in the icon art; components.test.js is updated CONSCIOUSLY to `Object.keys(AW.KIT).length === 20` with the lantern-gold assertion dropped, GLYPHS staying 13, every value still starting with `<svg`, and the suite stays green — never silently shrunk"
  artifacts:
    - path: "shared/awba-engine.js"
      provides: "re-inked AW.KIT (20 scenes, currentColor + --icon-accent model) + AW.GLYPHS (13 re-inked), lantern-gold entry removed"
      contains: "var(--icon-accent)"
    - path: "scripts/tests/components.test.js"
      provides: "registry-integrity assertions updated to KIT===20 (lantern-gold dropped), GLYPHS===13"
      contains: "20"
  key_links:
    - from: "icon fill=var(--icon-accent) + currentColor"
      to: ".reg-* --icon-accent / color from @layer base (plan 03-06)"
      via: "register-scoped custom properties resolve per ground at render"
      pattern: "--icon-accent"
    - from: "AW.KIT registry (20 scenes)"
      to: "AW.icon consumers + preview §1/§4 (plan 11)"
      via: "author-time inline SVG, no runtime recolour"
      pattern: "AW.KIT"
---

<objective>
Re-ink the icon registry to the Athar one-colour model: retire the two-blue illustrations + glow so each SVG inherits its register's ink via `currentColor` and carries one spot of `--icon-accent`. Delete the now-redundant `lantern-gold` variant (D-A6) — the single lantern renders gold on any dark ground automatically. This is a deterministic transform pass driven by the §5.1 hex→Athar map; no path geometry changes, only fills/strokes/opacity.

Purpose: FND-04 — the one icon registry re-inked to the register model (colour rules per ground, no runtime recolour), the last hex-literal art variant removed.
Output: `AW.KIT` (20 scenes) + `AW.GLYPHS` (13) re-inked in `shared/awba-engine.js`; the `lantern-gold` entry removed; `scripts/tests/components.test.js` registry counts updated to 20/13.

Executor routing: opus + frontend-design skill (orchestrator override — icons are part of the look). The transform is a deterministic hex→token substitution per the §5.1 and §5 tables — apply the map exactly per icon, preserve every path's geometry byte-for-byte, change only fill/stroke/opacity attributes; do NOT invent or re-draw art. Opus's craft judgement applies only where §5.1 offers a choice (low-opacity wash vs `fill="none"`): pick per-icon by whether the "quiet ink line-drawing" read survives at UI size, and record each choice.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-components-icon-kit-motion-language/03-UI-SPEC-ATHAR.md

<interfaces>
<!-- Edit surface: the icon art strings only. -->
shared/awba-engine.js AW.KIT (:459-778, the 20 scene entries) — re-ink each entry's fills/strokes in place; geometry (`d="…"` path data) stays byte-identical.
shared/awba-engine.js AW.KIT['lantern-gold'] (:779-803, the authored gold variant) — DELETE this entry entirely (its banner comment too). AW.UNIT_ICON (:805-806) never referenced it.
shared/awba-engine.js AW.GLYPHS (:808-839, the 13 glyphs) — re-ink per the §5 glyph map.
shared/awba-engine.js AW.icon (:874-883) — reads AW.KIT then AW.GLYPHS; DO NOT change it (icons re-ink via markup, not the accessor). No runtime `.replace(/#…/)` may be introduced (grep gate).
scripts/tests/components.test.js :114-136 (registry integrity: currently asserts KIT===21 + `'lantern-gold' in AW.KIT` + GLYPHS===13) — update KIT to 20 and remove the lantern-gold assertion.
03-UI-SPEC-ATHAR.md §5.1 (the deterministic source-hex→Athar map table + the currentColor/--icon-accent/opacity-wash model), §5.2 (colour rules per ground), §5.3 (D-A6 lantern-gold deletion), §5 glyph map table (:690-703), §10 cut ledger, §11 D-A5/A6.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-ink AW.KIT (20 scenes) + delete the lantern-gold entry</name>
  <files>shared/awba-engine.js</files>
  <read_first>
    - shared/awba-engine.js :459-803 (all 20 scene entries + the lantern-gold entry to delete).
    - 03-UI-SPEC-ATHAR.md §5.1 (the full source-hex→Athar deterministic map, incl. the kaaba dimensional-greys flatten and the `#A9BCEB` quran-stand lines → `currentColor`@.4), §5.2 (register inheritance — never hardcode crimson/gold), §5.3 + D-A6 (delete lantern-gold).
  </read_first>
  <action>For each of the 20 `AW.KIT` scene entries, apply the §5.1 map to fills/strokes ONLY (leave every `d="…"` path geometry byte-identical, leave `viewBox="0 0 240 300"` intact): `#2E6BF5` and `#2536CE` structural ink → `currentColor`; `#C9D7F5` blob halo → `currentColor` at `opacity=".12"`; `#EAF0FE`/`#F4F7FE`/`#DCE6FB` lit panels → `currentColor` at `opacity=".06"` (or `fill="none"` where the panel should read as negative space); `#2536CE` sparkle/star accent marks → `var(--icon-accent)`; kaaba dimensional greys (`#5486F8`/`#2450C9`/`#1C3C9C`/`#31489C`) → `currentColor` at `.85`/`.62`/`.45` opacity steps (drop the blue dimensionality, keep the light/dark reading in ink); `#A9BCEB` quran-stand text lines → `currentColor` at `opacity=".4"`. Never hardcode a crimson/gold hex in an icon — the single expressive spot is always `var(--icon-accent)` so the asset picks up the register's accent. Then DELETE the entire `AW.KIT['lantern-gold'] = \`…\`` entry and its banner comment (D-A6). Do not touch AW.UNIT_ICON, AW.GLYPHS (Task 2), or AW.icon. Do not introduce any `.replace(/#…/)` and do not leave the retired blue hexes in comments.</action>
  <verify>
    <automated>node -e "const {makeLS,loadEngine}=require('./scripts/tests/ls-stub');const s=loadEngine(makeLS({}),'globalThis.__k=Object.keys(AW.KIT).length;globalThis.__lg=(\"lantern-gold\" in AW.KIT);globalThis.__u=Object.keys(AW.UNIT_ICON).length;globalThis.__svg=Object.values(AW.KIT).every(function(v){return typeof v===\"string\"&&v.indexOf(\"<svg\")===0});globalThis.__hex=Object.values(AW.KIT).some(function(v){return /#(2E6BF5|2536CE|C9D7F5|EAF0FE|F4F7FE|DCE6FB)/i.test(v)});');if(s.__k!==20||s.__lg||s.__u!==4||!s.__svg||s.__hex){console.error({k:s.__k,lg:s.__lg,u:s.__u,svg:s.__svg,hex:s.__hex});process.exit(1);}console.log('KIT_REINK_OK',s.__k);" && ! grep -q \"KIT\\['lantern-gold'\\]\" shared/awba-engine.js && ! grep -q '\.replace(/#' shared/awba-engine.js && grep -q 'var(--icon-accent)' shared/awba-engine.js</automated>
  </verify>
  <acceptance_criteria>
    - `Object.keys(AW.KIT).length === 20`; `'lantern-gold' in AW.KIT` is false; `AW.UNIT_ICON` still has 4 keys; every KIT value still starts with `<svg`.
    - No retired blue source hex remains **within AW.KIT values** (checked in-registry, not whole-file — AW.GLYPHS still carries source hexes until Task 2 re-inks it; the whole-file absence gate closes in Task 2); no runtime `.replace(/#…)`; `var(--icon-accent)` present in the art.
    - (components.test.js registry count is updated in Task 2; the JS suite is asserted green there.)
  </acceptance_criteria>
  <done>All 20 scene icons re-inked to currentColor + --icon-accent + opacity washes (geometry untouched); the lantern-gold entry is deleted; no blue hex or runtime recolour remains.</done>
</task>

<task type="auto">
  <name>Task 2: Re-ink AW.GLYPHS (13) + update components.test.js registry counts</name>
  <files>shared/awba-engine.js, scripts/tests/components.test.js</files>
  <read_first>
    - shared/awba-engine.js :808-839 (the 13 glyph entries) and the §5 glyph map table in 03-UI-SPEC-ATHAR.md (:690-703).
    - scripts/tests/components.test.js :114-136 (the registry-integrity block asserting KIT===21 + lantern-gold + GLYPHS===13).
  </read_first>
  <action>Apply the §5 glyph map to each `AW.GLYPHS` entry (fills/strokes only, geometry untouched): `flame` `#F0730B` → `var(--ember)`; `spark` `#E8A400` → `var(--gold)`; `star` `#FFD34D`/`#E8A400` → fill `var(--gold)` + stroke `var(--icon-accent)`; `cite` `#2536CE` → `currentColor`; `lamp` `#E8A400`/`#FFF3CC` → `var(--gold)` + panel `currentColor`@.08; `check` `#fff` → `currentColor` (contrast comes from the dab it sits on); `lock` `#AAB4CC` → `currentColor`@.45; `chest` `#fff`/`#B4720C` → `currentColor` + latch `var(--gold)`; `trophy` `#fff` → `var(--gold)`; and the 4 lesson markers `fact`/`remember`/`fard`/`angle` `#2E6BF5` → `var(--icon-accent)`. Then update `scripts/tests/components.test.js` registry-integrity block CONSCIOUSLY: change the KIT assertion from `21` to `20`, remove the `'lantern-gold' in AW.KIT` assertion (and rename the test title to reflect 20 scenes), keep GLYPHS at `13` and the `every value starts with <svg` checks. Never silently shrink the suite — only the KIT count + the one dropped lantern-gold assertion change.</action>
  <verify>
    <automated>node -e "const {makeLS,loadEngine}=require('./scripts/tests/ls-stub');const s=loadEngine(makeLS({}),'globalThis.__g=Object.keys(AW.GLYPHS).length;globalThis.__gsvg=Object.values(AW.GLYPHS).every(function(v){return typeof v===\"string\"&&v.indexOf(\"<svg\")===0});');if(s.__g!==13||!s.__gsvg){console.error({g:s.__g,gsvg:s.__gsvg});process.exit(1);}console.log('GLYPH_REINK_OK');" && ! grep -qE '#(2E6BF5|2536CE|C9D7F5|EAF0FE|F4F7FE|DCE6FB)' shared/awba-engine.js && ! grep -q \"'lantern-gold' in AW.KIT\" scripts/tests/components.test.js && OUT=$(node --test scripts/tests/*.test.js 2>&1); echo "$OUT" | grep -qE 'fail 0' && echo "$OUT" | grep -qE 'pass (4[2-9]|[5-9][0-9]|[0-9]{3,})'</automated>
  </verify>
  <acceptance_criteria>
    - All 13 glyphs re-inked per the §5 map; `Object.keys(AW.GLYPHS).length === 13`; every glyph value starts with `<svg`; the whole-file retired-hex absence gate now closes (`! grep -qE '#(2E6BF5|…)' shared/awba-engine.js`).
    - `scripts/tests/components.test.js` now asserts `Object.keys(AW.KIT).length === 20` and no longer asserts `'lantern-gold' in AW.KIT`.
    - `node --test scripts/tests/*.test.js` → `fail 0`, total unchanged-or-adjusted (KIT count assertion updated, not a test removed) — includes the ring tests from plan 07.
  </acceptance_criteria>
  <done>The 13 glyphs are re-inked to the Athar tokens (ember/gold/currentColor/--icon-accent); components.test.js registry integrity updated to 20 scenes with lantern-gold dropped; the full suite is green with fail 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored SVG art → innerHTML | Icon strings are injected via innerHTML (AW.icon). They are author-controlled static art; this plan only rewrites colour attributes, never geometry, never dynamic data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08 | Tampering (DOM) | inline SVG fills/strokes | mitigate | Only fill/stroke/opacity attribute values change, per the deterministic §5.1 map; geometry stays byte-identical; no runtime `.replace()` recolour is introduced (grep gate). |
| T-03-SC | Tampering | npm installs | accept | No package installs; hand-edited art; nothing fetched. No `[ASSUMED]`/`[SUS]` package to gate. |
</threat_model>

<verification>
- KIT===20, lantern-gold gone, UNIT_ICON===4, GLYPHS===13, all values start `<svg`.
- No retired blue hex (`#2E6BF5`/`#2536CE`/`#C9D7F5`/`#EAF0FE`/`#F4F7FE`/`#DCE6FB`); no `.replace(/#`; `var(--icon-accent)` present.
- components.test.js updated to 20 (lantern-gold assertion dropped).
- `node --test scripts/tests/*.test.js` → `fail 0`.
</verification>

<success_criteria>
- FND-04: the icon registry re-inked to the one currentColor + `--icon-accent` model (register colour rules, no runtime recolour); the redundant lantern-gold variant removed; registry integrity re-proven at 20 scenes / 13 glyphs.
</success_criteria>

<output>
Create `.planning/phases/03-components-icon-kit-motion-language/03-08-SUMMARY.md` when done.
</output>
