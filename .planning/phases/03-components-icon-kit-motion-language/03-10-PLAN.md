---
phase: 03-components-icon-kit-motion-language
plan: 10
type: execute
wave: 5
executor_hint: opus-visual
depends_on: ["03-09"]
files_modified: [shared/awba-engine.js, shared/awba-engine.css, scripts/tests/sky.test.js]
autonomous: true
requirements: [MOT-01, MOT-04]
must_haves:
  truths:
    - "Per §7.1/§7.2 a PURE now→temperature function returns one of the five temperatures (dawn/day/dusk/night/lastthird) from the stored prayer times, using LOCAL time only (reuse the D-16 local-date helpers — NEVER a UTC serialization); midnight is local 00:00; with fixed now fixtures the mapping is deterministic"
    - "Per §7.2 + D-A13 prayer times are manual v1 (no geolocation, no network EVER): awba_prefs.prayerTimes = {fajr,dhuhr,asr,maghrib,isha} and awba_prefs.skyMode = manual|off are read via AW.prefs.get with a sensible DEFAULT schedule (fajr 05:00, dhuhr 13:00, asr 16:30, maghrib 19:30, isha 21:00) so the sky always has a temperature even when unset — implemented WITHOUT an awba_prefs schema bump (get-with-default tolerates absence)"
    - "Per §7.2 boot sets document.documentElement.dataset.sky (or the home shell) to the computed value inside the existing typeof document !== 'undefined' block, so §3.2's CSS tint paints over the Kiswah Orbit ground (law 1); skyMode:off ⇒ always day; re-evaluated on open/visibility-change, no timers"
    - "Per §7.3 course progress adds ONE degree of horizon apricot via --dawn (0..1, scaled by atomsDone/65, capped) driving a second faint apricot horizon glow — ambient, never the metric (the Ring is the metric); it never competes with the real prayer-clock tint or scripture/Ring"
    - "Per §7.4 + MOT-04 the sky temperature is information applied STATICALLY (a static tint is not motion, so it stays under reduced motion — the learner still sees the right time-of-day); only the breathe opacity pulse (.sky-breathe) is gated off by BOTH triggers (prefers-reduced-motion / [data-motion=reduce])"
  artifacts:
    - path: "shared/awba-engine.js"
      provides: "the pure now→temperature function + prayerTimes/skyMode defaults + the boot-time dataset.sky wiring + --dawn progress degree"
      contains: "prayerTimes"
    - path: "scripts/tests/sky.test.js"
      provides: "node:test coverage: fixed-now→temperature fixtures, prayerTimes boundary shift, skyMode:off⇒day, determinism/no-geolocation"
      contains: "skyMode"
    - path: "shared/awba-engine.css"
      provides: "the --dawn second-apricot horizon glow + the .sky-breathe ambient gated under both reduced-motion triggers"
      contains: "sky-breathe"
  key_links:
    - from: "boot dataset.sky value"
      to: "§3.2 [data-sky] CSS tints (plan 03-06)"
      via: "documentElement.dataset.sky drives --sky-tint"
      pattern: "dataset.sky"
    - from: "the now→temperature function"
      to: "the D-16 local-date helpers"
      via: "local getters only, never UTC"
      pattern: "getHours|getMinutes"
---

<objective>
Give the black home world its Sky: a manual-times prayer clock (the v1 floor — no geolocation) that computes one of five canvas temperatures and paints it as the §3.2 tint over the Kiswah Orbit ground, plus a --dawn degree of horizon warmth that grows with course progress but is never the metric. The tint is ambient information applied statically (it stays under reduced motion); only the slow breathe pulse is gated off.

Purpose: MOT-01 (the Sky breathe ambient on the one easing family) and MOT-04 (static-tint-stays / breathe-gated under both reduced-motion triggers); the second flagship feature the adoption schedules into Phase 3 before the lesson port.
Output: the pure temperature function + prayerTimes/skyMode defaults + boot wiring + --dawn degree in shared/awba-engine.js; the --dawn glow + .sky-breathe gating in shared/awba-engine.css; scripts/tests/sky.test.js proving the five-temperature mapping and the manual/off behaviour.

Executor routing: opus + load the frontend-design skill (flagship visual plan). The read must be "the sky matches the time of day without feeling like a fake reward" (§9 gate item 8) — the prayer-clock temperature is truthful ambience, the --dawn progress warmth is a separate, subordinate degree. Reuse the existing prefs seam and D-16 local-date discipline; never reach for geolocation or a timer.

CONTRACT NOTE: §7.2 says "add to defaultPrefs; absent → the DEFAULT". Because AW.prefs.get(k, d) already returns d when a key is absent, this is implemented by adding the fields to defaultPrefs() for fresh installs AND reading them via AW.prefs.get('prayerTimes', DEFAULTS) / AW.prefs.get('skyMode', 'manual') — NO awba_prefs CURRENT bump is needed, so existing v1 prefs blobs are not reset.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03-components-icon-kit-motion-language/03-UI-SPEC-ATHAR.md

<interfaces>
<!-- Survivors + edit surface. -->
shared/awba-engine.js AW.prefs IIFE (:257-303): defaultPrefs (:262-264) — add prayerTimes+skyMode; AW.prefs.get(k,d) returns d when absent (so no schema bump). D-24: read prefs ONLY via AW.prefs.
shared/awba-engine.js the D-16 local-date helper (:34-44, pad2/ymd via getFullYear/getMonth/getDate) — the local-time discipline to reuse; the temperature function reads now.getHours()/getMinutes() locally, never a UTC/ISO serialization.
shared/awba-engine.js the boot-stamp block (:431-438, `if (typeof document !== 'undefined')`) — extend it to set document.documentElement.dataset.sky; it is the only parse-time DOM touch and stays guarded for headless.
shared/awba-engine.js AW.state()/stars — the atomsDone source for the --dawn degree (atomsDone/65). The RING seed/generator (plan 07) is unrelated; the Sky is ambient, the Ring is the metric.
shared/awba-engine.css §3.2 [data-sky] tints (added in plan 03-06) — this plan does NOT re-add them; it adds only the --dawn glow + .sky-breathe gating in @layer base/motion (additive; do not disturb plan 06/09 content).
scripts/tests/ls-stub.js + scripts/tests/state-helpers.test.js — the node:test file shape; sky.test.js runs via the `node --test scripts/tests/*.test.js` glob only.
03-UI-SPEC-ATHAR.md §7 (all: five temperatures :806-814, trigger logic :816-832, --dawn :834-839, reduced motion :841-846, acceptance :848-857), §3.2 the data-sky tints, §11 D-A13.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The pure now→temperature function + prayerTimes/skyMode defaults + boot wiring + --dawn</name>
  <files>shared/awba-engine.js</files>
  <behavior>
    - The temperature function with the DEFAULT schedule and a fixed now of 06:00 (after fajr, before dhuhr) → 'dawn'; 14:00 → 'day'; 20:00 (after maghrib, before isha) → 'dusk'; 22:00 (after isha, before midnight) → 'night'; 02:00 (after midnight, before fajr) → 'lastthird'.
    - Passing a prayerTimes with a later maghrib shifts the dusk boundary accordingly (deterministic with the same fixed now).
    - skyMode 'off' → always 'day' regardless of now.
    - No geolocation/network API is referenced anywhere; the function is pure given (now, prayerTimes, skyMode).
  </behavior>
  <read_first>
    - shared/awba-engine.js :34-44 (D-16 local helpers), :257-303 (AW.prefs/defaultPrefs), :431-438 (boot-stamp), AW.state()/stars accessor for atomsDone.
    - 03-UI-SPEC-ATHAR.md §7.1 (five temperatures + windows), §7.2 (trigger logic, the DEFAULT schedule, skyMode, boot dataset.sky, no geolocation), §7.3 (--dawn scaled by atomsDone/65, capped), §7.5 acceptance 1-3/6.
  </read_first>
  <action>Add a pure temperature function (e.g. AW.skyTemp(now, times, mode)) that: returns 'day' immediately if mode === 'off'; else reads now.getHours()/getMinutes() (LOCAL — reuse the D-16 discipline, never a UTC/ISO string) and the five times boundaries and returns 'dawn' (fajr→dhuhr window), 'day' (dhuhr→maghrib), 'dusk' (maghrib→isha), 'night' (isha→local midnight), or 'lastthird' (midnight→fajr). Add the fields to defaultPrefs() — prayerTimes: { fajr:'05:00', dhuhr:'13:00', asr:'16:30', maghrib:'19:30', isha:'21:00' } and skyMode: 'manual' — WITHOUT bumping CURRENT (reads tolerate absence via AW.prefs.get('prayerTimes', DEFAULTS) / AW.prefs.get('skyMode', 'manual')). Extend the boot-stamp block: compute the current temperature from new Date() + the stored prefs and set document.documentElement.dataset.sky to it (re-evaluate on visibilitychange/open; NO timers, NO geolocation, NO network). Add an AW.skyDawn() (or equivalent) that returns min(cap, atomsDone/65) and, at boot, sets --dawn on the home shell as the second, subordinate apricot degree (ambient, never the metric). Keep every definition parse-time-safe and guarded for headless.</action>
  <verify>
    <automated>node -e "const {makeLS,loadEngine}=require('./scripts/tests/ls-stub');const s=loadEngine(makeLS({}),'globalThis.__f=AW.prefs.get(\"prayerTimes\",null);globalThis.__m=AW.prefs.get(\"skyMode\",null);');const t=loadEngine(makeLS({}),'var T={fajr:\"05:00\",dhuhr:\"13:00\",asr:\"16:30\",maghrib:\"19:30\",isha:\"21:00\"};var H=function(h){return {getHours:function(){return h},getMinutes:function(){return 0}}};globalThis.__dawn=AW.skyTemp(H(6),T,\"manual\");globalThis.__day=AW.skyTemp(H(14),T,\"manual\");globalThis.__dusk=AW.skyTemp(H(20),T,\"manual\");globalThis.__night=AW.skyTemp(H(22),T,\"manual\");globalThis.__lt=AW.skyTemp(H(2),T,\"manual\");globalThis.__off=AW.skyTemp(H(2),T,\"off\");');const ok=(s.__f&&s.__f.fajr===\"05:00\")&&s.__m===\"manual\"&&t.__dawn===\"dawn\"&&t.__day===\"day\"&&t.__dusk===\"dusk\"&&t.__night===\"night\"&&t.__lt===\"lastthird\"&&t.__off===\"day\";if(!ok){console.error({f:s.__f,m:s.__m,dawn:t.__dawn,day:t.__day,dusk:t.__dusk,night:t.__night,lt:t.__lt,off:t.__off});process.exit(1);}console.log('SKY_TEMP_OK');" && ! grep -qE 'geolocation|getCurrentPosition' shared/awba-engine.js && node --test scripts/tests/*.test.js 2>&1 | grep -qE 'fail 0'</automated>
  </verify>
  <acceptance_criteria>
    - The temperature function returns dawn/day/dusk/night/lastthird for the five fixed-now fixtures and day for skyMode:'off'; it is pure and references no geolocation/network API.
    - defaultPrefs() exposes prayerTimes (the DEFAULT schedule) + skyMode: 'manual'; no awba_prefs schema bump.
    - node --test scripts/tests/*.test.js → fail 0 (sky.test.js is added in Task 2).
  </acceptance_criteria>
  <done>The pure prayer-clock temperature function + manual prayerTimes/skyMode defaults + boot-time dataset.sky wiring + the --dawn progress degree exist, local-time-only, no geolocation, no schema bump; the suite stays green.</done>
</task>

<task type="auto">
  <name>Task 2: --dawn glow + .sky-breathe reduced-motion gating (CSS) + sky.test.js</name>
  <files>shared/awba-engine.css, scripts/tests/sky.test.js</files>
  <read_first>
    - shared/awba-engine.css §3.2 tints (plan 06) + @layer motion dual reduced-motion block (plan 09) — the additive insertion points.
    - 03-UI-SPEC-ATHAR.md §7.3 (--dawn second apricot horizon), §7.4 (static tint stays / only breathe gated), §7.5 acceptance 4; scripts/tests/state-helpers.test.js (node:test shape).
  </read_first>
  <action>In shared/awba-engine.css add the --dawn second apricot horizon glow (a low-alpha apricot gradient whose opacity is driven by var(--dawn, 0), layered under the Ring on the home shell, capped so it never competes with the prayer-clock tint or scripture) and a .sky-breathe ambient (the slow breathe opacity pulse from plan 09's @keyframes breathe, var(--dur-amb) var(--ease) infinite alternate). Then gate ONLY .sky-breathe off under BOTH reduced-motion triggers (@media (prefers-reduced-motion: reduce) and [data-motion="reduce"]) via animation: none — the static tint itself is information and must remain (§7.4). Do NOT re-declare the [data-sky] tints (plan 06 owns them) and do NOT re-declare the layer-order line. Then create scripts/tests/sky.test.js (node:test + node:assert/strict + {makeLS,loadEngine}), asserting via the loadEngine probe: (1) the five fixed-now→temperature fixtures (dawn/day/dusk/night/lastthird); (2) a shifted prayerTimes moves a boundary (same now, different result); (3) skyMode:'off' ⇒ day; (4) determinism — the same (now,times,mode) yields the same result and no geolocation symbol is referenced. Run only via the glob.</action>
  <verify>
    <automated>grep -q 'sky-breathe' shared/awba-engine.css && grep -q -- '--dawn' shared/awba-engine.css && grep -qE '@media \(prefers-reduced-motion: reduce\)' shared/awba-engine.css && grep -q '\[data-motion="reduce"\]' shared/awba-engine.css && test "$(grep -c '@layer tokens, base, components, screens, motion;' shared/awba-engine.css)" -eq 1 && OUT=$(node --test scripts/tests/*.test.js 2>&1); echo "$OUT" | tail -4; echo "$OUT" | grep -qE 'fail 0' && node --test scripts/tests/sky.test.js 2>&1 | grep -qE 'fail 0'</automated>
  </verify>
  <acceptance_criteria>
    - .sky-breathe + --dawn present; only .sky-breathe is gated off under both reduced-motion triggers; the static tint is untouched; layer-order line still exactly once.
    - scripts/tests/sky.test.js exists under scripts/tests/ and is picked up by the glob; covers the five temperatures, a boundary shift, skyMode:off⇒day, and determinism/no-geolocation (≥4 tests).
    - node --test scripts/tests/*.test.js → fail 0, total raised by the sky tests.
  </acceptance_criteria>
  <done>The --dawn progress glow + the reduced-motion-gated .sky-breathe are in place (static tint preserved), and sky.test.js proves the five-temperature mapping, boundary shift, skyMode:off, and determinism; the full glob suite is green with fail 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| awba_prefs.prayerTimes / skyMode → temperature + dataset.sky | Locally-stored manual times drive a CSS attribute; device-local, no network, no geolocation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-10 | Information disclosure (location) | prayer-clock temperature | mitigate | Manual times only — NO geolocation, NO network call ever fires (grep gate on geolocation symbols); the sky reveals nothing about the learner's location. |
| T-03-SC | Tampering | npm installs | accept | No package installs; hand-authored pure function; nothing fetched. No [ASSUMED]/[SUS] package to gate. |
</threat_model>

<verification>
- Five fixed-now fixtures → dawn/day/dusk/night/lastthird; skyMode:off ⇒ day; prayerTimes shift moves a boundary; pure + no geolocation symbol.
- defaultPrefs exposes prayerTimes + skyMode; no awba_prefs schema bump; boot sets dataset.sky.
- --dawn glow + .sky-breathe present; only .sky-breathe gated under both reduced-motion triggers (static tint stays); layer-order line once.
- node --test scripts/tests/*.test.js → fail 0, total raised by sky tests.
</verification>

<success_criteria>
- MOT-01: the Sky breathe ambient on the one easing family; the prayer-clock temperature painting §3.2's tint over the Orbit ground.
- MOT-04: the static tint is information that survives reduced motion; only the breathe pulse is gated off, by both triggers.
- The second flagship (prayer-clock sky, manual-times floor, no geolocation) lands in Phase 3 before the lesson port, ready for the preview (11) and the human gate (12).
</success_criteria>

<output>
Create `.planning/phases/03-components-icon-kit-motion-language/03-10-SUMMARY.md` when done.
</output>
