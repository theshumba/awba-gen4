---
phase: 03-components-icon-kit-motion-language
plan: 01
type: execute
wave: 1
executor_hint: opus-visual
depends_on: []
files_modified: [shared/awba-engine.css]
autonomous: true
requirements: [MOT-01, MOT-03, MOT-04]
must_haves:
  truths:
    - "Per D-40, every tappable surface class (.btn, .opt, .tf, .tile, .tab, .hstat) shares ONE token-defined gummy press (shadow-collapse + translateY on :active), with inline chips (.cite/.term) sharing the same --dur-2/--ease-press timing at translateY(1px)"
    - "Per D-39, the @layer components and @layer motion blocks consume ONLY Phase-1 motion tokens (--dur-*/--ease-*) — zero new cubic-bezier/linear() easing literals appear from the @layer components block onward"
    - "Per D-42, ONE reduced-motion quieting is defined once with TWO triggers sharing rule bodies: @media (prefers-reduced-motion: reduce) AND [data-motion=\"reduce\"] both collapse --dur-1..6 to 1ms AND set animation:none on the enumerated ambient loops (.companion, .breathing-ring, [data-ambient])"
    - "The four Phase-3 tokens (--press-rest, --press-active, --scrim, --overlay-hero) are added to @layer tokens :root; the immutable layer-order statement is never redeclared"
  artifacts:
    - path: "shared/awba-engine.css"
      provides: "@layer components chrome (cite/term/sheet/scrim/pills/btn/opt/tf/tile/tab/hstat/combo/perfect/cf) + gummy press; @layer motion keyframes + dual-trigger reduced-motion; 4 new tokens"
      contains: "prefers-reduced-motion: reduce"
  key_links:
    - from: "@layer motion reduced-motion block"
      to: "--dur-1..6 tokens + ambient loop classes"
      via: "highest-priority layer overrides tokens defaults; animation:none on loops"
      pattern: "animation: *none"
    - from: ".btn,.opt,.tf,.tile,.tab,.hstat gummy press"
      to: "--press-rest/--press-active/--accent-deep/--ease-press tokens"
      via: "shared :active rule, no literal px/easing"
      pattern: "--press-rest"
---

<objective>
Fill the two empty CSS layers that the whole component system paints from: `@layer components` (static chrome for every shared surface — citation/term chips, the bottom sheet + its citation/gloss content classes, the pill set, buttons/options/tiles/tabs/HUD-stats, the combo chip, the PERFECT overlay, confetti) and `@layer motion` (the five motion-vocabulary keyframes, the ambient loops, and the ONE dual-trigger reduced-motion block). Add the four new depth/overlay tokens so the press rule stays literal-free. This is CSS-only: no JS, provable by grep gates + the 26 tests staying green.

Purpose: MOT-01/MOT-03 (one motion vocabulary + gummy press on the full inventory) and the CSS half of MOT-04 (global reduced-motion). Everything the Phase-3 JS (KIT/COMPONENTS, plans 02-03) and preview (plan 04) render leans on these class names and tokens.
Output: `shared/awba-engine.css` grows from 307 lines to a filled `@layer components` + `@layer motion`; the layer-order line 16 is untouched.

Load the `frontend-design` skill (emil-design-eng craft bar) during execution — this is FLAGSHIP visual CSS: the press must feel gummy-not-mushy and the sheet must read premium. Elevate Gen-3's warmth (cream, soft large radii, indigo-tinted shadows), never redirect into generic minimal.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-components-icon-kit-motion-language/03-UI-SPEC.md
@.planning/phases/03-components-icon-kit-motion-language/03-PATTERNS.md
@.planning/phases/03-components-icon-kit-motion-language/03-RESEARCH.md

<interfaces>
<!-- Live Phase-1 tokens this plan consumes (measured, shared/awba-engine.css). Executor uses these directly. -->
Motion tokens (:root, lines 138-150): --dur-1 80ms · --dur-2 120ms (press) · --dur-3 200ms · --dur-4 280ms (sheet) · --dur-5 350ms (pop-in) · --dur-6 600ms · --dur-amb 4800ms; --ease-standard, --ease-out, --ease-in, --ease-press, --ease-spring (verbatim linear() — never hand-edit), --ease-gentle.
Layer stubs to REPLACE (never redeclare the name-list at line 16):
  line 305: @layer components { /* reserved ... */ }
  line 307: @layer motion    { /* reserved ... */ }
Colour/type tokens already available: --accent, --accent-deep, --accent-on, --accent-ink, --accent-soft, --accent-line; --paper, --bg, --tint, --ink, --ink2, --ink3, --line, --line2; --green, --green-bg, --green-ink, --green-line; --amber, --amber-bg, --amber-ink, --amber-line; --gold, --gold2, --gold-deep, --flame, --flame2, --accent-bright; --font-disp, --font-ar, --font-quran; --fs-* set; --r-xs/--r-md/--r-xl/--r-pill; --sh-1/--sh-3; --sp-* set (--sp-1 4px, --sp-2 2px, --sp-2s 8px, --sp-3, --sp-4, --sp-6, --sp-8); [data-register="night"] focus hook (lines 275-278).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the 4 Phase-3 tokens + fill @layer components chrome and gummy press</name>
  <files>shared/awba-engine.css</files>
  <read_first>
    - shared/awba-engine.css :18-163 (tokens block, esp. motion tokens 138-150 for the format-to-mirror) and :191-303 (@layer base — the only prior rule-authoring example in this file: every value is var(--token), section comments precede each group, [data-register="night"] override follows its base rule); line 305 is the @layer components stub to replace.
    - 03-UI-SPEC.md "Phase-3 Token Additions" (the 4 tokens + values), "Component Anatomy & States" (.cite/.term/sheet/citation-sheet/gloss-sheet field contract), "Press Physics" (shared inventory + inline-chip reconciliation + --press-rest/--press-active/calc drop), "Color" (component colour contract table incl. combo=gold, pending pill neutral, grade pill green), "Typography" (Phase-3 applications table incl. .ayah face-split, PERFECT=Poppins 700), "Spacing Scale" (Phase-3 applications).
    - 03-PATTERNS.md "@layer components" assignment (Gen-3 gummy press _MVP-BUILD/awba-engine.css:182-191; .cite:65-70; .tile:active:160-161; .opt/.tf:142-152; sheet chrome:268-287; combo/perfect:41-46,257-263 — behaviour to re-tokenize) and "@layer tokens (4 additions)" assignment.
    - 03-RESEARCH.md Pattern 6 (Gen-3→Gen-4 token translation table: --blue2→--accent-deep, --naskh→--font-ar or .ayah→--font-quran, literal px→--fs-* tokens) and Pattern 8 (gummy press signature).
  </read_first>
  <action>Inside the existing `:root` block, near the motion tokens (lines 138-150), add the four tokens with the same comment-above-group/one-per-line/trailing-purpose-comment format: `--press-rest: 5px`, `--press-active: 2px`, `--scrim: rgba(30,40,80,.42)`, `--overlay-hero: rgba(37,54,206,.94)` (values verbatim from the UI-SPEC Token Additions table). Then replace the single-line `@layer components { ... }` stub (line 305) with a full multi-line block authoring the static chrome (colours/borders/radii/padding/rest box-shadow/type) for: `.cite` + `.term` (per Component Anatomy — chip padding `2px var(--sp-2s) 2px var(--sp-1)`, radius `--r-xs`, 0.9em Inter 600, leading-glyph gap `--sp-1`, accent-ink/accent-soft/accent-line rest + hover fill --accent/--accent-on; .term dotted --accent underline); the sheet system `.scrim` (fixed inset:0, bg `--scrim`, `.open`→opacity 1) / `.sheet` (fixed, left/right:0, bottom:0, margin-inline:auto, max-width:480px so it centres like the shell column, bg --paper, radius `var(--r-xl) var(--r-xl) 0 0`, padding `var(--sp-6) var(--sp-6) var(--sp-8)`, --sh-3, max-height:82%, overflow-y:auto) / `.grip` / `.sheet-x` (44×44 hit area) / `html.sheet-lock{overflow:hidden}`; citation-sheet classes `.r-src` (--fs-kicker Poppins 600 uppercase, --accent-ink, centered) / `.r-ar` (general Amiri --font-ar --fs-ar-body, rtl) with the `.r-ar.ayah` face-split variant (--font-quran --fs-ayah lh 1.9) / `.r-mean` (--fs-body --ink) / `.r-ref` (--fs-kicker italic --ink3) / `.r-pill` (NEUTRAL: --ink2 text, --line2 border, transparent bg — never amber/red/--danger) / `.r-pill.grade` (--green-ink/--green-line); gloss classes `.g-ar` (--font-ar --fs-ar-display centered) / `.g-tl` / `.g-wd` (Poppins 600 --fs-h2) / `.g-df` / `.g-cx`; the shared gummy-press inventory as ONE selector group `.btn, .opt, .tf, .tile, .tab, .hstat` with rest `box-shadow: 0 var(--press-rest) 0 var(--accent-deep)` and `:active { transform: translateY(calc(var(--press-rest) - var(--press-active))); box-shadow: 0 var(--press-active) 0 var(--accent-deep); }` (the 3px drop derives from the two tokens — no third literal), plus the inline-chip reconciliation `.cite:active, .term:active { transform: translateY(1px); }`; the `.btn` state contract (rest --accent/--accent-on fill, hover `filter:brightness(1.03)`, disabled opacity/no-shadow); the `.combo` chip in the GOLD register (`background: color-mix(in srgb, var(--gold2) 22%, var(--paper))`, border --gold, text/spark-glyph --gold-deep, radius --r-pill, padding `var(--sp-2) var(--sp-3)`, Poppins 700 --fs-kicker, transform-origin right center) — NOT amber (mercy-coherence, per Color decision); the `.perfect` overlay (inset:0, field `--overlay-hero`, heading `.perfect h1` at --fs-display Poppins **700** with gold-gradient text-clip `--gold2`→`--flame2` via background-clip:text + -webkit-text-fill-color:transparent, body rgba(255,255,255,.9) --fs-ui); the confetti layer `.confetti` (fixed inset:0, pointer-events:none, high z) + `.cf` (≈9×14px, radius 2px) — colour comes from JS, not a hardcoded palette here; and a seeded `.sheet-row:active { transform: translateY(1px); }` (Phase 5 consumes). Put the motion-vocabulary TRANSITION declarations (press/hover/sheet timing referencing var(--dur-*)/var(--ease-*)) either here on the components or in @layer motion (executor discretion per CONTEXT "keyframes/transitions in @layer motion") — but NEVER a raw cubic-bezier/linear() outside the tokens layer. Every colour/spacing/radius/shadow value is a var(--token); zero literal hex/px except where a token genuinely does not exist. Do NOT touch line 16 (the @layer name-list) or @layer base.</action>
  <verify>
    <automated>node --test scripts/tests/*.test.js 2>&1 | grep -qE 'pass 26' && for t in press-rest press-active scrim overlay-hero; do grep -q -- "--$t" shared/awba-engine.css || exit 1; done && for c in btn opt tf tile tab hstat sheet-row cite sheet scrim r-pill combo perfect; do grep -q "\.$c" shared/awba-engine.css || exit 1; done && grep -q 'ayah' shared/awba-engine.css && grep -q '@layer tokens, base, components, screens, motion;' shared/awba-engine.css && echo COMPONENTS_OK</automated>
  </verify>
  <acceptance_criteria>
    - `node --test scripts/tests/*.test.js` reports `pass 26 / fail 0` (regression baseline intact).
    - All four tokens present in the file: `--press-rest`, `--press-active`, `--scrim`, `--overlay-hero`.
    - Every inventory class exists: `.btn .opt .tf .tile .tab .hstat .sheet-row` and chrome classes `.cite .sheet .scrim .r-pill .combo .perfect` (sheet rows are named in MOT-03's inventory — the `.sheet-row:active` press must exist, not just be seeded in prose).
    - `grep -q 'ayah' shared/awba-engine.css` passes (the `.r-ar.ayah` Quran face-split variant exists).
    - `grep -q '@layer tokens, base, components, screens, motion;' shared/awba-engine.css` passes (layer-order line untouched).
    - `! grep -q 'color:.*#\|background:.*#[0-9A-Fa-f]\{3,6\}' ` is NOT required (art literals / rgba tokens allowed) — instead the easing-literal gate in Task 2 covers the "no raw curve outside tokens" law across both layers.
  </acceptance_criteria>
  <done>The 4 tokens are in :root; @layer components is a full block styling the entire shared surface inventory + gummy press with the gold combo, 700-weight PERFECT, neutral pending pill and green grade pill; the .ayah face-split variant exists; 26 tests green; the layer-order line is byte-identical.</done>
</task>

<task type="auto">
  <name>Task 2: Fill @layer motion — vocabulary keyframes + ambient loops + dual-trigger reduced-motion</name>
  <files>shared/awba-engine.css</files>
  <read_first>
    - shared/awba-engine.css line 307 (the @layer motion stub to replace) + the file header :8-16 (motion is the LAST-declared, highest-priority layer — the a11y-safety property this block relies on) + motion tokens :138-150.
    - 03-RESEARCH.md Pattern 7 (the verified dual-trigger reduced-motion block reproduced verbatim + the infinite-loop caveat: collapsing --dur-amb does NOT stop a loop) and "Reduced-motion — the verified technique and its limit".
    - 03-PATTERNS.md "@layer motion" assignment (Gen-3 @keyframes bob/glow/fall _MVP-BUILD/awba-engine.css:193-202,265-266 to re-token; and the dual-trigger block reproduced there — write it exactly once, both triggers sharing bodies).
    - 03-UI-SPEC.md "Motion Vocabulary" (the signature→token table + keyframe list: fall/bob/breathe/glow/popIn; ambient classes .companion/.breathing-ring/[data-ambient]) and "Reduced Motion" (the per-component contract + why loops need animation:none not duration-collapse).
  </read_first>
  <action>Replace the `@layer motion { ... }` stub (line 307) with a full multi-line block. Author the five vocabulary `@keyframes`: `fall` (confetti — port Gen-3 `to{transform:translateY(840px) rotate(720deg);opacity:.3}`, transform-only/compositor-friendly), `bob` (companion vertical bob), `breathe` (ring pulse), `glow` (drop-shadow loop, re-tokenized off Gen-3's 3s/2.8s to `--dur-amb`), `popIn` (overshoot entrance for node pop-in / combo / icon reveal). Assign the ambient loops: `.companion { animation: bob var(--dur-amb) var(--ease-gentle) infinite }`, `.companion svg { animation: glow var(--dur-amb) var(--ease-gentle) infinite }`, `.breathing-ring { animation: breathe var(--dur-amb) var(--ease-gentle) infinite }`. If you chose to house the motion-vocabulary transitions here (press/hover/sheet-in/out timing), each references only var(--dur-*)/var(--ease-*) tokens (sheet IN = --dur-4 --ease-gentle; OUT = --dur-4 --ease-in; press = --dur-2 --ease-press; pop-in = --dur-5 --ease-spring). Then, at the END of the layer, write the ONE dual-trigger reduced-motion block EXACTLY as specified in RESEARCH Pattern 7 / PATTERNS: `@media (prefers-reduced-motion: reduce)` collapses `--dur-1..6` to `1ms` on `:root` AND sets `animation: none !important` on `.companion, .breathing-ring, [data-ambient]`; then the identical rule bodies under `[data-motion="reduce"]` (the Phase-2 boot-stamp) for the same duration-collapse and the same `animation:none` on those loops. Do NOT collapse `--dur-amb` (the loop is stopped outright by animation:none — collapsing it would spin the loop at ~1ms, a seizure risk). Both triggers are REQUIRED (the boot-stamp fires only on the in-app override, never the OS setting — Pitfall 2). No raw cubic-bezier/linear() anywhere from here (only var() token references). Do not touch @layer base/components or line 16.</action>
  <verify>
    <automated>node --test scripts/tests/*.test.js 2>&1 | grep -qE 'pass 26' && grep -q 'prefers-reduced-motion: reduce' shared/awba-engine.css && grep -q '\[data-motion="reduce"\]' shared/awba-engine.css && grep -qE 'animation: *none' shared/awba-engine.css && for k in fall bob breathe glow popIn; do grep -q "@keyframes $k" shared/awba-engine.css || exit 1; done && ! sed -n '/@layer components {/,$ p' shared/awba-engine.css | grep -qE 'cubic-bezier|linear\(' && echo MOTION_OK</automated>
  </verify>
  <acceptance_criteria>
    - `node --test scripts/tests/*.test.js` → `pass 26 / fail 0`.
    - BOTH triggers present: `grep -q 'prefers-reduced-motion: reduce'` AND `grep -q '\[data-motion="reduce"\]'`.
    - Loops stopped: `grep -qE 'animation: *none' shared/awba-engine.css` passes (animation:none on the enumerated loops).
    - All five keyframes exist: `@keyframes fall|bob|breathe|glow|popIn`.
    - NO raw easing literal from @layer components onward: `! sed -n '/@layer components {/,$ p' shared/awba-engine.css | grep -qE 'cubic-bezier|linear\('` passes (linear-gradient() does not match `linear\(`; the --ease-* definitions live in the tokens layer above line 305 and are excluded by the sed range).
    - `--dur-amb` is NOT set to 1ms anywhere (loop is stopped, not sped): `! grep -qE '\-\-dur-amb: *1ms' shared/awba-engine.css`.
  </acceptance_criteria>
  <done>@layer motion holds the five vocabulary keyframes, the ambient loop assignments, and ONE dual-trigger reduced-motion block whose two triggers share bodies (collapse --dur-1..6 + animation:none on loops); zero raw easing literal escapes the tokens layer; 26 tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none | CSS-only plan — no input crosses any boundary, no script executes, no network/storage surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering (a11y integrity) | reduced-motion of infinite ambient loops | mitigate | `animation: none !important` on `.companion/.breathing-ring/[data-ambient]` under BOTH triggers — collapsing `--dur-amb` to 1ms would spin loops at ~1ms period (a seizure risk); loops are stopped outright, durations collapsed only for finite transitions. |
| T-03-SC | Tampering | npm/svgo installs | accept | No package installs this phase — `svgo` skipped per the D-31 amendment (hand-authored KIT in plan 02); zero runtime deps, zero CDN (CLAUDE.md). No `[ASSUMED]`/`[SUS]` packages to gate; the RESEARCH Package Legitimacy Audit lists `svgo` as optional-and-dropped. |
</threat_model>

<verification>
- `node --test scripts/tests/*.test.js` → `pass 26 / fail 0` (both tasks).
- 4 tokens present; full inventory + chrome classes present; `.r-ar.ayah` face-split exists.
- Both reduced-motion triggers present; `animation:none` on loops; five keyframes exist.
- `! sed -n '/@layer components {/,$ p' shared/awba-engine.css | grep -qE 'cubic-bezier|linear\('` — no raw easing literal outside tokens.
- `grep -q '@layer tokens, base, components, screens, motion;'` — layer-order line immutable.
</verification>

<success_criteria>
- MOT-03: one gummy press (shadow-collapse + translateY) on the full tappable inventory, token-defined depths, inline chips reconciled.
- MOT-01: one motion vocabulary in @layer motion consuming only Phase-1 tokens; five keyframes + ambient loops.
- MOT-04 (CSS half): one dual-trigger reduced-motion block; loops stopped, transitions shortened; both OS-setting and boot-stamp triggers covered.
</success_criteria>

<output>
Create `.planning/phases/03-components-icon-kit-motion-language/03-01-SUMMARY.md` when done.
</output>
