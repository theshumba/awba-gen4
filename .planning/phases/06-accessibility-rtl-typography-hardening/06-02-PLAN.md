---
phase: 06-accessibility-rtl-typography-hardening
plan: 02
type: execute
wave: 2
executor_hint: opus
depends_on: ["06-01"]
files_modified:
  - scripts/check-glyph-coverage.py
  - scripts/tests/rtl-audit.mjs
  - scripts/fixtures/typo-stress.html
autonomous: true
requirements: [ACC-03]
must_haves:
  truths:
    - "Per D-66 / FND-03: scripts/check-glyph-coverage.py is REWRITTEN and exits 0 — it no longer references the deleted poppins-600.woff2 (currently crashes exit 1), it covers the 14-face Athar roster (readex-pro 300/400/500/600/700, amiri 400/700, amiri-quran-400, marcellus-400, aref-ruqaa 400/700, rakkas-400, courier-prime-400, inter-400 as the bracket fallback), it HARVESTS required codepoints from the real app strings (learn.html + lessons/ + reviews/ + engine emitters) rather than a hand list, and it proves every harvested codepoint is covered by its role-stack via fontTools cmap inspection — including Ḥ U+1E24 and Ḏ U+1E0E (uppercase, no current list covers them) and ā U+0101 in courier-prime-400 (the Ibrahim line, untested today)"
    - "Per D-66: the settled fallback law is encoded — a workhorse codepoint passes if readex ∪ inter covers it (Readex legitimately lacks ˹ U+02F9 / ˺ U+02FA — Inter catches them), and Aref Ruqaa / Rakkas are EXEMPT from the rare Quranic marks U+0657–065F (heavy tashkeel is Amiri-only by law 3) so a missing rare mark in a display face is never a failure"
    - "Per D-66 / CNT-04: scripts/tests/rtl-audit.mjs is a PERMANENT exit-code gate (joins the full-suite command) that DOM-walks all 20 rendered pages — every element whose direct text matches the Arabic ranges has closest('[lang=ar]'); every .ayah/.scripture container computes direction:rtl + unicode-bidi:isolate; every .ayah container computes font-family starting 'Amiri Quran'; it renders scripts/fixtures/typo-stress.html at --window-size=320,800 AND desktop and asserts documentElement.scrollWidth <= innerWidth (no horizontal overflow); it exits 0 over the shipped surface (the 5 emitter paths already carry lang/dir/isolate per census css:378–414 — this PINS them)"
    - "Per D-66 / Phase-2 fixture law: scripts/fixtures/typo-stress.html is NEUTRAL copy only — NEVER scripture (no basmala/ayah/hadith); it exercises every declared face with the full diacritic set (ʿ ʾ ā ī ū ḥ ṣ ṭ ẓ ḍ ġ + the uppercase forms Ḥ U+1E24, Ḏ U+1E0E + ā in Courier Prime), the Khattab corner brackets ˹ ˺ (workhorse + the Inter fallback), ﷺ U+FDFA and the ornate ﴾ U+FD3E ﴿ U+FD3F (Amiri faces only), and mixed Arabic/Latin/digit + RTL-isolated-then-Latin bidi lines; it lives in scripts/fixtures/ (out of any Phase-7 precache) and carries NO gated literal"
    - "Per D-67: no product source is touched (learn.html/shared unchanged); the node:test suite stays green (fail 0, tests >= 114); grep -c localStorage shared/awba-engine.js stays 13; @layer order line count stays 1; glyphCount stays 13; render-smoke's 21 checks stay green"
  artifacts:
    - path: "scripts/check-glyph-coverage.py"
      provides: "the rewritten FND-03 glyph gate — 14-face roster, real-string codepoint harvest, cmap coverage proof, the encoded fallback law; exits 0"
      contains: "1E24"
    - path: "scripts/tests/rtl-audit.mjs"
      provides: "the permanent CNT-04 RTL/bidi census over 20 pages + the fixture overflow/mixed-bidi checks; exit-code gate"
      contains: "unicode-bidi"
    - path: "scripts/fixtures/typo-stress.html"
      provides: "the neutral-copy typography stress specimen — every face + the full diacritic/bracket/mark set + mixed-bidi lines"
      contains: "typo-stress"
  key_links:
    - from: "the real app strings (learn.html + lessons + reviews + engine emitters)"
      to: "check-glyph-coverage.py REQUIRED codepoint sets"
      via: "harvest-not-handlist — every non-ASCII codepoint the app renders is proven covered by its role-stack cmap"
      pattern: "1E24"
    - from: "scripts/tests/rtl-audit.mjs"
      to: "the full-suite command + the Phase-6 gate (06-07) + Phase-7 gate"
      via: "a permanent exit-code gate that pins lang/dir/isolate + no-overflow forever"
      pattern: "rtl-audit"
---

## Phase Goal

**As a** learner reading verbatim Arabic scripture and scholarly transliteration on any screen, **I want to** every diacritic, corner-bracket, honorific and mixed Arabic/Latin line to render correctly and never scramble, **so that** the FND-03 promise Phase 1 proved on the subset fonts is finally proven at full rendered-app scale — the reverence the words deserve holds edge to edge.

*(Derived from ROADMAP Phase-6 SC4 "a final typography/RTL stress-test verifying FND-03 and CNT-04 at full-app scale." Wave-0 foundation: it builds the permanent typography/RTL gates and the stress fixture, changes no product surface, and fixes the currently-broken glyph gate.)*

<objective>
Rebuild the typography/RTL proof from its broken state into a permanent, full-app gate: rewrite the crashing `check-glyph-coverage.py` (it still references the deleted Poppins file) for the Athar 14-face roster with codepoints HARVESTED from the real app strings (so the stale-list misses — Ḥ U+1E24, Ḏ U+1E0E, Courier ā — become covered), build `rtl-audit.mjs` to DOM-walk all 20 pages proving `lang/dir/isolate` + the Quran face + no horizontal overflow, and compose the neutral-copy `typo-stress.html` specimen that exercises every face and mark for the human tofu walk. All three pass over the shipped surface today (the census already carries `lang/dir/isolate`); they exist to prove it and to catch any future regression.

Purpose: close the 06-VALIDATION Wave-0 typography gaps (the glyph-gate rewrite is a prerequisite to any FND-03 claim) and give D-66/CNT-04 permanent teeth.
Output: `scripts/check-glyph-coverage.py` (rewritten), `scripts/tests/rtl-audit.mjs` (new), `scripts/fixtures/typo-stress.html` (new).

Executor routing: **opus + `~/.claude/skills/emil-design-eng/SKILL.md` (read directly, no Skill tool)** — the fixture is a composed typographic specimen (visual judgement: legible per-face rows, the diacritic/bracket set readable at 320px and desktop). The glyph-gate + rtl-audit are mechanical scripts; opus authors them alongside the fixture. Neutral copy ONLY in the fixture (Phase-2 law) — never scripture.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-accessibility-rtl-typography-hardening/06-RESEARCH.md
@.planning/phases/06-accessibility-rtl-typography-hardening/06-UI-SPEC.md
@.planning/phases/06-accessibility-rtl-typography-hardening/06-VALIDATION.md

<interfaces>
<!-- Extracted this session; locate by content — line numbers drift. -->

The broken gate (`scripts/check-glyph-coverage.py`): run today → exit 1 (references `shared/fonts/poppins-600.woff2`, deleted in 03-11; predates the Athar roster). It IS the method to keep: fontTools `TTFont(path)['cmap'].getBestCmap()` per subset `.woff2` + exact-codepoint REQUIRED sets. Rewrite, do not discard the method.

Font roster on disk (`shared/fonts/`, verified): readex-pro-{300,400,500,600,700}.woff2, amiri-{400,700}.woff2, amiri-quran-400.woff2, marcellus-400.woff2, aref-ruqaa-{400,700}.woff2, rakkas-400.woff2, courier-prime-400.woff2, inter-{400,500,600,700}.woff2. python3 3.9.6 + fontTools 4.60.2 present (verified `import fontTools`).

RTL/bidi census already shipped (`shared/awba-engine.css`, grep `unicode-bidi`): `[lang="ar"]:not(.ayah):not(.scripture)` → isolate; `.ayah` → rtl + isolate; `.scripture` → rtl + isolate; `[dir="rtl"]` → isolate; `letter-spacing:0` on every Arabic container. The 5 Arabic-emitting paths (all carry `lang="ar" dir="rtl"`): `AW.sheetRef` `.r-ar`, `AW.sheetTerm` `.g-ar`, `verseHtml` `.ayah`, `duaClose` `.scripture` (cfg-gated), learn.html daily ayah `.ayah`.

The D-66 diacritic set (harvest + fixture): ʿ U+02BF, ʾ U+02BE, ā U+0101, ī U+012B, ū U+016B, ḥ U+1E25, ṣ U+1E63, ṭ U+1E6D, ẓ U+1E93, ḍ U+1E0D, ġ U+0121, plus the uppercase forms the real app uses (Ḥ U+1E24, Ḏ U+1E0E) + ā U+0101 in courier-prime-400; brackets ˹ U+02F9 ˺ U+02FA; ﷺ U+FDFA; ornate ﴾ U+FD3E ﴿ U+FD3F. Fallback law: workhorse codepoint passes if readex ∪ inter covers it; Aref Ruqaa / Rakkas exempt from U+0657–065F.

Harness templates: `scripts/tests/render-smoke.mjs` (findPages() over learn + lessons + reviews, `execFileSync` Chrome per page, `--window-size`, `--dump-dom`, exit-code-first `OK/FAIL`) — the template for `rtl-audit.mjs`. `CHROME = '/Applications/Google Chrome.app/Contents/MacOS/Google Chrome'`. Arabic ranges regex: `[؀-ۿݐ-ݿﭐ-﷿ﹰ-﻿]`.

Machine notes: run node tests via glob; ugrep paren-wrap/`--`-separate; NEVER put scripture in the fixture (Phase-2 law); the fixture uses common-tashkeel dictionary words + the Arabic alphabet, no basmala. `scripts/fixtures/` is out of any Phase-7 precache by location. Hand-edit STATE.md.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: rewrite check-glyph-coverage.py — 14-face roster, real-string harvest, cmap proof (exit 0)</name>
  <files>scripts/check-glyph-coverage.py</files>
  <read_first>
    - 06-RESEARCH.md §RTL/Bidi Verification "The Phase-1 glyph-gate method — FOUND, and it is BROKEN" (the exact rewrite spec: roster, harvest, the stale-list proof Ḥ U+1E24 / Ḏ U+1E0E / Courier ā, the fallback law, the honest tofu limit), §Don't Hand-Roll (why cmap not document.fonts.check).
    - 06-UI-SPEC.md §S5 (the glyph-gate rewrite bullet + the role-stack fallback law).
    - `scripts/check-glyph-coverage.py` (the current crashing script — keep its fontTools cmap method, replace the roster + required sets). `shared/awba-engine.css` (grep `@font-face` / `font-family` to confirm the 14 declared faces + their role assignments).
    - `learn.html`, `lessons/*.html`, `reviews/*.html`, and `shared/awba-engine.js` emitter strings — the harvest source (all non-ASCII codepoints the app renders).
  </read_first>
  <action>Rewrite `scripts/check-glyph-coverage.py`: (1) map the 14 declared `.woff2` faces to role-stacks — workhorse (readex 300–700, with inter-400 as the bracket fallback), scripture (amiri 400/700, amiri-quran-400), display (marcellus-400, aref-ruqaa 400/700, rakkas-400), marginalia (courier-prime-400); (2) HARVEST the REQUIRED codepoint set by scanning every non-ASCII codepoint out of learn.html + lessons/*.html + reviews/*.html + the engine emitter strings, bucketing each by the role of the element/face that renders it (so Ḥ U+1E24, Ḏ U+1E0E and Courier ā are picked up automatically); (3) for each face load its cmap via fontTools `getBestCmap()` and assert every harvested codepoint for that role is covered — applying the fallback law: a workhorse codepoint passes if readex ∪ inter covers it (˹ U+02F9 / ˺ U+02FA fall to Inter legitimately), and Aref Ruqaa / Rakkas are exempt from U+0657–065F (never required of them, law 3); (4) print a per-face coverage report and `exit(0)` on full coverage, non-zero + the missing codepoint list on any gap. Do NOT reference `poppins-600.woff2` or any deleted file. Keep the fontTools method; replace only the roster + required-set derivation. Run it → exit 0. Commit `fix(06-02): rewrite glyph gate for the Athar 14-face roster, harvest real-string codepoints (D-66)`.</action>
  <verify>
    <automated>python3 scripts/check-glyph-coverage.py > /tmp/glyph06.txt 2>&1; test $? -eq 0 && grep -qE '1E24|0x1e24|U\+1E24' scripts/check-glyph-coverage.py && ! grep -qi 'poppins' scripts/check-glyph-coverage.py && node --test scripts/tests/*.test.js 2>&1 | grep -qE 'fail 0' && test "$(grep -c 'localStorage' shared/awba-engine.js)" -eq 13</automated>
  </verify>
  <acceptance_criteria>
    - `python3 scripts/check-glyph-coverage.py` exits 0; it references none of the deleted Poppins files; it covers the 14-face roster and proves Ḥ U+1E24 / Ḏ U+1E0E / Courier ā are covered by their role-stacks.
    - The fallback law (readex ∪ inter for brackets; Aref/Rakkas exempt from rare marks) is encoded; suite still `fail 0`; localStorage count 13.
  </acceptance_criteria>
  <done>The FND-03 glyph gate is real again — every codepoint the app renders is proven present in its subset face, harvested from the actual strings.</done>
</task>

<task type="auto">
  <name>Task 2: typo-stress.html fixture — every face + the full diacritic/bracket/mark + mixed-bidi (neutral copy)</name>
  <files>scripts/fixtures/typo-stress.html</files>
  <read_first>
    - 06-UI-SPEC.md §S5 "Fixture" (the exact composition: every face's specimen row with its role label, the full diacritic set + uppercase forms + Courier ā, the bracket/mark cases, the mixed Arabic/Latin/digit + RTL-isolated-then-Latin bidi lines, rendered at 320px + desktop) and §Typography (the fixture table — Readex row headings, the face-under-test for Arabic samples, NEVER a basmala/ayah/hadith).
    - 06-RESEARCH.md §RTL/Bidi Verification (the diacritic set + fallback law) and "Fixture law" (neutral copy only, common-tashkeel dictionary words + the alphabet).
    - `~/.claude/skills/emil-design-eng/SKILL.md` (read directly) — apply to the specimen layout so each face's row is legible and the marks are inspectable at 320px and desktop.
    - `preview.html` head (the zero-CDN, page-relative font-preload + `@font-face`-via-engine include pattern to mirror) and `shared/awba-engine.css` (the declared face-families to reference by name).
  </read_first>
  <action>Compose `scripts/fixtures/typo-stress.html` as a zero-CDN dev specimen (include the engine stylesheet + page-relative font references exactly like the real pages; place it at `scripts/fixtures/` so it stays out of any Phase-7 precache). One labelled specimen row per declared face (Readex 300/400/500/600/700, Amiri 400/700, Amiri Quran 400, Marcellus 400, Aref Ruqaa 400/700, Rakkas 400, Courier Prime 400, Inter 400 as the `˹ ˺` fallback demonstrator), each rendering: the full transliteration diacritic set (ʿ ʾ ā ī ū ḥ ṣ ṭ ẓ ḍ ġ) plus the uppercase forms the app uses (Ḥ U+1E24, Ḏ U+1E0E) — and put ā U+0101 explicitly in the Courier Prime row (the untested Ibrahim case); a bracket/mark block with ˹ U+02F9 ˺ U+02FA, ﷺ U+FDFA, and the ornate ﴾ U+FD3E ﴿ U+FD3F (in the Amiri faces only); and a bidi block with a line mixing an Arabic run + Latin + digits and a line with an RTL-isolated Arabic span followed by a Latin run. Arabic samples are NEUTRAL common-tashkeel dictionary words + the 28-letter alphabet — NEVER scripture (Phase-2 law); no gated literal anywhere. Emil-tune the layout so each row and every mark is legibly inspectable at 320px and desktop. Verify it opens over `file://` with zero console errors. Commit `feat(06-02): typo-stress fixture — every face + diacritic/bracket/mark + mixed-bidi (neutral copy, D-66)`.</action>
  <verify>
    <automated>ls scripts/fixtures/typo-stress.html >/dev/null && grep -q 'typo-stress\|typography\|specimen' scripts/fixtures/typo-stress.html && python3 -c "import sys; s=open('scripts/fixtures/typo-stress.html',encoding='utf-8').read(); req=['ʿ','ʾ','ā','ī','ū','ḥ','ṣ','ṭ','ẓ','ḍ','ġ','Ḥ','Ḏ','˹','˺','ﷺ','﴾','﴿']; missing=[hex(ord(c)) for c in req if c not in s]; sys.exit(1 if missing else 0)" && ! grep -qiE 'poppins|confetti|lantern-gold|gold-bg|fonts\.googleapis|rgba\(37,54,' scripts/fixtures/typo-stress.html && ! grep -qE 'basmala|bismillah|Allah|˹.*Say' scripts/fixtures/typo-stress.html && "/Applications/Google Chrome.app/Contents/MacOS/Google Chrome" --headless --disable-gpu --dump-dom --virtual-time-budget=3000 "file://$(pwd)/scripts/fixtures/typo-stress.html" > /dev/null 2>/tmp/typo-err.txt; ! grep -qi 'error' /tmp/typo-err.txt</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/fixtures/typo-stress.html` exists, opens over `file://` with zero console errors, and contains every required codepoint (the python check above lists none missing) including the uppercase forms + the ornate marks.
    - Copy is neutral (no scripture); no gated literal; the fixture lives in `scripts/fixtures/`.
  </acceptance_criteria>
  <done>The typography stress specimen exists for the automated overflow check + the human tofu walk — every face and mark on one page.</done>
</task>

<task type="auto">
  <name>Task 3: rtl-audit.mjs — the permanent lang/dir/isolate + no-overflow census over 20 pages + the fixture</name>
  <files>scripts/tests/rtl-audit.mjs</files>
  <read_first>
    - 06-RESEARCH.md §RTL/Bidi Verification (the automated assertions: closest([lang=ar]), computed direction/unicode-bidi, ayah font-family, the mixed-line best-effort limit, the 320px + desktop overflow check) and §Code Examples (render-smoke as the sweep template).
    - 06-UI-SPEC.md §S5 "Audits" (the rtl-audit bullet — pins the 5 emitter paths; the honest visual-order limit stated in the output).
    - `scripts/tests/render-smoke.mjs` (findPages() + the per-page Chrome invocation + exit-code-first `OK/FAIL` shape to copy). `shared/awba-engine.css` (grep `unicode-bidi` — the census the audit reproduces).
  </read_first>
  <action>Write `scripts/tests/rtl-audit.mjs` on the `render-smoke.mjs` template (findPages() over learn + lessons + reviews = 20 pages; add `scripts/fixtures/typo-stress.html` as a 21st render target for the overflow + mixed-bidi checks). For each page, inject a `--dump-dom` driver that: (a) TreeWalks text nodes, and for any whose text matches the Arabic ranges asserts `el.closest('[lang="ar"]')` is non-null; (b) for every `.ayah`/`.scripture` container asserts `getComputedStyle(el).direction === 'rtl'` and `unicodeBidi === 'isolate'`; (c) for every `.ayah` asserts computed `fontFamily` starts with `Amiri Quran`; (d) asserts `documentElement.scrollWidth <= innerWidth` at `--window-size=320,800` and again at a desktop width. For the fixture, additionally assert the mixed-bidi line's container computes `unicode-bidi:isolate` (state in the output that glyph visual-order is best-effort / human-gate — do not over-claim). Emit `RTL OK/FAIL <page> <reason>` lines, exit non-zero on any FAIL (exit-code-first; never pipe to grep for the pass/fail decision). Run it → every page `RTL OK`, exit 0 (the census already passes — this pins it). Commit `feat(06-02): rtl-audit — lang/dir/isolate + Quran-face + no-overflow census, permanent gate (D-66/CNT-04)`.</action>
  <verify>
    <automated>ls scripts/tests/rtl-audit.mjs >/dev/null && node scripts/tests/rtl-audit.mjs > /tmp/rtl06.txt 2>&1; test $? -eq 0 && grep -q 'RTL OK' /tmp/rtl06.txt && ! grep -q 'RTL FAIL' /tmp/rtl06.txt && grep -q 'unicode-bidi\|unicodeBidi' scripts/tests/rtl-audit.mjs && node --test scripts/tests/*.test.js 2>&1 | grep -qE 'fail 0' && node scripts/tests/render-smoke.mjs 2>&1 | grep -q 'SMOKE OK' && ! node scripts/tests/render-smoke.mjs 2>&1 | grep -q 'SMOKE FAIL'</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/tests/rtl-audit.mjs` exits 0 with `RTL OK` for all 20 pages + the fixture and no `RTL FAIL`; it asserts closest([lang=ar]), computed rtl+isolate on scripture, the Quran face on ayah, and no 320px/desktop overflow.
    - The node:test suite stays `fail 0`; render-smoke's checks stay green; no product source touched.
  </acceptance_criteria>
  <done>The CNT-04 RTL/bidi promise is a permanent full-app gate — every Arabic run is proven isolated, RTL, and in the right face, with no overflow.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev tooling → filesystem / headless Chrome | the gates read the repo + font files, render pages/fixture over `file://`, and write only to `/tmp`; no network, no product-source writes |
| fixture content → scripture law | the fixture is neutral copy by construction; a grep gate + a scripture-substring gate enforce no sacred text enters it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02a | Tampering | a hand-listed (not harvested) codepoint set that silently misses a real glyph | mitigate | the gate harvests from the actual app strings; the verify pins Ḥ U+1E24 present + the fixture's required-codepoint check; a missing codepoint fails non-zero |
| T-06-02b | Information disclosure | scripture accidentally placed in the dev fixture | mitigate | Phase-2 fixture law: neutral copy only; the verify greps for common scripture substrings + gated literals and fails on a hit |
| T-06-02c | Tampering | glyph gate false-passing via document.fonts.check vacuous-true | mitigate | cmap inspection (fontTools) is authoritative for the closed self-hosted face set; the gate never uses document.fonts.check (06-RESEARCH §Don't Hand-Roll) |
| T-06-02-SC | Tampering | npm/pip/cargo installs | accept | zero packages installed; python3 + fontTools already present (verified); no `[ASSUMED]`/`[SUS]` package to gate |
</threat_model>

<verification>
- `python3 scripts/check-glyph-coverage.py` exit 0 (14-face roster, real-string harvest, Ḥ U+1E24 / Ḏ U+1E0E / Courier ā covered, no Poppins reference).
- `node scripts/tests/rtl-audit.mjs` exit 0 (`RTL OK` all 20 pages + fixture; closest([lang=ar]) + rtl+isolate + Quran face + no overflow).
- `scripts/fixtures/typo-stress.html` renders `file://` clean, contains every required codepoint, neutral copy, no gated literal.
- node:test suite `fail 0`, `tests >= 114`; render-smoke's 21 checks green; localStorage 13; @layer order line 1; glyphCount 13.
</verification>

<success_criteria>
- The typography/RTL proof (FND-03 + CNT-04 at full-app scale) is a set of permanent, passing gates + a human-walkable fixture — the currently-broken glyph gate is real again.
- No product surface changed; the gates catch any future regression (Phase-7 re-runs them).
</success_criteria>

<output>
Create `.planning/phases/06-accessibility-rtl-typography-hardening/06-02-SUMMARY.md` when done.
</output>
