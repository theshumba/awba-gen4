---
phase: 06-accessibility-rtl-typography-hardening
plan: 07
type: execute
wave: 7                     # wave = sequence position (Phase 6 is sequential-on-main, one executor at a time), NOT a parallel batch — see ROADMAP note
executor_hint: sonnet
depends_on: ["06-01", "06-02", "06-03", "06-04", "06-05", "06-06"]
files_modified: []
autonomous: false
requirements: [ACC-01, ACC-02, ACC-03]
must_haves:
  truths:
    - "Per D-68: the full automated gate passes as PRECHECKS before the human is asked — node --test scripts/tests/*.test.js (fail 0, tests >= 114 baseline never shrunk, and EVERY a11y probe assertion now ACTIVE, none left { todo } — the suite summary reports `todo 0` (extracted from the captured output) and no probe file retains a `{ todo` gate); node scripts/validate-content.js lessons/*.html reviews/*.html → exit 0 with exactly the 3 accepted note warnings + --self-test OK; node scripts/port-audit.mjs → BYTES OK all 19 + holds; node scripts/tests/render-smoke.mjs → all 21 checks SMOKE OK"
    - "Per D-68: the three NEW permanent audits pass — node scripts/tests/contrast-audit.mjs exit 0 (every text + UI boundary AA across all 20 pages + the forced-state table); node scripts/tests/rtl-audit.mjs exit 0 (lang/dir/isolate + Quran-face + no-overflow across 20 pages + the fixture); python3 scripts/check-glyph-coverage.py exit 0 (the rewritten 14-face gate, no Poppins reference)"
    - "Per D-67: every standing gate passes — no gated literal in learn.html or shared/ (poppins/confetti/amber/PERFECT/.combo/lantern-gold/gold-bg/rgba(37,54,/--accent/fonts.googleapis and the view-transition property name); no raw localStorage in learn.html; grep -c localStorage shared/awba-engine.js == 13; the @layer order line appears exactly once; glyphCount == 13; zero new hex introduced this phase; @view-transition + the reduced-motion VT kill block still present; no view-transition-name / celebration primitive adjacent to a scripture block"
    - "Per D-68: the human gate is a plain-language ~11-item checklist Melusi walks — a REAL-KEYBOARD walk of a lesson + a timed review + the learn path (Tab/Enter/Escape, visible focus ring on the grain), a VoiceOver walk (verdict/noor/screen-change/timeout announcements read like the app's calm voice), a tofu/diacritic eyeball of scripts/fixtures/typo-stress.html + the real ayah pages at 320px AND desktop, and a mixed Arabic/Latin visual-order read — a NO on ANY item is a blocker that routes back to the owning plan; this checkpoint is BLOCKING and NEVER auto-approved (workflow.auto_advance is ignored)"
    - "Per D-67: the R-8 announcement-only timer decision is surfaced to the owner as the one new gate-walk ledger item (a visible Courier seconds readout remains a deferred owner choice); the owner ledger is otherwise unchanged (Arabic chapter-terms R-6, Ibrahim splice R-7, sound cues, du'a, scholar gate, licensing)"
  artifacts: []
  key_links:
    - from: "the automated prechecks (suite + the 3 new audits + the standing gates)"
      to: "the human plain-language checklist"
      via: "every precheck must pass before Melusi is asked to walk the keyboard/VoiceOver/tofu/bidi flows"
      pattern: "fail 0"
---

## Phase Goal

**As a** blind or keyboard-only learner, **I want to** complete a full lesson, a timed review, and claim a chest using only the keyboard and a screen reader — with the same mercy and calm every sighted learner gets, **so that** the accessibility pass Gen-3 never had makes Awba usable by everyone without changing what it is.

*(This is the phase-closing gate: every automated check green as a precondition, then Melusi walks the real app with a keyboard, VoiceOver, and an eye on the type at 320px/desktop. Verification-only — it modifies no source.)*

<objective>
Close Phase 6 with a two-stage gate that mirrors 04-07/05-06: run the full automated precheck set (the node:test suite with EVERY a11y probe assertion now active + the validator over all 19 + self-test + port-audit + all 21 render-smoke checks + the three NEW permanent audits contrast/rtl/glyph + every standing grep gate) as a hard precondition, then STOP and present Melusi a ~11-item plain-language checklist to walk against the real app — a real-keyboard walk of a lesson, a timed review, and the learn path; a VoiceOver walk of the announcements; a tofu/diacritic eyeball of the stress fixture + real ayah pages at 320px and desktop; and a mixed Arabic/Latin visual-order read. A failing precheck routes back to the owning plan (never patched at the gate); the human checkpoint is BLOCKING and is never auto-approved.

Purpose: the phase-level proof that the whole app is keyboard-operable with a visible focus ring, announces in its own calm voice, passes WCAG AA contrast with non-colour signals, and renders every diacritic/bracket/mixed-bidi line correctly — the "same mercy, same calm" bar (ACC-01/02/03).
Output: a green automated-precheck report, then a blocking human checkpoint.

Executor routing: **sonnet** (mechanical / verification). Run the prechecks exactly; do NOT edit source to make a gate pass — a failing gate routes back to its owning plan (01 probes, 02 typo/rtl/glyph, 03 contrast, 04 primitives, 05 runners, 06 learn.html). The human checkpoint is BLOCKING and must NEVER be auto-approved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-accessibility-rtl-typography-hardening/06-UI-SPEC.md
@.planning/phases/06-accessibility-rtl-typography-hardening/06-VALIDATION.md
@.planning/phases/06-accessibility-rtl-typography-hardening/06-RESEARCH.md

<interfaces>
<!-- The gate reads only; it changes nothing. -->
Full suite: `node --test scripts/tests/*.test.js` (glob only; fail 0, tests >= 114; every a11y probe assertion now ACTIVE — grep the probe files for a residual `todo` should return none of the un-todoed ones).
Content gate: `node scripts/validate-content.js lessons/*.html reviews/*.html` (exit 0, the 3 accepted `note:` warnings — u3-m1 unused ref baqarah-2-163, u3-m3 unused ref imran-3-19, u4-m2 unused term rububiyah — none to be "fixed") + `--self-test` (OK).
Port + smoke: `node scripts/port-audit.mjs` (BYTES OK all 19 + holds), `node scripts/tests/render-smoke.mjs` (all 21 checks SMOKE OK).
The three NEW permanent audits (D-68): `node scripts/tests/contrast-audit.mjs` (exit 0), `node scripts/tests/rtl-audit.mjs` (exit 0), `python3 scripts/check-glyph-coverage.py` (exit 0).
Grep gates: no `fonts.googleapis|confetti|class="perfect"|class="combo"|poppins|lantern-gold|gold-bg` in learn.html/shared; no `amber` / `rgba(37,54,` in learn.html + shared/awba-engine.js + shared/awba-engine.css (04-07 precedent scope, now including the JS) and no `--accent` in shared/awba-engine.css; no raw `localStorage` in learn.html; `grep -c localStorage shared/awba-engine.js` == 13; `@layer tokens, base, components, screens, motion;` count == 1; glyphCount == 13; `@view-transition` present; the reduced-motion `view-transition-group(*)` kill block present; no `view-transition-name` on learn.html.
Manual-only checks (06-VALIDATION "Manual-Only Verifications"): visible focus-ring aesthetics per register (real keyboard); SR announcement timing/voice (VoiceOver); tofu/diacritic rendering at pixel level (eyeball the fixture + real ayah pages 320px/desktop); mixed Arabic/Latin visual order (read the ayah ref + sheet source lines).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: run the full automated precheck set (suite + validator + port-audit + render-smoke + the 3 new audits + grep gates)</name>
  <files></files>
  <read_first>
    - 06-VALIDATION.md (full-suite command, sampling, the manual-only list) + 06-RESEARCH.md §Validation Architecture (the requirement→test map + the sampling rate) + §Common Pitfalls 7 (the gates the audit scripts themselves must not trip).
    - 06-UI-SPEC.md §S6 (the no-touch list — the standing gates to assert) + the Decision Log (R-8/R-9/R-10 the walk references).
  </read_first>
  <action>Run and record pass/fail for the full precheck set as automated preconditions (use `! grep -q` for every absence gate; paren-wrap or `--`-separate any `--`-leading pattern for ugrep; keep every exit-code decision exit-code-first, never piped to grep for the pass/fail). Suite: `node --test scripts/tests/*.test.js` → `fail 0`, `tests >= 114`, and the suite summary reports `todo 0` (extract `todo N` from the CAPTURED suite output and assert it equals 0) — EVERY a11y probe assertion is now ACTIVE, and `! grep -q '{ todo'` holds on all three probe files (a11y-keyboard/dialogs/announce). Content: `node scripts/validate-content.js lessons/*.html reviews/*.html` → exit 0 with exactly the 3 expected `note:` warnings + `--self-test` OK. Port + smoke: `node scripts/port-audit.mjs` → `BYTES OK` all 19 + holds (no `BYTES DRIFT`); `node scripts/tests/render-smoke.mjs` → all 21 checks `SMOKE OK`, no `SMOKE FAIL`. The three NEW audits: `node scripts/tests/contrast-audit.mjs` exit 0 (no `CONTRAST FAIL`), `node scripts/tests/rtl-audit.mjs` exit 0 (no `RTL FAIL`), `python3 scripts/check-glyph-coverage.py` exit 0. Grep gates: no gated literal in learn.html/shared; no `amber` / `rgba(37,54,` in learn.html + shared/awba-engine.js + shared/awba-engine.css (04-07 precedent scope, now including the JS); no raw localStorage in learn.html; `grep -c localStorage shared/awba-engine.js` == 13; `@layer` order line count == 1; glyphCount == 13; `@view-transition` + the reduced-motion VT kill block present; no `view-transition-name` in learn.html. If ANY precheck fails, STOP and report which gate failed and which plan owns it — do NOT edit source to force a pass.</action>
  <verify>
    <automated>TOUT=$(node --test scripts/tests/*.test.js 2>&1); TE=$?; VOUT=$(node scripts/validate-content.js lessons/*.html reviews/*.html 2>&1); VE=$?; STOUT=$(node scripts/validate-content.js --self-test 2>&1); STE=$?; POUT=$(node scripts/port-audit.mjs 2>&1); PE=$?; SMOUT=$(node scripts/tests/render-smoke.mjs 2>&1); SME=$?; COUT=$(node scripts/tests/contrast-audit.mjs 2>&1); CE=$?; RLOUT=$(node scripts/tests/rtl-audit.mjs 2>&1); RLE=$?; GOUT=$(python3 scripts/check-glyph-coverage.py 2>&1); GE=$?; test $TE -eq 0 && echo "$TOUT" | grep -qE 'fail 0' && test "$(echo "$TOUT" | grep -oE 'tests [0-9]+' | head -1 | grep -oE '[0-9]+')" -ge 114 && test "$(echo "$TOUT" | grep -oE 'todo [0-9]+' | head -1 | grep -oE '[0-9]+')" -eq 0 && ! grep -q '{ todo' scripts/tests/a11y-keyboard.test.js && ! grep -q '{ todo' scripts/tests/a11y-dialogs.test.js && ! grep -q '{ todo' scripts/tests/a11y-announce.test.js && test $VE -eq 0 && test "$(echo "$VOUT" | grep -c 'note:')" -eq 3 && test $STE -eq 0 && ! echo "$STOUT" | grep -qi 'FAIL' && test $PE -eq 0 && echo "$POUT" | grep -q 'BYTES OK' && ! echo "$POUT" | grep -qiE 'BYTES DRIFT' && test $SME -eq 0 && echo "$SMOUT" | grep -q 'SMOKE OK learn.html' && ! echo "$SMOUT" | grep -q 'SMOKE FAIL' && test $CE -eq 0 && echo "$COUT" | grep -q 'CONTRAST OK' && ! echo "$COUT" | grep -q 'CONTRAST FAIL' && test $RLE -eq 0 && echo "$RLOUT" | grep -q 'RTL OK' && ! echo "$RLOUT" | grep -q 'RTL FAIL' && test $GE -eq 0 && ! grep -rqiE 'fonts\.googleapis|confetti|class="perfect"|class="combo"|poppins|lantern-gold|gold-bg' learn.html shared/awba-engine.js shared/awba-engine.css && ! grep -qi 'amber' learn.html && ! grep -qiE 'amber' shared/awba-engine.js shared/awba-engine.css && ! grep -qE 'rgba\(37,54,' shared/awba-engine.js shared/awba-engine.css && ! grep -q -- '--accent' shared/awba-engine.css && ! grep -q 'localStorage' learn.html && test "$(grep -c 'localStorage' shared/awba-engine.js)" -eq 13 && test "$(grep -c '@layer tokens, base, components, screens, motion;' shared/awba-engine.css)" -eq 1 && grep -q '@view-transition' shared/awba-engine.css && grep -qE 'view-transition-group\(\*\)' shared/awba-engine.css && ! grep -q 'view-transition-name' learn.html</automated>
  </verify>
  <acceptance_criteria>
    - Suite `fail 0` (tests >= 114, `todo 0`, every a11y probe assertion active — no `{ todo` in any probe file); validator over all 19 exit 0 with exactly 3 accepted warnings + self-test OK; port-audit BYTES OK all 19 + holds; render-smoke all 21 checks SMOKE OK.
    - The three new audits exit 0 (contrast, rtl, glyph); every standing grep gate passes (no gated literal, no `amber`/`rgba(37,54,` in learn.html + shared/awba-engine.js + shared/awba-engine.css, no raw localStorage in learn.html; engine localStorage==13; order line==1; glyphCount==13; @view-transition + reduced-motion kill present; no view-transition-name in learn.html).
    - Any failure is reported with its owning plan, not patched here.
  </acceptance_criteria>
  <done>Every automated precondition of the phase is green (or precisely routed back).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Melusi walks the ~11-item plain-language a11y gate (real keyboard + VoiceOver + type at 320px/desktop)</name>
  <files></files>
  <action>Present the ~11-item plain-language checklist below to Melusi, then STOP and wait. This is a BLOCKING human checkpoint — do NOT auto-approve it (workflow.auto_advance is ignored here) and do NOT edit source at the gate. Resume only on the owner's explicit "approved" (all YES) or a list of NO items; a NO routes back to the owning plan named beside that item.</action>
  <what-built>
    The accessibility pass Gen-3 never had — and every automated precheck (Task 1) is green. The whole app is now operable by keyboard alone with a visible focus ring in each register's colour (gold on the dark path/review, crimson on cream lesson pages); one calm, hidden "live region" speaks each verdict, noor gain, screen change, review timeout and chest claim in the app's own warm voice (never like debug output); the quiz selection is felt without colour (pushed in and thicker, announced "pressed") and dead controls leave the keyboard's path; every path node says its state in its name; the streak card is a real button and the daily verse keeps its reverence with a quiet "Read the citation" control; every popup, sheet and the gift moment trap focus and hand it back where you were; and a rewritten glyph gate + a full RTL/typography stress fixture prove every diacritic, corner-bracket and mixed Arabic/Latin line renders correctly at every width. No feature changed, no colour invented, no mechanics touched.

    NOTE — items on the owner ledger (unchanged, not gate blockers): the R-8 announcement-only review timer (a visible Courier seconds readout remains a deferred owner choice — the 10-second spoken warning + the timeout mercy line are the text cues today); the 15 verified Arabic chapter-terms (R-6), the Ibrahim 14:24 splice (R-7), sound-cue assets (silent), the scholar gate, and Clear Quran licensing.
  </what-built>
  <how-to-verify>
    Use a real keyboard (Tab / Shift-Tab / Enter / Space / Escape), turn on VoiceOver for the announcement items, and open `scripts/fixtures/typo-stress.html` + a real lesson page at BOTH a narrow phone width (~320px) and desktop. Answer YES/NO to each.
    1. Can you complete a whole LESSON with the keyboard alone — Tab to each option, Enter to choose, land on each reward screen — with a clear, visible focus ring you can always see on the grain (never a lost cursor)?
    2. Can you complete a timed REVIEW and claim a CHEST with the keyboard alone — including Escape to close the gift moment — with focus always landing somewhere sensible, never dumped to the top of the page?
    3. On the learn PATH: does Tab move through the nodes in journey order, and does every popup/sheet keep your keyboard inside it until you close it, then hand focus back to where you were?
    4. (VoiceOver) In a lesson: are the verdict ("that's it" / "nothing lost"), the noor gain, and each new screen spoken in the app's calm, warm voice — brief, never like debug output, never doubled?
    5. (VoiceOver) In a review: do you hear a single soft "10 seconds" warning, a merciful timeout line, and the next question announced — and is the 100ms ticking NEVER spoken?
    6. (VoiceOver) Does every path node say its state in its name (e.g. "…, available", "…, complete, 2 of 3 stars", "A gift, ready to open"), and do icon-only controls (mute, HUD numbers, closes) all have clear names?
    7. Selecting a quiz answer: can you tell which one you picked WITHOUT relying on colour — does it look pushed in and thicker, and does a resolved/disabled option drop out of the keyboard path?
    8. Open `typo-stress.html` at 320px and desktop: do ALL the diacritics (ʿ ʾ ā ī ū ḥ ṣ ṭ ẓ ḍ ġ + the uppercase Ḥ Ḏ), the ˹ ˺ brackets, ﷺ and the ornate ﴾ ﴿ render as real glyphs — NO empty boxes (tofu) — in every face?
    9. On a real ayah page (and the daily verse) at 320px and desktop: is the Arabic in its Quran face, right-to-left, never scrambled, and does no line run off the edge of the screen?
    10. Read a MIXED Arabic + Latin line (the ayah reference line, a sheet source line): do the Arabic and Latin runs sit in the correct visual order — never jumbled?
    11. Does the whole app still feel like the same calm, world-class book — nothing about the a11y work made a surface louder, added a colour, or changed a word of scripture?
    A NO on any item is a blocker; note which item and it routes back to the responsible plan (item 1/2/3 keyboard+focus → 06-04 primitives / 06-05 runners / 06-06 learn; item 4/5 lesson+review announcements → 06-05; item 6 node names+icon controls → 06-06; item 7 selection cue → 06-05; item 8 glyph tofu → 06-02 glyph gate/fixture; item 9/10 RTL/bidi → 06-02 rtl-audit / the owning surface; item 11 calm/no-regression → the owning surface).
  </how-to-verify>
  <resume-signal>Type "approved" if all ~11 are YES, or list the item numbers that are NO with a one-line note each.</resume-signal>
  <done>Melusi has walked the ~11 items and either approved the phase or returned specific NO items routed to their owning plans; the checkpoint was never auto-approved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verification-only | this plan reads and asserts; it modifies no source. No new trust surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-07 | Repudiation / process | the gate itself | mitigate | the human checkpoint is blocking and never auto-approved (workflow.auto_advance ignored); a failing precheck routes to the owning plan rather than being patched at the gate |
| T-06-07-SC | Tampering | npm installs | accept | no package installs; verification-only. No `[ASSUMED]`/`[SUS]` package to gate |
</threat_model>

<verification>
- Every automated precheck (suite fail 0 tests>=114 with active a11y probes · validator 19 exit 0 + self-test · port-audit BYTES OK + holds · render-smoke all 21 · contrast-audit exit 0 · rtl-audit exit 0 · glyph-gate exit 0 · every grep gate) passes before the human is asked.
- The ~11-item plain-language checklist (real keyboard + VoiceOver + type at 320px/desktop + mixed-bidi read) is presented; the checkpoint blocks and is never auto-approved; a NO routes to the owning plan.
</verification>

<success_criteria>
- ACC-01, ACC-02, ACC-03 are verified automatically (permanent suite members) and then confirmed by the owner's keyboard + VoiceOver + typography walk of the real app.
- Phase 6 is gate-passed (or precisely routed back): the whole app is keyboard-operable, screen-reader-aware, contrast-safe, and renders Arabic + scholarly typography correctly at every width — the same mercy, same calm.
</success_criteria>

<output>
Create `.planning/phases/06-accessibility-rtl-typography-hardening/06-07-SUMMARY.md` when done.
</output>
