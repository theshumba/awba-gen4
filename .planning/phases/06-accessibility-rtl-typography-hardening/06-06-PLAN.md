---
phase: 06-accessibility-rtl-typography-hardening
plan: 06
type: execute
wave: 6                     # wave = sequence position (Phase 6 is sequential-on-main, one executor at a time), NOT a parallel batch — see ROADMAP note
executor_hint: opus
depends_on: ["06-01", "06-04"]
files_modified:
  - learn.html
  - scripts/tests/a11y-keyboard.test.js    # T1 un-todos node names + streak/ayah conversions → residue 0
  - scripts/tests/a11y-dialogs.test.js     # T2 popup → residue 2, T3 Festival → residue 0
autonomous: true
requirements: [ACC-01, ACC-02]
must_haves:
  truths:
    - "Per D-64: every .onode carries aria-label='{nd.label}, {state phrase}' composed in nodeHtml() from data-kind + data-nstate per the UI-SPEC node state-phrase table (lesson locked→'locked', active→'available', done→'complete, N of 3 stars'; review same; chest locked→'locked', available→'ready to open', done→'opened') — the STATE.md Pending Todo (path-node accessible names) is closed"
    - "Per D-62 / R-9: #streakStrip is converted from a role=button DIV (dead Enter/Space) to a real <button> (block styling, classes kept); the daily ayah card KEEPS its <section> (drops role=button/tabindex) and gains a native inner <button class='oayah-cite'>Read the citation</button> (Courier marginalia on the Orbit ground, gold focus ring) that opens AW.sheetRef — so the sacred .ayah keeps its isolated scripture SR reading (law 3) while the affordance is a native, keyboard-operable, ring-bearing control (no dead keydown)"
    - "Per D-63: the .npop node popup gains aria-modal='true' + aria-labelledby (the .np-label given an id) + tabindex='-1' on the .npop element itself (a plain div dialog ignores .focus() without it) + focus moved onto the dialog on open + AW._trapFocus applied on open and disposed in closePop + focus returned to popNode on close when popNode.isConnected (skipped in the render() path where the node is detached)"
    - "Per D-63 / Pitfall 6: the .ofest Festival gains AW._trapFocus on openFestival (a self-loop on .ofest-close) and focus RETURN on close — closeFestival re-renders the path first, so it re-queries app.querySelector('.onode[data-id=nodeId]') AFTER render() and focuses it (nodeId threaded from openFestival); the shipped role=dialog+aria-modal+name+Escape+focus-on-open + the claim-before-open +25 ordering are preserved untouched; the learn sheet callers (openStreakSheet/openNoorSheet/openSwitcher + the tab coming-soon sheets) pass a natural AW.sheet(html,label) name"
    - "Per D-67: fixes land at the learn.html page seam only; no mechanics/copy/layout redesign (the one new visible control is the small oayah-cite button); the .sheet-row.osw-row coming-soon divs are NOT given tabindex (display-only, LRN-06); learn.html localStorage count stays 0; no gated literal in learn.html (incl. the view-transition property name, and 'One religion, one thread' is immutable — class/attribute-scoped greps only); the suite is fail 0 / tests >= 114 and the a11y-keyboard (state-in-name, native streak strip, native ayah affordance) + a11y-dialogs (popup aria-modal/name/focus/containment/return, Festival containment/return) assertions flip todo→GREEN (RED confirmed first); render-smoke's learn.html + contrast-audit stay green"
  artifacts:
    - path: "learn.html"
      provides: "node state-in-name, streak-strip button, ayah Read-the-citation button, popup dialog contract + trap + return, Festival trap + focus-return, sheet-caller labels"
      contains: "oayah-cite"
  key_links:
    - from: "nodeHtml() data-kind + data-nstate"
      to: "the .onode aria-label state phrase"
      via: "compose the accessible name from the shipped state attrs — no new state source (STATE.md Pending Todo closed)"
      pattern: "aria-label"
    - from: "AW._trapFocus (06-04)"
      to: ".npop and .ofest"
      via: "the ONE shared containment helper applied to the two page-owned overlays (D-63 — one implementation for all three families)"
      pattern: "_trapFocus"
---

## Phase Goal

**As a** blind or keyboard-only learner on Awba's front door, **I want to** know each node's state from its name, operate the streak and daily-verse cards from the keyboard, and never get trapped or dumped out of a popup, sheet, or gift moment, **so that** the whole journey — path, popups, sheets, the chest — is walkable with the same calm every sighted learner gets.

*(Derived from ROADMAP Phase-6 SC1/SC2 for learn.html + the STATE.md Pending Todo path-node names + 05-REVIEW SG-01. This plan wires the 06-04 primitives into the page-authored surfaces and closes the two conversions per R-9.)*

<objective>
Harden the learn front door at its page seams: compose each node's accessible name from its shipped state (closing the STATE.md Pending Todo), convert the streak strip to a real button and give the daily-ayah card a native "Read the citation" affordance without flattening the sacred Arabic (R-9), and complete the dialog contract on the two page-owned overlays — the node popup gains aria-modal + name + focus-move + the shared trap + focus-return, and the Festival gains the trap + a focus-return that survives the path re-render (Pitfall 6). The learn sheet callers pass natural accessible names. Mechanics, copy, and layout are untouched (the one new visible control is the small Courier "Read the citation" button). This flips the remaining a11y-keyboard + a11y-dialogs `{ todo }` assertions to GREEN.

Purpose: ACC-01 keyboard operability + ACC-02 accessible names + D-63 focus management for learn.html — a blind/keyboard learner walks the path, opens popups/sheets, and claims a chest without a dead tap or a focus trap failure.
Output: the node names, the two conversions, the popup + Festival dialog contract, and the sheet-caller labels in `learn.html`.

Executor routing: **opus + `~/.claude/skills/emil-design-eng/SKILL.md` (read directly, no Skill tool)** — the "Read the citation" button is a NEW visible control on the Orbit ground (visual judgement: Courier marginalia, gold focus ring, quiet — not a display face; it must sit under the ayah's reverence budget without competing, law 3/7). The node names + dialog wiring are mechanical; opus does both. NEVER convert the ayah `<section>` wholesale (law 3 — keep the isolated scripture reading); NEVER give the coming-soon `.sheet-row.osw-row` divs a tabindex.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-accessibility-rtl-typography-hardening/06-RESEARCH.md
@.planning/phases/06-accessibility-rtl-typography-hardening/06-UI-SPEC.md
@.planning/phases/06-accessibility-rtl-typography-hardening/06-VALIDATION.md

<interfaces>
<!-- Extracted this session; locate by content — line numbers drift. -->

learn.html node emitter (grep `function nodeHtml`): builds `<button class="onode" ... data-id data-idx data-kind data-nstate aria-expanded="false">` + `.onode-label` + `.onode-meta` (stars via `.onode-stars`). `data-kind` ∈ lesson|review|chest; `data-nstate` ∈ locked|available|active|done. Stars count available from the rendered `.onode-stars` / the state seed. Compose the aria-label here.

Streak strip (grep `streakStrip`): `<div class="ob-streak" id="streakStrip" role="button" tabindex="0" aria-label="...">` with a click handler at the wiring block (grep `streakStrip.addEventListener`). Convert to `<button>`.

Daily ayah card (grep `ayahHost`): `<section class="oayah" id="ayahHost" tabindex="0" role="button" aria-label="Open the full citation for this verse">` with a click handler (grep `ayahHost` in the wiring). It hosts the `.ayah` scripture (lang/dir/isolate). R-9: keep `<section>`, drop role=button/tabindex, add inner `<button class="oayah-cite">`.

Node popup (grep `function openPopFor`/`function closePop`): `pop.setAttribute('role','dialog')`; mounted on `document.body` (Pitfall 3); `node.setAttribute('aria-expanded','true')` on open; singleton; outside-tap + Escape shipped; the head builds `<div class="np-label">...` (give it an id for aria-labelledby). `closePop()` (grep) — restore focus to `popNode` if `popNode.isConnected`.

Festival (grep `function openFestival`/`function closeFestival`): `overlay.setAttribute('role','dialog')` + `aria-modal='true'` + `aria-label='A gift of light'`; Escape scoped listener (removed on close); focus moved to `.ofest-close` on open (grep `ofest-close`); `closeFestival` calls `render()` first (the path rebuild). `__awbaClaimChest` sets +25 BEFORE `openFestival` (grep `__awbaClaimChest`). Thread `nodeId` into closeFestival for the focus-return re-query.

Learn sheet callers (grep `openStreakSheet`/`openNoorSheet`/`openSwitcher` + the tab handlers): each is a single `AW.sheet(html)` — add a natural label 2nd arg ("Your streak" / "Noor gathered" / "Switch course" / the tab name).

Primitives shipped by 06-04: `AW._trapFocus(overlayEl) → untrap()`, `AW.sheet(html, label)` (named + focus-into + trapped), the register `:focus-visible` grammar (gold on Orbit).

The a11y probes (from 06-01): `a11y-keyboard.test.js` (state-in-name, native streak strip, native ayah affordance — un-todo here); `a11y-dialogs.test.js` (popup aria-modal/name/focus/containment/return, Festival containment/return — un-todo here).

Machine notes: run node tests via glob; ugrep paren-wrap/`--`-separate; learn.html localStorage stays 0; no gated literal in learn.html incl. the view-transition property name; scope any `thread` grep to class/attribute (the immutable 'One religion, one thread' node label); `.click()` drives the flows headlessly; the coming-soon `.sheet-row.osw-row` divs must NOT gain tabindex. Hand-edit STATE.md.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: node state-in-name + the two conversions (streak strip button, ayah Read-the-citation) (RED→GREEN)</name>
  <files>learn.html</files>
  <read_first>
    - 06-UI-SPEC.md §S4 (the accessible-name grammar + the node state-phrase table + the two conversions) and R-9 (the ayah ruling — keep the section, add the inner button; the streak strip converts cleanly).
    - 06-RESEARCH.md §Inventory learn.html rows (the node/streak/ayah gaps + the exact seams) and Open Question 2 (the ayah-conversion reasoning).
    - `~/.claude/skills/emil-design-eng/SKILL.md` (read directly) — for the `oayah-cite` button: Courier marginalia on the Orbit ground, gold focus ring, quiet under the ayah's reverence (law 3/7), no competing display weight.
    - `learn.html`: grep `function nodeHtml` (compose the aria-label), `streakStrip` (+ its click wiring), `ayahHost` (+ its click wiring). `shared/awba-engine.css` `.reg-orbit :focus-visible` (the gold ring the new button inherits) + the Courier marginalia token.
  </read_first>
  <behavior>
    - Every `.onode` has `aria-label` = `"{nd.label}, {state phrase}"` per the state-phrase table (lesson/review: locked→"locked", active→"available", done→"complete, N of 3 stars"; chest: locked→"locked", available→"ready to open", done→"opened").
    - `#streakStrip` is a native `<button>` (no `role="button"`/`tabindex="0"` div); Enter/Space open the streak sheet.
    - The ayah `<section id="ayahHost">` drops `role="button"`/`tabindex`; it contains a native `<button class="oayah-cite">Read the citation</button>` that opens `AW.sheetRef`; the `.ayah` keeps its `lang/dir/isolate` scripture reading.
  </behavior>
  <action>Un-todo the state-in-name + native-streak-strip + native-ayah-affordance assertions in `a11y-keyboard.test.js`; run → confirm RED. Then in `nodeHtml()` compose `aria-label="{nd.label}, {state phrase}"` from `data-kind` + `data-nstate` (+ the star count for done) per the UI-SPEC table. Convert `#streakStrip` from a `role=button` div to a real `<button>` (keep the classes + the block styling; move the click handler to the button). For the ayah card: drop `role="button"`/`tabindex` from the `<section id="ayahHost">`, add a native inner `<button class="oayah-cite">Read the citation</button>` wired to `AW.sheetRef` (emil-tune it as quiet Courier marginalia on the Orbit ground with the gold focus ring; a pointer-only convenience click on the whole card may delegate to the button, but the button owns keyboard/SR). Do NOT flatten the `.ayah` into a control-label (law 3). Run the suite → GREEN — a11y-keyboard.test.js now has ZERO `{ todo }` remaining (state-in-name + streak strip + ayah affordance all flipped; the selection cue was flipped in 06-05; the verify pins `! grep -q '{ todo'`). Commit `feat(06-06): node state-in-name + streak-strip button + ayah Read-the-citation (D-62/D-64/R-9)`.</action>
  <verify>
    <automated>KOUT=$(node --test scripts/tests/a11y-keyboard.test.js 2>&1); KE=$?; SOUT=$(node --test scripts/tests/*.test.js 2>&1); SE=$?; SMOUT=$(node scripts/tests/render-smoke.mjs 2>&1); SME=$?; RLOUT=$(node scripts/tests/rtl-audit.mjs 2>&1); RLE=$?; grep -q 'oayah-cite' learn.html && grep -qE '<button[^>]*id="streakStrip"|id="streakStrip"[^>]*type="button"' learn.html && ! grep -qE 'id="ayahHost"[^>]*role="button"' learn.html && grep -qE 'aria-label' learn.html && test $KE -eq 0 && echo "$KOUT" | grep -qE 'fail 0' && ! grep -q '{ todo' scripts/tests/a11y-keyboard.test.js && test $SE -eq 0 && echo "$SOUT" | grep -qE 'fail 0' && test "$(echo "$SOUT" | grep -oE 'tests [0-9]+' | head -1 | grep -oE '[0-9]+')" -ge 114 && test "$(grep -c 'localStorage' learn.html)" -eq 0 && test $SME -eq 0 && echo "$SMOUT" | grep -q 'SMOKE OK learn.html' && ! echo "$SMOUT" | grep -q 'SMOKE FAIL' && test $RLE -eq 0 && echo "$RLOUT" | grep -q 'RTL OK' && ! echo "$RLOUT" | grep -q 'RTL FAIL'</automated>
  </verify>
  <acceptance_criteria>
    - Every `.onode` has a state-in-name aria-label; `#streakStrip` is a native button with working Enter/Space; the ayah `<section>` keeps the isolated `.ayah` reading and exposes a native `oayah-cite` button opening `AW.sheetRef`.
    - The a11y-keyboard state-in-name + conversion tests are GREEN (were RED); suite `fail 0`, `tests >= 114`; learn.html localStorage 0; render-smoke learn.html + rtl-audit green.
  </acceptance_criteria>
  <done>Each node announces its state, and the streak + daily-verse cards are keyboard-operable — the ayah keeps its reverence.</done>
</task>

<task type="auto">
  <name>Task 2: the node popup dialog contract — aria-modal + name + focus-move + trap + focus-return (RED→GREEN)</name>
  <files>learn.html</files>
  <read_first>
    - 06-UI-SPEC.md §S4 (the popup BUILD list: `aria-modal` + `aria-labelledby`→`.np-label` id + `tabindex="-1"` on the `.npop` itself + focus-move-on-open + focus-return) and the overlay accessible-names table.
    - 06-RESEARCH.md §Focus Containment (the `.npop` application facts — focus the popup element itself since the locked popup has no CTA; the closePop focus-return-if-connected rule; skip in the render() detach path) + §Common Pitfalls 5/8.
    - `learn.html`: grep `function openPopFor` (add aria-modal/name/focus-move/trap after append), `function closePop` (dispose the trap + return focus to popNode if connected), the `.np-label` build (give it an id).
    - `scripts/tests/a11y-dialogs.test.js` (the popup aria-modal/name/focus/containment/return todos to un-todo).
  </read_first>
  <behavior>
    - The `.npop` carries `aria-modal="true"` + `aria-labelledby` pointing at the `.np-label` (which now has an id) + `tabindex="-1"` on the popup element itself.
    - On open, focus moves onto the `.npop` dialog (the locked popup has no CTA, so the dialog is the focus target); `AW._trapFocus` is attached; a synthetic Tab wraps within the popup.
    - On close, the trap is disposed and focus returns to `popNode` when `popNode.isConnected` (skipped in the `render()` path where the node is detached — render's own flow re-lands focus).
  </behavior>
  <action>Un-todo the popup aria-modal/name/focus/containment/return assertions in `a11y-dialogs.test.js`; run → confirm RED. Then in `openPopFor`: give the `.np-label` an id and set `pop.setAttribute('aria-modal','true')` + `pop.setAttribute('aria-labelledby', <that id>)` + `pop.tabIndex = -1`; after `document.body.appendChild(pop)`, `pop.focus()` and `var untrap = AW._trapFocus(pop)` (store it on the popup or the module scope). In `closePop`: call the stored `untrap()` if present, then `if (popNode && popNode.isConnected) popNode.focus()` (skip in the render()-triggered close where popNode is detached). Do NOT change the singleton/outside-tap/Escape/aria-expanded behaviour (shipped). Run the suite → GREEN — a11y-dialogs.test.js now carries EXACTLY 2 `{ todo }` (the 5 popup assertions flipped; the 2 Festival assertions still staged for T3; the verify pins the count). Commit `feat(06-06): node popup dialog contract — aria-modal + name + focus + trap + return (D-63)`.</action>
  <verify>
    <automated>DOUT=$(node --test scripts/tests/a11y-dialogs.test.js 2>&1); DE=$?; SOUT=$(node --test scripts/tests/*.test.js 2>&1); SE=$?; SMOUT=$(node scripts/tests/render-smoke.mjs 2>&1); SME=$?; grep -q 'aria-modal' learn.html && grep -q 'aria-labelledby' learn.html && grep -q '_trapFocus' learn.html && test $DE -eq 0 && echo "$DOUT" | grep -qE 'fail 0' && test "$(grep -c '{ todo' scripts/tests/a11y-dialogs.test.js)" -eq 2 && test $SE -eq 0 && echo "$SOUT" | grep -qE 'fail 0' && test "$(echo "$SOUT" | grep -oE 'tests [0-9]+' | head -1 | grep -oE '[0-9]+')" -ge 114 && test "$(grep -c 'localStorage' learn.html)" -eq 0 && ! grep -q 'view-transition-name' learn.html && test $SME -eq 0 && echo "$SMOUT" | grep -q 'SMOKE OK learn.html' && ! echo "$SMOUT" | grep -q 'SMOKE FAIL'</automated>
  </verify>
  <acceptance_criteria>
    - The `.npop` has `aria-modal` + `aria-labelledby`→`.np-label` id + `tabindex="-1"`; opens with focus on the dialog + the shared trap; closes disposing the trap + returning focus to `popNode` when connected.
    - The a11y-dialogs popup tests are GREEN (were RED); singleton/outside-tap/Escape unchanged; suite `fail 0`, `tests >= 114`; learn.html localStorage 0; no view-transition property name in learn.html; render-smoke learn.html green.
  </acceptance_criteria>
  <done>The node popup is a proper dialog — named, focus-moved, contained, and it hands focus back where you were.</done>
</task>

<task type="auto">
  <name>Task 3: Festival trap + focus-return-after-render + the sheet-caller labels (RED→GREEN)</name>
  <files>learn.html</files>
  <read_first>
    - 06-UI-SPEC.md §S4 (Festival: add containment + focus-return; the overlay names table — Festival name shipped) and the sheet accessible-names row (learn callers pass natural labels).
    - 06-RESEARCH.md §Focus Containment (the `.ofest` application: trap = a self-loop on `.ofest-close`; focus-return re-queries the chest node by data-id AFTER render(); the claim-before-open +25 ordering to preserve) + §Common Pitfalls 6.
    - `learn.html`: grep `function openFestival` (attach the trap; thread `nodeId`), `function closeFestival` (re-query + focus after render()), `__awbaClaimChest` (+25 before open — preserve), `openStreakSheet`/`openNoorSheet`/`openSwitcher` + the tab sheet handlers (pass the label 2nd arg).
    - `scripts/tests/a11y-dialogs.test.js` (the Festival containment + focus-return todos to un-todo; the +25-survives-Escape ordering already ACTIVE from 06-01).
  </read_first>
  <behavior>
    - `openFestival(nodeId)` attaches `AW._trapFocus` (a self-loop on `.ofest-close`); the shipped role=dialog + aria-modal + name + Escape + focus-on-open + the claim-before-open +25 ordering are preserved.
    - `closeFestival` disposes the trap, calls `render()` (shipped), then re-queries `app.querySelector('.onode[data-id="'+nodeId+'"]')` and focuses it (focus returns to the chest node, not `<body>` — Pitfall 6).
    - Each learn sheet caller passes a natural `AW.sheet(html, label)` name ("Your streak" / "Noor gathered" / "Switch course" / the tab name).
  </behavior>
  <action>Un-todo the Festival containment + focus-return assertions in `a11y-dialogs.test.js`; run → confirm RED. Then in `openFestival`: after the shipped focus-to-`.ofest-close`, attach `var untrap = AW._trapFocus(overlay)` (store it); ensure `nodeId` is in scope for the return. In `closeFestival`: dispose `untrap()`, keep the shipped `render()` call, then re-query the chest node by `data-id` and `.focus()` it (thread `nodeId` in). Do NOT touch the +25 claim ordering (`__awbaClaimChest` sets +25 before `openFestival` — the +25-survives-Escape test stays green). Update `openStreakSheet`/`openNoorSheet`/`openSwitcher` + the tab coming-soon `AW.sheet(html)` calls to pass a natural label 2nd arg. Run the suite → GREEN — a11y-dialogs.test.js now has ZERO `{ todo }` remaining, and with it ALL a11y-probe todos across all three files are flipped (keyboard 0 / dialogs 0 / announce 0; suite `todo 0` — the 06-07 gate re-confirms). Commit `feat(06-06): Festival trap + focus-return-after-render + sheet-caller names (D-63/R-10)`.</action>
  <verify>
    <automated>DOUT=$(node --test scripts/tests/a11y-dialogs.test.js 2>&1); DE=$?; SOUT=$(node --test scripts/tests/*.test.js 2>&1); SE=$?; COUT=$(node scripts/tests/contrast-audit.mjs 2>&1); CE=$?; SMOUT=$(node scripts/tests/render-smoke.mjs 2>&1); SME=$?; grep -qE 'openFestival|closeFestival' learn.html && grep -q '_trapFocus' learn.html && grep -qE 'AW.sheet\([^)]+,' learn.html && test $DE -eq 0 && echo "$DOUT" | grep -qE 'fail 0' && ! grep -q '{ todo' scripts/tests/a11y-dialogs.test.js && test $SE -eq 0 && echo "$SOUT" | grep -qE 'fail 0' && test "$(echo "$SOUT" | grep -oE 'tests [0-9]+' | head -1 | grep -oE '[0-9]+')" -ge 114 && test "$(grep -c 'localStorage' learn.html)" -eq 0 && test $CE -eq 0 && echo "$COUT" | grep -q 'CONTRAST OK' && ! echo "$COUT" | grep -q 'CONTRAST FAIL' && test $SME -eq 0 && echo "$SMOUT" | grep -q 'SMOKE OK learn.html' && ! echo "$SMOUT" | grep -q 'SMOKE FAIL' && ! grep -qiE 'poppins|confetti|lantern-gold|gold-bg|fonts\.googleapis' learn.html</automated>
  </verify>
  <acceptance_criteria>
    - `openFestival` applies the shared trap; `closeFestival` returns focus to the chest node re-queried after `render()`; the +25 claim ordering + the shipped Festival role/aria-modal/name/Escape/focus-on-open are untouched.
    - Each learn sheet caller passes a natural accessible name; the a11y-dialogs Festival tests are GREEN (were RED); suite `fail 0`, `tests >= 114`; learn.html localStorage 0; contrast-audit + render-smoke learn.html green; no gated literal in learn.html.
  </acceptance_criteria>
  <done>Claiming a gift by keyboard traps focus in the Festival and hands it back to the chest node — every learn sheet is named. All a11y probes are GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| node data → aria-label | the accessible name is composed from `nd.label` (in-DOM content) + the shipped state attrs via textContent-style attribute set — no user/URL input, no injection |
| overlay DOM → focus trap / return | `AW._trapFocus` reads the overlay's own focusables; the Festival focus-return re-queries a known `data-id` from the just-rendered path (the hash→querySelector validation shipped in 05-03 is unchanged) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06a | Tampering | the Festival focus-return re-query landing on the wrong/absent node → focus dumped to body | mitigate | re-query `.onode[data-id=nodeId]` AFTER render() (the node exists post-render); the a11y-dialogs probe pins `document.activeElement` after Festival close (Pitfall 6) |
| T-06-06b | Tampering | the +25 claim ordering disturbed by the focus/trap edits | mitigate | `__awbaClaimChest` sets +25 BEFORE `openFestival` (unchanged); the +25-survives-Escape test (ACTIVE since 06-01) stays green |
| T-06-06c | Tampering | a gated literal (esp. the view-transition property name) entering learn.html via a comment | mitigate | Pitfall 7 discipline; the verify greps learn.html for the gated set + `view-transition-name`; `thread`/content matches stay class/attribute-scoped |
| T-06-06-SC | Tampering | npm/pip/cargo installs | accept | zero packages installed; no `[ASSUMED]`/`[SUS]` package to gate |
</threat_model>

<verification>
- Node state-in-name (STATE.md Pending Todo closed); streak strip → native button; ayah `<section>` keeps the isolated `.ayah` + gains the native `oayah-cite` button (R-9); the popup dialog contract (aria-modal + name + focus-move + trap + return); the Festival trap + focus-return-after-render; the learn sheet callers named.
- ALL a11y-keyboard + a11y-dialogs `{ todo }` assertions flip to GREEN (RED confirmed first); the +25 claim ordering + shipped overlay scaffolds untouched.
- suite `fail 0`, `tests >= 114`; learn.html localStorage 0; no gated literal (incl. the view-transition property name) in learn.html; render-smoke learn.html + rtl-audit + contrast-audit green.
</verification>

<success_criteria>
- ACC-01 (keyboard operability + conversions) + ACC-02 (node names) + D-63 (popup/Festival focus management) are live on the learn front door with mechanics + reverence intact.
- A blind or keyboard-only learner walks the path, opens every popup/sheet, and claims a chest with no dead tap and no focus trap failure.
</success_criteria>

<output>
Create `.planning/phases/06-accessibility-rtl-typography-hardening/06-06-SUMMARY.md` when done.
</output>
