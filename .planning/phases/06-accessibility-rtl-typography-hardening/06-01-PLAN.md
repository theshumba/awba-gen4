---
phase: 06-accessibility-rtl-typography-hardening
plan: 01
type: execute
wave: 1
executor_hint: sonnet
depends_on: []
files_modified:
  - scripts/tests/a11y-keyboard.test.js
  - scripts/tests/a11y-dialogs.test.js
  - scripts/tests/a11y-announce.test.js
autonomous: true
requirements: [ACC-01, ACC-02]
must_haves:
  truths:
    - "Per D-62 / D-68: scripts/tests/a11y-keyboard.test.js is a PERMANENT suite member (joins the *.test.js glob) that ACTIVELY pins the shipped-correct ACC-01 baseline — every inventoried interactive control is a native button/a, ZERO positive tabindex anywhere across learn.html + lessons/ + reviews/ + engine emitters, the reflect textarea has a real <label for=lsrt>, the .onode focusable DOM order equals the canonical journey sequence, and el.matches(':focus-visible') is true after programmatic focus with the register token colour (gold on .reg-orbit, crimson on .reg-page — empirically drivable per 06-RESEARCH §Keyboard-Walk Probe)"
    - "Per D-63 / D-68: scripts/tests/a11y-dialogs.test.js ACTIVELY pins the shipped overlay baseline — the Festival overlay is role=dialog + aria-modal + named + Escape-closes + focus-on-open, AW.sheet is role=dialog + aria-modal + Escape + focus-restore-on-close, the .npop is role=dialog + Escape + outside-tap + singleton, AND the claim-before-open ordering holds (an immediate Escape after a chest claim keeps the +25) — extending the proven learn-dom-flows driver"
    - "Per D-64 / D-68: scripts/tests/a11y-announce.test.js exists as the ACC-02 contract driver (learn-dom-flows harness + --virtual-time-budget for the 14s timeout path) with the { skip } Chrome guard"
    - "Per D-67: the not-yet-built assertions (node state-in-name, native streak strip, native ayah affordance, the body-level live region + composed announce + 10s single-fire + timeout narration + reward focus-to-heading, the ×3 focus-containment wrap + focus-into + focus-return + popup aria-modal/name, the sheet accessible name) are written but { todo }-gated so the suite reports fail 0 today — each downstream plan un-todos exactly its assertions with a RED→GREEN flip"
    - "Per D-67: the full node:test suite stays green (fail 0, tests >= 114 baseline never shrunk) with the three new probe files joined to the glob; grep -c localStorage shared/awba-engine.js stays 13; no gated literal (poppins/confetti/amber/PERFECT/.combo/lantern-gold/gold-bg/rgba(37,54,/--accent/fonts.googleapis and the view-transition property name) enters any probe file even in a comment (Pitfall 7); the 'One religion, one thread' content label is never matched by a naive thread grep (class/attribute-scoped only)"
  artifacts:
    - path: "scripts/tests/a11y-keyboard.test.js"
      provides: "ACC-01 keyboard/focus probe — native-semantics + no-positive-tabindex + journey DOM order + :focus-visible register token (active); state-in-name + conversions (todo)"
      contains: "matches(':focus-visible')"
    - path: "scripts/tests/a11y-dialogs.test.js"
      provides: "D-63 overlay-contract probe x3 (Festival/sheet/popup baseline active; containment + focus-into + focus-return + names todo); claim-before-open Escape ordering"
      contains: "aria-modal"
    - path: "scripts/tests/a11y-announce.test.js"
      provides: "ACC-02 live-region + composed-announce driver (all todo — nothing exists today); --virtual-time-budget timeout path"
      contains: "status"
  key_links:
    - from: "the three a11y probe test files"
      to: "the implementation plans 06-04 (primitives), 06-05 (runners), 06-06 (learn.html)"
      via: "each impl plan un-todos exactly the assertions it lands, RED then GREEN, so the probes are the executable contract"
      pattern: "todo"
---

## Phase Goal

**As a** blind or keyboard-only learner, **I want to** complete a full lesson, a timed review, and claim a chest using only the keyboard and a screen reader — with the same mercy and calm every sighted learner gets, **so that** the accessibility pass Gen-3 never had makes Awba usable by everyone without changing what it is.

*(Derived from ROADMAP Phase-6 Goal + 06-CONTEXT §specifics "a blind or keyboard-only user can complete a full lesson, a timed review, and claim a chest — same mercy, same calm." This is the Wave-0 test foundation: it renders nothing and changes no product surface — it writes the executable ACC-01/ACC-02 contracts the implementation plans satisfy.)*

<objective>
Lay the testable ground before any a11y fix lands: author the three permanent headless probe files (keyboard, dialogs, announce) that pin the ALREADY-STRONG shipped baseline (native controls, no positive tabindex, the reflect label, the Festival/sheet/popup dialog scaffolds, focus-restore-on-close) as ACTIVE assertions and stage every not-yet-built behaviour as `{ todo }`-gated assertions — so the suite reports `fail 0` today while the contract is fully written. Each downstream implementation plan un-todos exactly its own assertions with a RED→GREEN flip (the 05-01 idiom, applied across plan boundaries via node:test's todo gate).

Purpose: close the 06-VALIDATION Wave-0 gaps for ACC-01/ACC-02 so keyboard operability and announcements are provable headlessly, and make the probes permanent suite members (D-68) that Phase 7's gate re-runs.
Output: `scripts/tests/a11y-keyboard.test.js`, `scripts/tests/a11y-dialogs.test.js`, `scripts/tests/a11y-announce.test.js`.

Executor routing: **sonnet** (mechanical / test authoring — no visual judgement). Copy the driver idiom byte-faithfully from `learn-dom-flows.test.js`; do NOT invent any behaviour — the probes assert only what 06-RESEARCH §Inventory documents as shipped (active) or missing (todo).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-accessibility-rtl-typography-hardening/06-RESEARCH.md
@.planning/phases/06-accessibility-rtl-typography-hardening/06-UI-SPEC.md
@.planning/phases/06-accessibility-rtl-typography-hardening/06-VALIDATION.md

<interfaces>
<!-- Extracted this session; locate by content — line numbers drift. -->

Harness template (`scripts/tests/learn-dom-flows.test.js`): `CHROME = '/Applications/Google Chrome.app/Contents/MacOS/Google Chrome'`; a throwaway copy of `learn.html` at repo root with an injected `<script>` before `</body>`; `--headless --dump-dom --virtual-time-budget=5000`; a JSON results block round-tripped out of `--dump-dom`; `const skip = chromeMissing ? '…' : false;` guards every Chrome test; the probe file is cleaned in a `finally`; non-vacuous revert pairs prove a real assertion. `scripts/tests/render-smoke.mjs` is the multi-page sweep template (findPages() over learn + lessons + reviews, `execFileSync` per page).

Empirically-verified headless boundary (06-RESEARCH §Keyboard-Walk Probe — probe JSON already run this session): `el.focus()` + `document.activeElement` WORK; `el.matches(':focus-visible')` returns TRUE after programmatic focus (assertable); synthetic `KeyboardEvent('Tab')` does NOT move focus (isTrusted=false) — substitute a DOM-order focusable-list assertion; synthetic Enter does NOT click — assert nativeness + drive flows via `.click()`; synthetic Escape/Tab DO fire document/element keydown LISTENERS (so a focus-trap's wrap logic and all Escape handlers ARE synthetically testable); `--virtual-time-budget` fast-forwards the 14s review timer to a real timeout.

Shipped-correct baseline to pin ACTIVE (06-RESEARCH §Inventory, verified at source): every `.opt`/`.tf`/`.tile`/`.btn`/`.onode`/`.hstat`/tab is a native `<button>`/`<a>`; the reflect textarea has `<label for="lsrt">` (engine); Festival is `role="dialog"` + `aria-modal="true"` + `aria-label="A gift of light"` + Escape + focus-moved-to-`.ofest-close`-on-open; `AW.sheet` is `role="dialog"` + `aria-modal="true"` (grep `aria-modal`) + Escape + invoker captured on open and `.focus()`-restored on close; `.npop` is `role="dialog"` + Escape + outside-tap + singleton; `__awbaClaimChest` sets the noor +25 BEFORE `openFestival` runs (Escape-after-claim keeps the +25).

Not-yet-built (stage as `{ todo }`): node `aria-label` state-in-name; `#streakStrip` is a `role=button` DIV with dead Enter/Space; `#ayahHost` is a `role=button` SECTION with dead Enter/Space; NO body-level `role=status` region exists (grep 0 hits); NO tab-containment on any overlay; `.npop` lacks `aria-modal`/name/focus-move/focus-return; `AW.sheet` lacks an accessible NAME + focus-into-on-open; reward/review screen swaps evaporate focus to `<body>`.

Canonical journey order (the `.onode` DOM-order assertion): u1m1, u1m2, u1m3, u1m4, u1r, u1c, u2m1, u2m2, u2m3, u2m3b, u2r, u2c, u3m1, u3m2, u3m3, u3r, u3c, u4m1, u4m2, u4m2b, u4m3, u4r, u4c.

Machine notes: `node --test scripts/tests/*.test.js` (glob ONLY — directory form breaks Node v24.13.0). A failing `{ todo }` test reports `fail 0` and exits 0 (verified this session). ugrep is the grep — paren-wrap or `--`-separate any `--`-leading pattern. Gated literals never in comments (Pitfall 7); scope any `thread` grep to `class=`/attribute context ('One religion, one thread' is immutable content). Hand-edit STATE.md.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: a11y-keyboard.test.js — ACC-01 probe (active baseline + todo-gated gaps)</name>
  <files>scripts/tests/a11y-keyboard.test.js</files>
  <read_first>
    - 06-RESEARCH.md §THE INVENTORY (every element + its current keyboard/aria state), §Keyboard-Walk Probe (the empirical automation boundary + the three-walk plan), §Validation Architecture (the ACC-01 test-map rows).
    - 06-UI-SPEC.md §S1 (the register-aware focus grammar + the S1 palette table with cited ratios) and §S4 (the two conversions + the node state-phrase table).
    - `scripts/tests/learn-dom-flows.test.js` (the throwaway-copy + injected-driver + `{ skip }` + `finally`-clean idiom to copy verbatim) and `scripts/tests/render-smoke.mjs` (findPages() for the multi-page tabindex scan).
    - `learn.html`: grep `function nodeHtml` (the `.onode` emitter — DOM order + `data-nstate`/`data-kind`), `streakStrip`, `ayahHost`. The register `:focus-visible` rules the probe asserts apply live at `shared/awba-engine.css` (grep `focus-visible`).
  </read_first>
  <behavior>
    - ACTIVE (pass today): across learn.html + lessons/*.html + reviews/*.html, every element carrying a click/interaction role is a native `<button>` or `<a>` — no `<div onclick>` retrofit; ZERO elements have a positive `tabindex` (string+DOM scan → 0 matches for a `tabindex="[1-9]"` pattern); the reflect textarea's rendered markup contains `<label for="lsrt">`.
    - ACTIVE (Chrome-driven): after `el.focus()` on a `.reg-orbit` control, `el.matches(':focus-visible')` is true AND `getComputedStyle(el).outlineStyle !== 'none'` AND the outline colour resolves to the gold token; after focus on a `.reg-page` control the outline resolves to the crimson token (per the S1 palette).
    - ACTIVE (Chrome-driven, learn.html): the ordered list of `.onode[data-id]` equals the canonical journey sequence (interfaces block).
    - TODO (fails today, `{ todo }`-gated, un-todoed by 06-06): every `.onode` carries `aria-label` ending in its state phrase (e.g. `, available` / `, complete, 2 of 3 stars` / `, ready to open`); `#streakStrip` is a native `<button>` (not a `role=button` div); the ayah card exposes a native `<button class="oayah-cite">` (R-9) rather than a `role=button` section with dead keydown.
  </behavior>
  <action>Write `scripts/tests/a11y-keyboard.test.js` copying the `learn-dom-flows.test.js` harness shell (the `CHROME` const, `chromeMissing`/`skip` guard, throwaway-copy + injected JSON-emitting driver, `finally` cleanup). Split assertions into ACTIVE `test(...)` blocks and TODO `test('...', { todo: 'ACC-01: {what} lands in 06-06' }, ...)` blocks exactly per the behavior list. For the static native-semantics + positive-tabindex scan, read each page's HTML (and the engine emitter strings) and assert with plain string/DOM checks — no Chrome needed there; for `:focus-visible`/`activeElement`/DOM-order use the injected Chrome driver returning a JSON block (focus each control, push `{fv: el.matches(':focus-visible'), outline: getComputedStyle(el).outlineStyle, color: getComputedStyle(el).outlineColor}`). Resolve the register token by comparing the computed `outlineColor` against the ground's expected token read from a live reference element — do NOT hardcode a hex (stay token-only). For the tabindex scan use a class/attribute-scoped pattern; never a bare `tabindex` substring that would catch `tabindex="0"`/`"-1"` (only positive integers fail). Run the suite: the ACTIVE tests PASS, the TODO tests report todo, `fail 0`. Keep every gated literal out of the file (Pitfall 7). Commit `test(06-01): a11y-keyboard probe — ACC-01 baseline pinned, gaps todo-gated`.</action>
  <verify>
    <automated>ls scripts/tests/a11y-keyboard.test.js >/dev/null && node --test scripts/tests/a11y-keyboard.test.js 2>&1 | grep -qE 'fail 0' && node --test scripts/tests/*.test.js 2>&1 | grep -qE 'fail 0' && test "$(node --test scripts/tests/*.test.js 2>&1 | grep -oE 'tests [0-9]+' | head -1 | grep -oE '[0-9]+')" -ge 114 && grep -q "focus-visible" scripts/tests/a11y-keyboard.test.js && grep -q 'todo' scripts/tests/a11y-keyboard.test.js && test "$(grep -c 'localStorage' shared/awba-engine.js)" -eq 13 && ! grep -qiE 'poppins|confetti|lantern-gold|gold-bg|fonts\.googleapis|rgba\(37,54,' scripts/tests/a11y-keyboard.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/tests/a11y-keyboard.test.js` exists; ACTIVE tests pass (native-semantics, zero positive tabindex, reflect label, journey DOM order, register-token `:focus-visible`); the state-in-name / streak-strip / ayah-affordance tests are present and `{ todo }`-gated.
    - Suite `fail 0`, `tests >= 114`; `grep -c localStorage shared/awba-engine.js` === 13; no gated literal in the file.
  </acceptance_criteria>
  <done>The ACC-01 keyboard contract is written and running; the shipped baseline is pinned and the three gaps are staged for 06-06.</done>
</task>

<task type="auto">
  <name>Task 2: a11y-dialogs.test.js — D-63 overlay contract x3 (active baseline + todo-gated gaps)</name>
  <files>scripts/tests/a11y-dialogs.test.js</files>
  <read_first>
    - 06-RESEARCH.md §Focus Containment (the three overlays' exact open/close/focus seams + the Escape census + the Escape-after-claim ordering), §Common Pitfalls 5/6/8, §Validation Architecture (the D-63 test-map rows).
    - 06-UI-SPEC.md §S4 (the overlay accessible-name table + what BUILDS vs VERIFIES) and the R-10 sheet-name ruling.
    - `scripts/tests/learn-dom-flows.test.js` (the existing Festival + `__awbaClaimChest` driver tests — extend the SAME pattern, do not fork it).
    - `learn.html`: grep `function openPopFor` / `function closePop` (`.npop` role=dialog, singleton, outside-tap), `function openFestival` / `function closeFestival` (role=dialog + aria-modal + name + Escape + focus-on-open + the re-render), `__awbaClaimChest` (the +25 set before `openFestival`). `shared/awba-engine.js`: grep `AW.sheet = ` (role/aria-modal, Escape, invoker capture + restore).
  </read_first>
  <behavior>
    - ACTIVE (pass today): the `.ofest` Festival is `role="dialog"` + `aria-modal="true"` + `aria-label="A gift of light"`, closes on Escape, and moves focus to `.ofest-close` on open; `AW.sheet` renders `role="dialog"` + `aria-modal="true"`, closes on Escape, and restores focus to the invoker on close; the `.npop` is `role="dialog"`, singleton (opening a second removes the first), closes on Escape and on an outside tap.
    - ACTIVE (Chrome-driven): claim a chest via `__awbaClaimChest`, then dispatch Escape immediately — the noor delta is still exactly +25 and `chests[id]===true` (the claim-before-open ordering, 06-RESEARCH §Focus Containment "Escape never skips the +25").
    - TODO (fails today, un-todoed by 06-04 for the sheet, 06-06 for popup/Festival): dispatching a synthetic Tab at the last focusable inside each open overlay wraps `activeElement` to the first (and shift+Tab reverses) — the `AW._trapFocus` cycle; `AW.sheet` opens with focus moved into `.sheet-x` and carries an accessible NAME (`aria-label` from the 2nd arg, default "Details"); the `.npop` carries `aria-modal="true"` + an accessible name (`aria-labelledby` → `.np-label`) and moves focus onto the dialog on open + returns focus to its trigger node on close; closing the Festival returns focus to the chest node re-queried by `data-id` after the path re-render.
  </behavior>
  <action>Write `scripts/tests/a11y-dialogs.test.js` extending the `learn-dom-flows.test.js` driver shell (throwaway learn.html copy + injected driver + `{ skip }` + `finally`). ACTIVE blocks: parse/drive to assert the three overlays' shipped role/aria-modal/name/Escape/singleton/outside-tap/focus-restore per the behavior list, and drive `__awbaClaimChest` + synthetic Escape to pin the +25-survives-Escape ordering (extend, never edit, the shipped Festival/claim tests). TODO blocks (`{ todo: 'D-63: {what} lands in 06-0X' }`): the synthetic-Tab-at-trap wrap for each overlay (dispatch a `keydown` `Tab` at the overlay's last focusable and read `activeElement` from the JSON block — 06-RESEARCH confirms listeners fire on untrusted events, so the trap IS synthetically testable), the sheet focus-into + accessible name, the popup aria-modal + name + focus-move + focus-return, the Festival focus-return-after-render. Assert `activeElement` and attributes from the JSON dump; keep repeat non-vacuous pairs where the harness pattern shows them. `fail 0` overall (todo blocks report todo). Commit `test(06-01): a11y-dialogs probe — D-63 overlay baseline pinned, containment/names todo`.</action>
  <verify>
    <automated>ls scripts/tests/a11y-dialogs.test.js >/dev/null && node --test scripts/tests/a11y-dialogs.test.js 2>&1 | grep -qE 'fail 0' && node --test scripts/tests/*.test.js 2>&1 | grep -qE 'fail 0' && test "$(node --test scripts/tests/*.test.js 2>&1 | grep -oE 'tests [0-9]+' | head -1 | grep -oE '[0-9]+')" -ge 114 && grep -q 'aria-modal' scripts/tests/a11y-dialogs.test.js && grep -q 'todo' scripts/tests/a11y-dialogs.test.js && ! grep -qiE 'poppins|confetti|lantern-gold|gold-bg|fonts\.googleapis|rgba\(37,54,' scripts/tests/a11y-dialogs.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/tests/a11y-dialogs.test.js` exists; ACTIVE tests pin the three overlays' shipped role/aria-modal/name/Escape/singleton/focus-restore + the +25-survives-Escape ordering.
    - The containment-wrap, sheet focus-into/name, popup aria-modal/name/focus, and Festival focus-return tests are present and `{ todo }`-gated; suite `fail 0`, `tests >= 114`.
  </acceptance_criteria>
  <done>The D-63 dialog contract is written and running; the shipped overlay scaffolds are pinned and the containment/name/focus gaps are staged.</done>
</task>

<task type="auto">
  <name>Task 3: a11y-announce.test.js — ACC-02 live-region + composed-announce driver (all todo)</name>
  <files>scripts/tests/a11y-announce.test.js</files>
  <read_first>
    - 06-RESEARCH.md §aria-live Architecture (the region placement law, the insertion-point table, the composed-once-per-resolve rule, the countUp danger, the DCL-ensure), §Common Pitfalls 1/2/3/9, §Keyboard-Walk Probe (the `--virtual-time-budget` timeout drive + reading `#region.textContent` at checkpoints).
    - 06-UI-SPEC.md §S3 (the live region + the Copywriting Contract announcement table with each string cited to source) and R-10 (the focus-vs-announce rule).
    - `scripts/tests/learn-dom-flows.test.js` (harness shell) and, for the lesson/review drive, `scripts/tests/render-smoke.mjs` (how a lesson page is loaded headlessly) + `scripts/fixtures/valid-lesson.html` (the shortest driveable lesson if a real one is heavy).
    - `shared/awba-engine.js`: grep `function resolve` (the one composed announce point), `function timeUp` / `AW.QTIME` (the 14s timer + 10s single-fire seam), `function result` (the review result), `rw-word` / `rv-title` (the headings that will take focus). `learn.html`: grep `__awbaClaimChest` (the chest-claim announce point).
  </read_first>
  <behavior>
    - TODO (all fail today — nothing exists, un-todoed by 06-04 for the region, 06-05 for the insertions): exactly ONE `<div ... role="status">` exists as a DIRECT child of `document.body` (never inside `#app`/`#root`/`<main>`) and it survives a full lesson walk (same node identity, `isConnected` true after several `innerHTML` screen swaps); driving a lesson to a correct answer sets the region `textContent` exactly ONCE per resolve (a composed verdict+noor line, snapshot before/after); a wrong answer announces the "Nothing lost" + gentle line once; the 10s warning fires exactly once as the review timer crosses `tleft === 100`; a `--virtual-time-budget`-driven timeout lands the mercy line in the region and the next question narrates "Question N of M"; claiming a chest announces the +25 line once; each reward/review screen swap moves focus to that screen's heading (`document.activeElement` is the `.rw-word`/`.rv-title`, R-10 focus-vs-announce).
  </behavior>
  <action>Write `scripts/tests/a11y-announce.test.js` on the `learn-dom-flows` harness shell (the `{ skip }` Chrome guard is mandatory — every test here is Chrome-driven). Every assertion is `{ todo: 'ACC-02: {what} lands in 06-0X' }`-gated (region exists → 06-04; the insertions + focus-to-heading → 06-05). Drive real flows with `.click()` (never synthetic Enter) and read `#{region}.textContent` from the `--dump-dom` JSON at each checkpoint; for the timeout path load a review page and pass `--virtual-time-budget=20000` so the 14s timer + the 1500ms auto-skip elapse, then assert the region text. Assert the region is `document.body`'s direct child and `isConnected` after the screen swaps (Pitfall 1). Assert the single-set-per-resolve by snapshotting region text immediately before and after one answer (Pitfall 9). Do NOT wire any assertion onto a visible container (`#lsfoot`) — the region is the only speech path. `fail 0` overall (all todo). Keep gated literals out (Pitfall 7 — including the word for browser-persistent storage in a comment near the region). Commit `test(06-01): a11y-announce driver — ACC-02 contract staged (todo)`.</action>
  <verify>
    <automated>ls scripts/tests/a11y-announce.test.js >/dev/null && node --test scripts/tests/a11y-announce.test.js 2>&1 | grep -qE 'fail 0' && node --test scripts/tests/*.test.js 2>&1 | grep -qE 'fail 0' && test "$(node --test scripts/tests/*.test.js 2>&1 | grep -oE 'tests [0-9]+' | head -1 | grep -oE '[0-9]+')" -ge 114 && grep -q 'status' scripts/tests/a11y-announce.test.js && grep -q 'virtual-time-budget' scripts/tests/a11y-announce.test.js && grep -q 'todo' scripts/tests/a11y-announce.test.js && ! grep -qiE 'poppins|confetti|lantern-gold|gold-bg|fonts\.googleapis|rgba\(37,54,' scripts/tests/a11y-announce.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/tests/a11y-announce.test.js` exists with the `{ skip }` Chrome guard and the `--virtual-time-budget` timeout drive; every assertion `{ todo }`-gated (region + insertions + focus-to-heading).
    - Suite `fail 0`, `tests >= 114`; the file names the region body-level placement and reads `textContent` at checkpoints; no gated literal.
  </acceptance_criteria>
  <done>The ACC-02 announcement contract is written and running as a driver; the region + insertion assertions are staged for 06-04/06-05.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev tooling → filesystem / headless Chrome | the probes read the repo, write a throwaway copy at repo root (cleaned in `finally`), and spawn headless Chrome over `file://`; no network, no persistent writes |
| test assertions → shipped runtime | the probes drive the shipped inline scripts via `.click()`/`.focus()`/synthetic keydown; they assert, they never mutate product source |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01a | Tampering | probe-file leaks a gated literal into the file-wide sweeps | mitigate | Pitfall-7 discipline: no gated literal in any probe (incl. comments); the verify greps assert absence; `thread`/content matches stay class/attribute-scoped |
| T-06-01b | Repudiation | a vacuous todo test that never becomes a real RED | mitigate | each todo assertion is a concrete DOM/text check that fails today (proven by the inventory); downstream plans flip todo→active only after a RED confirmation |
| T-06-01-SC | Tampering | npm/pip/cargo installs | accept | zero packages installed (zero-dep static app); no `[ASSUMED]`/`[SUS]` package to gate |
</threat_model>

<verification>
- `node --test scripts/tests/*.test.js` → `fail 0`, `tests >= 114` (the three probes joined the glob; ACTIVE assertions pass, TODO assertions report todo).
- The three probe files exist as permanent suite members; each pins the shipped ACC-01/ACC-02/D-63 baseline and stages every gap `{ todo }`.
- `grep -c localStorage shared/awba-engine.js` === 13; no gated literal in any probe file; no product source touched.
</verification>

<success_criteria>
- ACC-01 (keyboard/focus) and ACC-02 (announcements) + D-63 (overlay contract) are executable, permanent, headless contracts (interface-first) the implementation plans satisfy.
- The suite stays green while the full contract is written — the todo gate holds `fail 0`.
</success_criteria>

<output>
Create `.planning/phases/06-accessibility-rtl-typography-hardening/06-01-SUMMARY.md` when done.
</output>
