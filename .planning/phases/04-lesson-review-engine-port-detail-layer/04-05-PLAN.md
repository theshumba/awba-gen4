---
phase: 04-lesson-review-engine-port-detail-layer
plan: 05
type: execute
wave: 5
executor_hint: opus
depends_on: ["04-04"]
files_modified:
  - shared/awba-engine.css
  - shared/awba-engine.js
  - reviews/u1-review.html
autonomous: true
requirements: [ENG-02, ENG-04, CNT-01, RWD-03, MOT-05]
must_haves:
  truths:
    - "Per ENG-02/D-46: AwbaReview(cfg) accepts Josh's Gen-3 review cfg shape byte-unchanged (items {q,quote?,o,c,t} MC or {tf:true,q,c,t} TF, mastery, next) and renders the whole session on the Orbit register (.reg-orbit black + Hajar Gold) — NO back button anywhere (awback hidden)"
    - "Per ENG-04/D-47: the 14s soft timer state-machine is byte-preserved — QTIME=14 (AW.QTIME), 100ms decisecond tick, a single timeout permanently kills allInTime; timeout → 'time — it will wait at the end', options disabled, auto-skip after 1500ms (no penalty); queue-exhausted-with-skipped → circle-back (phase='back', untimed, NO noor, but a named answer still lights its thread)"
    - "Per ENG-04: scoring is exact via the pure helpers — main-phase correct earns AW.reviewScore(inTime) = 15+(inTime?5:0) (swift label in time); result stars = AW.reviewStars(correct,total,allInTime) (3 all-in-time / 2 any-timeout / 1 partial); AW.S.set('noor',…) persists once at result; AW.touchDay on 'Begin the review'"
    - "Per D-45/RWD-03: the Gen-3 gold-lamp path is re-voiced to gold .thread arcs lit i<correct; the .low timer state is a quiet ember deepen (never red, never a buzzer, no shake); mastery is sealed by a gold .rosette (stamp); crimson is BANNED on Orbit (gold/ember only)"
    - "Per CNT-01/D-49: reviews/u1-review.html renders end-to-end — cfg byte-spliced from _MVP-BUILD (SHA-verified), CDN link stripped, ../shared paths kept — loading over file:// with ZERO console errors (render-smoke GREEN)"
    - "Per MOT-05: the 44px mute toggle is present in the review HUD (aria-pressed + aria-label swap, awba_prefs.soundMuted); AW.sound('correct'|'incorrect'|'complete') fires on meta moments, never on scripture"
  artifacts:
    - path: "shared/awba-engine.js"
      provides: "AwbaReview(cfg) runner in the RUNNERS section"
      contains: "function AwbaReview"
    - path: "shared/awba-engine.css"
      provides: "@layer screens review surfaces (Orbit + gold)"
      contains: "reg-orbit"
    - path: "reviews/u1-review.html"
      provides: "the first ported review — the review walking slice"
      contains: "AwbaReview({"
  key_links:
    - from: "AwbaReview scoring/result"
      to: "AW.reviewScore / AW.reviewStars / AW.QTIME (04-01)"
      via: "the runner calls the pure helpers, never re-derives"
      pattern: "AW\\.reviewScore|AW\\.reviewStars"
---

## Phase Goal

**As a** learner, **I want to** walk each of Josh's 15 lessons and 4 reviews end-to-end — reading verbatim scripture and being quietly celebrated at the end — **so that** the whole Aqeedah course feels like a warm, world-class manuscript rather than a game show.

*(This plan adds the second surface family — the elevated Orbit-dark review with its 14s soft timer, gold-thread progress, and rosette mastery seal — and ports the first review as its walking slice.)*

<objective>
Port Gen-3's legendary review runner into the RUNNERS section as `AwbaReview(cfg)` — the 14s timer state-machine, circle-back, and mastery scoring byte-preserved — expressed on the Orbit register with Hajar Gold per D-45 ("the circle gathers", gold-thread progress, rosette seal). Author the review `@layer screens` surfaces. Then port `u1-review.html` so one real review renders end-to-end with zero console errors.

Purpose: ENG-02/ENG-04 + CNT-01 + RWD-03 + MOT-05 — the rarer, elevated review register proven on a real file.
Output: `AwbaReview(cfg)` (`shared/awba-engine.js`); review classes in `@layer screens` (`shared/awba-engine.css`); `reviews/u1-review.html`.

Executor routing: **opus + frontend-design skill** — the review is a flagship surface. The timer state-machine is the highest-value logic to port exactly; call the 04-01 pure helpers for the numbers. Crimson is BANNED on Orbit — gold/ember only.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-lesson-review-engine-port-detail-layer/04-RESEARCH.md
@.planning/phases/04-lesson-review-engine-port-detail-layer/04-PATTERNS.md
@.planning/phases/04-lesson-review-engine-port-detail-layer/04-UI-SPEC.md

<interfaces>
<!-- BEHAVIOUR analog: _MVP-BUILD/shared/awba-engine.js AwbaReview 339-464 (the whole review runner). -->

Gen-3 review runner (preserve byte-for-byte, re-voice expression):
- constants: `PER=15, SWIFT=5, QTIME=14`; `var rb=document.getElementById('awback'); if(rb) rb.style.display='none'` (no back button, ever). progress = lamp row lit `i<correct` (→ gold `.thread` arcs).
- `startTimer` (364): `tleft=QTIME*10`, 100ms tick, bar width %, `.low` when `<28%`; on `tleft<=0` → `thisInTime=false; allInTime=false; timeUp()`.
- `timeUp` (371): push to `skipped`, note "time — it will wait at the end", disable options, `setTimeout(advance,1500)`.
- `advance` (382): `qi++`; queue exhausted + `phase==='main' && skipped.length` → `circleBackOffer()`; else `result()`.
- `circleBackOffer` (388): untimed replay ("no noor this time, but a named answer still lights its thread") OR straight to result; sets `phase='back'`, `queue=skipped.slice()`.
- `bind` (438): main correct → `noorEarned += 15 + (thisInTime?5:0)`; circle-back → thread only, no noor.
- `result` (451): stars `correct===CH.length ? (allInTime?3:2) : 1`; verdict Legendary/Mastered/Reviewed; `AW.S.set('noor',…)` persists here; best-of star persist. `AW.touchDay()` on "Begin the review" (407).

Re-voice map (D-45 / §S5): NOT `phone.classList.add('gold-bg')` (`.gold-bg` retired) → whole session `.reg-orbit` + Hajar Gold. `AW.LANTERNG` intro art retired → dab-drift in + `trophy`/`lamp` glyph. `paintLamps()` lamp row → gold `.thread` arcs. mastery seal → `.rosette` (shipped, stamp). Item `t` = explanation text (naming collision with lesson beat `t`; different context, no code conflict).

Shipped to consume: `.reg-orbit` ground (--icon-accent gold), shipped `.thread` (:890)/`.rosette` (:910)/`.dab` (:880), `AW.animate`, `AW.reviewScore/reviewStars/QTIME/PER_REVIEW/SWIFT` (04-01), `AW.touchDay`, `AW.S`, `AW.sound`, `AW.prefs.set('soundMuted')`. Port recipe = same as 04-03 (byte-splice cfg into a fresh `../shared` Athar shell, strip CDN).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Review @layer screens surfaces — Orbit-dark + Hajar Gold (S5)</name>
  <files>shared/awba-engine.css</files>
  <read_first>
    - 04-UI-SPEC §S5 (intro "circle gathers", 14s soft timer bar, gold-thread progress, timeout/circle-back, mastery + rosette) + the Orbit Color table (crimson BANNED; gold 8.40:1, ember 5.05:1).
    - `shared/awba-engine.css` `.reg-orbit`, shipped `.thread`/`.rosette`/`.dab`, `@layer screens` block — add inside it.
  </read_first>
  <action>Inside `@layer screens` (order line untouched), author the review surfaces in `.reg-orbit` scope, token-only, gold/ember accents only (crimson banned): a `.rv-intro` "the circle gathers" hero (ink `.dab` drift-in around the ring, `trophy`/`lamp` glyph in `--gold`); a thin decisecond `.rv-timer` bar with a `.rv-timer-fill` and a `.rv-timer.low` state that is a QUIET ember deepen (never red, never a buzzer, no shake); a `.rv-thread` gold-thread progress row (arcs consuming the shipped `.thread`, lit `i<correct`); a `.rv-q` question surface; a `.rv-timeout` / circle-back note style; a `.rv-result` mastery screen sealed by a gold `.rosette` (shipped stamp). Gold `:focus-visible` ring on the dark ground. No crimson, no `.gold-bg`, no poppins/confetti/amber.</action>
  <verify>
    <automated>grep -qE '\.rv-timer|\.rv-thread|\.rv-result|\.rv-intro' shared/awba-engine.css && test "$(grep -c '@layer tokens, base, components, screens, motion;' shared/awba-engine.css)" -eq 1 && ! grep -qiE 'gold-bg|poppins|confetti|amber' shared/awba-engine.css && ! grep -qE 'rgba\(37,54,' shared/awba-engine.css && node --test scripts/tests/*.test.js 2>&1 | grep -qE 'fail 0'</automated>
  </verify>
  <acceptance_criteria>
    - Review surfaces exist in `.reg-orbit` with gold/ember accents only; the `.low` timer is a quiet ember deepen (not alarm); mastery uses a gold `.rosette`.
    - No `.gold-bg`/crimson-on-orbit/poppins/confetti/amber; order line count == 1; suite `fail 0`.
  </acceptance_criteria>
  <done>The elevated review register reads as "the sacred circle gathers", calm even under time pressure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AwbaReview(cfg) — timer state-machine + circle-back + scoring + mastery + mute</name>
  <files>shared/awba-engine.js</files>
  <read_first>
    - Gen-3 `AwbaReview` 339-464 (intro/startTimer/timeUp/advance/circleBackOffer/renderQ/bind/result) — the whole runner.
    - 04-UI-SPEC §S5 (states + copy) + §S6 (mute toggle) + 04-01 helpers `AW.reviewScore/reviewStars/QTIME`.
  </read_first>
  <behavior>
    - No back button: `awback` (if present) is hidden; there is no back affordance in any review screen.
    - Timer: `tleft = AW.QTIME*10` (140), 100ms tick, fill width %; `.low` toggles under 28%; on `tleft<=0` → `thisInTime=false`, `allInTime=false` (permanent), `timeUp()`.
    - timeUp: the question is pushed to `skipped`, options disabled, note "time — it will wait at the end", auto-skip after 1500ms, NO penalty.
    - advance: `qi++`; queue exhausted with `phase==='main' && skipped.length` → circle-back offer; else result.
    - circle-back: `phase='back'`, `queue=skipped.slice()`, untimed, no noor, but a correct answer still lights its gold thread.
    - main-phase correct earns `AW.reviewScore(thisInTime)` (20 in time / 15 not); result stars = `AW.reviewStars(correct, total, allInTime)` (3/2/1); verdict Legendary/Mastered/Reviewed; noor persists once at result; best-of star persist; touchDay on "Begin the review".
  </behavior>
  <action>Author `function AwbaReview(cfg){…}` in the RUNNERS section porting the Gen-3 state-machine byte-for-byte, re-voiced to Orbit/gold: the whole session on `.reg-orbit` (not `.gold-bg`); intro = `.rv-intro` dab-drift + `trophy`/`lamp` glyph + "Begin the review" (fires `AW.touchDay()`); `startTimer`/`timeUp`/`advance`/`circleBackOffer` ported verbatim (100ms `setInterval`, `tleft=AW.QTIME*10`, permanent `allInTime` kill on any timeout, 1500ms auto-skip); `paintLamps` → light gold `.thread` arcs `i<correct`; `bind` scoring calls `AW.reviewScore(thisInTime)` for main-phase correct (circle-back = thread only, no noor); `result` uses `AW.reviewStars(correct, cfg.items.length, allInTime)`, verdict word, `AW.S.set('noor', AW.S.get('noor',0)+noorEarned)` once, best-of star persist, a gold `.rosette` mastery seal via `AW.animate(...,'--dur-stamp','--ease')`. Add `AW.sound('correct'|'incorrect'|'complete')` on meta moments (never scripture) + the 44px review-HUD mute toggle (§S6, `AW.prefs.set('soundMuted',…)`, aria-pressed + aria-label swap). No back button anywhere. Add `scripts/tests/runner-review.test.js` (ls-stub harness) asserting via pure-helper probes: `AW.reviewScore(true)===20`, `AW.reviewScore(false)===15`, `AW.reviewStars(6,6,true)===3`, `AW.reviewStars(6,6,false)===2`, `AW.reviewStars(5,6,true)===1` (the 2★ timeout cap + partial-run floor). No `localStorage` outside AW.S/AW.prefs (count stays 13).</action>
  <verify>
    <automated>grep -q 'function AwbaReview' shared/awba-engine.js && grep -qE 'AW\.reviewScore|AW\.reviewStars' shared/awba-engine.js && grep -qE "setInterval" shared/awba-engine.js && grep -q "AW.prefs.set('soundMuted'" shared/awba-engine.js && ! grep -qiE 'gold-bg|AW\.LANTERNG|confetti|amber' shared/awba-engine.js && test "$(grep -c 'localStorage' shared/awba-engine.js)" -eq 13 && node --test scripts/tests/runner-review.test.js 2>&1 | grep -qE 'fail 0' && node --test scripts/tests/*.test.js 2>&1 | grep -qE 'fail 0'</automated>
  </verify>
  <acceptance_criteria>
    - `AwbaReview(cfg)` exists; the 14s timer/circle-back/2★-cap state-machine is byte-preserved via the 04-01 helpers; no back button anywhere.
    - Progress is gold `.thread` arcs, mastery a gold `.rosette`; noor persists once at result; touchDay on begin; 44px mute toggle wired.
    - `runner-review.test.js` pins the review numbers; no `.gold-bg`/`AW.LANTERNG`/confetti/amber; `localStorage` count == 13; suite `fail 0`.
  </acceptance_criteria>
  <done>The review timer, circle-back mercy, and mastery scoring match Gen-3 exactly, in the elevated Athar register.</done>
</task>

<task type="auto">
  <name>Task 3: Port reviews/u1-review.html → one review renders end-to-end</name>
  <files>reviews/u1-review.html</files>
  <read_first>
    - `/Users/theshumba/Downloads/AWBA APP/_MVP-BUILD/reviews/u1-review.html` (the cfg body — shape `{id,title,sub,mastery,items:[{q,quote?,o,c,t}|{tf:true,q,c,t}],next}`; item `t` = explanation).
    - `lessons/u1-m1.html` (04-03) as the shell precedent + `scripts/port-audit.mjs` + `scripts/tests/render-smoke.mjs`.
  </read_first>
  <action>Create `reviews/u1-review.html`: a fresh Athar `../shared` page shell (self-hosted font preloads, one stylesheet, classic engine script, NO googleapis link) with the `AwbaReview({...})` cfg body BYTE-IDENTICAL to `_MVP-BUILD/reviews/u1-review.html` (items/mastery/next unchanged; any `AW.cite(...)` parse-time calls preserved). Strip the CDN `<link>`; keep `../shared/` paths. Verify: `node scripts/validate-content.js reviews/u1-review.html` → exit 0; `node scripts/port-audit.mjs` → `BYTES OK reviews/u1-review.html`; `node scripts/tests/render-smoke.mjs reviews/u1-review.html` → `SMOKE OK` (zero console errors, `.reg-orbit` root). Record the cfg SHA in the SUMMARY.</action>
  <verify>
    <automated>test -f reviews/u1-review.html && ! grep -q 'fonts.googleapis' reviews/u1-review.html && grep -q '../shared/awba-engine.js' reviews/u1-review.html && node scripts/validate-content.js reviews/u1-review.html 2>&1 | grep -qiE 'OK|0 errors' && node scripts/port-audit.mjs 2>&1 | grep -q 'BYTES OK' && node scripts/tests/render-smoke.mjs reviews/u1-review.html 2>&1 | grep -q 'SMOKE OK'</automated>
  </verify>
  <acceptance_criteria>
    - `reviews/u1-review.html` exists, cfg byte-identical to source (port-audit `BYTES OK`), CDN stripped, `../shared/` paths kept.
    - Validator exit 0; render-smoke `SMOKE OK`; a learner can open it over file:// and walk the full timed review + circle-back + mastery.
  </acceptance_criteria>
  <done>One Gen-3 review renders verbatim, end-to-end, in the elevated Athar register.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cfg author-content → DOM | trusted authored review items, not user input |
| timer state-machine | pure client timing (`setInterval`); no external input |
| review page → shared engine | classic `<script>` load order (engine first) |
| noor/stars persistence | only via `AW.S`; once at result |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05a | Tampering | double-persist / circle-back noor leak | mitigate | noor persists once at result; circle-back explicitly earns no noor (phase gate in scoring); test pins the review numbers |
| T-04-05b | Information disclosure | CDN/font leakage on the ported review | mitigate | CDN `<link>` stripped; grep gate + port-audit assert no `fonts.googleapis` survives |
| T-04-05c | Tampering | content byte-fidelity | mitigate | port-audit SHA-compares the cfg region vs `_MVP-BUILD`; validator exit 0 |
| T-04-05d | Tampering | storage seam | mitigate | grep gate pins `localStorage` count at 13 |
| T-04-SC | Tampering | npm installs | accept | zero packages |
</threat_model>

<verification>
- `AwbaReview(cfg)` preserves the 14s timer/circle-back/2★-cap exactly via 04-01 helpers; Orbit+gold, no back button, no `.gold-bg`.
- `reviews/u1-review.html`: validator exit 0, port-audit `BYTES OK`, render-smoke `SMOKE OK`.
- `runner-review.test.js` green; `localStorage` count == 13; `@layer` order line == 1; `node --test scripts/tests/*.test.js` → `fail 0`.
</verification>

<success_criteria>
- ENG-02/ENG-04 + CNT-01 + RWD-03 + MOT-05 hold on a real review file, in the elevated Athar register.
- Both runner families (lesson + review) are proven end-to-end on one real file each — ready for the full port.
</success_criteria>

<output>
Create `.planning/phases/04-lesson-review-engine-port-detail-layer/04-05-SUMMARY.md` when done.
</output>
