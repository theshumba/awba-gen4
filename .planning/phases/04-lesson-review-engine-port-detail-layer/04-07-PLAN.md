---
phase: 04-lesson-review-engine-port-detail-layer
plan: 07
type: execute
wave: 7
executor_hint: sonnet
depends_on: ["04-06"]
files_modified: []
autonomous: false
requirements: [ENG-01, ENG-02, ENG-03, ENG-04, ENG-05, CNT-01, CNT-02, CNT-04, RWD-01, RWD-02, RWD-03, MOT-05]
must_haves:
  truths:
    - "The full automated gate passes as PRECHECKS before the human is asked: node --test scripts/tests/*.test.js (fail 0, tests >= 64 baseline never shrunk); node scripts/validate-content.js over all 19 → exit 0 with exactly the 3 expected accepted warnings; node scripts/validate-content.js --self-test OK"
    - "Every grep gate passes: no fonts.googleapis in lessons/ reviews/; no confetti/perfect/combo/poppins in lessons/ reviews/ shared/awba-engine.js; no amber / rgba(37,54, / --accent in the CSS; no celebration primitive (dab|thread|plate|rosette) adjacent to a scripture (.ayah/.scard) block in the runner; grep -c localStorage shared/awba-engine.js == 13; the @layer order line appears exactly once"
    - "The port harnesses pass: node scripts/port-audit.mjs → BYTES OK for all 19 + holds recorded (U4-03 absent, U3-13 not surfaced, U3-16 principle-only, group-namings held); node scripts/tests/render-smoke.mjs over all 19 → every page SMOKE OK (zero console errors, covering ENG-01/02 render + MOT-05 silent-sfx)"
    - "The human gate is a plain-language, ~10-item checklist Melusi walks against 2-3 real lessons + 1 review over file://; a NO on ANY item is a blocker that routes back to the responsible plan — this checkpoint is BLOCKING and NEVER auto-approved by an executor (workflow.auto_advance is ignored here)"
  artifacts: []
  key_links:
    - from: "the automated prechecks"
      to: "the human plain-language checklist"
      via: "prechecks must all pass before the human is asked to walk the lessons/review"
      pattern: "fail 0"
---

## Phase Goal

**As a** learner, **I want to** walk each of Josh's 15 lessons and 4 reviews end-to-end — reading verbatim scripture and being quietly celebrated at the end — **so that** the whole Aqeedah course feels like a warm, world-class manuscript rather than a game show.

*(This is the phase-closing gate: every automated check green as a precondition, then Melusi walks real content and confirms it feels world-class. Verification-only — it modifies no source.)*

<objective>
Close Phase 4 with a two-stage gate that mirrors 03-12: run the full automated precheck set (test suite + validator over all 19 + self-test + every grep gate + the port-audit byte-fidelity/holds + whole-corpus render-smoke) as a hard precondition, then STOP and present Melusi a ~10-item plain-language visual checklist to walk against 2-3 real lessons + 1 review over file://. A failing precheck routes back to the owning plan (never patched at the gate); the human checkpoint is BLOCKING and is never auto-approved.

Purpose: the phase-level proof that all 19 files render verbatim end-to-end, mechanics preserved, holds intact, celebration never over scripture, and the whole thing reads as a warm manuscript (ENG-01..05, CNT-01/02/04, RWD-01..03, MOT-05).
Output: a green automated-precheck report, then a blocking human checkpoint.

Executor routing: **sonnet** (mechanical / verification). Run the prechecks exactly; do NOT edit source to make a gate pass — a failing gate routes back to its owning plan (01 helpers/harnesses, 02 lesson CSS, 03 lesson runner, 04 reward choreography, 05 review runner/CSS, 06 content port). The human checkpoint is BLOCKING and must NEVER be auto-approved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-lesson-review-engine-port-detail-layer/04-UI-SPEC.md
@.planning/phases/04-lesson-review-engine-port-detail-layer/04-VALIDATION.md

<interfaces>
<!-- The gate reads only; it changes nothing. -->
Full suite: `node --test scripts/tests/*.test.js` (glob only; fail 0, tests >= 64).
Content gate: `node scripts/validate-content.js lessons/*.html reviews/*.html` (exit 0, 3 accepted warnings) + `--self-test` (OK).
Port harnesses (04-01): `scripts/port-audit.mjs` (BYTES OK all 19 + holds), `scripts/tests/render-smoke.mjs` (all 19 SMOKE OK).
Grep gates: no `fonts.googleapis` in lessons/ reviews/; no `confetti|class="perfect"|class="combo"|poppins` in lessons/ reviews/ shared/awba-engine.js; no `amber`/`rgba(37,54,`/`--accent` in shared/awba-engine.css; no `(dab|thread|plate|rosette)` adjacent to `.ayah/.scard` in shared/awba-engine.js; `grep -c localStorage shared/awba-engine.js` == 13; `@layer tokens, base, components, screens, motion;` count == 1.
The manual-only checks (04-VALIDATION "Manual-Only Verifications"): reward choreography feel, celebration-never-over-scripture, mute-toggle visible + silent no-op.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the full automated precheck set (suite + validator + grep gates + port-audit + render-smoke)</name>
  <files></files>
  <read_first>
    - 04-VALIDATION.md (full-suite command, sampling, manual-only list) + 04-RESEARCH "Grep gate" + "Port-gate result" (the 3 accepted warnings).
  </read_first>
  <action>Run and record pass/fail for the full precheck set as automated preconditions (use `! grep -q` for every absence gate; paren-wrap any `--`-leading pattern for ugrep). Suite: `node --test scripts/tests/*.test.js` → `fail 0`, `tests >= 64`. Content: `node scripts/validate-content.js lessons/*.html reviews/*.html` → exit 0 (the 3 expected `note:` warnings present + accepted, no ERROR/FAIL) and `node scripts/validate-content.js --self-test` → OK. Port harnesses: `node scripts/port-audit.mjs` → `BYTES OK` for all 19 + holds recorded; `node scripts/tests/render-smoke.mjs` → every page `SMOKE OK`, no `SMOKE FAIL`. Grep gates: no `fonts.googleapis` in `lessons/ reviews/`; no `confetti|class="perfect"|class="combo"|poppins` in `lessons/ reviews/ shared/awba-engine.js`; no `amber`/`rgba(37,54,`/`--accent` in `shared/awba-engine.css`; no `(dab|thread|plate|rosette)` adjacent to a `.ayah/.scard` block in `shared/awba-engine.js`; `grep -c localStorage shared/awba-engine.js` == 13; `@layer` order line count == 1. If ANY precheck fails, STOP and report which gate failed and which plan owns it — do NOT edit source to force a pass.</action>
  <verify>
    <automated>node --test scripts/tests/*.test.js 2>&1 | grep -qE 'fail 0' && node scripts/validate-content.js lessons/*.html reviews/*.html 2>&1 | grep -qiE 'OK|0 errors' && ! node scripts/validate-content.js lessons/*.html reviews/*.html 2>&1 | grep -qiE 'ERROR|FAIL' && node scripts/validate-content.js --self-test 2>&1 | grep -qi 'SELF-TEST OK' && node scripts/port-audit.mjs 2>&1 | grep -q 'BYTES OK' && ! node scripts/port-audit.mjs 2>&1 | grep -qiE 'BYTES DRIFT' && node scripts/tests/render-smoke.mjs 2>&1 | grep -q 'SMOKE OK' && ! node scripts/tests/render-smoke.mjs 2>&1 | grep -q 'SMOKE FAIL' && ! grep -rq 'fonts.googleapis' lessons/ reviews/ && ! grep -rqiE 'confetti|class="perfect"|class="combo"|poppins' lessons/ reviews/ shared/awba-engine.js && ! grep -qiE 'amber' shared/awba-engine.css && ! grep -qE 'rgba\(37,54,' shared/awba-engine.css && ! grep -q -- '--accent' shared/awba-engine.css && ! grep -qE '(dab|thread|plate|rosette)[^;{}]*\.(ayah|scard)' shared/awba-engine.js && test "$(grep -c 'localStorage' shared/awba-engine.js)" -eq 13 && test "$(grep -c '@layer tokens, base, components, screens, motion;' shared/awba-engine.css)" -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - Suite `fail 0` (tests >= 64); validator over all 19 exit 0 with exactly the 3 accepted warnings; self-test OK.
    - port-audit `BYTES OK` all 19 + holds recorded; render-smoke every page `SMOKE OK`.
    - Every grep gate passes (no CDN/retired/amber/rgba(37,54,/--accent; no celebration-adjacent-scripture; localStorage==13; order line==1).
    - Any failure is reported with its owning plan, not patched here.
  </acceptance_criteria>
  <done>Every automated precondition of the phase is green (or precisely routed back).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Melusi walks the ~10-item plain-language visual gate (2-3 lessons + 1 review, file://)</name>
  <what-built>
    All 19 of Josh's data files (15 lessons + 4 reviews) now render end-to-end through the Athar engine, and every automated precheck (Task 1) is green: scripture is byte-identical and verbatim, the sensitive holds are intact (U4-03 absent, U3-13 not surfaced, U3-16 principle-only), quiz + review numbers match Gen-3 exactly (+12/correct, +15 reflect, 14s review timer, swift +5, 2★ timeout cap), wrongness is a gentle grey ink-blot never red, the post-lesson sequence plays as choreography ending on your own inked tawaf ring and a du'a, and celebration never touches scripture. Sound is wired but silent (no cue files yet) with a visible mute toggle.
  </what-built>
  <how-to-verify>
    Open a lesson (start with `lessons/u1-m1.html`) by double-clicking it (file:// — zero CDN, no server). Walk 2-3 different lessons and 1 review (`reviews/u1-review.html`) start→finish, and answer YES/NO to each:
    1. Does every lesson feel like a warm page of a book — crisp margins, easy to read, clearly the same calm app as the preview?
    2. Is the Arabic Qur'an the most important, most beautiful thing on any page it appears on — and never sitting on a busy background, never next to anything celebratory?
    3. When you get an answer wrong, is it gentle — a soft grey mark and a one-line explanation, nothing red, nothing shaking, nothing flashing, no "PERFECT" popup, no confetti?
    4. Do the correct/streak moments feel quiet and tasteful — a gold dot, a thin gold thread — never a game-show burst, never a mascot?
    5. At the end of a lesson, does the sequence read as a series of calm moments (your stars, your noor counting up, your returns) rather than one static screen?
    6. Does the ring at the very end feel like YOUR path being traced in ink — only the new bit drawing, never re-drawing the whole thing — followed by a quiet du'a and "Alhamdulillah — continue."?
    7. In a review: does the 14s timer feel calm (a quiet warm bar), and if it runs out does the question gently "wait at the end" rather than punish you?
    8. Does the review feel elevated and rarer than a lesson — dark and gold, "the circle gathers", a gold seal at mastery?
    9. Can you find the mute toggle on every lesson and review, and does turning sound on cause no errors (it's silent for now, by design)?
    10. Turn on your OS "reduce motion": does everything go still and calm — the ring shows finished, nothing keeps moving — while the lessons still read perfectly?
    A NO on any item is a blocker; note which item and it routes back to the responsible plan (item 1/2 → lesson CSS 04-02 + runner 04-03; item 3 wrong-answer → 04-03; item 4 combo/streak → 04-03; item 5/6 choreography+ring → 04-04; item 7/8 review → 04-05; item 9 mute/sound → 04-01/04-03/04-05; item 10 reduced-motion → 04-04; scripture verbatim/holds → 04-06).
  </how-to-verify>
  <resume-signal>Type "approved" if all ~10 are YES, or list the item numbers that are NO with a one-line note each.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verification-only | this plan reads and asserts; it modifies no source. No new trust surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | Repudiation / process | the gate itself | mitigate | the human checkpoint is blocking and never auto-approved (workflow.auto_advance ignored); a failing precheck routes to the owning plan rather than being patched at the gate |
| T-04-SC | Tampering | npm installs | accept | no package installs; verification-only. No `[ASSUMED]`/`[SUS]` package to gate |
</threat_model>

<verification>
- Every automated precheck (suite fail 0 · validator 19 exit 0 + self-test · port-audit BYTES OK + holds · render-smoke all 19 SMOKE OK · every grep gate) passes before the human is asked.
- The ~10-item plain-language checklist is presented; the checkpoint blocks and is never auto-approved; a NO routes to the owning plan.
</verification>

<success_criteria>
- ENG-01..05, CNT-01/02/04, RWD-01..03, MOT-05 are verified automatically and then confirmed by the owner's plain-language walk of real lessons + a review.
- Phase 4 is gate-passed (or precisely routed back): the lesson + review engines are ported verbatim, world-class, and Athar-coherent.
</success_criteria>

<output>
Create `.planning/phases/04-lesson-review-engine-port-detail-layer/04-07-SUMMARY.md` when done.
</output>
