---
phase: 04-lesson-review-engine-port-detail-layer
plan: 02
type: execute
wave: 2
executor_hint: opus
depends_on: ["04-01"]
files_modified:
  - shared/awba-engine.css
autonomous: true
requirements: [ENG-01, ENG-05, CNT-04]
must_haves:
  truths:
    - "Per D-47/S1: the lesson shell renders on the Page register (.reg-page cream, --go:.028) — HUD marginalia row, a per-beat thermal progress-dab row, a #root beat stage, and a foot — all authored INSIDE @layer screens (never a new token, never a new @layer order line)"
    - "Per S1-beats: every one of the 9 beat surfaces has a distinct authored class — read, frame, verse (.scard), panel (4 visually-distinct variants pull/tell/guard/check), depth (3-lens accordion), reflect, mc/tf/tile — and all four panel variants read differently"
    - "Per scripture-law (CNT-04/D-50/law-3): the .scard verse card sets --go:0 (no grain), .ayah is Amiri Quran >=24px line-height 1.9 strongest --kiswah ink, .trans + .tsrc beneath — and NO celebration primitive class (.dab/.thread/.plate/.rosette) is authored inside a .scard/.ayah block"
    - "Per D-45/ENG-05/S2: the 3-lens depth accordion cues each lens by SHAPE + LABEL (never colour only) — Reality→--madder, Revelation→--crimson (scripture inside follows scripture law), Ruling→--olive — each header a 44px-min tappable that expands its own body on settle, never blocking Continue"
    - "Per S4: the reward-screen surfaces exist on their registers — verdict/noor/returns/done on .reg-page (noorbig in Marcellus >=28px; returns hero carries a decorative --apricot horizon-warmth glow, never apricot text; weekcal days are lighter-ink presence dots, never a gap/red/miss), the Ring wrapper reuses the shipped .ring on .reg-orbit, the du'a close on .reg-sky-night"
    - "Retired elements stay retired: no poppins, no confetti/.combo/.perfect class, no amber, no rgba(37,54,, no --accent/data-unit colour theming, no gummy press — only the shipped one paper-press is used"
  artifacts:
    - path: "shared/awba-engine.css"
      provides: "@layer screens lesson + reward surface classes (Page/Orbit/Sky), token-only"
      contains: "@layer screens"
  key_links:
    - from: "@layer screens classes"
      to: "AwbaLesson renderers (04-03) + reward choreography (04-04)"
      via: "the runner emits these class names; the CSS gives them their Athar expression"
      pattern: "\\.scard|\\.lacc|\\.noorbig"
---

## Phase Goal

**As a** learner, **I want to** walk each of Josh's 15 lessons and 4 reviews end-to-end — reading verbatim scripture and being quietly celebrated at the end — **so that** the whole Aqeedah course feels like a warm, world-class manuscript rather than a game show.

*(This plan authors the visible manuscript surfaces — the CSS half of the lesson vertical slice. Its payoff becomes walkable when 04-03 wires the runner that emits these classes on the real u1-m1 page.)*

<objective>
Fill the reserved, empty `@layer screens` block (`shared/awba-engine.css:923`) with the lesson + reward screen surfaces — every one of the 9 beat renderers, the quiz option/tf/tile states (consuming the already-shipped law-8 wrong-answer), the 3-lens depth accordion, the scripture-law scripture card, and the verdict→noor→returns→done→Ring→du'a reward screens — expressed in the Athar registers using ONLY shipped tokens and primitives. Zero new tokens, zero new faces, one immutable `@layer` order line.

Purpose: the manuscript-grade surfaces (S1–S4 of 04-UI-SPEC) that make a lesson read like a warm page of a book and quietly celebrate at the end — the design half of the slice the runner (04-03) animates.
Output: `@layer screens` content in `shared/awba-engine.css`.

Executor routing: **opus + frontend-design skill** — this is flagship visual work (the manuscript page, the scripture card, the reward screens). Cite every colour pairing to `03-UI-SPEC-ATHAR §2.1`; invent no hex/px. This is the CSS layer of a coupled port — a deliberate horizontal split from the runner (04-03) because the two `@layer screens` / RUNNERS placeholders are the architecture's own seam and authoring all screen CSS in one pass is what keeps the manuscript coherent.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-lesson-review-engine-port-detail-layer/04-UI-SPEC.md
@.planning/phases/03-components-icon-kit-motion-language/03-UI-SPEC-ATHAR.md
@preview.html

<interfaces>
<!-- Shipped tokens/classes to CONSUME — extracted from shared/awba-engine.css. Do NOT re-author these. -->

- `@layer` order line (`shared/awba-engine.css:16`): `@layer tokens, base, components, screens, motion;` — NEVER re-declared. Write ONLY inside the `@layer screens { ... }` block at line 923.
- Register grounds (`@layer base`, ~:267-298): `.reg-page` (cream #F3EDE2, --go:.028, --icon-accent:var(--crimson)) · `.reg-orbit` (Kiswah #131013, --go:.07, --icon-accent:var(--gold)) · `.reg-sky-night` (Last-Third gradient, du'a close).
- Shipped component classes (consume, don't re-author): `.btn` (:655), `.opt` (:691) + `.opt.correct` (:792 gold dot draws) + `.opt.wrong` (:807 grey ink-blot) + `.opt-why` (:818 one-line signal) + `.btn.retry` (:825 --rose frame), `.tf` (:707), `.tile` (:721), `.tab` (:733), `.hstat` (:754), the ONE paper-press (:775).
- Shipped celebration primitives (reward only, NEVER in a scard): `.dab` (:880), `.thread` (:890), `.plate` (:897), `.rosette` (:910). Shipped `.ring` wrapper (:984).
- Motion tokens (reference, never raw ms): `--dur-settle:260` (Page verb, keyframe `settle` :705/:939), `--dur-draw:240` (Orbit), `--dur-drift:300` (Circle), `--dur-stamp:150`, `--dur-press:140`. Reduced-motion collapse to 1ms already wired (:1004-1014) — do not duplicate.
- Type roles (04-UI-SPEC Typography): `--fs-ayah` Amiri Quran 24-27px lh1.9 · `--fs-display` Marcellus 28-46px (noorbig, stat numerals, big returns) · `--fs-term` Aref Ruqaa 40-48px (opener chapter-term) · `--fs-marg` Courier Prime 11-12px (per-beat counter, source line) · `--fs-body`/`--fs-h1`/`--fs-h2`.
- Thermal state (shape-first, D-A8): not-yet = hollow ring `--ink-62` border (WR-04, clears 3:1) · progress = half-dab `--ember` border · mastered = filled `--gold` + `--rule` keyline + check glyph.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lesson shell + HUD marginalia + progress-dab row + opener, in .reg-page</name>
  <files>shared/awba-engine.css</files>
  <read_first>
    - 04-UI-SPEC.md §S1 (shell layout, HUD marginalia, per-beat progress dabs, opener Farag square + Aref Ruqaa chapter-term, foot with clear button separation) + Spacing Scale + Typography tables.
    - `shared/awba-engine.css` `@layer screens` placeholder (:923) and the `.reg-page`/`.hstat`/paper-press definitions to consume.
  </read_first>
  <action>Inside `@layer screens { ... }` (never touching the :16 order line), author the lesson shell in `.reg-page` scope, token-only: a `.stage` beat container (mounts on `settle` `--dur-settle`); a `.ls-hud` marginalia row — `.hstat` noor + returns as `background:transparent;border:none;color:var(--ink-62)`, numerals in Courier `--fs-marg`, leading glyph slot at 18px in `--icon-accent` (crimson), NO pill fills, a 44px mute-toggle slot; a `.ls-prog` per-beat progress-dab row using the thermal shape-first grammar (passed = filled `--gold` + `--rule` keyline + check; current = half-dab `--ember` border; upcoming = hollow `--ink-62` ring — D-A8/WR-04) with a Courier `.ls-count` counter ("3 / 6") beside it; the opener `.hero` with a `.journey` chapter-term in a Farag square (`--r-square`, `--crimson` ink, `--fs-term` Aref Ruqaa) + the unit scene-icon slot + optional `.thought` soft-intro (Readex 300 >=18px); a `.foot` with one primary `.btn` + a quiet Back (`--ink-62`, hidden at opener) — clear button/footer separation (Gen-3 v1.2 fix). Use `--sp-*` spacing tokens only. No poppins, no `--accent`, no amber, no confetti/.combo/.perfect.</action>
  <verify>
    <automated>test "$(grep -c '@layer tokens, base, components, screens, motion;' shared/awba-engine.css)" -eq 1 && grep -qE '\.stage|\.ls-prog|\.ls-hud' shared/awba-engine.css && ! grep -qiE 'poppins' shared/awba-engine.css && ! grep -q -- '--accent' shared/awba-engine.css && ! grep -qE 'rgba\(37,54,' shared/awba-engine.css && node --test scripts/tests/*.test.js 2>&1 | grep -qE 'fail 0'</automated>
  </verify>
  <acceptance_criteria>
    - `@layer` order line count still exactly 1; new classes live inside `@layer screens`.
    - HUD is transparent marginalia (no pill fill); progress dabs use the thermal shape-first grammar; opener uses the Aref Ruqaa chapter-term in a Farag square.
    - No poppins / --accent / rgba(37,54, / amber; suite `fail 0`.
  </acceptance_criteria>
  <done>The lesson shell + opener paint as a warm manuscript page in .reg-page, awaiting the runner.</done>
</task>

<task type="auto">
  <name>Task 2: The 9 beat surfaces + quiz states + 3-lens depth accordion + scripture-law scard</name>
  <files>shared/awba-engine.css</files>
  <read_first>
    - 04-UI-SPEC.md §S1-beats (the per-beat table), §S2 (verse scripture law + the 3-lens accordion cue table), §S3 (quiz option/tf/tile rest + correct/wrong/combo/streak).
    - `shared/awba-engine.css` shipped `.opt`/`.opt.correct`/`.opt.wrong`/`.opt-why`/`.btn.retry`/`.tf`/`.tile`/`.dab`/`.thread` — CONSUME, do not re-author.
  </read_first>
  <action>Author the beat surfaces in `.reg-page`, token-only:
  - `read` — Readex 400 body column, keyline-less; inline `.cite`/`.term` styling already shipped; marker chip `.marker` renders the marker glyph in `--icon-accent` + its label.
  - `frame` — single centered framing statement at `--fs-h2`, generous `--sp-6`, optional Courier kicker.
  - `verse` — `.scard` scripture card: **`--go:0` (no grain, law 3)**; `.slabel` Courier kicker; `.ayah` Amiri Quran `--fs-ayah` (>=24px, lh 1.9), strongest `--kiswah` ink, `lang/dir/unicode-bidi:isolate/letter-spacing:0`, NO glow (cream); `.trans` `--fs-body --ink-85` (carries `˹ ˺`); `.tsrc` source line Courier `--madder`. Author NOTHING celebratory inside `.scard`.
  - `panel` — `.pnl` ink-bordered card, `--keyline`, `--r-2`, numbered items (`.pnl-i` Courier margin); the FOUR variants render DISTINCTLY: `.v-check` default ruled card, `.v-guard` left jadwal-rule caution framing (still ink, never red), `.v-pull` pull-quote (`--fs-h2`, hanging rule), `.v-tell` reveal card with a `--madder` kicker.
  - `depth` — `.lacc` 3-lens accordion: three individually-expanding lenses in FIXED order, each cued by SHAPE + LABEL (never colour only) — Reality `--madder`, Revelation `--crimson` (the ayah inside follows scripture law: `.scard`-equivalent, `--go:0`), Ruling `--olive`; header `.lh` a 44px-min tappable that expands its `.lb` body on `settle`; Continue is never blocked.
  - `reflect` — `.reflect` private `<textarea>` styling (labelled) + a `.model` reveal block.
  - `mc/tf/tile` — consume shipped `.opt/.tf/.tile` rest + `.opt.correct`(gold dot draws)/`.opt.wrong`(grey ink-blot)/`.opt-why`/`.btn.retry`(--rose frame); add only the layout wrappers `.opts`, `.tfrow`, `.tilebox`+`.bank`, and the combo/streak META surface: an accruing gold `.dab` chip on the foot/result zone (never floating over content, never mid-scripture) + a Courier `.ls-count` "N in a row", and a quiet `.thread` gold flourish slot for the 3-streak. Never red, never shake, never amber.</action>
  <verify>
    <automated>grep -qE '\.scard' shared/awba-engine.css && grep -qE '\.v-guard|\.v-pull|\.v-tell|\.v-check' shared/awba-engine.css && grep -qE '\.lacc' shared/awba-engine.css && ! grep -qE '(dab|thread|plate|rosette)[^{}]*(\.ayah|\.scard)' shared/awba-engine.css && test "$(grep -c '@layer tokens, base, components, screens, motion;' shared/awba-engine.css)" -eq 1 && ! grep -qiE 'poppins|confetti' shared/awba-engine.css && node --test scripts/tests/*.test.js 2>&1 | grep -qE 'fail 0'</automated>
  </verify>
  <acceptance_criteria>
    - All 9 beat surfaces have distinct classes; all four panel variants (`.v-check/.v-guard/.v-pull/.v-tell`) are visually distinct.
    - `.scard` sets `--go:0`, `.ayah` is Amiri Quran with bidi-isolate; no celebration class is authored inside a `.scard`/`.ayah` block (grep-gated).
    - The 3-lens accordion cues by shape+label (Madder/Crimson/Olive); Continue is never blocked.
    - Order line count == 1; no poppins/confetti; suite `fail 0`.
  </acceptance_criteria>
  <done>Every beat, quiz, and lens surface reads as manuscript, with scripture under law and wrongness gentle.</done>
</task>

<task type="auto">
  <name>Task 3: Reward-screen surfaces — verdict/noor/returns/done (Page) + Ring (Orbit) + du'a (Sky)</name>
  <files>shared/awba-engine.css</files>
  <read_first>
    - 04-UI-SPEC.md §S4 (the 6-screen reward sequence: register per screen, apricot horizon-warmth ruling, weekcal never-miss, Marcellus numerals) + the "Returns hero — apricot warmth (D-45)" Color note.
    - `shared/awba-engine.css` shipped `.ring` (:984), `.dab`/`.rosette`, and the `.reg-sky-night::before` apricot-glow recipe referenced in §S4/Color.
  </read_first>
  <action>Author the reward surfaces, one register per screen (law 1), token-only:
  - Verdict (`.reg-page`): `.rw-verdict` with shape-first stars (gold `.dab`/`.rosette` hollow/half/filled grammar, consistent with `data-state`) + verdict word `--fs-h1` + a `.rw-stats` row of 3 `.rw-stat` tiles (Noor / Accuracy / Best run), marks from shipped GLYPHS (`spark`/`star`).
  - Noor (`.reg-page`): `.noorbig` headline in Marcellus `--fs-display` (>=28px, law 5) for the count-up; optional `.grew` "What changed" box.
  - Returns (`.reg-page`): `.rw-returns` hero with a DECORATIVE `--apricot` horizon-warmth glow behind it (the same gradient recipe as `--dawn` / `.reg-sky-night::before`, NEVER apricot text — not AA on cream); big returns count in Marcellus `--kiswah` ink; `.weekcal` days as lighter-ink presence dots — NEVER a gap, red, or "miss" state (RWD-02).
  - Done (`.reg-page`): `.rw-done` recap list `.recl` + `.grew` + handoff `.btn` "Next" + quiet "Back to the path".
  - Ring moment (`.reg-orbit`): a `.rw-ring` frame that hosts the shipped `.ring` SVG on the black ground.
  - Du'a close (`.reg-sky-night`): `.rw-dua` — the du'a in Amiri (scripture law, the permitted glow allowed on this dark ground) + "Alhamdulillah — continue." in `--moonmilk`.
  Cite each colour to §2.1. Author no celebration primitive inside any scripture/du'a block. Crimson is BANNED on `.reg-orbit` (§2.1) — use `--gold`/`--ember` there.</action>
  <verify>
    <automated>grep -qE '\.noorbig|\.rw-verdict|\.rw-returns|\.weekcal' shared/awba-engine.css && grep -qE '\.rw-ring|\.rw-dua' shared/awba-engine.css && ! grep -qE '(dab|thread|plate|rosette)[^{}]*(\.ayah|\.rw-dua)' shared/awba-engine.css && test "$(grep -c '@layer tokens, base, components, screens, motion;' shared/awba-engine.css)" -eq 1 && ! grep -qiE 'poppins|confetti|amber' shared/awba-engine.css && grep -q -- '--apricot' shared/awba-engine.css && node --test scripts/tests/*.test.js 2>&1 | grep -qE 'fail 0'</automated>
  </verify>
  <acceptance_criteria>
    - Reward screens exist on their registers; `.noorbig`/big-returns are Marcellus `--fs-display` (>=28px); returns apricot is a decorative glow, never text.
    - `.weekcal` has no miss/gap/red state; du'a on `.reg-sky-night` carries the permitted glow; no celebration class inside a scripture/du'a block (grep-gated).
    - Order line count == 1; no poppins/confetti/amber; suite `fail 0`.
  </acceptance_criteria>
  <done>The reward sequence's surfaces exist across Page→Orbit→Sky, ready for the WAAPI choreography (04-04).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS `@layer screens` → rendered page | pure styling; no script, no data; the only risk is re-introducing a retired visual token |
| font-face references | must remain self-hosted (no CDN); Poppins is fully retired |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02a | Tampering | `@layer` order line | mitigate | grep gate asserts the order line count stays exactly 1; only `@layer screens` content is added |
| T-04-02b | Tampering | retired visual tokens | mitigate | grep gates assert no poppins / confetti / amber / rgba(37,54, / --accent survive; celebration classes gated out of scripture blocks (law 3) |
| T-04-02c | Information disclosure | scripture legibility | accept | scripture-law styling (strongest ink, bidi-isolate, no grain) is a content-integrity control, not a data-leak surface; no PII exists |
| T-04-SC | Tampering | npm installs | accept | no packages; CSS-only edit |
</threat_model>

<verification>
- `@layer tokens, base, components, screens, motion;` appears exactly once; all new rules inside `@layer screens`.
- Grep gates: no poppins / confetti / amber / rgba(37,54, / `--accent`; no celebration primitive inside a `.scard`/`.ayah`/`.rw-dua` block.
- `node --test scripts/tests/*.test.js` → `fail 0` (CSS-only change does not touch the suite).
</verification>

<success_criteria>
- ENG-01/ENG-05 surfaces and the CNT-04 scripture-law scripture card exist for the runner to emit; every colour pairing cited to §2.1.
- The reward sequence's surfaces exist across the three registers, retired elements stay retired.
</success_criteria>

<output>
Create `.planning/phases/04-lesson-review-engine-port-detail-layer/04-02-SUMMARY.md` when done.
</output>
