---
phase: 04-lesson-review-engine-port-detail-layer
plan: 01
type: execute
wave: 1
executor_hint: sonnet
depends_on: []
files_modified:
  - shared/awba-engine.js
  - shared/sfx/.gitkeep
  - scripts/tests/runner-math.test.js
  - scripts/tests/render-smoke.mjs
  - scripts/port-audit.mjs
autonomous: true
requirements: [ENG-03, ENG-04, MOT-05]
must_haves:
  truths:
    - "Per D-47/ENG-03: AW.lessonStars(mistakes) returns 3 for 0 mistakes, 2 for 1, 1 for >=2 — NEVER 0; PER (12 noor/correct) and REFLECT (15) are exposed constants; AW.comboShow(combo) is true at combo>=2 and AW.comboPerfect(combo) is true only at combo===3"
    - "Per D-47/ENG-04: AW.reviewScore(inTime) === 15 + (inTime?5:0); AW.reviewStars(correct,total,allInTime) === (correct===total ? (allInTime?3:2) : 1) — a single timeout (allInTime=false) caps mastery at 2 stars, and a partial run is always 1 star"
    - "Per D-52/MOT-05: AW.sound(cue) is a clean silent no-op when shared/sfx/{cue}.mp3 is absent — zero console errors, returns early when awba_prefs.soundMuted is true, never throws"
    - "The Wave-0 gaps in 04-VALIDATION are closed: scripts/tests/runner-math.test.js pins every number headlessly; scripts/render-smoke.mjs loads a page list in system Chrome and asserts zero console errors; scripts/port-audit.mjs does byte-fidelity SHA of a spliced cfg region vs _MVP-BUILD plus the holds + CDN + retired-element greps"
    - "The full node:test suite stays green (fail 0; tests >= 64 baseline, never shrunk) after this plan"
  artifacts:
    - path: "scripts/tests/runner-math.test.js"
      provides: "headless unit proof of ENG-03/ENG-04 numbers"
      contains: "AW.lessonStars"
    - path: "scripts/tests/render-smoke.mjs"
      provides: "system-Chrome page loader asserting zero console errors + key DOM nodes"
    - path: "scripts/port-audit.mjs"
      provides: "byte-fidelity SHA + holds/CDN/retired grep harness for the content port"
    - path: "shared/sfx/.gitkeep"
      provides: "the page-relative sfx directory (silent v1, owner drops assets later)"
  key_links:
    - from: "AW.lessonStars / AW.reviewScore / AW.reviewStars / AW.comboShow"
      to: "AwbaLesson (04-03) and AwbaReview (04-05)"
      via: "the runners CALL these pure helpers instead of re-deriving math inline"
      pattern: "AW\\.(lessonStars|reviewScore|reviewStars)"
---

## Phase Goal

**As a** learner, **I want to** walk each of Josh's 15 lessons and 4 reviews end-to-end — reading verbatim scripture and being quietly celebrated at the end — **so that** the whole Aqeedah course feels like a warm, world-class manuscript rather than a game show.

*(Derived from ROADMAP Phase-4 `**Goal:**` + PROJECT.md's established learner role. This plan is the Wave-0 test foundation + math contracts the vertical slices build on.)*

<objective>
Lay the testable ground before either runner exists: extract Gen-3's quiz/star/review math as PURE, DOM-free helpers on the `AW` namespace (the contracts the runners will call), pin every number in a headless unit test (RED → GREEN), ship the `AW.sound` silent-plumbing no-op, and build the two Wave-0 harnesses (headless-Chrome render-smoke + the content byte-fidelity/holds/CDN port-audit) that every later wave's verify depends on.

Purpose: close the 04-VALIDATION Wave-0 gaps so ENG-03/ENG-04 numbers are provable without a DOM and the content port has an executable fidelity gate — quality is measured, not asserted.
Output: pure math helpers + AW.sound in the engine, `runner-math.test.js`, `render-smoke.mjs`, `port-audit.mjs`, `shared/sfx/`.

Executor routing: **sonnet** (mechanical / test authoring — no flagship visual work here). Extract math byte-faithfully from Gen-3; do not "improve" any number.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-lesson-review-engine-port-detail-layer/04-RESEARCH.md
@.planning/phases/04-lesson-review-engine-port-detail-layer/04-VALIDATION.md

<interfaces>
<!-- Extracted from the codebase; the executor uses these directly, no exploration needed. -->

Gen-3 math ground-truth (`/Users/theshumba/Downloads/AWBA APP/_MVP-BUILD/shared/awba-engine.js`):
- resolve() line 274-287: correct → `noorEarned+=PER(12)`, `combo++`, `comboBest=max`; `combo===3 → firePerfect (once)`; `showCombo` when `combo>=2`. miss → `mistakes++; combo=0`.
- starsFor() line 289: `mistakes===0?3:(mistakes===1?2:1)` (never 0).
- reflect line 219: `noorEarned+=REFLECT(15)`.
- review bind() line 438: `if(phase==='main')noorEarned+=PER(15)+(thisInTime?SWIFT(5):0)`.
- review result() line 451: `stars = correct===CH.length ? (allInTime?3:2) : 1`; `allInTime` is killed permanently by ANY timeout (line 369).

In-repo engine harness + prefs (`shared/awba-engine.js`):
- `AW.prefs.get(key, default)` (line ~284) — returns default when a key is absent; `awba_prefs.soundMuted` default false (no schema bump).
- test harness `scripts/tests/ls-stub.js`: `makeLS(seed)` (Map-backed localStorage), `loadEngine(ls, probeSrc)` (engine + probe in ONE runInContext — `const AW` is lexical), `readOut(sandbox)` (JSON round-trip for object/array returns).
- RUNNER MATH + AW.sound land in the RUNNERS banner region (JS line 1358 placeholder). Cite by banner name, not line number.

System Chrome: `/Applications/Google Chrome.app/Contents/MacOS/Google Chrome` (headless; Phase-3 precedent). No puppeteer (zero-dep).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure runner-math helpers on AW + runner-math.test.js (RED → GREEN)</name>
  <files>shared/awba-engine.js, scripts/tests/runner-math.test.js</files>
  <read_first>
    - `/Users/theshumba/Downloads/AWBA APP/_MVP-BUILD/shared/awba-engine.js` lines 274-289 (resolve/starsFor), 438 (review scoring), 451 (review stars), 369 (allInTime kill) — the byte-faithful math.
    - `shared/awba-engine.js` RUNNERS banner region (grep `RUNNERS`) — where the helpers land; and any existing pure-helper precedent (`AW.deriveNodeState` ~line 437, `AW.skyDawn` ~1348) for the export idiom.
    - `scripts/tests/components.test.js` + `scripts/tests/ls-stub.js` (loadEngine/makeLS/readOut) — the test harness to copy verbatim.
  </read_first>
  <behavior>
    - AW.lessonStars(0) === 3 ; AW.lessonStars(1) === 2 ; AW.lessonStars(2) === 1 ; AW.lessonStars(5) === 1 (never 0).
    - AW.PER_LESSON === 12 ; AW.REFLECT === 15 ; AW.PER_REVIEW === 15 ; AW.SWIFT === 5 ; AW.QTIME === 14 (constants exposed for the runners + tests).
    - AW.comboShow(1) === false ; AW.comboShow(2) === true ; AW.comboShow(3) === true.
    - AW.comboPerfect(2) === false ; AW.comboPerfect(3) === true ; AW.comboPerfect(4) === false (fires once, at exactly 3).
    - AW.reviewScore(true) === 20 ; AW.reviewScore(false) === 15.
    - AW.reviewStars(6,6,true) === 3 ; AW.reviewStars(6,6,false) === 2 ; AW.reviewStars(5,6,true) === 1 ; AW.reviewStars(5,6,false) === 1.
  </behavior>
  <action>Write `scripts/tests/runner-math.test.js` FIRST (it must FAIL — the helpers do not exist yet), asserting every line in the behavior block via `loadEngine(makeLS({}), 'globalThis.__out = AW.lessonStars(0);')`-style probes (primitives read directly off `sandbox.__out`; the harness is `require('./ls-stub')`). THEN add a `RUNNER MATH` banner block inside the RUNNERS region of `shared/awba-engine.js` defining the pure helpers: `AW.lessonStars = function(mistakes){ return mistakes===0?3:(mistakes===1?2:1); }`; the constants `AW.PER_LESSON=12, AW.REFLECT=15, AW.PER_REVIEW=15, AW.SWIFT=5, AW.QTIME=14`; `AW.comboShow = function(c){ return c>=2; }`; `AW.comboPerfect = function(c){ return c===3; }`; `AW.reviewScore = function(inTime){ return AW.PER_REVIEW + (inTime?AW.SWIFT:0); }`; `AW.reviewStars = function(correct,total,allInTime){ return correct===total ? (allInTime?3:2) : 1; }`. These are the frozen mechanics of D-47/ENG-03/ENG-04 — copy the numbers from Gen-3, do not invent. Every helper is pure (no DOM, no localStorage). Run the suite: RED → GREEN.</action>
  <verify>
    <automated>node --test scripts/tests/runner-math.test.js 2>&1 | grep -qE 'fail 0' && node --test scripts/tests/*.test.js 2>&1 | grep -qE 'fail 0'</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/tests/runner-math.test.js` exists and every behavior-block assertion passes.
    - `AW.lessonStars`, `AW.reviewScore`, `AW.reviewStars`, `AW.comboShow`, `AW.comboPerfect` and the five constants are exported on `AW` and are pure.
    - `node --test scripts/tests/*.test.js` → `fail 0`, `tests` count >= 64 (baseline not shrunk).
    - `grep -c 'localStorage' shared/awba-engine.js` still equals 13 (helpers touch no storage).
  </acceptance_criteria>
  <done>The runner math is provable headlessly and the runners have their contracts to call.</done>
</task>

<task type="auto">
  <name>Task 2: AW.sound(cue) silent no-op plumbing + shared/sfx/ directory</name>
  <files>shared/awba-engine.js, shared/sfx/.gitkeep</files>
  <read_first>
    - 04-RESEARCH.md "Code Examples → Silent sound plumbing" + 04-PATTERNS.md "AW.sound(cue)" (the exact ~15-line body + the `../shared/sfx/` path caution).
    - `shared/awba-engine.js` `AW.prefs` closure (grep `AW.prefs`) — confirm `AW.prefs.get('soundMuted', false)` is the read.
  </read_first>
  <action>Add `AW.sound` to the RUNNER MATH / RUNNERS region: `AW.sound = function(cue){ if (AW.prefs.get('soundMuted', false)) return; try { var a = new Audio('../shared/sfx/' + cue + '.mp3'); a.play().catch(function(){}); } catch(e){} };` — cue ∈ correct|incorrect|complete|streak. Page-relative path is `../shared/sfx/` because lesson/review pages sit one level deep in `lessons/`/`reviews/` (same relativity as the `../shared/awba-engine.js` include — D-52 / Assumption A5: `.mp3` provisional, swappable with zero code change when the owner delivers cues). Missing asset or an autoplay block resolves to a clean silent no-op — zero console error. Create `shared/sfx/.gitkeep` so the empty directory is tracked. Do NOT add a mute-toggle UI here (that lives in the lesson/review HUD, plans 03/05); this is the audio plumbing + pref read only.</action>
  <verify>
    <automated>test -f shared/sfx/.gitkeep && grep -q "AW.sound = function" shared/awba-engine.js && grep -q "AW.prefs.get('soundMuted', false)" shared/awba-engine.js && grep -q "'../shared/sfx/'" shared/awba-engine.js && node --test scripts/tests/*.test.js 2>&1 | grep -qE 'fail 0'</automated>
  </verify>
  <acceptance_criteria>
    - `AW.sound` reads `awba_prefs.soundMuted` via `AW.prefs.get` and page-relative-loads `../shared/sfx/{cue}.mp3` inside a try/catch with a `.catch()` on play.
    - `shared/sfx/.gitkeep` exists; no cue assets are added (silent v1).
    - `grep -c 'localStorage' shared/awba-engine.js` still equals 13; suite `fail 0`.
  </acceptance_criteria>
  <done>Sound plumbing is fully wired and provably silent when assets are absent; assets drop in later with zero code change.</done>
</task>

<task type="auto">
  <name>Task 3: render-smoke.mjs (Chrome loader) + port-audit.mjs (byte-fidelity + holds/CDN/retired)</name>
  <files>scripts/tests/render-smoke.mjs, scripts/port-audit.mjs</files>
  <read_first>
    - 04-RESEARCH.md "Validation Architecture → Wave 0 Gaps" + "Environment Availability" (system Chrome path) + "The byte-fidelity / CDN pitfall" (SHA-of-cfg-region recipe, 100 `˹ ˺` brackets).
    - 04-RESEARCH.md "Sensitive holds (CNT-02)" (U4-03 absent, U3-13 not surfaced, U3-16 principle-only) + "Port-gate result" (the 3 expected `note:` warnings that MUST NOT be fixed).
    - 04-PATTERNS.md "No Analog Found → render-smoke" (Chrome via CLI, no puppeteer).
  </read_first>
  <action>Author two Node-core-only (zero-dep) harnesses used by every later wave's verify:

  (a) `scripts/tests/render-smoke.mjs` — takes a page-glob or a list (default: `lessons/*.html` + `reviews/*.html`); for each, spawns system Chrome `"/Applications/Google Chrome.app/Contents/MacOS/Google Chrome" --headless --disable-gpu --enable-logging=stderr --v=1 --virtual-time-budget=4000 --dump-dom "file://<abs>"`, captures stderr + the dumped DOM, and FAILS (non-zero exit) if stderr contains `Uncaught`, `AW is not defined`, or `SEVERE` for that page, or if the dumped DOM lacks the expected root node (`class="reg-page"` or `class="reg-orbit"`). Prints `SMOKE OK <page>` / `SMOKE FAIL <page> <reason>`. When no pages match (dirs empty at Wave 1), exit 0 with `no pages yet` — it becomes a real gate the moment pages exist (u1-m1 at 04-03).

  (b) `scripts/port-audit.mjs` — the content port gate helper. For each ported page in `lessons/`+`reviews/`, locate its `AwbaLesson({...})` / `AwbaReview({...})` cfg region and SHA-256 it, then SHA-256 the byte-identical cfg region in the matching `/Users/theshumba/Downloads/AWBA APP/_MVP-BUILD/{lessons,reviews}/<same-name>.html` and assert equality (byte-fidelity, CNT-01/D-49); print `BYTES OK <page>` / `BYTES DRIFT <page>`. Then run the grep gates: `! grep -rq 'fonts.googleapis' lessons/ reviews/` (no CDN survived), `! grep -rqiE 'confetti|class="perfect"|class="combo"|poppins' lessons/ reviews/` (no retired element), and record the holds pass (U4-03: assert NO `u4-m4`/`u4-03` file exists; U3 present as u3-m1/m2/m3; print the 3 expected validator `note:` warnings — u3-m1 unused ref baqarah-2-163, u3-m3 unused ref imran-3-19, u4-m2 unused term rububiyah — as ACCEPTED, never to be "fixed"). Exit non-zero on any byte-drift / CDN / retired hit. When dirs are empty, exit 0 with `no pages yet`.

  Neither harness is a `*.test.js` file — they are explicit per-wave tools, so the always-green `node --test` suite is untouched.</action>
  <verify>
    <automated>test -f scripts/tests/render-smoke.mjs && test -f scripts/port-audit.mjs && node scripts/tests/render-smoke.mjs 2>&1 | grep -qiE 'no pages yet|SMOKE OK' && node scripts/port-audit.mjs 2>&1 | grep -qiE 'no pages yet|BYTES OK' && node --test scripts/tests/*.test.js 2>&1 | grep -qE 'fail 0'</automated>
  </verify>
  <acceptance_criteria>
    - Both harnesses exist, run to a clean exit against empty `lessons/`/`reviews/` (`no pages yet`), and are zero-dependency (Node core + system Chrome CLI only).
    - render-smoke asserts zero console errors + a register root node per page; port-audit asserts cfg byte-fidelity vs `_MVP-BUILD` + the CDN/retired/holds greps.
    - The `node --test` suite is unaffected (`fail 0`).
  </acceptance_criteria>
  <done>The Wave-0 harnesses exist and are ready to turn green as the walking slices (04-03 u1-m1, 04-05 u1-review) and the full port (04-06) land.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cfg author-content → engine | scripture/beat strings are trusted authored files (Josh's redacted Gen-3), not runtime user input |
| audio file path → Audio() | `../shared/sfx/{cue}.mp3` is a fixed page-relative path, `cue` is an internal enum, never user-supplied |
| dev tooling → filesystem | port-audit/render-smoke read the repo + `_MVP-BUILD` source; no writes to source, no network |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01a | Tampering | AW.sound path | mitigate | `cue` is an internal enum concatenated into a constant relative path; no user input reaches `new Audio()`; failure is caught silently |
| T-04-01b | Information disclosure | CDN/font leakage | mitigate | port-audit's `fonts.googleapis` grep gate proves no third-party request survives the port (zero-CDN / offline promise) |
| T-04-01c | Tampering | content byte-fidelity | mitigate | port-audit SHA-compares each spliced cfg region vs `_MVP-BUILD` — any silent content drift (incl. the 100 `˹ ˺` brackets) fails the gate |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | zero packages installed (zero-dep static app; no `package.json`/`node_modules`); no `[ASSUMED]`/`[SUS]` package to gate |
</threat_model>

<verification>
- `node --test scripts/tests/*.test.js` → `fail 0`, `tests >= 64`.
- `runner-math.test.js` pins every ENG-03/ENG-04 number; `AW.sound` proven silent-plumbing; both harnesses run clean against empty content dirs.
- `grep -c 'localStorage' shared/awba-engine.js` === 13 (no new storage seam); `@layer` order line untouched (no CSS in this plan).
</verification>

<success_criteria>
- ENG-03/ENG-04 math is a set of pure, tested `AW.*` contracts the runners will call (interface-first).
- MOT-05 sound plumbing ships silent (D-52); the Wave-0 validation gaps in 04-VALIDATION are closed.
</success_criteria>

<output>
Create `.planning/phases/04-lesson-review-engine-port-detail-layer/04-01-SUMMARY.md` when done.
</output>
