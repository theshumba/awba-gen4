---
phase: 04-lesson-review-engine-port-detail-layer
plan: 03
type: execute
wave: 3
executor_hint: opus
depends_on: ["04-01", "04-02"]
files_modified:
  - shared/awba-engine.js
  - lessons/u1-m1.html
autonomous: true
requirements: [ENG-01, ENG-03, ENG-05, CNT-01, CNT-04, MOT-05]
must_haves:
  truths:
    - "Per ENG-01/D-46: AwbaLesson(cfg) accepts Josh's Gen-3 cfg shape byte-unchanged and renders all 9 beat types (read, frame, verse, panel×4 variants, depth, reflect, mc, tf, tile) consuming ONLY shipped AW.* primitives + the @layer screens classes — no new tokens, no layer-order change"
    - "Per ENG-03: quiz mechanics match Gen-3 exactly via the pure helpers — +12 noor/correct (AW.PER_LESSON), +15 reflect (AW.REFLECT), combo accrual at 2+ (AW.comboShow) as an accruing gold .dab on the META surface, 3-streak (AW.comboPerfect) as a quiet gold .thread flourish once per streak, stars via AW.lessonStars (3/2/1 never 0), best-of persisted never downgraded"
    - "Per D-45 law-8: a wrong answer is a grey ink-blot (.opt.wrong) + a one-line it.gentle explanation (.opt-why) + a --rose-framed retry, title 'Nothing lost' — NEVER red, never shake, never amber, never the retired PERFECT/combo-pill/companion"
    - "Per CNT-04/D-50: every Arabic span carries lang='ar' dir='rtl'; verse beats render as scripture law (Amiri Quran, --go:0, nothing celebratory adjacent); the icon re-map is applied at the renderer (quran→quran-stand, beads, dua, starpat→pattern, crescent, AW.UNIT_ICON) — cfg names stay byte-identical"
    - "Per CNT-01/D-49: lessons/u1-m1.html renders end-to-end — its AwbaLesson({...}) cfg is byte-spliced from _MVP-BUILD (SHA-verified, the 100 ˹ ˺ brackets intact), the Google Fonts CDN link stripped, ../shared paths kept — and loads over file:// with ZERO console errors (render-smoke GREEN)"
    - "The lesson completes: pos/stepIndex flow (back decrements pos, a filled progress dab NEVER un-fills), opener greet via AW.greetMode with AW.touchDay on 'Begin, gently', a plain verdict→noor→returns→done terminus that persists noor once + best-of stars (the WAAPI/Ring/du'a choreography is layered on in 04-04); AW.sound + a 44px mute toggle wired to awba_prefs.soundMuted (MOT-05)"
  artifacts:
    - path: "shared/awba-engine.js"
      provides: "AwbaLesson(cfg) runner in the RUNNERS section"
      contains: "function AwbaLesson"
    - path: "lessons/u1-m1.html"
      provides: "the first ported lesson — the walking slice"
      contains: "AwbaLesson({"
  key_links:
    - from: "AwbaLesson resolve()/verdict()"
      to: "AW.lessonStars / AW.PER_LESSON / AW.comboShow (04-01)"
      via: "the runner calls the pure helpers, never re-derives the numbers"
      pattern: "AW\\.lessonStars|AW\\.comboShow"
    - from: "lessons/u1-m1.html"
      to: "shared/awba-engine.js AwbaLesson + shared/awba-engine.css @layer screens"
      via: "classic <script src=../shared/awba-engine.js> load order, then AwbaLesson(cfg)"
      pattern: "AwbaLesson\\("
---

## Phase Goal

**As a** learner, **I want to** walk each of Josh's 15 lessons and 4 reviews end-to-end — reading verbatim scripture and being quietly celebrated at the end — **so that** the whole Aqeedah course feels like a warm, world-class manuscript rather than a game show.

*(This is the WALKING SLICE: after this plan a learner can open `lessons/u1-m1.html` and walk it opener→beats→quiz→verdict→noor→returns→done. The reward choreography + Ring + du'a get layered on in 04-04; all 19 files land in 04-06.)*

<objective>
Port Gen-3's lesson runner into the empty RUNNERS section as `AwbaLesson(cfg)` — every mechanic byte-preserved, every visual re-voiced to Athar per D-45 — rendering all 9 beat types through the shipped `AW.*` primitives and the `@layer screens` classes (04-02). Then port the first data file, `u1-m1.html`, by byte-splicing its cfg into a fresh zero-CDN Athar shell, so one real lesson renders end-to-end with zero console errors.

Purpose: prove the compatible engine on a real file (ENG-01/03/05, CNT-01/04, MOT-05) — the thin end-to-end path the rest of the phase widens.
Output: `AwbaLesson(cfg)` in `shared/awba-engine.js`; `lessons/u1-m1.html`.

Executor routing: **opus + frontend-design skill** — lesson beat rendering is flagship. Preserve Gen-3 numbers exactly (call the 04-01 pure helpers); re-voice expression per D-45. Plan line refs go stale — locate Gen-3 logic by function name, not line number. This plan ships a PLAIN reward terminus (static verdict→noor→returns→done) so the lesson completes; 04-04 replaces it with the WAAPI choreography + Ring + du'a.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-lesson-review-engine-port-detail-layer/04-RESEARCH.md
@.planning/phases/04-lesson-review-engine-port-detail-layer/04-PATTERNS.md
@.planning/phases/04-lesson-review-engine-port-detail-layer/04-UI-SPEC.md

<interfaces>
<!-- BEHAVIOUR analog (preserve byte-for-byte): /Users/theshumba/Downloads/AWBA APP/_MVP-BUILD/shared/awba-engine.js -->
<!-- EXPRESSION analog (consume): in-repo shared/awba-engine.js AW.* + shared/awba-engine.css @layer screens -->

Gen-3 lesson runner (behaviour to port): AwbaLesson lines 115-333 —
- setup vars 125-128: `pos=-1, stepIndex=0, combo=0, comboBest=0, correct=0, mistakes=0, quizN, noorEarned=0`. DO NOT port `AW.skeleton()` (116) or `if(cfg.unitColor)…--blue` (123): build the Athar skeleton in `.reg-page`; `unitColor` stays an inert cfg field.
- flow: `pos` back-decrements; `stepIndex` only increments; back clamps `stepIndex=max(pos,0)` (a filled dab never un-fills); back hidden at `pos<0`.
- opener 149-162: `AW.greetMode()` → first/streak(ret>1 "N returns")/returning(copy override); `AW.touchDay()` on "Begin, gently".
- 9 renderers 170-234; resolve 274-287; starsFor 289 (→ use AW.lessonStars); reward 292-330 (verdict/rewardNoor[persist noor]/rewardReturns[weekCal]/done[best-of star persist]).

Shipped AW.* to consume (in-repo): `AW.icon(name)`, `AW.cite`, `AW.wire(root,cfg)` (binds .cite→sheetRef/.term→sheetTerm), `AW.sheetRef/sheetTerm` (face-split + pills — do NOT rebuild), `AW.greetMode/touchDay/weekCal/state/S`, `AW.reducedMotion`, `AW.prefs.get/set('soundMuted')`, `AW.sound(cue)` (04-01). Pure math: `AW.lessonStars, AW.PER_LESSON(12), AW.REFLECT(15), AW.comboShow, AW.comboPerfect` (04-01).

Icon re-map (Gen-3 names → gen-4 KIT; renderer hardcodes; cfg unchanged): `AW.ill('quran')→AW.icon('quran-stand')` (verse) · `beads`→`AW.icon('beads')` (depth) · `dua`→`AW.icon('dua')` (reflect) · `starpat`→`AW.icon('pattern')` (rewardNoor) · `crescent`→`AW.icon('crescent')` (done) · `AW.UNIT_ICON[u1..u4]` = compass/lanterns/kaaba/mosque (opener). GONE: `AW.ill`, `AW.STARG/STARE/BOLT/TARGET/CLOCK/HEART` — reward-stat marks re-chosen from GLYPHS (`spark`/`star`/`check`); miss uses the law-8 grey blot (no glyph). Marker labels (AW.MLAB): fact="Worth knowing", remember="Worth remembering", fard="The first duty", angle="Another angle".

Port recipe (04-PATTERNS §page shells): splice the `AwbaLesson({...})` cfg body BYTE-IDENTICAL from `_MVP-BUILD/lessons/u1-m1.html` into a fresh Athar `<head>` (charset, viewport, theme-color, self-hosted `../shared/fonts/*` preloads, `<link rel=stylesheet href="../shared/awba-engine.css">`, classic `<script src="../shared/awba-engine.js">`, NO googleapis link). The `../shared/…` paths are identical to Gen-3 — no path change. Record the cfg-region SHA (port-audit).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AwbaLesson skeleton + flow + opener + the 6 content beat renderers</name>
  <files>shared/awba-engine.js</files>
  <read_first>
    - Gen-3 `AwbaLesson` lines 115-223 (setup, opener, read/frame/verse/panel/depth/reflect renderers) + `AW.wire`/`AW.markerHtml` (103-108).
    - 04-UI-SPEC §S1 (shell), §S1-beats (per-beat table), §S2 (verse scripture law + depth accordion), §S3 (reflect).
    - `shared/awba-engine.css` @layer screens classes from 04-02 (`.stage/.ls-hud/.ls-prog/.scard/.pnl/.v-*/.lacc/.reflect`) + shipped `AW.icon/wire/greetMode/touchDay`.
  </read_first>
  <behavior>
    - AwbaLesson(cfg) with a stub 1-beat cfg builds a `.reg-page` skeleton with a `#root`, a `.ls-prog` dab per beat, and an opener; no console error.
    - Opener with a fresh-install state shows the AW.greetMode 'first' greeting; clicking "Begin, gently" calls AW.touchDay() exactly once and advances to beat 0.
    - A `verse` beat renders `.scard` with `--go:0`, `.ayah lang="ar" dir="rtl"` (Amiri Quran), `.trans`, `.tsrc` — and no `.dab/.thread/.plate/.rosette` in that panel.
    - A `depth` beat renders the 3-lens `.lacc` accordion in fixed order (Reality/Revelation/Ruling); each `.lh` expands its own `.lb`; AW.wire runs on an opened lens so its `.cite` is tappable; Continue is present regardless.
    - A `reflect` beat: first "Show a reflection" tap reveals `it.model` and adds AW.REFLECT (15) noor; button relabels to "Continue"; the textarea value is never read back into the DOM or stored.
    - back decrements pos but `stepIndex` never decreases (a filled dab stays filled); back is hidden at the opener.
  </behavior>
  <action>Author `function AwbaLesson(cfg){…}` in the RUNNERS section. Port the Gen-3 setup vars + flow verbatim EXCEPT: build the Athar skeleton in `.reg-page` (not `AW.skeleton()`), and drop the `unitColor→--blue` line (inert field). Render the 6 content beats using the 04-02 classes and the icon re-map: `read` (body + `.marker` chip via AW.MLAB labels + AW.wire), `frame` (centered `--fs-h2`), `verse` (`.scard --go:0`, Amiri-Quran `.ayah lang/dir`, `.trans` carrying `˹ ˺`, `.tsrc` "Translation of the meaning: The Clear Quran, Dr. Mustafa Khattab · pending review"; wrap the illustration with `AW.icon('quran-stand')`), `panel` (all 4 variants `.v-check/.v-guard/.v-pull/.v-tell`, numbered items, marker), `depth` (`.lacc` fixed-order lenses Reality/Revelation/Ruling — Revelation's ayah under scripture law; `AW.icon('beads')`; AW.wire the opened `.lb`), `reflect` (`AW.icon('dua')`, private textarea NEVER persisted/re-rendered, first tap → reveal `it.model` + `noorEarned+=AW.REFLECT` + relabel Continue). Opener uses `AW.greetMode()`/`AW.UNIT_ICON[(cfg.id||'').slice(0,2)]` + `AW.touchDay()` on Begin. Wire the beat flow (`pos`/`stepIndex`/back-clamp). No `localStorage` outside AW.S/AW.prefs (count stays 13). Add a `runner-lesson.test.js` (harness = ls-stub `loadEngine`) asserting the behavior block via DOM-free probes where possible + a jsdom-free string-shape check that a `verse` renderer output contains `class="scard"` + `--go:0` + `lang="ar"` and NO `class="dab"`.</action>
  <verify>
    <automated>grep -q 'function AwbaLesson' shared/awba-engine.js && test "$(grep -c 'localStorage' shared/awba-engine.js)" -eq 13 && test "$(grep -c '@layer tokens, base, components, screens, motion;' shared/awba-engine.css)" -eq 1 && ! grep -qiE "AW\.skeleton|AW\.ill\(|AW\.STARG|AW\.confetti" shared/awba-engine.js && node --test scripts/tests/*.test.js 2>&1 | grep -qE 'fail 0'</automated>
  </verify>
  <acceptance_criteria>
    - `AwbaLesson(cfg)` exists; the 6 content renderers emit the 04-02 classes; verse is scripture-law (`.scard --go:0`, Amiri-Quran, lang/dir, no celebration class).
    - depth is a fixed-order shape+label accordion that never blocks Continue; reflect adds 15 noor once and keeps the textarea inert.
    - No `AW.skeleton`/`AW.ill`/`AW.STARG`/`AW.confetti` reintroduced; `localStorage` count == 13; suite `fail 0`.
  </acceptance_criteria>
  <done>The content beats render as manuscript through the compatible engine, scripture under law.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Quiz beats (mc/tf/tile) + resolve() re-voiced + a plain reward terminus + mute toggle</name>
  <files>shared/awba-engine.js</files>
  <read_first>
    - Gen-3 `bindChoice`/`renderTile`/`resolve`/`starsFor`/`verdict`/`rewardNoor`/`rewardReturns`/`done` (lines 225-330).
    - 04-UI-SPEC §S3 (quiz states, combo, streak, law-8 wrong-answer) + §S4 (the reward screen copy contract) + §S6 (mute toggle: 44px, aria-pressed + aria-label swap).
    - 04-01 helpers `AW.lessonStars/PER_LESSON/comboShow/comboPerfect`; shipped `.opt.correct/.opt.wrong/.opt-why/.btn.retry/.dab/.thread`, `AW.weekCal`, `AW.sound`.
  </read_first>
  <behavior>
    - Correct mc/tf/tile: `correct++`, `combo++`, `noorEarned += AW.PER_LESSON (12)`; when `AW.comboShow(combo)` an accruing gold `.dab` shows on the META (foot/result) surface; when `AW.comboPerfect(combo)` a quiet `.thread` gold flourish fires once (Gen-3 260ms delay) with praise copy — NO PERFECT overlay; praise rotates by `correct % 4` over ['That's it.','Beautiful.','Exactly right.','Masha'Allah.']; body = it.good; AW.sound('correct').
    - Wrong: `mistakes++`, `combo=0`; the chosen `.opt` gets `.wrong` (grey ink-blot), a one-line `.opt-why` = it.gentle appears, the retry is `--rose`-framed; title "Nothing lost"; NO red/shake/amber; AW.sound('incorrect').
    - tile: built order === it.solution (subset-of-bank build); correct/wrong via the same law.
    - Stars: `AW.lessonStars(mistakes)` (3/2/1, never 0); at done, best-of persist only upgrades (never downgrades) via AW.S.
    - Reward terminus (PLAIN, no WAAPI yet): verdict word (Flawless/Beautifully done/You made it through) + 3 stat tiles (Noor +n / Accuracy % / Best run n×, marks spark/star) → noor screen (persists `AW.S.set('noor', AW.S.get('noor',0)+noorEarned)` exactly once) → returns screen (AW.weekCal, never-miss) → done (best-of star persist, recap, next handoff). AW.sound('complete') at the noor/complete moment.
    - Mute toggle: a 44px HUD button, `aria-pressed` + `aria-label` swapped ("Mute sounds"/"Unmute sounds"), flips `AW.prefs.set('soundMuted', …)`, reflects the boot-stamped state.
  </behavior>
  <action>Author `bindChoice`/`renderTile`/`resolve` porting Gen-3 numbers exactly but re-voiced per D-45: combo → accruing gold `.dab` on the META surface (never a floating pill, never mid-scripture), 3-streak → quiet `.thread` flourish (once, `setTimeout(...,260)`), miss → law-8 `.opt.wrong` grey ink-blot + `.opt-why`(it.gentle) + `.btn.retry`(--rose), title "Nothing lost". Call `AW.lessonStars`/`AW.PER_LESSON`/`AW.comboShow`/`AW.comboPerfect` — do not inline the math. Add `AW.sound('correct'|'incorrect')`. Then author a PLAIN reward terminus (`verdict`/`rewardNoor`/`rewardReturns`/`done`) that renders the 04-02 `.rw-*`/`.noorbig`/`.weekcal`/`.recl` surfaces statically (no WAAPI, no Ring, no du'a — those are 04-04), persisting noor once at the noor moment and best-of stars at done (never downgrade), with `AW.sound('complete')`. Wire the 44px mute toggle into the HUD per §S6. Extend `runner-lesson.test.js` to assert: `resolve` correct adds exactly 12; miss zeroes combo; `AW.lessonStars` path; noor persisted exactly once (seed a Map-LS, complete a stub lesson via a probe, assert `awba_noor` incremented by noorEarned and not doubled). Reintroduce NOTHING retired (no confetti/PERFECT/combo-pill/companion/amber).</action>
  <verify>
    <automated>grep -qE 'AW\.lessonStars|AW\.comboShow|AW\.comboPerfect' shared/awba-engine.js && grep -q "AW.sound('correct')" shared/awba-engine.js && grep -q "AW.prefs.set('soundMuted'" shared/awba-engine.js && ! grep -qiE "confetti|>PERFECT<|class=\"combo\"|AW\.LANTERN|amber" shared/awba-engine.js && test "$(grep -c 'localStorage' shared/awba-engine.js)" -eq 13 && node --test scripts/tests/*.test.js 2>&1 | grep -qE 'fail 0'</automated>
  </verify>
  <acceptance_criteria>
    - Quiz math is exact (12/correct, stars 3/2/1 never 0) and comes from the 04-01 helpers; combo/streak re-voiced to `.dab`/`.thread` on the META surface; wrong-answer is law-8 (grey ink-blot + gentle line + rose retry), never red/amber.
    - The plain reward terminus completes and persists noor exactly once + best-of stars (never downgrade); the 44px mute toggle flips `awba_prefs.soundMuted` with correct aria.
    - No retired element reintroduced; `localStorage` count == 13; suite `fail 0`.
  </acceptance_criteria>
  <done>Quizzes resolve with exact Gen-3 numbers in Athar language and a lesson reaches a completed 'done' state.</done>
</task>

<task type="auto">
  <name>Task 3: Port lessons/u1-m1.html (byte-splice cfg) → one lesson renders end-to-end</name>
  <files>lessons/u1-m1.html</files>
  <read_first>
    - `/Users/theshumba/Downloads/AWBA APP/_MVP-BUILD/lessons/u1-m1.html` (the whole file — the cfg body is lines 6-55; it exercises read×2, depth, reflect, mc, panel(check), tf).
    - `preview.html` `<head>` (self-hosted font preloads, one stylesheet, classic engine script, zero CDN) — the shell analog, adjusted to `../shared/…` paths.
    - `scripts/port-audit.mjs` (04-01) — the byte-fidelity + CDN + retired gate to run.
  </read_first>
  <action>Create `lessons/u1-m1.html`: a fresh Athar page shell (`<!DOCTYPE html>`, charset, viewport, `<title>`, theme-color, self-hosted `../shared/fonts/readex-pro-400.woff2` + `../shared/fonts/amiri-quran-400.woff2` preloads, `<link rel="stylesheet" href="../shared/awba-engine.css">`, classic `<script src="../shared/awba-engine.js"></script>`, then `<script>AwbaLesson({…})</script>`) with the `AwbaLesson({...})` cfg body BYTE-IDENTICAL to `_MVP-BUILD/lessons/u1-m1.html` (including the parse-time `AW.cite(...)` calls inside the depth/panel strings, the 100-bracket scripture, `id:'u1m1'`, `unitColor` left inert). STRIP the `<link href="https://fonts.googleapis.com/...">` CDN line (Pitfall 2 — it references retired Poppins + violates zero-CDN). Do NOT whole-file-copy. Verify: `node scripts/validate-content.js lessons/u1-m1.html` → exit 0; `node scripts/port-audit.mjs` → `BYTES OK lessons/u1-m1.html` (cfg region SHA === source); `node scripts/tests/render-smoke.mjs lessons/u1-m1.html` → `SMOKE OK` (zero console errors, `.reg-page` root present); record the cfg SHA in the SUMMARY.</action>
  <verify>
    <automated>test -f lessons/u1-m1.html && ! grep -q 'fonts.googleapis' lessons/u1-m1.html && grep -q '../shared/awba-engine.js' lessons/u1-m1.html && node scripts/validate-content.js lessons/u1-m1.html 2>&1 | grep -qiE 'OK|exit 0|0 errors' && node scripts/port-audit.mjs 2>&1 | grep -q 'BYTES OK' && node scripts/tests/render-smoke.mjs lessons/u1-m1.html 2>&1 | grep -q 'SMOKE OK'</automated>
  </verify>
  <acceptance_criteria>
    - `lessons/u1-m1.html` exists, cfg byte-identical to source (port-audit `BYTES OK`, 100 `˹ ˺` brackets intact), CDN link stripped, `../shared/` paths kept.
    - `node scripts/validate-content.js lessons/u1-m1.html` exits 0; render-smoke `SMOKE OK` (zero console errors).
    - A learner can open the page over file:// and walk opener→beats→quiz→verdict→noor→returns→done.
  </acceptance_criteria>
  <done>The walking slice is real: one Gen-3 lesson renders verbatim, end-to-end, zero-CDN, through the Athar engine.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cfg author-content → DOM (string concat) | Josh's scripture/beat strings are trusted authored files, not runtime user input (Gen-3 parity) |
| reflect `<textarea>` → app | the ONE user-input surface — deliberately inert (never persisted, never re-rendered) |
| lesson page → shared engine | classic `<script>` load order (engine first) — a wrong type/order throws `AW is not defined` |
| progress writes → localStorage | only via `AW.S`/`AW.touchDay` (D-24) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03a | Tampering (XSS) | reflect textarea | mitigate | the textarea value is never inserted into the DOM or stored — no reflected/stored XSS path; keep it inert |
| T-04-03b | Tampering | innerHTML from cfg | accept | cfg content is author-controlled (scholar-gated files), not arbitrary user input (RESEARCH Security Domain, T-03-03 accept) |
| T-04-03c | Information disclosure | CDN/font leakage on the ported page | mitigate | CDN `<link>` stripped on port; grep gate + port-audit assert no `fonts.googleapis` survives |
| T-04-03d | Tampering | content byte-fidelity | mitigate | port-audit SHA-compares the cfg region vs `_MVP-BUILD`; validator exit 0; the 100 `˹ ˺` brackets survive byte-identical |
| T-04-03e | Tampering | storage seam | mitigate | grep gate pins `localStorage` count at 13 — the runner adds no new storage access outside `AW.S`/`AW.prefs` |
| T-04-SC | Tampering | npm installs | accept | zero packages; no `[ASSUMED]`/`[SUS]` to gate |
</threat_model>

<verification>
- `AwbaLesson(cfg)` renders all 9 beat types via shipped primitives; quiz numbers exact via 04-01 helpers; wrong-answer law-8, never red/amber; no retired element.
- `lessons/u1-m1.html`: validator exit 0, port-audit `BYTES OK`, render-smoke `SMOKE OK` (zero console errors), CDN stripped.
- `localStorage` count == 13; `@layer` order line == 1; `node --test scripts/tests/*.test.js` → `fail 0`.
</verification>

<success_criteria>
- ENG-01/ENG-03/ENG-05 + CNT-01/CNT-04 + MOT-05 hold on a real file: one Gen-3 lesson renders verbatim, end-to-end, in Athar language, zero-CDN.
- The thin end-to-end path exists for 04-04 (choreography) and 04-06 (all 19) to widen.
</success_criteria>

<output>
Create `.planning/phases/04-lesson-review-engine-port-detail-layer/04-03-SUMMARY.md` when done.
</output>
