---
phase: 04-lesson-review-engine-port-detail-layer
plan: 06
type: execute
wave: 6
executor_hint: sonnet
depends_on: ["04-05"]
files_modified:
  - lessons/u1-m2.html
  - lessons/u1-m3.html
  - lessons/u1-m4.html
  - lessons/u2-m1.html
  - lessons/u2-m2.html
  - lessons/u2-m3.html
  - lessons/u2-m3b.html
  - lessons/u3-m1.html
  - lessons/u3-m2.html
  - lessons/u3-m3.html
  - lessons/u4-m1.html
  - lessons/u4-m2.html
  - lessons/u4-m2b.html
  - lessons/u4-m3.html
  - reviews/u2-review.html
  - reviews/u3-review.html
  - reviews/u4-review.html
autonomous: true
requirements: [CNT-01, CNT-02, CNT-04]
must_haves:
  truths:
    - "Per CNT-01/D-49: all 19 data files exist (u1-m1 + u1-review from the slices + these 14 lessons + 3 reviews), each with its AwbaLesson/AwbaReview cfg byte-spliced from _MVP-BUILD (port-audit BYTES OK for every file), the Google Fonts CDN link stripped, ../shared paths kept — never regenerated, never 'fixed'"
    - "Per CNT-01/ENG-07: node scripts/validate-content.js over all 15 lessons + 4 reviews returns exit 0, with EXACTLY the 3 expected non-blocking note: warnings (u3-m1 unused ref baqarah-2-163, u3-m3 unused ref imran-3-19, u4-m2 unused term rububiyah) which are documented as ACCEPTED and MUST NOT be fixed (fixing them alters content, D-49)"
    - "Per CNT-02/D-49: the sensitive holds are verified present by an explicit grep/diff pass and recorded — U4-03 absent entirely (no u4-m4/u4-03 file: u4-* = u4-m1/u4-m2/u4-m2b/u4-m3), U3-13 not surfaced, U3-16 principle-only, group-namings held — the diff confirms NO new content appears vs source"
    - "Per CNT-04/D-50: every ported page carries its scripture under the render laws (verse→Amiri Quran, lang/dir, ˹ ˺ brackets byte-intact) because the runners already enforce it; every one of the 9 beat types + all 4 panel variants + all 3 lenses + all marker types is exercised across the corpus and renders without console error"
    - "All 19 pages load over file:// with ZERO console errors (render-smoke over the whole corpus GREEN, covering ENG-01/ENG-02 render + MOT-05 silent-sfx + the load-order pitfall); no CDN + no retired element survives anywhere in lessons/ reviews/"
  artifacts:
    - path: "lessons/"
      provides: "all 15 ported lesson pages"
    - path: "reviews/"
      provides: "all 4 ported review pages"
  key_links:
    - from: "each ported page"
      to: "shared/awba-engine.js (AwbaLesson/AwbaReview) + shared/awba-engine.css"
      via: "classic ../shared engine include + cfg call"
      pattern: "Awba(Lesson|Review)\\("
---

## Phase Goal

**As a** learner, **I want to** walk each of Josh's 15 lessons and 4 reviews end-to-end — reading verbatim scripture and being quietly celebrated at the end — **so that** the whole Aqeedah course feels like a warm, world-class manuscript rather than a game show.

*(The two runners are proven on one file each; this plan widens the slice to the WHOLE corpus — all 19 files — and records the sensitive holds. Mechanical, high-volume, byte-faithful.)*

<objective>
Port the remaining 14 lessons and 3 reviews by byte-splicing each Gen-3 cfg into a fresh zero-CDN Athar shell (same recipe proven on u1-m1 / u1-review), then run the full port gate: the validator over all 19 (exit 0, exactly the 3 expected warnings), the holds grep/diff pass recorded, and render-smoke over the whole corpus (zero console errors). Content is Josh's asset — spliced verbatim, never regenerated, never "fixed".

Purpose: CNT-01/CNT-02/CNT-04 at full corpus scale — every beat type exercised, every hold verified present.
Output: 14 `lessons/*.html` + 3 `reviews/*.html`.

Executor routing: **sonnet** (mechanical content splicing × 17). Verbatim-content law: splice, never retype; SHA-verify each cfg region. Do NOT resolve the 3 expected validator warnings — they are hold-consistent (D-49).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-lesson-review-engine-port-detail-layer/04-RESEARCH.md
@.planning/phases/04-lesson-review-engine-port-detail-layer/04-PATTERNS.md

<interfaces>
<!-- The recipe and the corpus — extracted from research + source listing. -->

Source corpus (`/Users/theshumba/Downloads/AWBA APP/_MVP-BUILD/`):
- lessons/ (15): u1-m1(done), u1-m2, u1-m3, u1-m4, u2-m1, u2-m2, u2-m3, u2-m3b, u3-m1, u3-m2, u3-m3, u4-m1, u4-m2, u4-m2b, u4-m3.
- reviews/ (4): u1-review(done), u2-review, u3-review, u4-review.
- THIS plan ports the 14 + 3 not covered by the walking slices.

Port recipe (proven at 04-03/04-05): fresh Athar `<head>` (charset, viewport, `<title>`, theme-color, self-hosted `../shared/fonts/*` preloads, `<link rel="stylesheet" href="../shared/awba-engine.css">`, classic `<script src="../shared/awba-engine.js">`), then the `AwbaLesson({...})` / `AwbaReview({...})` cfg body BYTE-IDENTICAL from source (including parse-time `AW.cite(...)` calls, the `˹ ˺` brackets, `unitColor` left inert). STRIP the `<link href="https://fonts.googleapis.com/...">` CDN line. Keep `../shared/` paths (identical to Gen-3). NEVER whole-file copy.

Gates (04-01 harnesses): `node scripts/validate-content.js` (all 19, exit 0), `node scripts/port-audit.mjs` (BYTES OK per file + holds + CDN + retired), `node scripts/tests/render-smoke.mjs` (whole corpus, zero console errors).

Expected validator warnings (ACCEPTED, DO NOT FIX — D-49): u3-m1 unused ref `baqarah-2-163`, u3-m3 unused ref `imran-3-19`, u4-m2 unused term `rububiyah`.
Holds (CNT-02): U4-03 absent (no u4-m4/u4-03 file), U3-13 not surfaced, U3-16 principle-only, group-namings held — verify by diff (no new content vs source), record in SUMMARY.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Splice the 14 remaining lessons into Athar shells (byte-verbatim, CDN stripped)</name>
  <files>lessons/u1-m2.html, lessons/u1-m3.html, lessons/u1-m4.html, lessons/u2-m1.html, lessons/u2-m2.html, lessons/u2-m3.html, lessons/u2-m3b.html, lessons/u3-m1.html, lessons/u3-m2.html, lessons/u3-m3.html, lessons/u4-m1.html, lessons/u4-m2.html, lessons/u4-m2b.html, lessons/u4-m3.html</files>
  <read_first>
    - 04-PATTERNS §"page shells" (the splice recipe + `../shared` path note + the byte-fidelity record) and the completed `lessons/u1-m1.html` as the shell precedent.
    - For each source file, its `AwbaLesson({...})` cfg body in `/Users/theshumba/Downloads/AWBA APP/_MVP-BUILD/lessons/<name>.html`.
  </read_first>
  <action>For each of the 14 remaining lesson names, create `lessons/<name>.html` by splicing the source file's `AwbaLesson({...})` cfg body BYTE-IDENTICAL into the same fresh Athar shell used for u1-m1 (self-hosted `../shared/fonts/*` preloads, one stylesheet, classic engine script, page-specific `<title>`). STRIP the `fonts.googleapis` CDN `<link>` on every file. Keep `../shared/` paths. Do NOT retype or "correct" any scripture, marker, or cfg field — splice only (the corpus exercises verse, frame, tile, and all 4 panel variants + all 3 lenses + all marker types not present in u1-m1). After all 14, run the byte-fidelity + validator gate.</action>
  <verify>
    <automated>test "$(ls lessons/*.html | wc -l | tr -d ' ')" -eq 15 && ! grep -rq 'fonts.googleapis' lessons/ && OUT=$(node scripts/validate-content.js lessons/*.html 2>&1) && ! echo "$OUT" | grep -q 'amber:' && node scripts/port-audit.mjs 2>&1 | grep -q 'BYTES OK' && ! node scripts/port-audit.mjs 2>&1 | grep -q 'BYTES DRIFT'</automated>
  </verify>
  <acceptance_criteria>
    - All 15 lesson pages exist; no `fonts.googleapis` anywhere in `lessons/`.
    - Every lesson's cfg is byte-identical to source (port-audit `BYTES OK`), validator reports no errors (the 3 expected `note:` warnings are non-blocking).
  </acceptance_criteria>
  <done>All 15 lessons are ported verbatim, zero-CDN, ready to render.</done>
</task>

<task type="auto">
  <name>Task 2: Splice the 3 remaining reviews into Athar shells (byte-verbatim, CDN stripped)</name>
  <files>reviews/u2-review.html, reviews/u3-review.html, reviews/u4-review.html</files>
  <read_first>
    - The completed `reviews/u1-review.html` (04-05) as the shell precedent.
    - Each source `AwbaReview({...})` cfg in `/Users/theshumba/Downloads/AWBA APP/_MVP-BUILD/reviews/<name>.html` (u2-review has 3 "Name it" quotes + 1 tf; u4-review has 2 tf).
  </read_first>
  <action>For u2-review, u3-review, u4-review, create `reviews/<name>.html` by splicing the source `AwbaReview({...})` cfg BYTE-IDENTICAL into the fresh Athar review shell (from u1-review). STRIP the CDN `<link>`; keep `../shared/` paths; page-specific `<title>`. Splice only — never retype. Run the byte-fidelity + validator gate.</action>
  <verify>
    <automated>test "$(ls reviews/*.html | wc -l | tr -d ' ')" -eq 4 && ! grep -rq 'fonts.googleapis' reviews/ && OUT=$(node scripts/validate-content.js reviews/*.html 2>&1) && ! echo "$OUT" | grep -q 'amber:' && node scripts/port-audit.mjs 2>&1 | grep -q 'BYTES OK' && ! node scripts/port-audit.mjs 2>&1 | grep -q 'BYTES DRIFT'</automated>
  </verify>
  <acceptance_criteria>
    - All 4 review pages exist; no `fonts.googleapis` anywhere in `reviews/`.
    - Every review's cfg is byte-identical to source (port-audit `BYTES OK`); validator reports no errors.
  </acceptance_criteria>
  <done>All 4 reviews are ported verbatim, zero-CDN.</done>
</task>

<task type="auto">
  <name>Task 3: Full port gate — validate 19, holds grep/diff pass, whole-corpus render-smoke</name>
  <files></files>
  <read_first>
    - 04-RESEARCH "Content Port — the 19-file inventory + gate" (the exit-0 result + the 3 expected warnings) + "Sensitive holds (CNT-02)".
    - `scripts/port-audit.mjs` + `scripts/tests/render-smoke.mjs` (04-01).
  </read_first>
  <action>Run and RECORD (in the SUMMARY) the full port gate across all 19 files: (1) `node scripts/validate-content.js lessons/*.html reviews/*.html` → exit 0 with EXACTLY the 3 expected `note:` warnings (u3-m1/u3-m3 unused ref, u4-m2 unused term) — documented as ACCEPTED, never to be "fixed"; (2) the holds grep/diff pass — assert U4-03 absent (`ls lessons/ | grep -E 'u4-m4|u4-03'` returns nothing), U3 present as u3-m1/u3-m2/u3-m3, and a diff confirming no new content appears vs source (the port added no scripture/atoms); record U3-13 not surfaced + U3-16 principle-only + group-namings held; (3) `! grep -rq 'fonts.googleapis' lessons/ reviews/` (no CDN) + `! grep -rqiE 'confetti|class="perfect"|class="combo"|poppins' lessons/ reviews/` (no retired element); (4) `node scripts/tests/render-smoke.mjs` over the whole corpus → every page `SMOKE OK` (zero console errors — covers ENG-01/02 render, MOT-05 silent-sfx, the load-order pitfall). This task edits no content — it is the recorded gate. (The `lessons/u1-m1.html` file entry is the touch-anchor for the SUMMARY's holds record; do not modify it.)</action>
  <verify>
    <automated>OUT=$(node scripts/validate-content.js lessons/*.html reviews/*.html 2>&1) && ! echo "$OUT" | grep -q 'amber:' && ! ls lessons/ | grep -qE 'u4-m4|u4-03' && ! grep -rq 'fonts.googleapis' lessons/ reviews/ && ! grep -rqiE 'confetti|class="perfect"|class="combo"|poppins' lessons/ reviews/ && node scripts/tests/render-smoke.mjs 2>&1 | grep -q 'SMOKE OK' && ! node scripts/tests/render-smoke.mjs 2>&1 | grep -q 'SMOKE FAIL' && node --test scripts/tests/*.test.js 2>&1 | grep -qE 'fail 0'</automated>
  </verify>
  <acceptance_criteria>
    - Validator over all 19 exits 0 with exactly the 3 expected warnings, documented ACCEPTED (not fixed).
    - Holds recorded: U4-03 absent (no u4-m4/u4-03), U3-13 not surfaced, U3-16 principle-only, group-namings held; diff confirms no new content vs source.
    - No CDN / no retired element anywhere in `lessons/`+`reviews/`; render-smoke over all 19 → every page `SMOKE OK`; suite `fail 0`.
  </acceptance_criteria>
  <done>All 19 files render verbatim end-to-end, zero-CDN, with the sensitive holds verified and recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source cfg (author) → ported page | scholar-gated authored content; the holds live in Josh's omissions — never add, never "fix" |
| ported page → shared engine | classic `../shared` load order (engine first) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06a | Tampering | content integrity / holds | mitigate | port-audit SHA-compares every cfg region vs source; a diff/grep pass confirms no new content; U4-03 absence asserted; the 3 warnings are documented, not resolved |
| T-04-06b | Information disclosure | CDN leakage across 19 pages | mitigate | recursive `fonts.googleapis` grep gate over `lessons/`+`reviews/`; every CDN `<link>` stripped on port |
| T-04-06c | Tampering | retired element reintroduction | mitigate | recursive grep gate (confetti/perfect/combo/poppins) over the corpus |
| T-04-06d | Denial of service (render) | load-order / silent-sfx | mitigate | whole-corpus render-smoke asserts zero console errors on every page (covers the `AW is not defined` pitfall + MOT-05 missing-sfx no-op) |
| T-04-SC | Tampering | npm installs | accept | zero packages; pure content splicing |
</threat_model>

<verification>
- All 19 files ported byte-verbatim (port-audit `BYTES OK`), CDN stripped, `../shared` paths kept.
- Validator over all 19 → exit 0, exactly the 3 expected warnings (accepted); holds verified + recorded (U4-03 absent, U3-13/U3-16, group-namings).
- No CDN / no retired element in `lessons/`+`reviews/`; render-smoke whole corpus every page `SMOKE OK`; `node --test scripts/tests/*.test.js` → `fail 0`.
</verification>

<success_criteria>
- CNT-01/CNT-02/CNT-04 hold at full corpus scale: all 19 render verbatim, holds intact and recorded, every beat type exercised.
- The phase is content-complete, ready for the final gate.
</success_criteria>

<output>
Create `.planning/phases/04-lesson-review-engine-port-detail-layer/04-06-SUMMARY.md` when done.
</output>
