---
phase: 04-lesson-review-engine-port-detail-layer
plan: 04
type: execute
wave: 4
executor_hint: opus
depends_on: ["04-03"]
files_modified:
  - shared/awba-engine.js
  - scripts/tests/runner-lesson.test.js
autonomous: true
requirements: [RWD-01, RWD-02, RWD-03]
must_haves:
  truths:
    - "Per RWD-01/D-51: the post-lesson sequence plays as WAAPI-chained choreography — verdict→noor→returns→done→Ring→du'a — via await AW.animate(el,kf,'--dur-*','--ease').finished with a 60ms stagger between staggered reveals; every animation self-guards reduced motion (collapses to 1ms) so the centre never animates"
    - "Per RWD-01/S4: the verdict reveals shape-first stars (gold .dab/.rosette hollow/half/filled) + 3 stat tiles staggered on settle; the noor count-up runs in Marcellus display numerals and noor persists exactly once at the noor moment (AW.S.set('noor', get+noorEarned))"
    - "Per RWD-02/D-45: the returns hero carries the decorative --apricot horizon-warmth glow (never apricot text), a big Marcellus --kiswah returns count, and AW.weekCal() days as lighter-ink presence dots that NEVER show a gap/red/miss state"
    - "Per RWD-01/D-51/WR-01: the terminal Ring moment renders on .reg-orbit — preLessonAtoms is captured at runner init BEFORE this lesson's best-of star persist, and postAtoms is recomputed AFTER that persist as Object.keys(stars).length*3; AW.ringSVG({atomsDone: postAtoms, animateFrom: preLessonAtoms}) draws ONLY the newly-earned frontier on a genuine completion, never replays the established ring (law 9) — on a replay (the star key already present pre-persist) postAtoms===preLessonAtoms so the span is empty and no phantom celebration fires; reduced motion → final inked state, static"
    - "Per D-51/RWD-03: the du'a close renders on .reg-sky-night — the du'a in Amiri (scripture law, the permitted glow) + 'Alhamdulillah — continue.' — and celebration (.dab/.thread/.plate/.rosette) fires ONLY on these meta-progress screens, NEVER on or adjacent to a verse/scripture beat (grep-gated)"
    - "Retired elements stay retired: no AW.confetti, no PERFECT overlay, no combo pill, no companion mascot, no amber — the celebration is ink (dabs + thread + Ring)"
  artifacts:
    - path: "shared/awba-engine.js"
      provides: "the WAAPI reward choreography + Ring moment + du'a close inside AwbaLesson"
      contains: "AW.ringSVG"
  key_links:
    - from: "the reward choreography"
      to: "AW.animate / AW.ringSVG / AW.weekCal (shipped)"
      via: "await .finished chaining + animateFrom=preLessonAtoms + weekCal never-miss"
      pattern: "AW\\.animate|animateFrom"
---

## Phase Goal

**As a** learner, **I want to** walk each of Josh's 15 lessons and 4 reviews end-to-end — reading verbatim scripture and being quietly celebrated at the end — **so that** the whole Aqeedah course feels like a warm, world-class manuscript rather than a game show.

*(This plan replaces 04-03's plain reward terminus with the flagship choreography — the emotional payoff of the whole Athar pivot: your own tawaf ring inking your new frontier, then a du'a close. After this, the lesson slice is fully world-class.)*

<objective>
Upgrade the plain reward terminus (04-03) into the choreographed post-lesson sequence per D-51: verdict → noor claim → returns → done → the Ring moment → the du'a close, WAAPI-chained via `AW.animate(...).finished`, transitioning Page→Orbit→Sky. The Ring draws ONLY the newly-earned frontier (`animateFrom` = pre-lesson atoms, captured before the lesson's atoms are added — never replays). Celebration is ink, and it never touches scripture.

Purpose: RWD-01/02/03 — the "reads as a sequence of moments" payoff that makes the course feel alive without a game show.
Output: the reward choreography inside `AwbaLesson` (`shared/awba-engine.js`).

Executor routing: **opus + frontend-design skill** — reward choreography is the flagship visual moment of the phase. Every animation goes through `AW.animate` (never hand-roll duration/easing/reduced-motion). The Ring's `animateFrom` correctness is load-bearing (Pitfall 7).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-lesson-review-engine-port-detail-layer/04-RESEARCH.md
@.planning/phases/04-lesson-review-engine-port-detail-layer/04-PATTERNS.md
@.planning/phases/04-lesson-review-engine-port-detail-layer/04-UI-SPEC.md
@.planning/phases/03-components-icon-kit-motion-language/03-REVIEW-ATHAR.md

<interfaces>
<!-- Shipped primitives to consume — extracted from shared/awba-engine.js. -->

- `AW.animate(el, keyframes, durToken, easeToken)` (~:1090): reads ms off `--dur-*` and the `linear()`/cubic easing off `--ease` on `:root`; self-guards `dur=1` under `AW.reducedMotion()`; returns an awaitable (`.finished`). NEVER hand-roll duration parsing. Motion tokens: `--dur-settle` 260 (Page), `--dur-draw` 240 (Orbit), `--dur-drift` 300 (Circle), `--dur-stamp` 150.
- `AW.ringSVG({seed?, atomsDone, circuitsDone?, animateFrom?, size?})` (~:1145): the tawaf-fingerprint; `animateFrom` = pre-lesson atoms draws ONLY `[animateFrom, atomsDone)`; omitting it defaults `animateFrom=atomsDone` → static (safe); passing 0/too-small re-draws the established frontier (law 9 violation, Pitfall 7). Reduced motion → final inked state, static. `AW.ringSeed()` (~:1114) = lazy per-learner seed (no schema bump).
- `AW.weekCal()` (~:415) → `[{label,on}]` (Mon-first 7 days from `awba_days`); the runner builds the markup — re-voice to lighter-ink presence dots, NEVER a miss state.
- `AW.S` / `AW.state()` — noor/stars/returns/days; the noor persist point is the noor moment (Gen-3 parity). `.reg-orbit` / `.reg-sky-night` grounds + `.rw-ring`/`.rw-dua`/`.noorbig`/`.weekcal` classes from 04-02.
- WAAPI chain exemplar (04-PATTERNS Shared Patterns): `for (const el of statEls){ AW.animate(el, [...], '--dur-settle', '--ease'); await new Promise(r=>setTimeout(r,60)); }  await AW.animate(noorEl, [...], '--dur-settle','--ease').finished; AW.S.set('noor', AW.S.get('noor',0)+noorEarned);`

Atoms proxy (Phase-4 scope; Phase-5 CNT-03 wires exact atoms): capture `preLessonAtoms` at AwbaLesson init (BEFORE the best-of star persist) as `(Object.keys((AW.state().stars)||{}).length) * ATOMS_PER_NODE` with `ATOMS_PER_NODE = 3` (a documented deterministic proxy); AFTER `done()` persists this lesson's best-of star, recompute `postAtoms = (Object.keys((AW.state().stars)||{}).length) * ATOMS_PER_NODE` (read fresh, post-persist) and call `AW.ringSVG({atomsDone: postAtoms, animateFrom: preLessonAtoms})`. On a genuine first completion the star key is newly added by the persist, so `postAtoms > preLessonAtoms` (a real new frontier draws). On a replay — the star key already present before the persist — `postAtoms === preLessonAtoms`, so `[animateFrom, atomsDone)` is empty and the Ring renders the static final inked state with no phantom celebration.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: WAAPI choreography — verdict → noor (persist) → returns (apricot + weekCal never-miss)</name>
  <files>shared/awba-engine.js, scripts/tests/runner-lesson.test.js</files>
  <read_first>
    - 04-UI-SPEC §S4 steps 1-4 (verdict/noor/returns/done copy + register + Marcellus + apricot ruling) + the "Returns hero — apricot warmth" Color note.
    - Gen-3 `verdict`/`rewardNoor`/`rewardReturns`/`done` (292-330) for the four-moment structure + the noor persist point.
    - 04-PATTERNS "WAAPI reward choreography" + `AW.animate`/`AW.weekCal` signatures.
  </read_first>
  <behavior>
    - The 3 stat tiles reveal staggered (a 60ms setTimeout gap) via AW.animate(el, opacity/translateY, '--dur-settle','--ease'); under reduced motion each collapses to 1ms and simply appears.
    - Verdict stars are shape-first gold .dab/.rosette (hollow/half/filled), consistent with data-state; the verdict word matches Gen-3 (Flawless/Beautifully done/You made it through).
    - The noor count-up runs in Marcellus (.noorbig); noor persists exactly once: AW.S.set('noor', AW.S.get('noor',0)+noorEarned) at the noor moment — a completed stub lesson increments awba_noor by exactly noorEarned (not zero, not doubled).
    - The returns hero shows the big returns count in Marcellus --kiswah ink over a decorative --apricot glow (no apricot text); AW.weekCal() renders lighter-ink presence dots with NO gap/red/miss markup.
  </behavior>
  <action>Replace the 04-03 plain terminus's verdict/noor/returns steps with WAAPI-chained versions. `verdict()`: render shape-first stars (`.dab`/`.rosette` per `AW.lessonStars(mistakes)`) + verdict word + 3 `.rw-stat` tiles, revealing each with `AW.animate(el,[{opacity:0,transform:'translateY(8px)'},{opacity:1,transform:'none'}],'--dur-settle','--ease')` and a 60ms stagger between them (each anim self-guards reduced motion). Marks from shipped GLYPHS (`spark`/`star`), never the absent Gen-3 `BOLT/TARGET/CLOCK`. `rewardNoor()`: `.noorbig` Marcellus count-up via `AW.animate`, then persist `AW.S.set('noor', AW.S.get('noor',0)+noorEarned)` exactly once + `AW.sound('complete')` + optional `cfg.grew` box. `rewardReturns()`: the `.rw-returns` hero with the decorative `--apricot` horizon glow + big Marcellus returns count + `AW.weekCal()` mapped to lighter-ink presence dots (never a miss). `done()`: best-of star persist (never downgrade) + `cfg.recap` + `cfg.next` handoff. Keep all four as an async sequence chained on `.finished`. Extend `runner-lesson.test.js`: assert noor persists exactly once by completing a stub lesson through a probe (seed Map-LS, drive to done, assert `awba_noor` delta === noorEarned).</action>
  <verify>
    <automated>grep -qE "AW\.animate\(" shared/awba-engine.js && grep -q "AW.S.set('noor'" shared/awba-engine.js && grep -q 'rw-returns' shared/awba-engine.js && test "$(grep -c 'localStorage' shared/awba-engine.js)" -eq 13 && ! grep -qiE 'confetti|>PERFECT<|amber' shared/awba-engine.js && node --test scripts/tests/*.test.js 2>&1 | grep -qE 'fail 0'</automated>
  </verify>
  <acceptance_criteria>
    - verdict/noor/returns play as staggered WAAPI reveals via `AW.animate` (self-guarded reduced motion); noor persists exactly once; count-up + big returns are Marcellus.
    - The returns apricot is a decorative glow (never text); `AW.weekCal` days are presence dots with no gap/red/miss.
    - `localStorage` count == 13; no confetti/PERFECT/amber; suite `fail 0`.
  </acceptance_criteria>
  <done>The verdict→noor→returns moments read as a warm, staggered sequence, never a game show.</done>
</task>

<task type="auto">
  <name>Task 2: The Ring moment (Orbit) + the du'a close (Sky) — celebration never on scripture</name>
  <files>shared/awba-engine.js</files>
  <read_first>
    - 04-UI-SPEC §S4 steps 5-6 (Ring on .reg-orbit, animateFrom capture; du'a on .reg-sky-night, scripture law) + the "Ring moment" Shared Pattern in 04-PATTERNS.
    - 03-REVIEW-ATHAR.md WR-01 (animateFrom semantics / no-replay) + `AW.ringSVG`/`AW.ringSeed` signatures.
    - 04-RESEARCH Pitfall 7 (animateFrom misuse replays the ring).
  </read_first>
  <action>Capture `var preLessonAtoms = (Object.keys((AW.state().stars)||{}).length) * 3;` at `AwbaLesson` INIT — before the opener, before any `AW.touchDay`/best-of star persist (Pitfall 7: it must be the pre-lesson value). After `done()` persists this lesson's best-of star, recompute `var postAtoms = (Object.keys((AW.state().stars)||{}).length) * 3;` (read AFTER the persist) and append the Ring moment on a `.reg-orbit` screen: `root.querySelector('.rw-ring').innerHTML = AW.ringSVG({ atomsDone: postAtoms, animateFrom: preLessonAtoms });` (draws ONLY this node's new frontier on a genuine completion; on a replay — the star key already present before the persist — `postAtoms === preLessonAtoms` so the range is empty and the Ring renders the static final inked state, no phantom celebration; reduced motion also → final inked state static — all handled inside `AW.ringSVG`). Document `ATOMS_PER_NODE = 3` as a Phase-4 proxy (Phase-5 CNT-03 wires exact atoms). Then render the du'a close on a `.reg-sky-night` screen: `.rw-dua` with the du'a in Amiri (scripture law — `lang="ar" dir="rtl"`, the permitted glow allowed on this dark ground) + "Alhamdulillah — continue." in `--moonmilk`. Author NO celebration primitive (`.dab/.thread/.plate/.rosette`) inside the du'a/scripture block. Confirm the grep gate: no celebration class is emitted on/adjacent to a `verse`/`.scard`/`.ayah` beat anywhere in the runner.</action>
  <verify>
    <automated>grep -q 'AW.ringSVG(' shared/awba-engine.js && grep -q 'animateFrom' shared/awba-engine.js && grep -qE 'preLessonAtoms' shared/awba-engine.js && grep -qE 'reg-sky-night|rw-dua' shared/awba-engine.js && ! grep -qE '(dab|thread|plate|rosette)[^;{}]*\.(ayah|scripture|scard)' shared/awba-engine.js && node scripts/tests/render-smoke.mjs lessons/u1-m1.html 2>&1 | grep -q 'SMOKE OK' && node --test scripts/tests/*.test.js 2>&1 | grep -qE 'fail 0'</automated>
  </verify>
  <acceptance_criteria>
    - `preLessonAtoms` is captured at init (before the best-of star persist); `postAtoms` is recomputed after the persist and `AW.ringSVG({atomsDone: postAtoms, animateFrom: preLessonAtoms})` draws only the new frontier on a genuine completion, and an empty (no-op) span on a replay — no phantom celebration; reduced motion static.
    - The du'a close renders on `.reg-sky-night` under scripture law; no celebration class touches a scripture block (grep-gated).
    - `lessons/u1-m1.html` render-smoke `SMOKE OK` through the full choreography; suite `fail 0`.
  </acceptance_criteria>
  <done>The lesson closes on your own inked tawaf frontier and a du'a — presence without a mascot, celebration that never touches scripture.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reward DOM (string concat of author content) | trusted authored copy (praise pool, du'a), not user input |
| ring atoms proxy → AW.ringSVG | integer arithmetic from local state; no external input; deterministic |
| noor/stars persistence | only via `AW.S` (D-24), once per moment (Gen-3 parity) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04a | Tampering | double-persist of noor/stars | mitigate | noor persists exactly once at the noor moment; best-of star persist only upgrades; a headless test asserts the delta === noorEarned |
| T-04-04b | Repudiation | Ring replay (law 9) | mitigate | `animateFrom = preLessonAtoms` captured at init; a plain reload draws nothing; render-smoke + WR-01 semantics enforce no-replay |
| T-04-04c | Tampering | celebration over scripture | mitigate | grep gate asserts no `.dab/.thread/.plate/.rosette` on/adjacent to a `.ayah/.scard`/verse block; du'a close is silent-reverent |
| T-04-SC | Tampering | npm installs | accept | zero packages |
</threat_model>

<verification>
- Choreography is WAAPI-chained via `AW.animate` (self-guarded reduced motion); noor persists once; returns apricot decorative + weekCal never-miss.
- Ring draws only the new frontier (`animateFrom=preLessonAtoms`); du'a under scripture law; no celebration on scripture (grep gate).
- render-smoke `lessons/u1-m1.html` `SMOKE OK`; `localStorage` count == 13; `node --test scripts/tests/*.test.js` → `fail 0`.
</verification>

<success_criteria>
- RWD-01/RWD-02/RWD-03 hold: the post-lesson sequence reads as choreography, the returns screen never shows a miss, celebration is ink and never over scripture.
- The lesson vertical slice is fully world-class end-to-end.
</success_criteria>

<output>
Create `.planning/phases/04-lesson-review-engine-port-detail-layer/04-04-SUMMARY.md` when done.
</output>
