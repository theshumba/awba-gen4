---
phase: 01-foundation-design-tokens-responsive-shell-fonts
plan: 03
type: execute
wave: 3
executor_hint: opus-visual
depends_on: ["01-02"]
files_modified:
  - preview.html
autonomous: true
requirements: [FND-01, FND-02, FND-03, PLT-01]
must_haves:
  truths:
    - "Opening preview.html renders the full token sheet, responsive shell skeleton, motion demos, and permanent glyph test — and feels premium, not a debug sheet (D-12)"
    - "The data-unit switch live-recolors the sample cluster across all 4 units with zero residual indigo on u2/u3/u4"
    - "There are zero fonts.googleapis.com / fonts.gstatic.com fetch references in the shipped page — fonts load font-display:swap with the 2 critical faces preloaded and identical resolved URLs on every page (D-11)"
    - "The glyph-test block renders ˹˺, transliteration diacritics, î â û, middle dot, dashes, curly quotes, ﷺ, an Amiri-Quran ayah and an Amiri hadith — with a tofu FAIL exemplar"
  artifacts:
    - path: "preview.html"
      provides: "The D-12 living verification vehicle — 8 required sections, shared head boilerplate, unit-switch JS"
      contains: "viewport-fit=cover"
      min_lines: 200
  key_links:
    - from: "preview.html"
      to: "shared/awba-engine.css"
      via: "single <link rel=stylesheet> (one CSS file per page, D-04)"
      pattern: "shared/awba-engine.css"
    - from: "unit-switch control (inline JS)"
      to: "document.documentElement data-unit attribute"
      via: "setAttribute('data-unit', ...) recolors the whole sample cluster"
      pattern: "data-unit"
---

## Phase Goal

**As a** reviewer, **I want to** open `preview.html` and see the full design system — one token layer, a true responsive shell, per-unit theming, and self-hosted fonts — render at world-class quality, **so that** every later screen builds on a proven, CDN-free, half-theming-proof foundation instead of Gen-3's fixed 380px card and Google Fonts dependency.

<objective>
Build `preview.html` at repo root — the D-12 verification vehicle and living style reference. It renders all eight required sections against `shared/awba-engine.css`, proves the full per-unit recolor via an interactive `data-unit` switch, carries the permanent glyph test, and feels premium (spacing, shadows, type rhythm) rather than a debug sheet.

Purpose: This is how all four Phase-1 success criteria get checked (resize, unit-switch recolor, zero-CDN network, glyph render) AND it stays as Josh's living reference. It is the user-visible slice that proves the whole static stack (fonts → stylesheet → rendered page) works end-to-end over `file://`. Load the `frontend-design` skill before writing.
Output: `preview.html` (self-contained page + minimal classic inline JS for the unit switch and reduced-motion-aware demos).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-design-tokens-responsive-shell-fonts/01-UI-SPEC.md
@.planning/phases/01-foundation-design-tokens-responsive-shell-fonts/01-RESEARCH.md
@.planning/phases/01-foundation-design-tokens-responsive-shell-fonts/01-PATTERNS.md
@shared/awba-engine.css

<interfaces>
<!-- preview.html CONSUMES the token/class names from shared/awba-engine.css (Plan 02). Use those exact names; invent no literals. -->
- Shared <head> boilerplate every page repeats identically (D-11): the `viewport-fit=cover` meta (mandatory — without it every env(safe-area-inset-*) silently resolves to 0 on notched devices); two `<link rel="preload" as="font" type="font/woff2" crossorigin>` for the 2 critical faces (`shared/fonts/poppins-700.woff2`, `shared/fonts/inter-400.woff2` — page-relative, NO leading slash: root-absolute `/shared/...` resolves to the FILESYSTEM ROOT under file:// per RFC 3986 and 404s in Josh's double-click review; later nested pages will use `../shared/fonts/...`, resolving to the same URL so the cache still dedupes per D-11. `crossorigin` is REQUIRED even same-origin or the browser double-fetches); then the single `<link rel="stylesheet" href="shared/awba-engine.css">`. No Google Fonts CDN link; no deprecated `apple-mobile-web-app-capable` tag.
- The 8 required sections come from UI-SPEC §"preview.html Contract": (1) color token sheet (surface/ink/line + 4 semantics + all 4 unit scales side-by-side with hex + AA badge), (2) unit-switch control that live-recolors a sample cluster (primary button, citation chip, active node ring, inline link, tinted panel, progress fill), (3) type scale (every role at token size/weight/line-height, a clamp() resize demo, Arabic samples), (4) one tile per --r-* and --sh-*, (5) motion demos (gummy press, node-pop, sheet-in — each labelled with its easing token, reduced-motion collapses to instant), (6) responsive shell skeleton (HUD/stage/footer placeholder — the resize target), (7) permanent glyph-test block, (8) zero-CDN assertion note.
- Glyph-test block exact contents (UI-SPEC item 7): ˹ ˺ (U+02F9/02FA) labelled; diacritics ʿ ʾ ā Ā ī Ī ū ḥ Ḥ ṣ Ṣ ṭ Ṭ ḏ Ḏ + typographic ’ in Poppins+Inter; middle dot · (U+00B7) in Poppins with a "Unit 1 · Lesson 1" exemplar; punctuation — – ‘ ’ “ ” in Poppins+Inter; circumflex î â û with the "Iblîs · Suwâ · Yaghûth · Yaûq" exemplar; ﷺ (U+FDFA) in Amiri/Amiri Quran (forward-looking, render defensively); a verbatim ayah in `.ayah`/Amiri Quran (RTL, lang="ar" dir="rtl") + a hadith line in general Amiri (proving the face split); a `.notdef`/tofu FAIL exemplar cell.
- Motion/press/sheet demos may reuse Gen-3 shapes re-expressed in Phase-1 tokens (01-PATTERNS #4 gummy `0 5px 0 var(--accent-deep)` → `0 2px 0` + `translateY(3px)` on `:active`; #5 scrim/sheet `translateY(100%)→0` with `--dur-4`/`--ease-gentle`, grip handle) — self-contained showcase tiles, not live app shell.
- Copy exemplars (UI-SPEC §Copywriting): primary CTA "Begin, gently"; secondary "Continue" (preview-only exemplar); glyph FAIL cell "Glyph missing — re-subset this face." No red for any non-safety state; no emoji near the ayah/hadith.
- ZERO-CDN NOTE PHRASING (binding, to keep the automated gate clean): write section 8's on-page instruction WITHOUT the literal host substrings `fonts.googleapis.com`/`fonts.gstatic.com` — phrase it as "Open DevTools › Network, filter by 'font', reload: every request must be same-origin `shared/fonts/…` — zero Google-Fonts-CDN requests." This keeps the shipped page free of those host strings so the grep gate is unambiguous.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shared head boilerplate + token sheets + unit-switch recolor + shell skeleton + motion demos (sections 1–6)</name>
  <files>preview.html</files>
  <read_first>
    - shared/awba-engine.css (the exact token + class names this page consumes)
    - .planning/phases/01-foundation-design-tokens-responsive-shell-fonts/01-UI-SPEC.md (§preview.html Contract items 1–6; §Responsive Shell Anatomy for the skeleton; §Motion Tokens for easing labels; focal-point declaration — the eye lands first on the unit-switch cluster)
    - .planning/phases/01-foundation-design-tokens-responsive-shell-fonts/01-PATTERNS.md (preview.html #1 head REPLACE — delete CDN link, add viewport-fit; #2 shell nesting KEEP-THE-FEEL as grid rows; #4 gummy button, #5 scrim/sheet demos)
  </read_first>
  <action>
    Create `preview.html` with the shared `<head>` boilerplate: charset, the `viewport-fit=cover` viewport meta, two `<link rel="preload" as="font" type="font/woff2" crossorigin>` for `shared/fonts/poppins-700.woff2` and `shared/fonts/inter-400.woff2` (page-relative — never a leading slash, which breaks file:// review), then the single `<link rel="stylesheet" href="shared/awba-engine.css">`. Add no Google Fonts CDN link and no deprecated `apple-mobile-web-app-capable` tag.
    Build sections 1–6 from UI-SPEC's contract: (1) color token sheet — swatch grid of surface/ink/line + amber/green/flame/gold + all 4 unit scales side-by-side, each text-bearing pairing carrying hex label + an AA badge; (2) the interactive unit-switch — a segmented control whose handlers call `document.documentElement.setAttribute('data-unit', 'u1'|'u2'|'u3'|'u4')` and recolor a sample cluster (primary gummy button labelled "Begin, gently", citation chip, active node ring, inline link, tinted panel, progress fill) — this is the page's focal point and the Success-Criterion-2 proof; (3) type scale — every `--fs-*` role at its token size/weight/line-height with a label, a clamp() resize demo, Arabic samples at `--fs-ar-body`/`--fs-ayah`; (4) radii + shadow tiles — one labelled tile per `--r-*` and `--sh-*`; (5) motion demos — a gummy-press button, a node-pop, a sheet-in, EACH labelled with its easing token, all wrapped so `@media (prefers-reduced-motion: reduce)` collapses them to instant; (6) responsive shell skeleton — a real `.app-shell` grid (HUD/stage/footer placeholder) full-bleed under 600px, centered column above (the resize target). Use only tokens/classes from `shared/awba-engine.css` — invent no literal hex/px. Keep inline JS classic (no `type="module"`), minimal, `file://`-safe.
  </action>
  <verify>
    <automated>grep -c 'viewport-fit=cover' preview.html && grep -c 'shared/awba-engine.css' preview.html && grep -c 'rel="preload"' preview.html && grep -c 'data-unit' preview.html && grep -c 'prefers-reduced-motion' preview.html && ! grep -Eq '"/(shared|fonts)' preview.html && echo "NO ROOT-ABSOLUTE PATHS"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'viewport-fit=cover' preview.html` == 1 (the safe-area prerequisite is present).
    - Exactly one stylesheet link (`grep -c 'shared/awba-engine.css' preview.html` == 1) and ≥ 2 font `rel="preload"` tags each with `crossorigin`.
    - `grep -Ec '"/(shared|fonts)' preview.html` == 0 — no root-absolute (leading-slash) asset path anywhere in the page; preload/stylesheet references are page-relative so the page works opened directly via `file://`.
    - The unit-switch control exists and mutates `data-unit` on the document element (`grep -c 'data-unit' preview.html` ≥ 2, including a `setAttribute` call); toggling it visibly recolors the sample cluster (verified in Plan 04's human gate).
    - Sections 1–6 are all present (color sheet with all 4 unit scales, unit-switch cluster, type scale + clamp demo + Arabic, radii+shadow tiles, motion demos, shell skeleton).
    - Motion demos are guarded by `@media (prefers-reduced-motion: reduce)` (`grep -c 'prefers-reduced-motion' preview.html` ≥ 1).
    - No hardcoded hex/px literals for tokenized values in the sample cluster/type/radii/shadow markup (spot-check: swatches and demos reference `var(--...)`, not raw hex).
  </acceptance_criteria>
  <done>preview.html renders sections 1–6 against the engine stylesheet with a working live unit-switch and a resizable shell skeleton; head boilerplate carries viewport-fit=cover + two crossorigin font preloads + one CSS link, no CDN.</done>
</task>

<task type="auto">
  <name>Task 2: Permanent glyph-test block + zero-CDN note + premium polish (sections 7–8)</name>
  <files>preview.html</files>
  <read_first>
    - preview.html (Task 1 output — extend it)
    - .planning/phases/01-foundation-design-tokens-responsive-shell-fonts/01-UI-SPEC.md (§preview.html Contract items 7–8 — the exact glyph inventory and the zero-CDN note; §Typography Arabic laws for the ayah/hadith split)
    - .planning/phases/01-foundation-design-tokens-responsive-shell-fonts/01-RESEARCH.md (§Font Subset Pipeline Step 1 — the verified codepoints and their real-content exemplars; Pitfall 4 — do NOT force .ayah font onto hadith)
  </read_first>
  <action>
    Add section 7 (permanent glyph-test block) and section 8 (zero-CDN note), then do a premium polish pass. Glyph block must render, each visibly labelled with its face: the corner brackets ˹ ˺ (labelled U+02F9/02FA); the transliteration diacritic set ʿ ʾ ā Ā ī Ī ū ḥ Ḥ ṣ Ṣ ṭ Ṭ ḏ Ḏ and ’ in both Poppins and Inter; the middle dot · in Poppins inside a "Unit 1 · Lesson 1" exemplar; the punctuation set — – ‘ ’ “ ” in Poppins and Inter; the circumflex vowels î â û inside the "Iblîs · Suwâ · Yaghûth · Yaûq" exemplar; ﷺ (U+FDFA) rendered in Amiri and Amiri Quran (forward-looking/defensive); a verbatim ayah in `.ayah` (Amiri Quran, `lang="ar" dir="rtl"`) beside a hadith line in general Amiri (`--font-ar`) — proving the face split without forcing the Quran face onto hadith; and a deliberate `.notdef`/tofu FAIL exemplar cell with the copy "Glyph missing — re-subset this face." so a missing glyph is unmistakable.
    Add section 8: the zero-CDN assertion note phrased WITHOUT the literal `fonts.googleapis.com`/`fonts.gstatic.com` host substrings (per the interfaces note) — "Open DevTools › Network, filter by 'font', reload: every request must be same-origin `shared/fonts/…` — zero Google-Fonts-CDN requests."
    Polish pass: verify the page reads as a premium living style reference (generous spacing on the `--sp-*` scale, indigo-tinted `--sh-*` elevation, Poppins/Inter type rhythm, the unit-switch cluster as the clear focal point) — not a flat debug dump. No literal hex/px for anything the token layer already defines.
  </action>
  <verify>
    <automated>grep -c '˹' preview.html && grep -c 'lang="ar"' preview.html && grep -Ec 'amiri-quran|Amiri Quran|ayah' preview.html && ! grep -Eq 'fonts\.(googleapis|gstatic)\.com' preview.html && grep -ci 'Network' preview.html</automated>
  </verify>
  <acceptance_criteria>
    - The bracket glyph ˹ appears in the page (`grep -c '˹' preview.html` ≥ 1) and is labelled with its codepoint; the FAIL exemplar cell text "Glyph missing — re-subset this face." is present.
    - The ayah/hadith split is demonstrated: at least one `.ayah` element with `lang="ar" dir="rtl"` and a separate general-Amiri hadith line (`grep -c 'lang="ar"' preview.html` ≥ 2); no blanket rule forces `--font-quran` onto the hadith.
    - `grep -Ec 'fonts\.(googleapis|gstatic)\.com' preview.html` == 0 (zero literal CDN host strings anywhere in the shipped page — the note is phrased to avoid them).
    - The zero-CDN instruction note is present (`grep -ci 'Network' preview.html` ≥ 1) and directs the reviewer to confirm same-origin-only font requests.
    - The circumflex exemplar ("Iblîs · Suwâ · Yaghûth · Yaûq"), the middle-dot exemplar ("Unit 1 · Lesson 1"), and ﷺ all render (no tofu) — confirmed visually in Plan 04's human gate.
  </acceptance_criteria>
  <done>preview.html carries the full permanent glyph test (all required codepoints in the correct faces, ayah/hadith split, a tofu FAIL exemplar) and the zero-CDN note; the page reads as premium.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none (new) | Static HTML + inline classic JS (unit-switch only) + same-origin CSS/fonts. No user input, no auth, no persistence, no runtime third-party network. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-ID | Information disclosure | Any Google Fonts CDN reference in shipped HTML/CSS | mitigate | Zero-CDN gate: `grep -Ec 'fonts\.(googleapis|gstatic)\.com' preview.html` == 0 AND the same over `shared/awba-engine.css`; all fonts are page-relative same-origin `shared/fonts/` preloads (no leading slash — file://-safe). The on-page note is phrased to avoid the literal host strings so the gate is unambiguous. |
| T-01-SI | (Supply-chain / injection) | Inline JS | accept | The only script is a few lines of classic vanilla JS toggling a `data-unit` attribute — no third-party script, no eval, no remote fetch. Low-value static page, no data handled. |

ASVS V2–V6 do not apply to a static token/shell showcase page (01-RESEARCH §Security Domain). Security hardening/verification is revisited at Phase 6.
</threat_model>

<verification>
- `grep -c 'viewport-fit=cover' preview.html` → 1 (PLT-01 safe-area prerequisite).
- `grep -Ec 'fonts\.(googleapis|gstatic)\.com' preview.html` → 0 (FND-03 zero-CDN).
- Bracket ˹, ayah `.ayah`/RTL, and the tofu FAIL cell all present (FND-03 glyph test).
- `data-unit` switch mutates the document element (FND-02 recolor proof).
- `python3 scripts/check-glyph-coverage.py` still exits 0.
</verification>

<success_criteria>
- Opening preview.html over `file://` renders the entire design system end-to-end at premium quality.
- The unit-switch proves full per-unit recolor; the glyph test proves every required glyph renders in the correct self-hosted face; the shell skeleton adapts full-bleed→column on resize; the page ships zero CDN font references.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-design-tokens-responsive-shell-fonts/01-03-SUMMARY.md` when done.
</output>
