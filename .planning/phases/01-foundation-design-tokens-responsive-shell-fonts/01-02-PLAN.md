---
phase: 01-foundation-design-tokens-responsive-shell-fonts
plan: 02
type: execute
wave: 2
executor_hint: opus-visual
depends_on: ["01-01"]
files_modified:
  - shared/awba-engine.css
autonomous: true
requirements: [FND-01, FND-02, FND-03, PLT-01]
must_haves:
  truths:
    - "All colors, type, radii, shadows, and motion values come from one @layer tokens source in a single stylesheet — motion durations and linear() easings defined now for Phase 3 to consume (D-04, D-08)"
    - "Components consume semantic accent tokens (--accent, --accent-deep, …); each unit u1–u4 defines its FULL 7-slot accent scale explicitly — no shared --blue2/--blue3 exists for a screen to inherit (D-05, D-06)"
    - "Unit colors exist ONCE as CSS custom properties — the single source of truth Phase 2's engine will read via getComputedStyle for its cfg.unitColor hex→data-unit mapping (D-07)"
    - "The shell is full-bleed 100dvh grid on mobile with safe-area insets honored and overscroll contained, and an intentional centered ≤480px column on a calm cream backdrop at ≥600px — no fixed 380×788 card (D-01, D-02)"
    - "Display sizes and hero numerals scale fluidly via rem + clamp(); body stays comfortably readable at 320px without overflow (D-03)"
    - "The five-name @layer order is declared exactly once at the top of the file (D-04)"
  artifacts:
    - path: "shared/awba-engine.css"
      provides: "The one engine stylesheet: @layer tokens+base, 11 @font-face, per-unit accent scales, responsive shell, focus-visible"
      contains: "@layer tokens, base, components, screens, motion"
      min_lines: 200
  key_links:
    - from: "shared/awba-engine.css @font-face"
      to: "shared/fonts/*.woff2"
      via: "CSS-relative src:url('fonts/...') — @font-face URLs resolve from the CSS file's own shared/ location; never root-absolute"
      pattern: "fonts/.*woff2"
    - from: "[data-unit='u2'|'u3'|'u4']"
      to: "--accent, --accent-deep, --accent-bright, --accent-soft, --accent-line, --accent-ink, --accent-on"
      via: "per-unit custom-property scale in the tokens layer"
      pattern: "data-unit=.u[2-4]."
---

## Phase Goal

**As a** reviewer, **I want to** open `preview.html` and see the full design system — one token layer, a true responsive shell, per-unit theming, and self-hosted fonts — render at world-class quality, **so that** every later screen builds on a proven, CDN-free, half-theming-proof foundation instead of Gen-3's fixed 380px card and Google Fonts dependency.

<objective>
Build the one engine stylesheet `shared/awba-engine.css`: the complete `@layer tokens, base, components, screens, motion;` order (declared once), the full `tokens` layer (fonts, spacing, type, color, per-unit accent scales, radii, shadows, motion), and the `base` layer (reset, responsive shell grid, HUD/stage/footer regions, focus-visible + accessible-name foundation, Arabic/RTL laws). This is the style engine every later page and phase consumes.

Purpose: This file is the structural fix for Gen-3's two worst bugs — the fixed phone card (PLT-01) and the half-themed page (FND-02) — and the single source of truth every screen greps against (FND-01). Load the `frontend-design` skill before writing: the bar is "feels like the best UX/UI developers in the world made it," elevating Gen-3's cream/gummy/soft warmth, not generic minimal SaaS.
Output: `shared/awba-engine.css` with `tokens` + `base` filled and `components`/`screens`/`motion` present as empty declared layers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-design-tokens-responsive-shell-fonts/01-UI-SPEC.md
@.planning/phases/01-foundation-design-tokens-responsive-shell-fonts/01-RESEARCH.md
@.planning/phases/01-foundation-design-tokens-responsive-shell-fonts/01-PATTERNS.md

<interfaces>
<!-- 01-UI-SPEC.md is the AUTHORITATIVE source for EVERY token VALUE. Read its tables directly; do not invent or round values. Key anchors the executor must not get wrong: -->
- @layer statement (write ONCE, at top, verbatim): `@layer tokens, base, components, screens, motion;` — then content blocks for `tokens` and `base`, and empty/comment-only blocks for `components`, `screens`, `motion`. `motion` is LAST = highest priority (deliberate: reduced-motion guards win). Never write a bare `@layer name-list` statement again anywhere in the project.
- 11 @font-face src URLs — CRITICAL PATH RULE (file:// compatibility): `@font-face` URLs resolve relative to the CSS FILE's own location (`shared/awba-engine.css`), so every src MUST be the CSS-relative form `url('fonts/<family>-<weight>.woff2')` for {poppins-500,poppins-600,poppins-700,poppins-800,inter-400,inter-500,inter-600,inter-700,amiri-400,amiri-700,amiri-quran-400}.woff2 — NEVER root-absolute `/shared/fonts/...` (a leading slash resolves to the FILESYSTEM ROOT under file:// per RFC 3986 and 404s every font in exactly the double-click review this project mandates). Pages reference fonts only via preloads: repo-root pages use `shared/fonts/...`; later nested pages (lessons/, reviews/) will use `../shared/fonts/...` — both resolve to the same URL so the browser cache still dedupes (D-11). All 11 faces: `font-display:swap`, `font-style:normal`, correct `font-weight`. Font-role vars: `--font-disp:'Poppins'` · `--font-body:'Inter'` · `--font-ar:'Amiri'` · `--font-quran:'Amiri Quran'` (metric-tuned system fallback stacks, NOT Gen-3's bare `system-ui,...` chain).
- Per-unit accent scales — take the EXACT hexes from UI-SPEC §"Per-Unit Accent Scales" table (7 slots × 4 units). Critical corrections baked into UI-SPEC (use these, NOT the Gen-3 originals): u4 `--accent-on` = `#241A00` (NOT Gen-3's failing value, which measured 3.92:1 at the real 16px/700 button size); u3 teal `--accent` = `#0A7575` (deepened from Gen-3's `#0FA3A3` for contrast).
- Semantic non-unit tokens (from UI-SPEC §Color): `--bg #EEF2FB`, `--card #F6F9FF`, `--paper #FFFFFF`, `--tint #E7EEFF`; `--amber #C68A2E`, `--green #1F9D6B`, `--flame #F0730B`/`--flame2 #FFB03A`, `--gold #E8A400`/`--gold2 #FFD34D`/`--gold-deep #B47F00`; `--danger #8A2E28` (safety callouts ONLY, never learner error); ink `--ink #232A3D`/`--ink2 #59607A`/`--ink3 #98A0B8`; lines `--line #DEE5F3`/`--line2 #C9D3EA`.
- Spacing `--sp-*`, radii `--r-*`, shadows `--sh-*` (indigo-tinted `rgba(37,54,206,α)` — never grey/black), type `--fs-*` (7 Latin roles + Arabic sizes), motion `--dur-*` + easings incl. the two verified `linear()` curves `--ease-spring`/`--ease-gentle` — all from UI-SPEC tables, copy the curve strings verbatim (do NOT hand-edit `linear()` datasets).
- Responsive shell (UI-SPEC §Responsive Shell Anatomy + 01-RESEARCH Pattern 2/3): `.app-shell{min-height:100dvh; display:grid; grid-template-rows:auto 1fr auto}`; mobile = undecorated full-bleed; `@media (min-width:600px)` adds `max-width:480px`, centering, `--r-2xl` corners, `--sh-4`, `--sh-inset`, and a subtle backdrop wash on `body`; use `min-height:min(100dvh,900px)` on the column (never a fixed height). Stage row: `overflow-y:auto; overscroll-behavior:contain`. HUD gets `env(safe-area-inset-top)`, footer `env(safe-area-inset-bottom)`.
- Focus-visible (UI-SPEC §Focus-Visible): `:focus-visible{ outline:3px solid var(--accent); outline-offset:2px; box-shadow:0 0 0 6px var(--accent-soft); }` on `:focus-visible` only; gold/night register switches ring to `--gold2`. Global `-webkit-tap-highlight-color:transparent`.
- Arabic laws (UI-SPEC §Typography): `.ayah` binds `--font-quran` ONLY; general Arabic uses `--font-ar`; `letter-spacing:0` on all Arabic; `line-height≥1.8` (ayah 1.9); Arabic containers `unicode-bidi:isolate`. Do NOT write a blanket `.scard .arabic{font-family:var(--font-quran)}` rule — the ayah-vs-hadith font choice is per-instance Phase-4 logic (01-RESEARCH Pitfall 4).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: @layer order + full tokens layer (fonts, spacing, type, color, per-unit scales, radii, shadows, motion)</name>
  <files>shared/awba-engine.css</files>
  <read_first>
    - .planning/phases/01-foundation-design-tokens-responsive-shell-fonts/01-UI-SPEC.md (ALL token tables: Spacing, Typography, Color + Per-Unit Accent Scales + WCAG pairings, Radii, Shadows, Motion Tokens — the authoritative values)
    - .planning/phases/01-foundation-design-tokens-responsive-shell-fonts/01-RESEARCH.md (§Font Subset Pipeline Step 5 — @font-face + crossorigin loading; Pattern 1 — @layer full-order-once)
    - .planning/phases/01-foundation-design-tokens-responsive-shell-fonts/01-PATTERNS.md (#1 :root REPLACE, #6 font-var REPLACE, Shared Patterns for what to port-forward vs. structurally kill)
  </read_first>
  <action>
    Create `shared/awba-engine.css`. First line region: the single `@layer tokens, base, components, screens, motion;` statement, followed by `@layer components {}`, `@layer screens {}`, `@layer motion {}` empty declared blocks (content lands in later phases) so the order is locked immutable.
    Inside `@layer tokens`: (a) the 11 `@font-face` rules with CSS-relative `src:url('fonts/<name>.woff2')` paths (relative to the stylesheet's own `shared/` location — NEVER a leading-slash `/shared/fonts/...` form, which resolves to the filesystem root under file:// and 404s in Josh's review) and `font-display:swap`; (b) `:root` with the four `--font-*` role vars (metric-tuned fallback stacks), the full `--sp-*` spacing scale, the `--fs-*` type scale (7 Latin roles with `clamp()` where UI-SPEC specifies + Arabic `--fs-ar-body`/`--fs-ayah`/sheet-Arabic sizes), all neutral + semantic color tokens, `--r-*` radii, `--sh-*` indigo-tinted shadows, and `--dur-*`/easing motion tokens (copy the `--ease-spring` and `--ease-gentle` `linear()` strings verbatim from UI-SPEC); (c) four `[data-unit="u1"]`…`[data-unit="u4"]` blocks each defining the full 7-slot accent scale from UI-SPEC's per-unit table. Set a default (`:root` or `html`) accent equal to u1's so a page without the attribute still themes.
    Take EVERY value from UI-SPEC — invent no literal hex/px. Apply the two baked-in corrections: u4 `--accent-on:#241A00`, u3 `--accent:#0A7575`. Do NOT emit any `--blue2`/`--blue3`-style shared literal-color triplet (that shared value is the exact half-theming bug being structurally killed). Do NOT mention the forbidden Gen-3 gold-ink literal anywhere (including comments) so the absence gate stays valid.
  </action>
  <verify>
    <automated>grep -c '^@layer [a-z]*, ' shared/awba-engine.css && grep -c '@font-face' shared/awba-engine.css && grep -Eo '\[data-unit="u[1-4]"\]' shared/awba-engine.css | sort -u | wc -l | tr -d ' ' && grep -c '#241A00' shared/awba-engine.css && ! grep -q '#3A2B00' shared/awba-engine.css && ! grep -Eq "url\(['\"]?/" shared/awba-engine.css && echo "GATES PASS: no banned gold ink, no root-absolute URLs"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^@layer [a-z]*, ' shared/awba-engine.css` returns exactly 1 (the full five-name order declared once).
    - `grep -c '@font-face' shared/awba-engine.css` returns exactly 11; each `src:url()` uses the CSS-relative form `fonts/<name>.woff2` (resolving to `shared/fonts/<name>.woff2`, which exists in Plan 01's output).
    - `grep -Ec "url\(['\"]?/" shared/awba-engine.css` == 0 — NO root-absolute (leading-slash) URL anywhere in the stylesheet; under `file://` a leading slash resolves to the filesystem root and 404s every font in Josh's double-click review.
    - `grep -Eo '\[data-unit="u[1-4]"\]' shared/awba-engine.css | sort -u | wc -l` returns 4 (all four unit themes defined).
    - Each `[data-unit]` block defines all seven slots: `--accent`, `--accent-deep`, `--accent-bright`, `--accent-soft`, `--accent-line`, `--accent-ink`, `--accent-on` (`grep -c -- '--accent-on' shared/awba-engine.css` ≥ 4).
    - `grep -c '#241A00' shared/awba-engine.css` ≥ 1 AND `grep -c '#3A2B00' shared/awba-engine.css` == 0 (uses the AA-passing u4 gold ink, never the failing Gen-3 value).
    - The two `linear()` easing curves are present verbatim (`grep -c 'linear(' shared/awba-engine.css` ≥ 2) and no literal `--blue2`/`--blue3` shared triplet exists (`grep -Ec -- '--blue[23]' shared/awba-engine.css` == 0).
  </acceptance_criteria>
  <done>The stylesheet exists with the full @layer order and a complete `tokens` layer sourced entirely from UI-SPEC; all 4 unit themes carry all 7 accent slots; the two contrast corrections are applied.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: base layer — responsive shell grid, HUD/stage/footer regions, focus-visible + Arabic/RTL foundation</name>
  <files>shared/awba-engine.css</files>
  <read_first>
    - shared/awba-engine.css (the tokens layer from Task 1 — build the base layer against those exact token names)
    - .planning/phases/01-foundation-design-tokens-responsive-shell-fonts/01-UI-SPEC.md (§Responsive Shell Anatomy, §Focus-Visible Treatment, §Typography Arabic/RTL laws)
    - .planning/phases/01-foundation-design-tokens-responsive-shell-fonts/01-RESEARCH.md (Pattern 2 min-height:100dvh, Pattern 3 mobile-default/desktop-progressive, Pitfall 2 viewport-fit prerequisite context, Pitfall 4 no blanket .ayah rule)
    - .planning/phases/01-foundation-design-tokens-responsive-shell-fonts/01-PATTERNS.md (#2 .phone REPLACE, #3 HUD KEEP-THE-FEEL re-hosted as grid rows)
  </read_first>
  <action>
    Add `@layer base` content to `shared/awba-engine.css`: (a) reset — `*{box-sizing:border-box; -webkit-tap-highlight-color:transparent}`, `html,body{height:100%; margin:0}`, body font/color from `--font-body`/`--ink`; (b) the shell — `.app-shell{min-height:100dvh; display:grid; grid-template-rows:auto 1fr auto; background:var(--paper)}` with three region classes for HUD (row 1, padding-top adds `env(safe-area-inset-top)`), stage (row 2, `overflow-y:auto; overscroll-behavior:contain`), footer (row 3, padding-bottom adds `env(safe-area-inset-bottom)`); (c) the desktop enhancement `@media (min-width:600px)` — center the shell (`body` becomes a centering flex context with the subtle brand backdrop wash on `--bg`), `.app-shell{max-width:480px; width:100%; min-height:min(100dvh,900px); border-radius:var(--r-2xl); box-shadow:var(--sh-4); overflow:hidden}` plus `--sh-inset` top highlight — never a fixed height, never a fake bezel; (d) global `:focus-visible` per the UI-SPEC spec (3px `--accent` outline + `--accent-soft` companion ring), plus the gold/night `--gold2` ring variant; (e) Arabic/RTL base laws — a `.ayah` class binding `--font-quran` with `line-height:1.9; letter-spacing:0`, a general-Arabic class binding `--font-ar` with `line-height:1.8; letter-spacing:0`, and `[dir="rtl"]`/Arabic containers `unicode-bidi:isolate`. Do NOT add any `components`/`screens`/`motion`-layer content — those stay empty. Do NOT write a blanket rule that forces `--font-quran` onto all Arabic (ayah-vs-hadith is per-instance Phase-4 logic).
  </action>
  <verify>
    <automated>grep -c '100dvh' shared/awba-engine.css && grep -c 'grid-template-rows' shared/awba-engine.css && grep -c 'safe-area-inset' shared/awba-engine.css && grep -Ec 'min-width: ?600px' shared/awba-engine.css && grep -c ':focus-visible' shared/awba-engine.css && grep -c 'unicode-bidi' shared/awba-engine.css</automated>
  </verify>
  <acceptance_criteria>
    - `.app-shell` uses `min-height:100dvh` with `grid-template-rows:auto 1fr auto` (`grep -c '100dvh'` ≥ 1, `grep -c 'grid-template-rows'` ≥ 1); no fixed `788px`/`380px` height or width appears (`grep -Ec '(788px|380px)' shared/awba-engine.css` == 0).
    - Safe-area insets are applied on HUD top and footer bottom (`grep -c 'safe-area-inset' shared/awba-engine.css` ≥ 2).
    - A `@media (min-width:600px)` block adds `max-width:480px` + `border-radius:var(--r-2xl)` + `box-shadow:var(--sh-4)` and uses `min-height:min(100dvh,900px)` (never a bare fixed column height).
    - The stage region declares `overflow-y:auto` and `overscroll-behavior:contain` (`grep -c 'overscroll-behavior' shared/awba-engine.css` ≥ 1).
    - `:focus-visible` styling exists (`grep -c ':focus-visible' shared/awba-engine.css` ≥ 1) with a `--accent` outline; a `.ayah` rule binds `--font-quran` and `unicode-bidi:isolate` is present for Arabic containers (`grep -c 'unicode-bidi' shared/awba-engine.css` ≥ 1).
    - `components`, `screens`, `motion` layers remain empty of rules (only their declared blocks exist).
  </acceptance_criteria>
  <done>The base layer renders a true full-viewport shell (full-bleed mobile, centered column ≥600px), honors safe-area insets, provides the global focus-visible treatment, and establishes the Arabic/RTL laws — with the three later layers left empty.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none (new) | Pure static CSS — no user input, no auth, no persistence, no runtime network. No new trust boundary is introduced by this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-ID | Information disclosure | `@font-face src` URLs | mitigate | All `src:url()` use CSS-relative `fonts/*.woff2` (→ same-origin `shared/fonts/`, file://-safe) — no `fonts.googleapis.com`/`fonts.gstatic.com` at runtime. Enforced by the zero-CDN grep gate in Plan 03 across shipped HTML/CSS. |
| T-01-A11Y | (Accessibility-as-safety) | `:focus-visible` + accessible-name foundation | mitigate | Global `:focus-visible` is defined in the `base` layer now (tap-highlight is suppressed, so keyboard users would otherwise be stranded — a Gen-3 gap); the accessible-name contract is inherited by every later component. |

ASVS V2–V6 do not apply to a pure CSS token/shell plan (01-RESEARCH §Security Domain). Security hardening/verification is revisited at Phase 6.
</threat_model>

<verification>
- `grep -c '^@layer [a-z]*, ' shared/awba-engine.css` → exactly 1 (FND-01: order declared once).
- `grep -c '@font-face' shared/awba-engine.css` → 11 (FND-03: self-hosted faces).
- All 4 `[data-unit]` themes present with 7 slots each (FND-02).
- `.app-shell` uses `100dvh` grid + `@media (min-width:600px)` column, no fixed card dimensions (PLT-01).
</verification>

<success_criteria>
- One stylesheet holds every color/type/radii/shadow/motion token; the five-name `@layer` order is immutable.
- Per-unit theming is structurally complete (no shared `--blue2`/`--blue3` escape hatch remains) — the half-theming bug is impossible.
- The responsive shell is full-bleed on mobile and an intentional centered column on desktop, safe-area-aware, with a global focus-visible treatment.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-design-tokens-responsive-shell-fonts/01-02-SUMMARY.md` when done.
</output>
