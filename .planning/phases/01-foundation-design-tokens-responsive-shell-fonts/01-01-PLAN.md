---
phase: 01-foundation-design-tokens-responsive-shell-fonts
plan: 01
type: execute
wave: 1
executor_hint: sonnet-mechanical
depends_on: []
files_modified:
  - .gitignore
  - scripts/check-glyph-coverage.py
  - shared/fonts/poppins-500.woff2
  - shared/fonts/poppins-600.woff2
  - shared/fonts/poppins-700.woff2
  - shared/fonts/poppins-800.woff2
  - shared/fonts/inter-400.woff2
  - shared/fonts/inter-500.woff2
  - shared/fonts/inter-600.woff2
  - shared/fonts/inter-700.woff2
  - shared/fonts/amiri-400.woff2
  - shared/fonts/amiri-700.woff2
  - shared/fonts/amiri-quran-400.woff2
autonomous: true
requirements: [FND-03]
must_haves:
  truths:
    - "Four font families are self-hosted as 11 subset .woff2 files with zero runtime CDN dependency"
    - "The glyph-coverage script passes, proving ˹˺ brackets, transliteration diacritics, î â û, ﷺ, and Arabic/Quranic marks exist in the subset fonts"
    - "Raw downloaded TTFs are gitignored; only the subset .woff2 outputs are committed"
  artifacts:
    - path: "scripts/check-glyph-coverage.py"
      provides: "fontTools cmap glyph-coverage gate for the 4 subset families"
      contains: "getBestCmap"
    - path: "shared/fonts/poppins-700.woff2"
      provides: "Poppins display subset (Latin + diacritics + brackets + punctuation)"
    - path: "shared/fonts/inter-400.woff2"
      provides: "Inter body subset (carries bracketed translations + diacritics)"
    - path: "shared/fonts/amiri-quran-400.woff2"
      provides: "Amiri Quran subset for .ayah verbatim Qur'an (Arabic block + Quranic marks + ﷺ)"
    - path: ".gitignore"
      provides: "excludes shared/fonts/src/ raw TTF intermediates"
      contains: "shared/fonts/src/"
  key_links:
    - from: "scripts/check-glyph-coverage.py"
      to: "shared/fonts/*.woff2"
      via: "fontTools TTFont().getBestCmap() codepoint membership check"
      pattern: "getBestCmap"
---

## Phase Goal

**As a** reviewer, **I want to** open `preview.html` and see the full design system — one token layer, a true responsive shell, per-unit theming, and self-hosted fonts — render at world-class quality, **so that** every later screen builds on a proven, CDN-free, half-theming-proof foundation instead of Gen-3's fixed 380px card and Google Fonts dependency.

> MVP adaptation note: this is a zero-build static-site foundation phase, so the "user" is the reviewer (Josh) plus the developer building later phases, and the vertical slice is the reviewable `preview.html` end-to-end (assets → stylesheet → rendered page). The ROADMAP `**Goal:**` line is outcome-shaped; this user story is the honest MVP reframing directed by the orchestrator's Walking-Skeleton brief.

<objective>
Scaffold the repo's static-asset layer and prove it end-to-end: create the glyph-coverage check script FIRST (it fails until fonts exist — the walking-skeleton "failing test"), then acquire and subset all four font families to committed `.woff2` files so the script goes GREEN.

Purpose: Fonts are the data/asset layer of this static site. Getting them self-hosted, correctly subset, and glyph-verified is the prerequisite for zero-CDN rendering (FND-03) and for `preview.html` to render every scripture/transliteration glyph without tofu.
Output: `scripts/check-glyph-coverage.py`, `.gitignore`, and 11 subset `.woff2` files under `shared/fonts/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-design-tokens-responsive-shell-fonts/01-RESEARCH.md
@.planning/phases/01-foundation-design-tokens-responsive-shell-fonts/01-UI-SPEC.md
@.planning/phases/01-foundation-design-tokens-responsive-shell-fonts/SKELETON.md

<interfaces>
<!-- Verified facts from 01-RESEARCH (read it — commands and codepoints are supplied verbatim there). -->
- fontTools present locally: v4.60.2. `pyftsubset` is NOT on PATH — invoke as `python3 -m fontTools.subset`.
- Verified subset ranges (byte-scanned against Josh's 19 real content files — do NOT use CLAUDE.md's generic illustrative range):
  - Latin (Poppins, Inter): `U+0000-00FF,U+0100-017F,U+1E00-1EFF,U+02B0-02FF,U+2000-206F`
  - Arabic (Amiri, Amiri Quran): `U+0600-06FF,U+FDFA`
- Confirmed weights (exact match to Gen-3): Poppins 500/600/700/800 · Inter 400/500/600/700 · Amiri 400/700 · Amiri Quran 400 (its only weight) = 11 files.
- Confirmed bracket codepoints: `˹` = U+02F9, `˺` = U+02FA (NOT the U+2E32-area speculated in D-10).
- The glyph-coverage script body (REQUIRED dict + TTFont cmap loop + sys.exit) is given verbatim in 01-RESEARCH §Code Examples and 01-PATTERNS — use it exactly, do not reinvent.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold asset layer + create the (initially-failing) glyph-coverage gate</name>
  <files>scripts/check-glyph-coverage.py, .gitignore</files>
  <read_first>
    - .planning/phases/01-foundation-design-tokens-responsive-shell-fonts/01-RESEARCH.md (§Code Examples — the verbatim script; §Recommended Project Structure — the dir layout and the `shared/fonts/src/` gitignore rule)
    - .planning/phases/01-foundation-design-tokens-responsive-shell-fonts/01-PATTERNS.md (scripts/check-glyph-coverage.py section — same script, tagged no-analog "use verbatim")
  </read_first>
  <action>
    Create `scripts/check-glyph-coverage.py` verbatim from 01-RESEARCH §Code Examples: a `REQUIRED` dict mapping each of the four target woff2 paths (`shared/fonts/inter-400.woff2`, `shared/fonts/poppins-600.woff2`, `shared/fonts/amiri-400.woff2`, `shared/fonts/amiri-quran-400.woff2`) to its list of required hex codepoints, then a loop that opens each with `fontTools.ttLib.TTFont`, reads `getBestCmap()`, prints `MISSING U+XXXX in <path>` for any absent codepoint, and calls `sys.exit(1 if failed else 0)`. Keep the exact codepoint lists from RESEARCH (inter includes 0x02F9, 0x02FA, 0x02BF, 0x02BE, 0x0101, 0x012B, 0x016B, 0x1E25, 0x1E63, 0x1E6D, 0x1E0F, 0x00B7, 0x2014, 0x2019; amiri/amiri-quran include 0xFDFA and Quranic marks).
    Create `.gitignore` with an entry `shared/fonts/src/` (the raw downloaded TTF intermediates — never shipped, never committed) plus standard noise (`.DS_Store`).
    Do NOT create any font files in this task — the script is expected to FAIL now (fonts absent). This is the walking-skeleton "failing test first" step.
  </action>
  <verify>
    <automated>python3 -c "import ast,sys; ast.parse(open('scripts/check-glyph-coverage.py').read()); print('parse OK')" && grep -c 'getBestCmap' scripts/check-glyph-coverage.py && grep -c 'shared/fonts/src/' .gitignore</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/check-glyph-coverage.py` parses as valid Python (`ast.parse` succeeds).
    - `grep -c 'getBestCmap' scripts/check-glyph-coverage.py` returns ≥ 1.
    - The `REQUIRED` dict contains all four woff2 paths and the codepoint 0x02F9 AND 0x02FA appear for the Inter entry (confirmed bracket glyphs).
    - `grep -c 'shared/fonts/src/' .gitignore` returns exactly 1.
    - Running `python3 scripts/check-glyph-coverage.py` at this point exits non-zero (fonts do not exist yet) — this failing state is expected and correct.
  </acceptance_criteria>
  <done>Glyph-coverage gate exists and runs; `.gitignore` excludes raw TTFs; script fails only because no fonts are subset yet.</done>
</task>

<task type="auto">
  <name>Task 2: Acquire + subset all four families → 11 committed .woff2, drive the gate GREEN</name>
  <files>shared/fonts/poppins-500.woff2, shared/fonts/poppins-600.woff2, shared/fonts/poppins-700.woff2, shared/fonts/poppins-800.woff2, shared/fonts/inter-400.woff2, shared/fonts/inter-500.woff2, shared/fonts/inter-600.woff2, shared/fonts/inter-700.woff2, shared/fonts/amiri-400.woff2, shared/fonts/amiri-700.woff2, shared/fonts/amiri-quran-400.woff2</files>
  <read_first>
    - .planning/phases/01-foundation-design-tokens-responsive-shell-fonts/01-RESEARCH.md (§Font Subset Pipeline Steps 1–4 — the verified `curl` acquisition + `python3 -m fontTools.subset` commands, already run this session; and the "never pass custom --layout-features for Arabic" rule)
    - .planning/phases/01-foundation-design-tokens-responsive-shell-fonts/01-UI-SPEC.md (§Font Subset Contract — the per-family required coverage table)
    - scripts/check-glyph-coverage.py (the gate this task must satisfy)
  </read_first>
  <action>
    Acquire the raw TTFs into `shared/fonts/src/` using the RESEARCH Step 1 approach: `curl` the Google Fonts CSS2 URL for `Poppins:wght@500;600;700;800`, `Inter:wght@400;500;600;700`, `Amiri:wght@400;700`, `Amiri Quran` with a `Mozilla/5.0` user-agent, then `grep -oE 'https://fonts\.gstatic\.com/s/[^)]+\.ttf'` and download each. URLs rotate — always fetch fresh, never hardcode. `fonts.gstatic.com` is Google's own first-party font origin (data acquisition, not a third-party mirror); the resulting `.woff2` are committed but these raw `.ttf` are gitignored (`shared/fonts/src/`).
    Subset with `python3 -m fontTools.subset` (NOT bare `pyftsubset` — not on PATH), `--flavor=woff2`, producing exactly these 11 outputs: `poppins-{500,600,700,800}.woff2`, `inter-{400,500,600,700}.woff2`, `amiri-{400,700}.woff2`, `amiri-quran-400.woff2` in `shared/fonts/`.
    Latin families (Poppins, Inter): `--unicodes="U+0000-00FF,U+0100-017F,U+1E00-1EFF,U+02B0-02FF,U+2000-206F"` with `--layout-features='*'`.
    Arabic families (Amiri, Amiri Quran): `--unicodes="U+0600-06FF,U+FDFA"` and OMIT any custom `--layout-features` (rely on pyftsubset's default set which auto-preserves `init/medi/fina/isol/curs/mark/mkmk` Arabic shaping — passing a hand-picked list silently breaks letter-joining and diacritic positioning).
    Then run `python3 scripts/check-glyph-coverage.py` and confirm it exits 0.
  </action>
  <verify>
    <automated>ls shared/fonts/*.woff2 | wc -l | tr -d ' ' && python3 scripts/check-glyph-coverage.py && echo "GLYPH GATE GREEN"</automated>
  </verify>
  <acceptance_criteria>
    - `ls shared/fonts/*.woff2 | wc -l` equals 11.
    - All four filenames the gate checks exist: `inter-400.woff2`, `poppins-600.woff2`, `amiri-400.woff2`, `amiri-quran-400.woff2`.
    - `python3 scripts/check-glyph-coverage.py` exits 0 with no `MISSING U+XXXX` lines printed (proves ˹˺ U+02F9/02FA, diacritics, ·, dashes, curly quote, and Arabic/Quranic marks + ﷺ U+FDFA are all present).
    - Arabic subsets were produced WITHOUT a custom `--layout-features` flag (the subset commands used for `amiri-*` and `amiri-quran-*` do not pass `--layout-features`).
    - `git status --porcelain shared/fonts/src` shows no tracked files under `shared/fonts/src/` (raw TTFs are gitignored).
  </acceptance_criteria>
  <done>11 subset `.woff2` files exist under `shared/fonts/`; the glyph-coverage gate is GREEN; raw TTFs remain gitignored.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| network → dev machine (one-time) | Font binaries fetched from `fonts.gstatic.com` during the one-time acquisition step; subset locally and committed. No runtime network crossing exists in the shipped product. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | Downloaded font binaries + `fonttools` dev tool | mitigate | Fetch ONLY from `fonts.gstatic.com` (Google's first-party font origin, not a third-party mirror); subset locally with `fonttools` v4.60.2 (already installed & verified functional this session — no install task, dev-tool only, never shipped); a corrupted or wrong font fails `check-glyph-coverage.py` (cmap membership check) before it can be committed. Per 01-RESEARCH Package Legitimacy Audit, `fonttools` is `[ASSUMED]` but functionally verified present — no blocking checkpoint required in this environment. |
| T-01-ID | Information disclosure | Any residual Google Fonts CDN reference | mitigate | Zero `.woff2` is loaded from a CDN at runtime; all fonts are same-origin static files. The zero-CDN grep gate is enforced downstream in Plan 03 (shipped HTML/CSS). |

No user input, auth, persistence, or runtime network exists in this phase — ASVS V2–V6 do not apply (01-RESEARCH §Security Domain). Threat surface is limited to the supply-chain of the one-time font acquisition, covered above.
</threat_model>

<verification>
- `python3 scripts/check-glyph-coverage.py` exits 0 (the phase's one automatable check).
- `ls shared/fonts/*.woff2 | wc -l` → 11.
- `git check-ignore shared/fonts/src/AnyFile.ttf` confirms raw TTFs are ignored.
</verification>

<success_criteria>
- The glyph-coverage gate is GREEN, proving every required codepoint (brackets, diacritics, circumflex vowels, middle dot, dashes, curly quotes, Arabic block + Quranic marks + ﷺ) exists in the correct subset faces.
- 11 subset `.woff2` files are committed; raw TTFs are not.
- Zero runtime dependency on any font CDN is established at the asset layer.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-design-tokens-responsive-shell-fonts/01-01-SUMMARY.md` when done.
</output>
