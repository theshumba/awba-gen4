---
phase: 01-foundation-design-tokens-responsive-shell-fonts
plan: 04
type: execute
wave: 4
executor_hint: sonnet-mechanical
depends_on: ["01-03"]
files_modified: []
autonomous: false
requirements: [FND-01, FND-02, FND-03, PLT-01]
must_haves:
  truths:
    - "A reviewer confirms, against the D-12 8-item checklist, that the shell adapts, units fully recolor, glyphs render, and zero CDN font requests fire"
  artifacts: []
  key_links:
    - from: "reviewer (Josh)"
      to: "preview.html + check-glyph-coverage.py"
      via: "the D-12 8-item manual checklist + the automated gate"
      pattern: "preview.html"
---

## Phase Goal

**As a** reviewer, **I want to** open `preview.html` and see the full design system — one token layer, a true responsive shell, per-unit theming, and self-hosted fonts — render at world-class quality, **so that** every later screen builds on a proven, CDN-free, half-theming-proof foundation instead of Gen-3's fixed 380px card and Google Fonts dependency.

<objective>
Run the phase's designed verification vehicle: the reviewer opens `preview.html` and walks the D-12 8-item checklist to confirm all four Phase-1 success criteria that are inherently visual (they cannot be fully proven by grep or script). This is the human gate before `/gsd:verify-work`.

Purpose: Most of this phase's success criteria are visual/structural (resize behavior, full recolor, glyph rendering, no-FOUC, zero-CDN) — `preview.html` exists precisely so a human can confirm them in ~60 seconds. This gate also matches Josh's real review workflow: open the file directly.
Output: reviewer approval (or a punch list of gaps to route to `/gsd:plan-phase --gaps`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/01-foundation-design-tokens-responsive-shell-fonts/01-UI-SPEC.md
@.planning/phases/01-foundation-design-tokens-responsive-shell-fonts/01-VALIDATION.md
@preview.html
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Reviewer walkthrough of preview.html against the D-12 checklist</name>
  <files>(none — verification only)</files>
  <action>Present preview.html to the reviewer and walk the D-12 8-item checklist below. Run the automated prechecks first (glyph script green, viewport-fit present, zero CDN host strings), then collect the human verdict. Do not mark the phase complete until every checklist item passes or specific gaps are recorded.</action>
  <what-built>
    A complete, self-hosted, zero-CDN design foundation rendered in `preview.html`: one `@layer tokens, base` stylesheet (`shared/awba-engine.css`), 11 subset `.woff2` fonts, a true responsive shell (full-bleed mobile / centered column desktop), full per-unit theming, and a permanent glyph test. The one automated gate (`python3 scripts/check-glyph-coverage.py`) is green and all structural greps pass.
  </what-built>
  <how-to-verify>
    Open `preview.html` directly (double-click / `file://`) and confirm each D-12 item:
    1. Resize the window 320px → 1440px: below 600px the shell is full-bleed edge-to-edge; above 600px it becomes an intentional centered column on a calm cream backdrop. NO fixed 380px card, and NO horizontal scroll at 320px.
    2. Click the unit switch through u1 → u2 → u3 → u4: the WHOLE sample cluster recolors (button, chip, node ring, inline link, tinted panel, progress fill). Confirm ZERO residual indigo/blue survives on u2/u3/u4 (the Gen-3 half-theming bug is gone).
    3. Read the type scale + Arabic samples: hierarchy is clear; Arabic body is comfortably large (≥1.4× Latin) with generous line-height.
    4. Scan the radii + shadow tiles: shadows are indigo-tinted (never grey/black), corners soft/warm.
    5. Interact with the motion demos (gummy press, node-pop, sheet-in): they feel springy/gentle; then enable OS "Reduce Motion" and reload — animations collapse to instant.
    6. Read the glyph-test block: ˹ ˺, the diacritics, the middle dot in "Unit 1 · Lesson 1", the circumflex "Iblîs · Suwâ · Yaghûth · Yaûq", ﷺ, the Amiri-Quran ayah and the Amiri hadith all render as real glyphs — NO tofu boxes (except the intentional FAIL exemplar cell, which SHOULD show its "re-subset" message).
    7. Open DevTools › Network, filter by "font", reload: every font request is same-origin `/shared/fonts/…` — ZERO Google Fonts CDN requests. Navigating/reloading shows no font flash (FOUC).
    8. Overall: the page reads as a premium living style reference, not a debug sheet.
  </how-to-verify>
  <verify>
    <automated>python3 scripts/check-glyph-coverage.py && grep -c 'viewport-fit=cover' preview.html && test $(grep -Ec 'fonts\.(googleapis|gstatic)\.com' preview.html) -eq 0 && echo "AUTOMATED PRECHECKS PASS"</automated>
    <human-check>Reviewer confirms all 8 D-12 checklist items above pass on a real browser.</human-check>
  </verify>
  <acceptance_criteria>
    - Automated prechecks pass: glyph script exits 0, `viewport-fit=cover` present, zero literal CDN host strings in `preview.html`.
    - Reviewer confirms items 1–8; any failure is captured as a specific gap (which item, what was wrong) to feed `/gsd:plan-phase --gaps`.
  </acceptance_criteria>
  <done>Reviewer has confirmed all 8 D-12 checklist items (or a specific gap list is recorded for /gsd:plan-phase --gaps); automated prechecks are green.</done>
  <resume-signal>Type "approved" to close Phase 1, or list the failing checklist item(s) and what was wrong.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none | Verification-only plan — no code changes, no new surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-VER | (Verification integrity) | Manual visual sign-off | accept | The zero-CDN and glyph gates run automatically as prechecks before the human sign-off, so the visual approval cannot pass while the objective gates are red. No runtime security surface exists. |
</threat_model>

<verification>
- `python3 scripts/check-glyph-coverage.py` exits 0.
- `grep -c 'viewport-fit=cover' preview.html` → 1.
- `grep -Ec 'fonts\.(googleapis|gstatic)\.com' preview.html` → 0.
- Reviewer approves the D-12 8-item checklist.
</verification>

<success_criteria>
- All four Phase-1 success criteria are confirmed: responsive shell adapts at every width; unit theming fully recolors with no half-theming; all tokens come from one source; fonts are self-hosted with zero CDN and correct glyph rendering.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-design-tokens-responsive-shell-fonts/01-04-SUMMARY.md` recording the reviewer verdict (approved / gap list) when done.
</output>
